% =====================================================================
%  causalsmith Stage 0 derivation skeleton.
%  Written automatically by the causalsmith TS pipeline before Stage 0 runs.
%  Locked parameters: qid=__QID__, specialization=__SPECIALIZATION__
%
%  HOW TO USE THIS FILE
%  --------------------
%  - The preamble (everything above the `% --- BEGIN CONTENT ---` marker)
%    and the `\section{...}` headers below are fixed by the pipeline.
%    Do NOT rewrite or rename them; downstream stages (0.5 reviewer,
%    Stage 1, Stage 4d) grep for these section titles and macros.
%  - Each section contains exactly one `% TODO(stage0 ...): ...`
%    placeholder. Replace each TODO with the content described for that
%    section in `CausalSmith/tools/src/discovery/prompts/stage0_outputFormat.txt`.
%  - The `\paragraph{Locked parameters.}` block inside Formal setup must
%    pin the convention precisely. For free-form panel / ExactID /
%    PartialID anchors, reproduce the locked-parameter block
%    from the Stage -1 proposal verbatim. Do not rephrase locked parameters;
%    Stage 1 quotes them.
%  - Removing a section header, renaming a macro, or rewriting the
%    preamble is a regression: the file must still match this skeleton
%    after you finish.
%  - Immutability is SCOPED to section headers, macro names, and the
%    preamble. The PLACEMENT of theorem/proposition/lemma/assumption
%    environments is YOUR responsibility: a `theorem` env may be freely
%    moved into whichever fixed-header section fits (e.g. promote the
%    field-level result from \S11 into \S9). Relocating a block between
%    fixed headers is NOT a skeleton violation.
% =====================================================================

\documentclass[11pt]{article}

\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{enumitem}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}

\newcommand{\E}{\mathbb{E}}
\renewcommand{\Pr}{\mathbb{P}}
\newcommand{\one}{\mathbf{1}}
\newcommand{\transpose}{\mathsf{T}}
\newcommand{\calH}{\mathcal{H}}
\newcommand{\calF}{\mathcal{F}}
\newcommand{\calR}{\mathcal{R}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\plim}{\operatorname*{plim}}

\theoremstyle{definition}
\newtheorem{definition}{Definition}
\newtheorem{assumption}{Assumption}
\newtheorem{example}{Example}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}

\theoremstyle{remark}
\newtheorem{remark}{Remark}

\title{causalsmith Stage 0 derivation: \texttt{__QID__} / \texttt{__SPECIALIZATION__}%
  \\ \large\textit{TODO(stage0 \S2): replace with one-line theorem name}}
\author{causalsmith Stage 0 solver}
\date{\today}

\begin{document}
\maketitle

% --- BEGIN CONTENT ---  (everything above is fixed by the pipeline)

\begin{abstract}
% TODO(stage0 \S2 Abstract): 4--6 sentences stating the cluster-appropriate estimand-defining functional
%   (Panel: regression formula; ExactID: identifying functional; PartialID: bounding functional),
%   the target causal estimand, and the main conclusion.
\end{abstract}

\section{Introduction}
% TODO(stage0 \S3 Introduction):
%   - Why the problem matters (econometric / causal question at stake).
%   - What is known before this note (without overstating novelty).
%   - What is new in this note: exact formula / decomposition / condition / comparison.
%   - Why useful for downstream theory or formula design.
%   - Do NOT motivate by Lean formalization --- Stage 1 owns that gate; Stage 0 is a math note.
%   - REQUIRED when a Stage -1 proposal is present: close \S3 with a
%       \paragraph{Motivating conjecture(s).}
%     block that reproduces VERBATIM every Conjecture N from the proposal's \S8 the note
%     will verify or refute. The note must be self-contained: downstream sections may
%     then say "Conjecture 1" freely, but the FIRST appearance of any conjecture in the
%     body must happen here, not as a bare reference. Omit the block if no Stage -1
%     proposal exists.

\section{Related work and positioning}
% TODO(stage0 \S4 Related work):
%   - Targeted review of published and in-repository work needed to position this note.
%   - One sentence of relevance per citation.
%   - Compare with the closest known result classes; say precisely what differs.
%   - Do not claim an exhaustive literature review unless the brief supplies enough evidence.

\section{Formal setup}
% TODO(stage0 \S5 Formal setup): use the cluster-appropriate setup (per the brief's anchor cluster):
%   - Panel: panel index, treatment history, potential outcomes, response surface, boundary conventions.
%   - Exact identification: causal model (DAG / SWIG / NPSEM), variables (Y, A, X, Z, M, U), counterfactual notation, support / measurability conventions.
%   - Partial identification: observable distribution P, unobserved parameter \theta, identified set \Theta_I(P), maintained restrictions.

\paragraph{Locked parameters.}
% TODO(stage0 \S5 Locked parameters): pin the convention precisely.
%   For free-form panel / ExactID / PartialID, reproduce the locked-parameter block from the
%   Stage -1 proposal verbatim. Format as an itemized list keyed by axis. Pick the cluster-appropriate
%   template (DELETE the other two before submitting; replace angle-bracket placeholders with the
%   actual locked values from the brief; do NOT add or rephrase axes).
%
%   --- Panel template ---
%   \begin{itemize}
%     \item qid: __QID__
%     \item specialization: __SPECIALIZATION__
%     \item memory length $p$: <as fixed by the qid block>
%     \item assignment process: <iid Bernoulli / first-order Markov / staggered / etc.>
%     \item untreated baseline: $\E[Y_{it}(0_p)\mid D_i]=\alpha_i+\lambda_t$
%     \item target estimand: <e.g.\ $\tau_{\text{on}}=m_t(0,1)-m_t(0,0)$>
%     \item regression family: <TWFE / saturated-history / clean-history TWFE / etc.>
%   \end{itemize}
%
%   --- ExactID template ---
%   \begin{itemize}
%     \item qid: __QID__
%     \item specialization: __SPECIALIZATION__
%     \item causal model: <DAG / SWIG / NPSEM, with vertex set and edges>
%     \item treatment: <A (binary / continuous / time-varying)>
%     \item target estimand: <e.g.\ $\E[Y(a)]$, ATE $\E[Y(1)]-\E[Y(0)]$, CATE, LATE, NDE / NIE>
%     \item identifying assumptions: <e.g.\ conditional ignorability given X; positivity on the support of X;
%       consistency; exclusion + relevance + monotonicity for IV; sequential ignorability for DTR>
%   \end{itemize}
%
%   --- PartialID template ---
%   \begin{itemize}
%     \item qid: __QID__
%     \item specialization: __SPECIALIZATION__
%     \item observable distribution: $P$ over <observable variables and support assumptions>
%     \item parameter $\theta$: <scalar / vector causal target, e.g.\ $\E[Y(1)]$ or ATE>
%     \item identifying restrictions: <e.g.\ MTR / MTS / MIV; IV exclusion + mean independence;
%       marginal sensitivity bound $\Lambda$; bounded outcome on $[y_L,y_U]$>
%     \item bounding functional: $(\theta_L(P),\theta_U(P))$ defined as <explicit functionals of $P$>;
%       sharpness verdict <sharp / outer bound>
%   \end{itemize}

\section{Assumptions}
% TODO(stage0 \S6 Assumptions): each assumption gets its own `assumption` environment with a unique label
%   so Stage 1 P-block / Stage 2.5 H.1 can cite it. Group by kind, cluster-appropriate:
%   - Panel: untreated-baseline, finite-memory, assignment, rank/support.
%   - ExactID: consistency, positivity / overlap, conditional ignorability / exchangeability, exclusion (IV),
%       monotonicity (LATE), sequential ignorability (DTR), no-unmeasured-mediator (mediation).
%   - PartialID: shape (MTR / MTS / MIV), IV-style (exclusion, mean-independence), sensitivity (bounded
%       log-odds, marginal sensitivity \Lambda), missing-data, support / boundedness on Y.

\section{Estimand-defining functional}
% TODO(stage0 \S7 Estimand-defining functional --- section title fixed by skeleton; role is cluster-aware):
%   - Panel: define the regression formula, all regressors and nuisance controls; state the FWL
%     residualized representation.
%   - ExactID: define the identifying functional explicitly --- back-door adjustment, front-door formula,
%     Wald ratio, g-formula, sequential-adjustment expression, mediation formula --- as an observable
%     expression in P.
%   - PartialID: define the bounding functional (\theta_L(P), \theta_U(P)) explicitly as functionals of P;
%     distinguish "outer bound" from "sharp bound".

\section{Target causal estimand}
% TODO(stage0 \S8 Target estimand): define the intended causal target precisely BEFORE proving what the
%   \S7 functional estimates (Panel / ExactID) or brackets (PartialID). For PartialID, the target is the
%   scalar / vector $\theta$; the \S9 conclusion is a containment / bound on $\theta$, not an equality.

\section{Main theorems}
% TODO(stage0 \S9 Main theorems):
%   - Each main theorem = one contribution. Incremental results go in `proposition`, not `theorem`.
%   - The HEADLINE theorem here must be the field-level contribution itself. For PartialID that is the
%     sharp identified-set / bound theorem; for ExactID the identification equality. An inference /
%     coverage / calibration result whose force comes mainly from an assumed expansion is NOT the
%     headline --- state it as a `proposition` or corollary, and if the field-level theorem is currently
%     sitting in \S11 as a "supporting lemma", MOVE it up here (block relocation is allowed; see preamble).
%   - Immediately after each theorem: what is new, why important, comparison to closest prior result.

\section{Interpretation}
% TODO(stage0 \S10 Interpretation): cluster-appropriate:
%   - Panel: when the coefficient is clean and when it is contaminated; which terms drive the contamination.
%   - ExactID: which assumption(s) carry the identification, which strata require positivity, what kind of
%     mis-specification breaks the identification.
%   - PartialID: when the bound collapses to point identification, when it widens to the natural Manski
%     no-restriction bound, and which restriction is doing the tightening work.
%   - Universal: what each theorem means in words; connect back to Introduction and Related-work positioning.

\section{Step-by-step proof}
% TODO(stage0 \S11 Proof): cluster-appropriate technique:
%   - Panel: FWL, finite-memory potential outcomes, residualization / rank / support steps.
%   - ExactID: do-calculus rules (1/2/3), SWIG counterfactual independence, g-formula / sequential
%     adjustment, mediation factorization. Cite the rule at each application.
%   - PartialID: extreme-point / LP arguments, Manski worst-case completion, MTR / MTS algebra,
%     Balke--Pearl LP, sensitivity-model duality. State the technique used at each step.
%   - Universal: each equality (or each \subseteq / \le in the partial-ID case) must be traceable to
%     a named assumption or algebraic identity. Each theorem proof must explicitly cite the assumptions
%     and lemmas it uses.

\section{Sanity checks}
% TODO(stage0 \S12 Sanity checks --- REQUIRED, Stage 0.5 verifies these): cluster-appropriate corner cases:
%   - Panel: homogeneous effects; no carry-over; contamination terms vanishing; T=2 reduces to canonical DID.
%   - ExactID: X = \emptyset (unconditional ignorability); random treatment (E[Y(a)] = E[Y | A=a]); null
%     effect E[Y(1)] = E[Y(0)]; named special case (e.g. backdoor with binary treatment under randomization
%     reduces to E[Y | A=1] - E[Y | A=0]).
%   - PartialID: vacuous bounds under no restriction (natural Manski [E[Y_L], E[Y_U]] bound); zero-width
%     bounds under point-identification; MTR specialization; named special case (e.g. Balke--Pearl with
%     binary IV recovers the Wald bound).

\section{Counterexample or minimality check}
% TODO(stage0 \S13 Counterexample / minimality): cluster-appropriate:
%   - Positive result: small counterexample showing why weakening the result fails, when one is available.
%     * Panel: weaker basis / shorter memory / reduced support.
%     * ExactID: dropping one identifying assumption (ignorability, positivity, exclusion, monotonicity, etc.).
%     * PartialID: dropping one shape / IV / sensitivity restriction; non-sharp completion.
%   - Negative result: nearest formula / assumption / restriction that fixes the problem if possible.
%   - If no useful counterexample applies, say so explicitly and name the condition playing the minimality role:
%     * Panel: rank / support / basis-completeness condition.
%     * ExactID: positivity / overlap on the strata actually used; identifying-assumption tightness.
%     * PartialID: shape / IV / sensitivity restriction whose removal makes the bound vacuous or non-sharp.

\section{Formalization checklist}
% TODO(stage0 \S14 Math-objects checklist --- REQUIRED, read by Stage 1 to derive the formalization plan;
%   section header stays "Formalization checklist" by skeleton invariant):
%   - Objects, definitions, lemmas, and identities the proof relies on as *mathematical* objects
%     (typed signatures, dependencies, role in the proof).
%   - Separate probabilistic assumptions from deterministic identities.
%   - Do NOT cite `*.lean` files or Mathlib lemmas --- Stage 1 owns the Lean-substrate mapping
%     and the feasibility gate.

\section{Conclusion and references}
% TODO(stage0 \S15 Conclusion): restate the theorem-level contribution and the key cluster-appropriate condition:
%   - Panel: clean-coefficient condition and main contamination source.
%   - ExactID: which identifying assumption carries the result and what breaks identification.
%   - PartialID: which restriction tightens the bound and when the bound becomes vacuous / point-identifying.

\subsection*{References}
% TODO(stage0 \S15 References): bibliography entries for every cited published work.
%   Use `\begin{thebibliography}{99} \bibitem{key} ... \end{thebibliography}` or a self-contained list.
%   No separate bibliographic note --- all substantive positioning belongs in \S4 Related work.

\end{document}
