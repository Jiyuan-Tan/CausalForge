Specify ONE schematic diagram for a seminar slide of an econometrics talk. You provide only the CONTENT — boxes and arrows — as the line DSL below; all geometry (box sizes, positions, arrow routing) is computed deterministically, so never think about layout, only about what the boxes say and which arrows exist. The figure renders labeled "illustrative": it clarifies structure and must never assert quantitative content (no data values, no invented constants).

OUTPUT — ONLY DSL lines, nothing else (no prose, no code fences):
  node <id> | <Title> | <detail line> | <detail line>
  edge <src> -> <dst>
- `<id>` is lowercase kebab-case.
- The Title is 1–3 words; each detail line at most ~5 short words; at most 2 detail lines per node. Use Unicode for math symbols (σ, τ, ℓ₁, d/n², ×, ≍ — NO LaTeX markup).
- At most 8 nodes. Arrows flow left-to-right (sources → sinks); include EVERY arrow the flow logically needs — a reader must be able to trace each input to each output.
- Arrows assert REAL dependency: draw `a -> b` only when a's output actually feeds b according to the slide content. Alternatives that a selector or comparison chooses among are PARALLEL nodes, each with its own arrow into the chooser — never chained through one another. A sub-step that belongs to one branch (a split, a per-case method) hangs off that branch only, never shared with a sibling branch. When unsure whether a dependency exists, omit the arrow.
- Every label must come from the slide content or caption below — never introduce a claim, quantity, or component the slide does not state.
- The output is ONLY boxes and arrows: if the caption suggests an axis, grid, or plot, express the same content as labeled boxes and arrows instead of attempting geometry.
{{dsl_findings}}

Example:
  node data | Observed data
  node split | Sample split | heavy vs light cells
  node est | Estimator | clipped to scale M
  edge data -> split
  edge split -> est

Figure name: {{name}}
Caption (what the figure must show): {{caption}}

Slide title: {{slide_title}}

Slide content (the figure illustrates exactly this):
{{slide_content}}

Audited formal statements referenced on this slide — the GROUND TRUTH for dependencies; every arrow must agree with the structure these define (which inputs feed which construction, what a selector chooses among, where clipping or thresholds actually apply):
{{formal_context}}

Paper abstract (context only):
{{abstract}}
