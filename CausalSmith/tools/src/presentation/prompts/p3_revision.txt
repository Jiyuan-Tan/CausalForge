Revise an econometrics working paper to fix the review problems below. The formal results are machine-verified and their statement environments are FROZEN.

Problems to fix (each names its gate):
{{problems}}

The paper source is the file at:
{{paper_path}}

Read it, then fix the problems with targeted Edit operations on that file. Do not rewrite the whole file.

HARD RULES:
- Never change any text inside any obj_id-carrying frozen formal environment: `\begin{theoremv}{...}`/`\end{theoremv}`, `\begin{assumptionv}{...}`/`\end{assumptionv}`, `\begin{lemmav}{...}`/`\end{lemmav}`, `\begin{definitionv}{...}`/`\end{definitionv}`, `\begin{citedv}{...}`/`\end{citedv}`, `\begin{propositionv}{...}`/`\end{propositionv}`, or `\begin{remarkv}{...}`/`\end{remarkv}`. Fix prose AROUND the environments only. If a problem says a statement drifted, fix the surrounding prose claim, not the environment.
- Cite only keys already cited in the paper or named in a problem as allowed.
- Fix overclaim problems by weakening the prose to the theorem's actual strength, never by restating the theorem more strongly.
- The asymmetric rule binds EVERY revision, including soft `[rubric]` (quality/positioning/writing) problems: a prose claim may never exceed the strength of its formal statement, and no revision may remove or soften a substrate-gate / hypothesis caveat or generated cited-dependency “Formalization scope” footnote. If addressing a rubric weakness would require strengthening a claim past its verified strength, dropping a gate/conditionality qualifier, or asserting an optimality/identification/necessity statement the formal results do not establish, DO NOT do it — instead improve clarity, structure, motivation, or positioning around the claim while leaving its hedged strength intact. A quality or positioning improvement that reintroduces an overclaim is itself a defect.
- Keep all section structure and obj_id anchors intact.

CROSS-REFERENCES: cite every anchored formal object only through `\cref{obj:<exact-id>}` (or `\Cref` at sentence start). Cleveref supplies the kind and number; never write a manual kind prefix, never use legacy `\ref{...}` for any target, and never invent note-style aliases. Obj_ids may appear only in environment arguments and cleveref commands; never emit `\label` yourself.

REQUIRED OUTPUT FORMAT: after the file edits are complete, emit exactly the single line `REVISED` on stdout — no JSON, no commentary, no LaTeX.
NEVER write \label{obj:...} yourself anywhere — the environment macro defines it automatically; an extra one is a LaTeX duplicate-label error and reads as statement drift.
