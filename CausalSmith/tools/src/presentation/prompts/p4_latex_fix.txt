Fix the LaTeX compilation errors below. Change only what is necessary to compile.

Compile log (errors first):
{{compile_log}}

The LaTeX source is the file at:
{{paper_path}}

The authored/cache sources from which P2 assembles that file are:
{{source_paths}}

Read the assembled file and locate the same text in its authored source. Apply each targeted
mechanical fix to the authored source AND to paper.tex, keeping the two copies identical. If the
fault exists only in assembly boilerplate/preamble, edit paper.tex only. Do not rewrite whole files.

HARD RULES:
- Never change any text inside any obj_id-carrying frozen formal environment: `\begin{theoremv}{...}`/`\end{theoremv}`, `\begin{assumptionv}{...}`/`\end{assumptionv}`, `\begin{lemmav}{...}`/`\end{lemmav}`, `\begin{definitionv}{...}`/`\end{definitionv}`, `\begin{citedv}{...}`/`\end{citedv}`, `\begin{propositionv}{...}`/`\end{propositionv}`, or `\begin{remarkv}{...}`/`\end{remarkv}`. If an error points inside one, fix it by adjusting the preamble (packages/macros), never the body.
- No content edits, no rewording, no reflowing of prose; mechanical fixes only (missing packages, unescaped characters outside frozen blocks, broken refs, math-mode errors in YOUR prose).
- Never patch only paper.tex when the faulty text came from front_matter.tex, sections/*.tex, or proofs/*.tex; that is a non-durable repair.

REQUIRED OUTPUT FORMAT: after the file edits are complete, emit exactly the single line `FIXED` on stdout — no JSON, no commentary, no LaTeX.
