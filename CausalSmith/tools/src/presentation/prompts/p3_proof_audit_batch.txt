You are auditing whether a BATCH of prose appendix proofs faithfully render the Lean proofs they claim to render. Judge each proof INDEPENDENTLY and with full care — the batch is a packaging convenience, not a license to skim.

Proofs under audit:
{{proofs_block}}

Notation table (paper notation ↔ Lean identifiers — apply it before judging a mismatch):
{{notation_table}}

Calibration (asymmetric, mirrors the statement gate): the prose may phrase displays in the PAPER'S notation — including its frozen-statement form of the same lemma (e.g. conditional-expectation framing that Lean encodes by passing fixed nuisances as function arguments, clamped denominators written with the paper's subscript convention, named differences like δ-terms the paper defines). Apply the notation table and the lemma's own paper statement before judging; flag a mismatch only when the prose asserts mathematical CONTENT the Lean proof does not deliver (wrong sign after translation, a bound or case the Lean route lacks, an attribution to a helper that does not do what the prose says).

Check, per proof, step by step:
1. Does every tagged prose step correspond to a real piece of the Lean proof (its `% lean:` tag exists and does what the prose says)? Read the Lean file with your tools; do not guess.
2. Are the case splits and intermediate bounds the SAME as in Lean (not a classical argument that happens to reach the same conclusion)?
3. Is anything in the Lean proof's load-bearing structure silently omitted (collapsed bookkeeping is fine; skipped case splits are not)?

Output exactly one JSON object with one entry PER proof (every obj_id present):
{"results": [{"obj_id": "<id>", "verdict": "faithful" | "unfaithful", "issues": ["<each concrete mismatch, with the prose step>"]}]}
Default to unfaithful when uncertain.
