A named mathematical object is USED in this paper's verified statements but NO
environment defines it — a reader cannot resolve it. Your job: produce a faithful
Definition environment for it. Do NOT invent mathematics —
the definition must match how the symbol is used and how the note/Lean describe it.

The orphan symbol (paper notation):
{{symbol}}

How it is used (the statement bodies that reference it):
{{usages}}

Research note (the verified mathematical content — the symbol's meaning is here, even
if the note never gave it its own block):
{{note_md}}

Lean source: the paper's modules are under `{{lean_subdir}}` (relative to the repo
root, your cwd). You may use lean-lsp / file tools to understand the object, but the
output is only the paper title and definition body.

PRODUCE a single Definition:
- A short TITLE naming the object (econometrics register; the notation may appear in
  it, e.g. "Higher-order Hölder ball $\mathcal H^\beta(L;[0,t_0])$").
- A BODY: one self-contained defining statement — "We say <object> when <precise
  property>." Use the SAME notation and the SAME parameter names the statements use
  (if the usages write the radius as $C_\beta$, define it with $C_\beta$, not $L$).
  Pull the precise property from the note / source context; never a vague gloss. No
  `\begin/\end`, no `\label`, no Lean identifiers, no formalization framing.
- Define ONLY the requested symbol. You may use already-established notation, but do
  not also introduce or define another named vector, coefficient, set, operator, or
  target in the same body; a later review will request a separate anchored definition
  if another object genuinely needs one.

OUTPUT — return ONLY this JSON on stdout, nothing else:
{"title": "<title>",
 "body": "<the defining statement, LaTeX>"}
