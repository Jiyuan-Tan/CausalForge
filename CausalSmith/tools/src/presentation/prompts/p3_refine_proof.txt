You are refining ONE appendix proof of an econometrics working paper so its PROSE faithfully describes the machine-verified Lean proof of the same result. The Lean proof is the ground truth: it type-checks, so every step the paper proof states must correspond to something the Lean proof actually establishes — and the paper proof must NOT claim more (an extra step, a stronger conclusion, a quantifier/constant/scope the Lean does not carry).

Object: {{obj_id}}

Current paper proof (LaTeX — refine ONLY this prose):
{{proof_tex}}

Lean proof (ground truth — open it with your tools to see exactly what is proved and how):
{{lean_proof_source}}

Definitions the Lean references (their ACTUAL bodies — judge against THESE, never trust a name):
{{referenced_defs}}

What the proof audit flagged as unfaithful:
{{audit_issues}}

Notation table (use these symbols; introduce no new notation):
{{notation_table}}

Rules:
- The Lean proof is VERIFIED and is the ground truth. Rewrite the paper proof so every step it states is one the Lean proof establishes, and DELETE or CORRECT any step that asserts more than Lean proves — e.g. an expectation under the full law when the Lean stops at a marginal/integral; an infimum/supremum the Lean never takes; a membership or availability claim absent from the Lean conclusion; a constant or factor the Lean premise does not carry. Match constants, quantifiers, and scope to the Lean exactly.
- RESTRUCTURE freely toward the Lean — drop a spurious step, reorder, retitle, weaken an over-stated claim down to what is proved. This is restructuring TOWARD the verified proof; never the reverse (never make the prose claim something the Lean does not prove just to look complete or to satisfy the audit).
- Keep it a REAL proof narrative: the retained steps must still form a readable argument a reader can follow to the conclusion — do not gut it to a vacuous stub. If the Lean establishes the result, describe that argument honestly.
- SELF-CONTAINED journal prose: no Lean identifiers (camelCase, field projections), no meta-references to the formalization ("the Lean proof", "machine-checked", "the formal proof"). Write mathematics; Lean belongs only in a trailing `% lean: <decl>` comment if the proof already carries one.
- If the proof already faithfully matches the Lean (the flag was spurious), return it unchanged with changed=false.

REQUIRED OUTPUT: a single JSON object on stdout and nothing else:
{"refined_proof": "<the refined LaTeX proof>", "changed": true|false, "note": "<one line: what was corrected, or why unchanged>"}
