=== REVISE MODE (targeted, but you MAY re-render freely) ===
Your prior scaffold from the previous attempt is ALREADY ON DISK (files listed below).
The CORRECTION block above names the items to change. You have FREEDOM to fix them
PROPERLY — getting the statement faithful matters more than keeping the diff small:

- Prefer a targeted Read + Edit of the flagged declarations plus any that must change to
  stay consistent. But you are NOT restricted to a minimal in-place diff: when faithfully
  realizing the correction needs a DIFFERENT encoding — a faithful EQUIVALENT formulation,
  new auxiliary defs, or splitting/restructuring the flagged declaration — DO IT. F2.5
  re-reviews everything you emit, so a bolder re-render that is correct beats a timid tweak
  that is not. F2.5 reviews only the resulting dirty/not-cleared frontier; it does
  not re-review unrelated declarations that retain their accepted statement hash.
- Leave declarations already correct and UNRELATED to the correction alone; don't churn or
  reformat them gratuitously. The goal is a faithful fix, not a rebuild of passed items.
- MAXIMALLY REUSE EXISTING PROOFS. When you change a statement, adapt the existing proof
  body (rename a binder, re-thread a moved hypothesis, adjust a rewrite) so parts that still
  go through stay proved. Only the genuinely-new obligation may be left as `sorry`; never
  replace a working proof with `sorry` wholesale, and never `sorry` a declaration the change
  did not affect. Hand the proof loop the smallest possible new gap.
- For a reuse correction, build on the named `reuse` decl (verify its signature with
  `lean_hover_info`) instead of re-deriving; if you change a node's disposition, sync it back
  into plan.json.
- When one core node is rendered as SEVERAL Lean decls (a primary plus companions), tag
  EXACTLY ONE (the primary) with `-- @node: <id>`; leave companion decls untagged.
- After editing, run lean_diagnostic_messages on each CHANGED file (sorry-only OK).
- Return the SAME artifacts list and theorems manifest as the prior attempt.
=== END REVISE MODE ===
