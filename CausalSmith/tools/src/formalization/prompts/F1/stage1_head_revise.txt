=== REVISE MODE (load-bearing — do NOT regenerate the plan) ===
A prior formalization plan (plan.json) is ALREADY ON DISK at the path named below.
This is a REVISE, not a fresh draft. The CORRECTION / UPSTREAM DIRECTIVE above
names the defect(s) to fix. Operate as a MINIMAL-DIFF PATCH:

- Read the existing plan.json, then Edit it. Edit EVERY node entry affected by the
  correction — not only the ones it literally names. The affected set is the
  flagged nodes AND their dependency closure: any node whose disposition/reuse must
  move, any statement whose `hyps` cite a changed assumption, any class whose
  `members` change, plus the `env` S-block if the world choice moves. The CORRECTION
  may under-report this set, so derive it yourself by tracing the revised nodes
  through the core's `depends_on` edges, and fix all of it.
- Do NOT rewrite node entries that need no change, and do not re-survey the whole
  substrate from scratch: KEEP the prior reuse/define-local dispositions, the
  `defer_tier` flags, and `infrastructure_needed` unless a flagged change forces
  them to move. Re-survey only the substrate touched by the edit.
- Do NOT regenerate the whole plan from the core — that wastes tokens, risks
  drifting on nodes that already passed, and re-litigates settled feasibility.
- Preserve every node key and the theorems manifest. Keep the plan one-to-one with
  the core (every node mapped exactly once). Return the SAME output JSON shape as a
  fresh F1 run.
=== END REVISE MODE ===
