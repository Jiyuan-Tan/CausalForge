# stat_sa_plm_cumulant_converse — formalization note (bridge; rendered from core + plan)

_Auto-generated from the typed core + F1 plan. The structural source of truth is the formalization graph; this note is the human-readable / causalsmith bridge._

## Environment (S)

**S-1 (LOCAL analytic-transform / contour world (revised round 3)).** LOCAL analytic-transform / contour world (revised round 3): a two-family transform layer over the S1 law - the UNWEIGHTED transforms M, H_n, F_n, which are `ProbabilityTheory.complexMGF` instances, and the WEIGHTED transforms B_n(z) = E_P[b_n(X) e^{z D_n}], G_n(z) = E_P[Y e^{z Z_n}], which are locally defined weighted Bochner integrals; plus their empirical split-fold counterparts Fhat/Ghat and positively oriented circles with the associated logarithmic-derivative and ratio contour functionals. — **ROUND-19 CLOSURE EDIT (FLAG-radius-maxAbs-fit; read first).** THIS WORLD'S CERTIFIED SHADOW KEEPS EXACTLY THE ROUND-17 NODE AND THE ROUND-16 TANGENT; only the magnitude arithmetic attached to `bankRadiusRect` changes. The reused `ComplexRatInterval.maxAbs_le_of_contains_width` (Rectangles.lean:61-71) concludes `I.maxAbs ≤ C + w`, so `bankRadiusRect 𝔯_j ε` has maxAbs at most `U_{rho,j} + ε` and this world carries the explicit slack constant `U⁺_{rho,j} = U_{rho,j} + 1` with ε ≤ 1; the box on which the local `BoundedComplexMap` layer is certified is the rational square `[-(U_R + 2), U_R + 2]²`, and the `Subinterval` premise for `certifiedRadiusNode` is discharged by that same lemma applied directly to the node. THE MODULE SET IS UNCHANGED (`Complex.Rectangles` already carries the lemma, `Complex.Basic` its real-side companion), the layer distinction stays sharp - the EXACT `thetaC`, `NC` and `circleContourIntegral` values remain the mathematical layer and are never computed - and no symbol binding, disposition or module moves. HISTORICAL (ROUND 17, standing except for those constants): **ROUND-17 CLOSURE EDIT (FLAG-radius-type-fit; read first). THIS IS AGAIN THE S-BLOCK CARRYING THE CERTIFIED CONTOUR SHADOW AND THE CCIA/Complex MODULE SET (S1 is the observed-data PLM law world and imports no certified-arithmetic module).** This world's bank radii `rho_j = R0 + 1/4 + q·2^{-m_star}` are CERTIFIED-REAL, not rational, so the reused `circleNode (radius : ℚ)` (CircleSchedule.lean:138), whose `circleNode_sound` (:401) certifies only `exactCircleNode (radius : ℚ)`, CANNOT carry them; the round-16 spelling `circleNode ρ` is withdrawn from this world, as is `ComplexRatInterval.point ρ` (that decl is `point (x y : ℚ)`). The certified shadow's circle node is now the LOCAL `certifiedRadiusNode 𝔯_j ε schedule k := (bankRadiusRect 𝔯_j ε).mul (circleNode 1 schedule k)` with `bankRadiusRect 𝔯 ε := ⟨CertifiedReal.refine 𝔯 ε, RatInterval.point 0⟩`, sound and width-bounded from the REUSED `CertifiedReal.refine_contains`/`refine_width`, `RatInterval.point_sound`, `ComplexRatInterval.mul_sound`/`mul_width`/`mul_maxAbs` and `circleNode_sound`/`circleNode_width_at_selected_precision` AT RADIUS 1; the round-16 tangent correction is unchanged and merely respelled, `tangentNode 𝔯_j ε prec schedule k := (twoPiIRect prec).mul (certifiedRadiusNode 𝔯_j ε schedule k)`, so each mesh node still encloses `f (circleMap 0 ρ_j u) * circleTangent ρ_j u` and still type-fits `CircleMesh.integralEnclosure_sound`/`circleContourIntegral_enclosed`, whose `{r : ℝ}` already accepts the exact real radius. THIS WORLD'S MODULE SET IS UNCHANGED AND ALREADY SUFFICIENT: the wrapper needs only `Complex.CircleSchedule`, `Complex.Rectangles`, `Complex.Transcendental` and the top-level `API` (which carries `CertifiedReal.refine`/`refine_contains`/`refine_width` and `RatInterval.point`/`point_sound`), all already listed; nothing is added or removed, and the layer distinction stays sharp - the EXACT `thetaC`, `NC` and `circleContourIntegral` values remain the mathematical layer and are never computed. HISTORICAL (ROUND-16 CLOSURE EDIT, standing except for the node spelling just fixed): (FLAG-contour-tangent). THIS IS THE S-BLOCK THE DIRECTIVE MEANS BY 'S-1': S1 is the observed-data PLM law world and imports no certified-arithmetic module, while S3 is the world that carries the certified contour shadow and the CCIA/Complex module set.** The certified shadow of this world's contour functionals is now certified against the FULL parameterized integrand, not the bare quotient: the mesh node family is `((num/den).mul (tangentNode ρ schedule k))` with `tangentNode ρ schedule k = twoPiIRect prec |>.mul (circleNode ρ schedule k)`, so each node encloses `f (circleMap 0 ρ u) * circleTangent ρ u` and therefore type-fits `CircleMesh.integralEnclosure_sound` / `circleContourIntegral_enclosed`, whose `circleIntegrand` already contains `circleTangent r u = (2π i) r e^{2π i u}` (Mesh.lean:88-93). The round-15 family `(num/den).mul circleNode` did NOT realize the contour integral and that claim is deleted from this world. THIS WORLD'S MODULE SET IS UNCHANGED AND ALREADY SUFFICIENT: the tangent rectangle needs only `Complex.Transcendental` (the reused Machin `piInterval`/`piName`), `Complex.Rectangles` (`ComplexRatInterval.mul`/`conj`/`normInterval` with `mul_sound`/`mul_width`/`mul_maxAbs`) and `Complex.CircleSchedule` (`circleNode`/`circleNode_sound`/`circleNode_width_at_selected_precision`), all three of which are already listed; nothing is added or removed. Keep the layer distinction sharp exactly as before - the EXACT `thetaC`, `NC` and `circleContourIntegral` values remain the mathematical layer and are never computed - and note additionally that the certified enclosure is now of the UNNORMALIZED `∮ f dz`, with the `(2π i)^{-1}` / `(N_j 2π i)^{-1}` normalizations applied afterwards by guarded division against the `piInterval` lower endpoint. HISTORICAL (rounds 3-15, unchanged below): LOCAL analytic-transform layer: unweighted M, H, and F are Mathlib complexMGF instances; weighted B and G are local Bochner integrals. Circle counts and their argument-principle, homotopy, and Rouche facts are discharged by the promoted Causalean ArgumentPrincipleCircle modules, with no external-interface premise. ROUND-6 ADDITION (certified-layer closure): this world also carries the CERTIFIED shadow of its own objects - the symbols rho, Cj, J, aStar, Nhat, jhat and thetahatSpec it binds are produced by rational-interval computation, not by exact evaluation - so the two-layer split recorded in decisions.certified_arithmetic_split belongs here. The Causalean CCIA API is added to this layer's module set because its REAL interval algebra, `ComplexRatInterval` carrier, mesh/quadrature aggregation (`infEnclosure`, `supEnclosure`, `integralEnclosure`, `circleContourIntegral_enclosed`) and total finite search (`leastTrue`, `finiteRefine`, `measurable_finiteRefine`) are reused verbatim. ROUND-14 CORRECTION, superseding the rest of this clause: the sentence that followed - 'the COMPLEX rectangle algebra, modulus, guarded division and transcendental extensions those aggregators consume are the bounded local build L-3, since Causalean exports none of them' - IS FALSE AND IS WITHDRAWN. Causalean exports the PRIMITIVES, compiled and sorry-free, in the `Complex/` subtree of the same module (`ComplexRatInterval.mul/conj/normSq/div/normInterval`, `RatInterval.sq/sqrtInterval`, `Transcendental.piName`/`complexExp`/`complexExpName`, `TrigNames.sinNameApprox`/`cosNameApprox`, `Schedule`/`circleNode` - full inventory in `round14_complex_api_reuse_patch`), so this world's module set gains the Complex modules and the certified shadow's ARITHMETIC is REUSED rather than built. ROUND-15 CORRECTION to the preceding sentence's tail: the `CertifiedComplexMap`/`ContourProgram`/`evaluate`/`certified_contour_evaluation` PROGRAM layer is NOT reusable here and is dropped from this world - `CertifiedComplexMap` demands one global rational width amplification valid on every rectangle in ℂ, which the empirical exponential-sum transforms of this world do not have, and `certified_contour_evaluation` is a conditional theorem about a supplied program rather than a substrate. What this world uses instead, and what makes the certified shadow work, is the GENERIC quadrature `CircleMesh.integralEnclosure`/`integralEnclosure_sound`/`integralEnclosure_width`/`circleContourIntegral_enclosed` over an arbitrary node family (hence the `Mesh` and `Quadrature` modules), wrapped by the LOCAL bounded-domain evaluator that L-3 builds; `Complex.ContourProgram` and `Complex.Program` leave this module set, `Complex.ContourEvaluation` stays only for the generic `midpoint_error_le_half_width` and `inverseMaxTolerance`. L-3 is therefore the bounded-domain empirical-transform evaluator build (interface + combinators + guarded node family + containment/width theorems), not a `ContourProgram` adapter. Keep the distinction sharp when reading any node in this world: the EXACT objects (M, H, F, G, NC, thetaC, circleIntegral) are the mathematical layer and are never computed; the certified rectangles are their sound enclosures. ROUND-5 ADDITION (FLAG-wrong-decl closure): `Mathlib.Probability.Independence.Integration` is added to this layer's module set, because the ℂ-valued product-integral factorization that splits BOTH families across the independence eta ⟂ X - `ProbabilityTheory.IndepFun.integral_fun_comp_mul_comp`, which is stated over `[RCLike 𝕜]` and so instantiates at ℂ - is the layer's shared workhorse, used by lem:observable-factorization and by the weighted step of thm:known-zero-instrument. The formerly cited `Causalean.Mathlib.IndepIntegral` is NOT in this layer: it is real-valued and restricted-event only. ROUND-12 ADDITION (FLAG-missed-reuse closure): `Mathlib.Analysis.Complex.BorelCaratheodory` joins this layer's module set. It supplies the disk real-part-to-norm bound `Complex.borelCaratheodory_zero` used TWICE inside this world - by lem:zero-localization (with `Complex.norm_iteratedDeriv_le_of_forall_mem_sphere_norm_le` for the local Cauchy/coefficient corollary) and by the local helper L-2 (Harnack on -(h - h 0)) - so neither the theorem nor the Moebius+Schwarz argument behind it is rebuilt here. The world's disposition stays define-local: what is bespoke is the transform layer itself (the weighted B and G integrals and the certified shadow), not the classical complex-analysis inputs.
**required modules.** Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.API, Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.API, Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.CircleSchedule, Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ComplexExp, Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.ContourEvaluation, Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Rectangles, Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Complex.Transcendental, Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Mesh, Causalean.Mathlib.Analysis.CertifiedContourIntervalArithmetic.Quadrature, Mathlib.Analysis.Calculus.ParametricIntegral, Mathlib.Analysis.Complex.BorelCaratheodory, Mathlib.Analysis.Complex.CauchyIntegral, Mathlib.Analysis.Complex.Harmonic.Analytic, Mathlib.Analysis.Complex.Harmonic.MeanValue, Mathlib.Analysis.Complex.JensenFormula, Mathlib.Analysis.Complex.Schwarz, Mathlib.MeasureTheory.Integral.CircleIntegral, Mathlib.Probability.Independence.Integration, Mathlib.Probability.Moments.ComplexMGF

**S-2 (Published Jin-Mackey-Syrgkanis order-r ACE comparator int…).** Published Jin-Mackey-Syrgkanis order-r ACE comparator interface: the eligibility quantities of condition (21), the bound (22), the constant C_gamma, and the ACE estimator handle - all taken as an external cited interface, never reconstructed. — a1n, b1n, a2, b2n, BACE are literal closed-form real expressions (define-local, trivial), scaffolded as `jmsBound p n Cgamma delta` with BOTH the constant and the separation constant as explicit arguments. REVISED (round 11, DEFECT B closure). The previous entry made thetahatACE 'an abstract real-valued statistic VARIABLE', and F2 realized that as the abbrev `AceEstimator := forall (r n : Nat), (Nat -> Xspace -> Real) -> (Nat -> Xspace -> Real) -> Estimator n` (Helpers/JmsComparator.lean:19-21) with every consumer universally quantifying over an inhabitant of it. THAT IS THE DEFECT: the core's thetahatACE is not a variable ranging over all functions of that shape, it is ONE named published procedure - 'the order-r ACE estimator of Jin-Mackey-Syrgkanis Theorem 5.4' - and quantifying over the type lets a consumer instantiate at a degenerate estimator, which is what weakens the named ACE branch. CORRECTED ENCODING: keep `AceEstimator` as the TYPE ABBREV (it is a faithful signature and is still needed to type the handle), but fix ONE sealed inhabitant at this interface, `opaque thetaHatACE : AceEstimator`, declared once and shared by every consumer. `opaque` is the right primitive and is NOT laundering: the type is inhabited (the constant-zero estimator inhabits it), so the declaration is legal and introduces NO axiom, and it names the published procedure without reproducing or constraining its internals - which is exactly the provenance of a cited comparator. F2 MUST NOT emit it as an `axiom`, an unbuilt Prop binder, a typeclass without an instance, or a `variable` that consumers re-quantify; and F2 MUST NOT construct the ACE algorithm (that would be reconstructing a cited node). Equivalent fallback if F2 prefers a record: ONE `structure JmsAceComparator` with a `thetaHatACE` field, instantiated once - but the two cited Props must still be Props ABOUT that single fixed handle, never over a quantified estimator argument. Cgamma likewise stops being an existential witness and becomes an explicit real parameter of both cited Props (see those two nodes). SEARCH TRACE: `lean_local_search` for ACE/orthogonal-moment estimators + ../doc/API.md 12g (`Estimation/OrthogonalMoments/`) - Causalean's DML/orthogonal-moment framework builds first-order Neyman-orthogonal scores, not the arbitrary-order ACE estimator of JMS Section 5; not found, and irrelevant since the node is cited.
**required modules.** Mathlib.Analysis.SpecialFunctions.Log.Basic

**S-3 (Observed-data partially linear conditional-mean law on a…).** Observed-data partially linear conditional-mean law on a general measurable covariate space, with a deterministic externally supplied and clipped code pair. One observation O = (X, T, Y) with X : Xspace (a general MeasurableSpace), T Y : ℝ; a law P : Measure (Xspace × ℝ × ℝ); the residual functionals g0 = E_P[T|X], q0 = E_P[Y|X], eta = T - g0 X, xi = Y - q0 X - theta0 * eta; the clipped codes bargn = proj [-Cg,Cg] ∘ gcode n, barqn = proj [-Cq,Cq] ∘ qcode n; and the derived D = g0 - bargn, Z = T - bargn X, b = q0 - theta0 * D. — SEARCH TRACE (bypass-justified, per the substrate-survey rule): surveyed ../doc/API.md sections 9g''' (`PO/ID/Exact/PartialLinear/{Setup,Identification}.lean`: POPartialLinearSystem, POPartialLinearModel, robinson_estimand_eq_theta) and 13plr (`Estimation/PLR/*`: PLRSystem, plrGeneralMoment, plr_remainder_bound), plus 13sa (`Estimation/MinimaxATE/Model.lean`: Obs, ValidDGP, InClass, productLaw, minimaxMiss) and 9i0 (`PO/Analysis/Regression.lean`). Result: no fit at this abstraction. POPartialLinearModel/PLRSystem are single-model PO objects (dose-response Y(d), backdoor mean-independence on a fixed Omega, one fixed theta, nondegeneracy E[(D-m0)^2] != 0) and cannot index a triangular CLASS of laws whose member functional theta0(P) varies; MinimaxATE.Model is hard-wired to a finite covariate `Fin M`, Bool treatment/outcome and the ATE functional. bypass-justified: scaffold S1 over Mathlib measure theory. Note the exact Luxemburg psi_2 convention required by ass:eta-subgaussian is NOT Mathlib's `ProbabilityTheory.HasSubgaussianMGF` convention (MGF-based) - checked `lean_local_search` "cgf"/"complexMGF" and `Mathlib.Probability.Moments.SubGaussian`; a PARTIAL fit only, so it is defined locally and threaded, not discharged.
**required modules.** Mathlib.MeasureTheory.Measure.MeasureSpace, Mathlib.Probability.ConditionalExpectation, Mathlib.Probability.Distributions.Gaussian.Real, Mathlib.Probability.Moments.Basic

**S-4 (Class-indexed decision-theoretic world).** Class-indexed decision-theoretic world: sets of laws (the broad spectral class, the source-stated Gaussian intersection, the published ACE class, the ACE-comparison subclass), estimators as measurable maps (Fin n -> Obs) -> ℝ, and the squared-error / generalized-quantile minimax losses over each class. — SEARCH TRACE: surveyed ../doc/API.md section 12w (Stat/Minimax/) and 13sa (Estimation/MinimaxATE/). Confirmed in source: `Causalean.Stat.tvDist` (Stat/Minimax/TotalVariation.lean:49), `half_one_sub_tvDist_le_max_error` (LeCam.lean:75), `real_two_point_lower_bound` / `two_point_lower_bound_of_tvDist_le` / `iid_two_point_lower_bound` (MinimaxRisk.lean:58,68,81), `PinskerBound` / `pinskerBound_pi_iid` / `klForm_two_point_lower_bound` (Pinsker.lean:251,442,483), `Causalean.Stat.quantile` (Stat/Quantile/Quantile.lean:50). The Q_{P,1-gamma} generalized quantile IS `Causalean.Stat.quantile (Measure.map W P) (1-gamma)` - reused verbatim. NO generic class-indexed minimax-risk functional exists (MinimaxATE.minimaxMiss is hard-wired to the finite-X ATE model), so the sup/inf risk defs are define-local, but every converse step routes through the reused Le Cam / Pinsker chain. ROUND-5 NOTE (FLAG-finite-vs-stream closure): this world's estimator type `(Fin n -> Obs) -> ℝ` was already finite and is UNCHANGED; what changes is that the sample law it is scored against is now pinned to the matching finite product `Measure.pi (fun _ : Fin n => P)` (see the re-planned S2 and def:minimax-risks). That alignment is what makes the reused converse decls apply on the nose: `iid_two_point_lower_bound` (MinimaxRisk.lean:81-89) is stated for precisely this estimator type and this product law, and `pinskerBound_pi_iid` (Pinsker.lean:442-445) for the same product. ROUND-21 CLOSURE (FLAG-product-KL-reuse): the converse chain in this world is corrected to route the PRODUCT-DIVERGENCE VALUE through `Causalean.Mathlib.InformationTheory.productKL_tensorization_of_finite` (ProductKLLeCam.lean:379-389), which gives `(klDiv (Measure.pi fun _ : Fin n => mu) (Measure.pi fun _ : Fin n => nu)).toReal = n * (klDiv mu nu).toReal` from the SAME one-sample AC/llr-integrability inputs that `pinskerBound_pi_iid` already consumes. [ROUND-22, FLAG-product-KL-precondition: those one-sample inputs are the JOINT tilted ONE-OBSERVATION facts `P_{n,v0} << P_{n,v1}` and `Integrable (llr P_{n,v0} P_{n,v1}) P_{n,v0}` on `Obs = Xspace x R x R` - NOT the Gaussian outcome-fibre facts, which alone do not typecheck as these hypotheses. They are discharged by the explicit compProd/prodAssoc transport recorded in lem:non-gaussian-hard-submodel, and that transport must run BEFORE every use of `productKL_tensorization_of_finite` or `pinskerBound_pi_iid` in this world. No S4 world choice, symbol binding, module, disposition or defer_tier changes.] `pinskerBound_pi_iid` is RETAINED for the finite-product Pinsker step but is NOT a source of a numeric product-KL bound, so the earlier reading that it removes any need for tensorisation is withdrawn; the module list gains `Causalean.Mathlib.InformationTheory.ProductKLLeCam`. This world's `world`, `binds_symbols`, `binds_sampling_model`, `disposition`, `reuse`, `defer_tier` and `gate` are UNCHANGED, and the estimator type `(Fin n -> Obs) -> Real` and finite product law `Measure.pi (fun _ : Fin n => P)` are exactly the ones the tensorisation decl is stated over, so the chain still composes with NO transport step.
**required modules.** Causalean.Mathlib.InformationTheory.ProductKLLeCam, Causalean.Stat.Minimax.LeCam, Causalean.Stat.Minimax.MinimaxRisk, Causalean.Stat.Minimax.Pinsker, Causalean.Stat.Minimax.TotalVariation, Causalean.Stat.Quantile.Quantile

**S-5 (Benchmark and local-to-Gaussian world).** Benchmark and local-to-Gaussian world: the explicit symmetric-mixture / Gaussian-Rademacher treatment-noise paths, the shrinking cumulant-separation sequence, and the sought adaptive estimator handle. — SEARCH TRACE: `lean_local_search` "complexMGF" -> `ProbabilityTheory.complexMGF_gaussianReal` and `cgf_gaussianReal` (Mathlib/Probability/Distributions/Gaussian/Real.lean) give the Gaussian factor of eta_a = sqrt(1-a^2) G + a S exactly; the Rademacher factor is an explicit two-point law (`Causalean.Mathlib.Probability.bernoulliLaw`, ../doc/API.md 10a''''''' ) whose MGF is cosh. REVISED (round 11, DEFECT A closure): the previous entry called thetahatAd 'an existential handle inside the open question'. It is no longer existentially quantified anywhere, because the open question is no longer an asserted existential Prop. thetahatAd is a RESERVED SYMBOL: the core says 'The symbol thetaHatAd is reserved for a future estimator satisfying the desiderata in oeq:local-to-gaussian-frontier; that estimator and its performance are unresolved.' So it is bound here by NAME ONLY - it appears in the docstring prose of the question record and nowhere else, carries NO construction, NO type-level witness, NO existential binder and NO defining equation, and F2 must NOT emit any declaration whose statement quantifies over it. deltaSeq and etaSeq keep their round-9 status: deltaSeq is the positive shrinking separation sequence used by the LOCAL benchmark branch of thm:local-to-gaussian-partial-benchmarks (where it is a genuine parameter of a PROVED result), while etaSeq is descriptive only and is not pinned to any exact law.
**required modules.** Mathlib.Analysis.SpecialFunctions.Trigonometric.Basic, Mathlib.Probability.Distributions.Gaussian.Real

**S-6 (FINITE i.i.d. inference sample of size n).** FINITE i.i.d. inference sample of size n: the data law is the n-fold product `Measure.pi (fun _ : Fin n => P)` on the sample space `Fin n -> Obs`, with the deterministic two-fold index split I0 = {i : Fin n | (i : ℕ) < n / 2}, I1 = I0ᶜ as a `Finset (Fin n)` pair, drawn after the code pair is fixed. — RE-PLANNED (round 5, FLAG-finite-vs-stream). The previous entry set `disposition:"reuse", reuse:"Causalean.Stat.IIDSample"`. That is a TYPE MISMATCH and is withdrawn: re-confirmed this round at Causalean/Stat/Sample.lean:42-48, `structure IIDSample (Ω X : Type*) ... (μ : Measure Ω) (P : Measure X)` carries `Z : ℕ → Ω → X` with `indep : iIndepFun Z μ`, `identDist : ∀ i, IdentDistrib (Z 0) (Z i) μ μ`, `law : μ.map (Z 0) = P` - a COUNTABLY INDEXED stream of coordinate maps on an abstract ambient Ω, and its only lemma is `IIDSample.map_eq` (Sample.lean:62); the advertised `IIDSample.integral_comp_eq` / `IIDSample.iIndepFun_comp` do not exist. This core binds only a FINITE `Fin n` sample (symbols i, I0, I1, and the estimator domain of def:adaptive-contour-estimator), and finite i.i.d. coordinates do not construct a ℕ-indexed stream without an unplanned extension (one would have to invent laws for the indices n, n+1, ... and a carrier Ω, which the note never supplies). S2 is therefore a FINITE-SAMPLE LOCAL interface: the sample space is `Fin n -> Obs` and the data law is `Measure.pi (fun _ : Fin n => P)`, with the i.i.d. content supplied by ass:iid-sampling and derived from the product law rather than assumed on a stream. NO finite-to-stream bridge is added; none is needed. THE PRODUCT-LAW TRANSPORT LEMMAS ARE STILL REUSED, and in fact fit BETTER than the stream would, because every Causalean converse/concentration decl this plan consumes is already stated over `Measure.pi (fun _ : Fin n => ·)` or over a bare measure - all re-confirmed in source this round: `Causalean.Stat.iid_two_point_lower_bound` (Stat/Minimax/MinimaxRisk.lean:81-89) takes `{est : (Fin n → S) → ℝ}` against `Measure.pi fun _ : Fin n => P₀/P₁`; `Causalean.Stat.pinskerBound_pi_iid` (Stat/Minimax/Pinsker.lean:442-445) produces `PinskerBound (Measure.pi fun _ : Fin n => μ) (Measure.pi fun _ : Fin n => ν)` from the ONE-SAMPLE marginals; `Causalean.Mathlib.IIDCenteredSum.pi_centered_sum_sq_lintegral_le` (Causalean/Mathlib/IIDCenteredSum.lean:183-190) is stated verbatim over `Measure.pi P` on a `Fintype ι`; and the Chebyshev bound `Causalean.Stat.Concentration.probability_abs_sub_mean_gt_le` (Chebyshev.lean:26-32) is index-free - `(Q : Measure Ω) [IsFiniteMeasure Q] (F : Ω → ℝ)` - so it applies to `Q = Measure.pi (fun _ : Fin n => P)` and `F` the empirical mean with no sample structure at all. The two coordinate facts F3 needs from the product law are Mathlib's, both confirmed this round: `ProbabilityTheory.iIndepFun_pi` (Mathlib/Probability/Independence/Basic.lean:784) gives joint independence of the coordinate evaluations, and `MeasureTheory.measurePreserving_eval` (Mathlib/MeasureTheory/Constructions/Pi.lean), whose `.map_eq` gives `Measure.map (Function.eval i) (Measure.pi _) = P`, gives identical distribution. SEARCH TRACE for a finite i.i.d. sample structure to reuse instead: `lean_local_search` "IIDSample" (empty in the CausalSmith index) then direct grep `structure IIDSample` under ../Causalean -> the single ℕ-indexed hit above; grepped ../doc/API.md 12w/13sa and `Causalean/Stat/Sample.lean` for a `Fin n` sample structure - none exists (Causalean's OneShotSplit is a cross-fitting object with nuisance re-estimation, not needed here since the codes are external and deterministic). Mathlib-side check: `lean_local_search` "iIndepFun_pi" / "measurePreserving_eval" - Mathlib has the product-law primitives but no packaged finite-sample structure. NOT FOUND; define-local is a thin wrapper (`abbrev SampleLaw n P := Measure.pi (fun _ : Fin n => P)` plus the two coordinate lemmas), not a rebuild.
**required modules.** Causalean.Mathlib.IIDCenteredSum, Causalean.Stat.Concentration.Chebyshev, Causalean.Stat.Concentration.TailBounds.Hoeffding, Mathlib.MeasureTheory.Constructions.Pi, Mathlib.Probability.Independence.Basic

## Assumptions (A)

**A-1 (assumption).** \(O_1,\ldots,O_n\stackrel{\mathrm{iid}}{\sim}P\).

**A-2 (assumption).** \(\eta\perp X\).

**A-3 (assumption).** \(E_P[\xi\mid X,T]=0\) almost surely.

**A-4 (assumption).** \(|\theta_0|\le C_{\theta}\).

**A-5 (assumption).** \(\|g_0\|_{\infty}\le C_g\).

**A-6 (assumption).** \(\|q_0\|_{\infty}\le C_q\).

**A-7 (assumption).** \(|Y|\le C_q\) almost surely.

**A-8 (assumption).** With the exact Luxemburg convention \[ \lVert W\rVert_{\psi_2} =\inf\{u>0:E_P[\exp(W^2/u^2)]\le2\}, \] the treatment noise satisfies \(\lVert\eta\rVert_{\psi_2}\le\psi_{\eta}\).

**A-9 (assumption).** \(\|\xi\|_{\psi_2\mid X}\le\psi_{\xi}\) almost surely.

**A-10 (assumption).** \(|\kappa_k(\eta)|\ge\delta\).

**A-11 (assumption).** For every \(n\), \(\|\bar g_n-g_0\|_{L^s(P_X)}\le\varepsilon_{1,n}\).

**A-12 (assumption).** For every \(n\), \(\|\bar q_n-q_0\|_{L^s(P_X)}\le\varepsilon_{2,n}\).

**A-13 (assumption).** For every \(n\), \(\|\bar g_n-g_0\|_{L^r(P_X)}\le\varepsilon_{1,n}\).

**A-14 (assumption).** For every \(n\), \(\|\bar q_n-q_0\|_{L^r(P_X)}\le\varepsilon_{2,n}\).

**A-15 (assumption).** \(\eta\sim N(0,\sigma^2)\).

**A-16 (assumption).** For every \(n\), \(\|\bar g_n-g_0\|_{L^1(P_X)}\le\varepsilon_{1,n}\).

## Definitions (P)

**P-1 (…).** \(\mathcal P_{\mathrm{NG},n}=\{P: \eta\perp X,\ E_P[\xi\mid X,T]=0,\ |\theta_0|\le C_{\theta},\ \|g_0\|_{\infty}\le C_g,\ \|q_0\|_{\infty}\le C_q,\ \|\eta\|_{\psi_2}\le\psi_{\eta},\ \|\xi\|_{\psi_2\mid X}\le\psi_{\xi},\ |\kappa_{r+1}(\eta)|\ge\delta,\ \|\bar g_n-g_0\|_{L^1(P_X)}\le\varepsilon_{1,n}\}\), where \(g_0,q_0,\eta,\xi\) are the displayed residual functionals of \(P\). No restriction involving a supplied outcome code is imposed.

**P-2 (…).** \(\mathcal P_{\mathrm G,n}^{\mathrm{JMS}}=\{P:E_P[\xi\mid X,T]=0,\ |\theta_0|\le C_{\theta},\ |Y|\le C_q,\ \eta\perp X,\ \eta\sim N(0,\sigma^2),\ \|g_0\|_{\infty}\le C_g,\ \|\bar g_n-g_0\|_{L^s(P_X)}\le\varepsilon_{1,n},\ \|\bar q_n-q_0\|_{L^s(P_X)}\le\varepsilon_{2,n}\}\), with \(g_0,q_0,\eta,\xi\) given by their displayed residual definitions.

**P-3 (…).** \(\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}=\{P:E_P[\xi\mid X,T]=0,\ |\theta_0|\le C_{\theta},\ \|g_0\|_{\infty}\le C_g,\ \|q_0\|_{\infty}\le C_q,\ \|\eta\|_{\psi_2}\le\psi_{\eta},\ \|\xi\|_{\psi_2\mid X}\le\psi_{\xi},\ \eta\perp X,\ |\kappa_{r+1}(\eta)|\ge\delta,\ \|\bar g_n-g_0\|_{L^r(P_X)}\le\varepsilon_{1,n},\ \|\bar q_n-q_0\|_{L^r(P_X)}\le\varepsilon_{2,n}\}\), with the residual functionals defined from \(P\).

**P-4 (Given a zero \(z_0\) of \(M\) with multiplicity \(\ell\),…).** Given a zero \(z_0\) of \(M\) with multiplicity \(\ell\), set \(J_{z_0,\ell}(w)=w^{\ell-1}e^{z_0w}\).

**P-5 (For a positively oriented circle \(C_j\) on which \(F_n\)…).** For a positively oriented circle \(C_j\) on which \(F_n\) is zero-free and \(N_{C_j}(F_n)\ge1\), set \(\Theta_{C_j}(F_n,G_n)=\{N_{C_j}(F_n)2\pi i\}^{-1}\oint_{C_j}G_n(z)/F_n(z)\,dz\).

**P-6 (…).** \(\mathfrak R_n^{\mathrm{NG}}=\inf_{\widetilde\theta(O_1,\ldots,O_n;\bar g_n,\bar q_n)}\sup_{P\in\mathcal P_{\mathrm{NG},n}}E_P[(\widetilde\theta-\theta_0(P))^2]\), \(\mathfrak R_n^{\mathrm G}=\inf_{\widetilde\theta(O_1,\ldots,O_n;\bar g_n,\bar q_n)}\sup_{P\in\mathcal P_{\mathrm G,n}^{\mathrm{JMS}}}E_P[(\widetilde\theta-\theta_0(P))^2]\), and \(\mathfrak M_{n,1-\gamma}^{\mathrm G}=\inf_{\widetilde\theta}\sup_{P\in\mathcal P_{\mathrm G,n}^{\mathrm{JMS}}}Q_{P,1-\gamma}(|\widetilde\theta-\theta_0(P)|)\).

**P-7 (Compute \(a_{1,n},b_{1,n},a_2,b_{2,n}\) exactly as in con…).** Compute \(a_{1,n},b_{1,n},a_2,b_{2,n}\) exactly as in condition (21) and set \(\mathsf E_n^{\mathrm{JMS}}\) to the condition \(r\le\min\{b_{1,n}/a_{1,n}-a_{1,n}^{-1}\log(b_{1,n}/a_{1,n}),b_{2,n}/[a_2\log(a_2b_{2,n})]\}\) on indices for which these expressions are defined.

**P-8 (…).** \(\widehat\theta_{\sin,n}=\Pi_{[-C_{\theta},C_{\theta}]}\{\mathbb P_n[Y\sin(\pi Z_n/2)]/\mathbb P_n[Z_n\sin(\pi Z_n/2)]\}\), with fallback zero when the empirical denominator is below one half of its population lower bound.

**P-9 (For each library depth \(m\), let \(\mathscr C_m\) be a f…).** For each library depth \(m\), let \(\mathscr C_m\) be a finite deterministic library of rational circles, increasing with \(m\). On prespecified independent folds, form the empirical transforms \(\widehat F_{a,n}\), \(\widehat G_{a,n}\), and their derivatives. For a circle \(C=\{\rho e^{it}:0\le t\le2\pi\}\) on which the empirical denominator is certified positive, the candidate record may contain only quantities definitionally computed from the actual empirical contour integrands \[ W_{a,C}(t)=\rho e^{it} \frac{\widehat F'_{a,n}(\rho e^{it})} {\widehat F_{a,n}(\rho e^{it})}, \qquad V_{a,C}(t)=\rho e^{it} \frac{\widehat G_{a,n}(\rho e^{it})} {\widehat F_{a,n}(\rho e^{it})}. \] Specifically, it may record the boundary modulus \(\inf_{z\in C}|\widehat F_{a,n}(z)|\), the winding enclosure for \((2\pi)^{-1}\int_0^{2\pi}W_{a,C}(t)\,dt\), the contour-moment enclosure for \((2\pi)^{-1}\int_0^{2\pi}V_{a,C}(t)\,dt\), and the angular variance \[ \widehat v_{a,C} =\frac1{2\pi}\int_0^{2\pi} \left|V_{a,C}(t)- \frac1{2\pi}\int_0^{2\pi}V_{a,C}(u)\,du\right|^2dt. \] These records constitute candidate data only. No rule selecting among DML, finite-order ACE, and contour moments, no contribution formula beyond the displayed empirical integrands, no argmin or tie-breaking prescription, and no risk, coverage, critical-scaling, or lower-bound guarantee is defined. The symbol \(\widehat\theta_{\mathrm{ad}}\) is reserved for a future estimator satisfying the desiderata in \(\text{oeq:local-to-gaussian-frontier}\); that estimator and its performance are unresolved.

**P-10 (…).** \(\mathcal P_{\mathrm{NG},n}^{\mathrm{ACE}}=\{P\in\mathcal P_{\mathrm{NG},n}:\|\bar g_n-g_0\|_{L^s(P_X)}\le\varepsilon_{1,n},\ \|\bar q_n-q_0\|_{L^s(P_X)}\le\varepsilon_{2,n}\}\). Thus this comparison class restores both supplied-code restrictions while retaining every condition of the broad spectral class.

**P-11 (The paper-independent bounded-build obligation…).** The paper-independent bounded-build obligation \(\mathsf{CCIA}_{\mathrm{build}}\) is the following finite implementation specification. A certified real record consists of rational closed intervals \(I_m=[I_m^-,I_m^+]\), an executable modulus \(\mu:\mathbb Q_{>0}\to\mathbb N\), and the semantic contract \[ x\in I_{m+1}\subseteq I_m, \qquad I_{\mu(e)}^+-I_{\mu(e)}^-\le e. \] A positive record additionally supplies a rational \(p>0\) with \(p\le x\). Executable code may inspect only the interval, modulus, and positivity fields. It may not inspect a semantic exact-real value. For rational intervals \(A=[a_-,a_+]\) and \(B=[b_-,b_+]\), define \[ A+B=[a_-+b_-,a_++b_+], \qquad A-B=[a_--b_+,a_+-b_-], \] \[ A B= [\min\{a_-b_-,a_-b_+,a_+b_-,a_+b_+\}, \max\{a_-b_-,a_-b_+,a_+b_-,a_+b_+\}]. \] A complex rectangle is \(A+iB\). Complex addition, subtraction, conjugation, and multiplication are implemented by these endpoint operations and \[ (A+iB)(C+iD)=(AC-BD)+i(AD+BC). \] For \(A=[a_-,a_+]\), put \[ \operatorname{sq}_{-}(A)= \begin{cases} 0,&a_-\le0\le a_+,\\ \min\{a_-^2,a_+^2\},&\text{otherwise}, \end{cases} \qquad \operatorname{sq}_{+}(A)=\max\{a_-^2,a_+^2\}. \] Thus \(|A+iB|^2\) is enclosed by the rational interval obtained by adding the corresponding square bounds. Rational bisection between nonnegative rational endpoints gives lower and upper square-root enclosures: after \(q\) bisections their width is at most the initial width times \(2^{-q}\). This supplies certified modulus lower and upper bounds. Division is the guarded operation \[ \frac{A+iB}{C+iD} =(A+iB)(C-iD)(C^2+D^2)^{-1}, \] and is executed only when the modulus routine has returned a rational lower bound \(m>0\); outward division by the positive rational interval enclosing \(C^2+D^2\) is then sound. The certified name of \(\pi\) is constructed, rather than postulated, from Machin's identity \[ \pi=16\arctan(1/5)-4\arctan(1/239). \] For \(0<x<1\), the first \(N+1\) terms of \[ \arctan x=\sum_{j=0}^{N}(-1)^j\frac{x^{2j+1}}{2j+1}+R_N(x), \qquad |R_N(x)|\le\frac{x^{2N+3}}{2N+3}, \] give rational nested enclosures after intersecting the first \(m+1\) requested enclosures. If \(e=p/q>0\) is in lowest terms, the explicit choice \(N=5q+5\) makes the propagated Machin remainder smaller than \(e\), so this is an effective modulus with finite fuel. For a rational interval contained in \([-B,B]\), with rational \(B\ge0\), evaluate the Taylor polynomials for \(\exp\), \(\sin\), and \(\cos\) by the preceding interval operations. The Lagrange remainders are bounded respectively by \[ 3^{\lceil B\rceil}\frac{B^{N+1}}{(N+1)!}, \qquad \frac{B^{N+1}}{(N+1)!}, \qquad \frac{B^{N+1}}{(N+1)!}. \] For a requested rational error \(e=p/q>0\), take \[ N=\lceil2B\rceil+q+2\lceil B\rceil+2. \] After the index \(\lceil2B\rceil\), successive absolute Taylor terms decrease by at least a factor \(1/2\); since \(3^{\lceil B\rceil}<2^{2\lceil B\rceil}\) and \(2^{-q}\le q^{-1}\le e\), this displayed \(N\) gives a sound error at most \(e\). Finite intersections make the output rectangles nested. Hence \[ \exp(x+iy)=\exp(x)\{\cos y+i\sin y\} \] has a nested complex-rectangle extension with an effective modulus on every bounded input rectangle. Combining the constructed \(\pi\) name with these trigonometric operations gives, from a positive radius record \(\mathfrak r\) and integers \(N\ge1\), \(0\le q<N\), certified circle-node records for \[ r\exp(2\pi iq/N) \quad\text{and}\quad 2\pi i r\exp(2\pi iq/N). \] Finite rectangle sums and products are primitive recursive. If a complex-valued function on \([0,1]\) has a supplied rational Lipschitz bound \(L\), a uniform mesh with \[ N_{\mathrm{mesh}}=\lceil L/e\rceil+1 \] has oscillation at most \(e\) on each cell. Evaluating every node to a rational error budget divided by the displayed finite number of operations therefore yields sound finite infimum, supremum, and trapezoidal-integral enclosures. Their fuel consists of \(N_{\mathrm{mesh}}+1\) node calls, the displayed Taylor cutoffs, square-root bisection cutoffs, and a finite number of endpoint operations. Absolute value of a real interval is implemented by the square-free endpoint rule implicit in \(\operatorname{sq}_{-}\) and \(\operatorname{sq}_{+}\), and has the same compositional modulus property. A concrete implementation discharges \(\mathsf{CCIA}_{\mathrm{build}}\) only if it implements these algorithms, exports their soundness and effective-modulus contracts, and compiles the resulting module. This definition is the explicit bounded-build obligation; it does not state or imply that the missing complex operations are already exported by Causalean. Until such an implementation is added and compiled, later executable conclusions are conditional on a build satisfying this specification.

**P-12 (Let \(\operatorname{Name}_{+}(x)\) denote an actually sup…).** Let \(\operatorname{Name}_{+}(x)\) denote an actually supplied positive certified-real record satisfying \(\text{def:certified-contour-arithmetic-substrate}\). As part of the experiment convention, fix once for all the single actual record \[ \mathfrak p_{\star}= \bigl(k,\mathfrak d_{\star},\mathfrak p_{\eta,\star}, \mathfrak r_{0,\star},\mathfrak r_{1,\star}\bigr), \] where \(k\) is exact and \[ \mathfrak d_{\star}:\operatorname{Name}_{+}(\delta),\quad \mathfrak p_{\eta,\star}:\operatorname{Name}_{+}(\psi_{\eta}),\quad \mathfrak r_{0,\star}:\operatorname{Name}_{+}(R_0),\quad \mathfrak r_{1,\star}:\operatorname{Name}_{+}(R_1), \] with the value contracts \[ k=r+1,\qquad R_0=(2^{k+4}k!)^{1/(k-2)} (\psi_{\eta}^2/\delta)^{1/(k-2)}, \qquad R_1=R_0+1. \] Each positive name includes its rational positivity witness. The record \(\mathfrak p_{\star}\) is fixed before \(n\), the law, or the sample varies, and it is the only primitive record used by both the bank and the estimator. The construction reads only its supplied interval, modulus, and positivity fields. It never reconstructs a name from an exact real and does not quantify over alternative records representing the same constants. Invoke the supplied moduli of \(\mathfrak p_{\eta,\star}\) and \(\mathfrak r_{1,\star}\) at error one. If the returned intervals have rational upper endpoints \(u_{\psi}\) and \(u_R\), put \[ U_{\psi}=\left\lceil\max\{1,u_{\psi}\}\right\rceil, \qquad U_R=\left\lceil\max\{1,u_R\}\right\rceil, \qquad N_{\mathrm{cert}}=4U_{\psi}^2(U_R+2)^3. \] Set \[ m_{\star}=N_{\mathrm{cert}}+3, \qquad d_{\star}=2^{-m_{\star}}, \qquad h_{\star}=d_{\star}/3, \qquad J=2^{m_{\star}-1}+1. \] For \(q=0,\ldots,2^{m_{\star}-1}\), use certified addition on the fixed record \(\mathfrak r_{0,\star}\) and the rational point record for \(1/4+q2^{-m_{\star}}\) to form \[ \mathfrak\rho_q =\mathfrak r_{0,\star}+\operatorname{Name}(1/4+q2^{-m_{\star}}). \] Its value is \(\rho_q=R_0+1/4+q2^{-m_{\star}}\). Enumerate these radii increasingly and set \(C_j=\{z\in\mathbb C:|z|=\rho_j\}\). Define the integers \[ e_{\star}=8U_RU_{\psi}^2(U_R+1)^2, \qquad d_{\star}^{\mathrm{den}}=3(2U_R+1), \] \[ u_{\star}=2e_{\star} +N_{\mathrm{cert}}(m_{\star}+d_{\star}^{\mathrm{den}}), \qquad a_{\star}=2^{-u_{\star}}>0. \] Every output datum is obtained by the displayed finite modulus calls, rational endpoint operations, and primitive recursion over displayed integer bounds. There is no unbounded search, \(\operatorname{Classical.choose}\), proof-selected \(\operatorname{Nat.find}\), exact-real inspection, or query to \(M\), \(\eta\), or its law. Reusing the identical fixed record \(\mathfrak p_{\star}\) gives the same bank and certificate for every \(n\). The universal guarantee is stated in \(\text{lem:finite-contour-bank}\).

**P-13 (The construction has a mathematical layer and a represent…).** The construction has a mathematical layer and a represented-data layer, both relative to exactly the same experiment-wide primitive records. Fix the record \(\mathfrak p_{\star}\) from \(\text{def:contour-bank-handle}\) and, once for all, fix one actual record \[ \mathfrak c_{\theta,\star}:\operatorname{Name}_{+}(C_{\theta}). \] These records are fixed before \(n\), the law, or the sample varies. They are never replaced by different canonical names. At the mathematical layer, for an ordinary real observation coordinate set \[ \mathcal I_m(x)= \left[2^{-m}\lfloor2^m x\rfloor, 2^{-m}\{\lfloor2^m x\rfloor+1\}\right], \qquad m\ge1. \] Canonicalize only \(T_i\), \(Y_i\), and \(\bar g_n(X_i)\) through these floor-dyadic records. Run the finite rational procedure below with those observation records and with the already fixed, unchanged records \(\mathfrak p_{\star}\) and \(\mathfrak c_{\theta,\star}\), and take the value represented by its output record. This defines \(\widehat\theta_{\mathrm{spec},n}\) as a total map on ordinary real samples relative to the fixed full primitive record. The canonical observation digits are part of this mathematical interpretation; they are not claimed to be Type-2 computable from an arbitrary real name at a dyadic boundary. In particular, the ordinary interpretation does not silently substitute canonical primitive names. At the represented-data layer, the full input record is \[ \mathfrak s_{\star}= \bigl((\mathfrak t_i,\mathfrak y_i,\mathfrak g_i)_{i=1}^n, \mathfrak p_{\star},\mathfrak c_{\theta,\star}\bigr), \] where \(\mathfrak t_i\), \(\mathfrak y_i\), and \(\mathfrak g_i\) are supplied canonical digital records for \(T_i\), \(Y_i\), and \(\bar g_n(X_i)\). The transducer is defined only relative to the displayed fixed primitive components; its correspondence claim does not quantify over alternative primitive records. A transducer exists at this layer only relative to a concrete implementation that has been added and compiled with all contracts of \(\text{def:certified-contour-arithmetic-substrate}\). It reads no semantic exact-real field. Invoke \(\text{def:contour-bank-handle}\) on \(\mathfrak p_{\star}\), and form \(\mathfrak z_i=\mathfrak t_i-\mathfrak g_i\) by certified subtraction. At every certified circle node, use only the substrate's complex rectangle operations and nested complex-exponential extension to construct, by finite sums, enclosures of \[ \widehat F_{a,n}^{(p)}(z) =|I_a|^{-1}\sum_{i\in I_a}Z_{n,i}^{p}e^{zZ_{n,i}}, \quad p\in\{0,1,2\}, \] and \[ \widehat G_{a,n}^{(p)}(z) =|I_a|^{-1}\sum_{i\in I_a}Y_iZ_{n,i}^{p}e^{zZ_{n,i}}, \quad p\in\{0,1\}. \] Every node precision is obtained by applying the compositional modulus to a rational error budget. For explicit mesh fuel, refine each observation record at error one and take the resulting rational upper magnitude bounds \(U_{Z,i}\), \(U_{Y,i}\), and \(U_{\rho,j}\). Use \(e^x\le3^{\lceil x\rceil}\) for \(x\ge0\) and \(\pi<4\) to turn the finite empirical derivative sums into rational bounds for all functions below. With \(z_j(t)=\rho_je^{2\pi it}\), put \[ W_{0j}(t)= \rho_je^{2\pi it} \frac{\widehat F'_{0,n}(z_j(t))} {\widehat F_{0,n}(z_j(t))}, \] and, after a positive integer \(N_j\) has been certified, \[ V_j(t)= \frac{\rho_je^{2\pi it}}{N_j} \frac{\widehat G_{1,n}(z_j(t))} {\widehat F_{1,n}(z_j(t))}. \] If a rational lower modulus certificate is \(m>0\), use \[ |W'_{0j}(t)|\le 2\pi\left[ \rho_j\frac{\sup|\widehat F'_{0,n}|}{m} +\rho_j^2\left\{ \frac{\sup|\widehat F''_{0,n}|}{m} +\frac{\sup|\widehat F'_{0,n}|^2}{m^2} \right\}\right], \] and \[ |V'_j(t)|\le \frac{2\pi}{N_j}\left[ \rho_j\frac{\sup|\widehat G_{1,n}|}{m} +\rho_j^2\left\{ \frac{\sup|\widehat G'_{1,n}|}{m} +\frac{\sup|\widehat G_{1,n}|\sup|\widehat F'_{1,n}|}{m^2} \right\}\right]. \] For a rational tolerance \(e>0\) and any resulting rational bound \(L\), take the mesh size \(\lceil L/e\rceil+1\), and allocate node error by the finite number of operations. Thus every precision and mesh has displayed finite fuel. On the pilot fold, compute for every \(j\) a sound interval of width at most \(a_{\star}/64\) enclosing \(\inf_{C_j}|\widehat F_{0,n}|\). If its lower endpoint \(m_j\) is positive, guarded division and finite quadrature enclose \[ \int_0^1W_{0j}(t)\,dt =\frac{1}{2\pi i}\oint_{C_j} \frac{\widehat F'_{0,n}(z)}{\widehat F_{0,n}(z)}\,dz \] in a complex rectangle whose coordinate widths are strictly below \(1/4\). Record \(N_j\) exactly when the real interval contains a unique nonnegative integer and the imaginary interval contains zero; otherwise reject the circle. A circle is admissible exactly when \(m_j\ge a_{\star}/2\) and \(N_j\ge1\). Select an admissible circle having the greatest rational lower endpoint, with smallest-index tie-break, and return the certified zero name if none exists. On the evaluation fold, compute the analogous modulus enclosure and return the certified zero name if its lower endpoint is below \(a_{\star}/4\). Otherwise guarded division and finite quadrature give a rectangle of coordinate radius at most \(1/n\) enclosing \[ \int_0^1V_j(t)\,dt =\frac{1}{N_j2\pi i}\oint_{C_j} \frac{\widehat G_{1,n}(z)}{\widehat F_{1,n}(z)}\,dz. \] Let \(y\in\mathbb Q\) be the rational midpoint of its real interval, represented by its rational point name. Projection uses no exact comparison: certified addition, subtraction, multiplication by \(1/2\), and the interval absolute-value operation construct a certified name whose value is \[ \operatorname{clip}_{C_{\theta}}(y) =\frac{|y+C_{\theta}|-|y-C_{\theta}|}{2} =\Pi_{[-C_{\theta},C_{\theta}]}(y). \] This certified-real name is the transducer output. An execution trace is the finite list of input-modulus calls, returned precisions, Taylor and square-root cutoffs, mesh sizes, rational rectangle operations, guarded divisions, rational endpoint comparisons, fallback decisions, and the tie-break. Identical full input records \(\mathfrak s_{\star}\), including their fixed primitive components, have identical traces and output records. The value represented by that output is, by definition, the ordinary-sample Borel statistic evaluated with the same fixed primitive records. No extensionality across arbitrary alternative names is asserted. The procedure uses \(\bar g_n\), uses no supplied outcome code, and has no access to the law of \(\eta\). The represented-data transducer and its compiled computability conclusion remain conditional on completing the bounded-build obligation in \(\text{def:certified-contour-arithmetic-substrate}\); the ordinary-sample Borel statistic and its statistical risk are mathematical objects independently of that build status.

**P-14 (For triangular treatment-noise laws with…).** For triangular treatment-noise laws with \(|\kappa_k(\eta_n)|\ge\delta_n\downarrow0\) under the same supplied code sequences, does there exist a data-driven selector among ordinary DML, finite-order ACE, and global-contour procedures that attains the sharp minimax mean-squared-error rate as a function of \((n,\varepsilon_{1,n},\varepsilon_{2,n},\delta_n)\), yields uniformly valid inference across these regimes, and admits a matching local minimax lower bound? This is a research agenda, not a theorem. The present paper does not define a unique sharp-rate functional or a uniform-inference/coverage criterion, and asserts no existential selector, confidence procedure, or matching lower-bound witness.

## Lemmas (L)

**L-1 (For every \(P\in\mathcal P_{\mathrm{NG},n}\),…).** For every \(P\in\mathcal P_{\mathrm{NG},n}\), \(F_n(z)=M(z)H_n(z)\) and \(G_n(z)=\theta_0F_n'(z)+M(z)B_n(z)\) for every \(z\in\mathbb C\).

**L-2 (With the exact Luxemburg convention in…).** With the exact Luxemburg convention in \(\text{ass:eta-subgaussian}\), set \[ A_k=(2^{k+4}k!)^{1/(k-2)}, \qquad R_0=A_k(\psi_{\eta}^2/\delta)^{1/(k-2)}. \] Every centered law satisfying \(\|\eta\|_{\psi_2}\le\psi_{\eta}\) and \(|\kappa_k(\eta)|\ge\delta\) has a zero of \(M\) in \(\{z\in\mathbb C:|z|\le R_0\}\). Thus the zero-localization radius is the displayed numerical function of the primitive constants.

**L-3 (Under the certified-real input convention of…).** Under the certified-real input convention of \(\text{def:contour-bank-handle}\), its terminating finite searches compute, from the displayed primitive class constants and without access to the unknown law of \(\eta\), a finite translated-dyadic bank \(C_j=\{z:|z|=\rho_j\}\), \(1\le j\le J\), with \(R_0<\rho_j<R_1\), and a positive dyadic rational \(a_{\star}\). Every treatment-noise transform represented in \(\mathcal P_{\mathrm{NG},n}\) has some \(j\) such that \(N_{C_j}(M)\ge1\) and \[ \inf_{z\in C_j}|M(z)|\ge a_{\star}. \] The same computed bank and certificate work uniformly in \(n\).

**L-4 (If \(\mathcal P_{\mathrm{NG},n}\neq\varnothing\), then it…).** If \(\mathcal P_{\mathrm{NG},n}\neq\varnothing\), then it contains a one-dimensional submodel \(\{P_{n,\vartheta}:|\vartheta|\le c_0\}\) with the same law of \((X,T)\), the same \(g_0,q_0,\bar g_n,\bar q_n\), independent Gaussian outcome innovation of fixed positive variance not exceeding the \(\psi_{\xi}\) envelope, and \(Y_{\vartheta}=q_0(X)+\vartheta\eta+\xi_{\star}\). For \(|\vartheta_1-\vartheta_0|=h/\sqrt n\), \(D_{\mathrm{KL}}(P_{n,\vartheta_0}^{\otimes n}\|P_{n,\vartheta_1}^{\otimes n})\le C_0h^2\), where \(c_0,C_0\) depend only on fixed class constants. Moreover, whenever \(\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\neq\varnothing\), the same construction may be started from any \(P^{\circ}\in\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\) and preserves its law of \((X,T)\), the functions \(g_0,q_0\), the noise \(\eta\), both clipped supplied codes, and therefore the two bounds \[ \|\bar g_n-g_0\|_{L^r(P_X)}\le\varepsilon_{1,n}, \qquad \|\bar q_n-q_0\|_{L^r(P_X)}\le\varepsilon_{2,n}. \] Every law on this second path belongs to \(\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\), has target \(\theta_0(P_{n,\vartheta})=\vartheta\), and obeys the same product relative-entropy bound. Consequently there is \(c_{\mathrm{ACE}}>0\), depending only on the fixed class constants, such that for every \(n\ge2\) with \(\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\neq\varnothing\), \[ \inf_{\widetilde\theta(O_1,\ldots,O_n;\bar g_n,\bar q_n)} \sup_{P\in\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}} E_P[(\widetilde\theta-\theta_0(P))^2] \ge \frac{c_{\mathrm{ACE}}}{n}. \]

**L-5 (Specialize to \(k=4\)).** Specialize to \(k=4\). For \(0<a\le1\), let \[ \eta_a=\sqrt{1-a^2}\,G_{\circ}+aS, \qquad t_a=\frac{\pi}{2a}, \qquad A_a=a\exp\!\left\{-\frac{(1-a^2)\pi^2}{8a^2}\right\}, \] where \(G_{\circ}\sim N(0,1)\), \(S\) is symmetric Rademacher, and \(G_{\circ}\perp S\). Consider any law satisfying \(O_1,\ldots,O_n\stackrel{\mathrm{iid}}{\sim}P\), \(\eta=\eta_a\perp X\), \(E_P[\xi\mid X,T]=0\) almost surely, \(\lvert\theta_0(P)\rvert\le C_{\theta}\), \(\lVert g_0\rVert_{\infty}\le C_g\), \(\lVert q_0\rVert_{\infty}\le C_q\), \(\lVert\xi\rVert_{\psi_2\mid X}\le\psi_{\xi}\) almost surely, and the direct condition \(\lVert\bar g_n-g_0\rVert_{L^1(P_X)}\le\varepsilon_{1,n}\). Then \[ M_a(z)=\exp\!\left(\frac{1-a^2}{2}z^2\right)\cosh(az), \qquad \kappa_4(\eta_a)=-2a^4, \] \(t_a\) is the first positive characteristic zero, and \(E[\sin\{t_a(\eta_a+d)\}]=0\) for every real \(d\). If \(t_a\varepsilon_{1,n}\le1/2\), then \[ E_P[Z_n\sin(t_aZ_n)] =A_aE_P[\cos(t_aD_n)] \ge A_a/2. \] Define \[ \widehat\theta_{a,n} = \begin{cases} \displaystyle \Pi_{[-C_{\theta},C_{\theta}]} \frac{\mathbb P_n[Y\sin(t_aZ_n)]} {\mathbb P_n[Z_n\sin(t_aZ_n)]}, &\displaystyle \mathbb P_n[Z_n\sin(t_aZ_n)]\ge A_a/4,\\[3mm] 0,&\text{otherwise}. \end{cases} \] There is \(C<\infty\), depending only on \(C_{\theta},C_g,C_q,\psi_{\xi}\), such that uniformly over these laws, \[ E_P[(\widehat\theta_{a,n}-\theta_0(P))^2] \le C\min\!\left\{1,\frac1n a^{-2} \exp\!\left(\frac{(1-a^2)\pi^2}{4a^2}\right)\right\}. \] Equivalently, for \(\Delta_a=2a^4\), the condition is \(\varepsilon_{1,n}\le\Delta_a^{1/4}/(2^{1/4}\pi)\), and the risk is at most \[ C\min\!\left\{1,\frac1n\Delta_a^{-1/2} \exp\!\left(\frac{\pi^2\sqrt2}{4}\Delta_a^{-1/2}\right)\right\}. \]

**L-6 (For \(n\ge2\), there is \(K<\infty\), depending only on \…).** For \(n\ge2\), there is \(K<\infty\), depending only on \(C_{\theta},C_g,C_q,\psi_{\eta},\psi_{\xi}\), and \(R_1\), such that, for every \(P\in\mathcal P_{\mathrm{NG},n}\) and every \(a\in\{0,1\}\) with \(|I_a|\ge1\), \[ E_P\!\left[\sup_{|z|\le R_1}|\widehat F_{a,n}(z)-F_n(z)|^2\right] +E_P\!\left[\sup_{|z|\le R_1}|\widehat G_{a,n}(z)-G_n(z)|^2\right] \le \frac{K}{|I_a|}. \]

**L-7 (For every \(P\in\mathcal P_{\mathrm{NG},n}\), the class's…).** For every \(P\in\mathcal P_{\mathrm{NG},n}\), the class's \(L^1(P_X)\) treatment-code restriction and clipping imply \(|D_n|\le2C_g\) almost surely and \(E_P|D_n|\le\varepsilon_{1,n}\). Hence \[ \sup_{|z|\le R_1}|H_n(z)-1| \le R_1e^{2C_gR_1}\varepsilon_{1,n}. \] If \(\varepsilon_{1,n}\le\varepsilon_0\), then \(|H_n(z)|\ge3/4\) throughout \(\{|z|\le R_1\}\), and \(F_n=M H_n\) and \(M\) have the same zeros there with multiplicity.

**L-8 (For every \(n\),…).** For every \(n\), \[ \mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}} \subseteq \mathcal P_{\mathrm{NG},n}. \] For \(s\ge r\), \[ \mathcal P_{\mathrm{NG},n}^{\mathrm{ACE}} \subseteq \mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}, \] and, under the common clipped-code convention in the displayed class definitions, equality holds when \(s=r\).

## Theorems (T)

### T-block: t1 — If \(z_0\) is a zero of \(M\) of multiplicity \(\ell\), t…
**Statement.** If \(z_0\) is a zero of \(M\) of multiplicity \(\ell\), then for every real shift \(d\), \(E_P[J_{z_0,\ell}(\eta+d)]=0\). For every \(P\in\mathcal P_{\mathrm{NG},n}\), this gives \(E_P[J_{z_0,\ell}(Z_n)\mid X]=0\) and \(E_P[Z_nJ_{z_0,\ell}(Z_n)]=M^{(\ell)}(z_0)H_n(z_0)\); whenever this value is nonzero, \(\theta_0=E_P[YJ_{z_0,\ell}(Z_n)]/E_P[Z_nJ_{z_0,\ell}(Z_n)]\).

### T-block: t2 — For every \(P\in\mathcal P_{\mathrm{NG},n}\), let \(C_j\)…
**Statement.** For every \(P\in\mathcal P_{\mathrm{NG},n}\), let \(C_j\) be positively oriented. If \(F_n\) is zero-free on \(C_j\), \(H_n\) is zero-free on and inside \(C_j\), and \(N_{C_j}(F_n)\ge1\), then \(\theta_0=\Theta_{C_j}(F_n,G_n)=\{N_{C_j}(F_n)2\pi i\}^{-1}\oint_{C_j}G_n(z)/F_n(z)\,dz\).

### T-block: t3 — Fix once for all the actual positive primitive-name recor…
**Statement.** Fix once for all the actual positive primitive-name record \(\mathfrak p_{\star}\) in \(\text{def:contour-bank-handle}\) and the actual represented range record \(\mathfrak c_{\theta,\star}\) in \(\text{def:adaptive-contour-estimator}\). Under this shared-record convention, \(\text{def:contour-bank-handle}\) and \(\text{lem:finite-contour-bank}\) specify once for all a deterministic translated-dyadic bank and a positive dyadic rational \(a_{\star}\) from the displayed primitive class constants, without access to the unknown law of \(\eta\). There are constants \(0<c<C<\infty\), depending only on \(r,\delta,C_{\theta},C_g,C_q,\psi_{\eta},\psi_{\xi}\), such that the associated total Borel statistic \(\widehat\theta_{\mathrm{spec},n}\) in \(\text{def:adaptive-contour-estimator}\), whose ordinary-sample interpretation uses those same fixed primitive records and canonicalizes only observation coordinates, satisfies, for every \(n\ge2\) with \(\mathcal P_{\mathrm{NG},n}\neq\varnothing\) and \(\varepsilon_{1,n}\le\varepsilon_0\), \[ \frac cn\le\mathfrak R_n^{\mathrm{NG}} \le\sup_{P\in\mathcal P_{\mathrm{NG},n}} E_P[(\widehat\theta_{\mathrm{spec},n}-\theta_0(P))^2] \le\frac Cn. \] The statistic uses \(\bar g_n\), uses no supplied outcome code, and does not know the law of \(\eta\). If a concrete implementation satisfying \(\text{def:certified-contour-arithmetic-substrate}\) is added and compiled, then, relative to the canonical floor-dyadic observation records and exactly the same fixed records \(\mathfrak p_{\star}\) and \(\mathfrak c_{\theta,\star}\), the statistic is also the value of a total executable represented-data transducer returning a certified-real name. The correspondence claim does not quantify over alternative primitive records. Identical full input records, including their primitive components, give identical traces and output records; no Type-2 computability or trace extensionality across arbitrary alternative real names is asserted. Its MSE bound implies \[ \sup_{P\in\mathcal P_{\mathrm{NG},n}} Q_{P,1-\gamma}(|\widehat\theta_{\mathrm{spec},n}-\theta_0(P)|) \le\sqrt{C/(\gamma n)}. \] Moreover, \(\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\subseteq\mathcal P_{\mathrm{NG},n}\). Hence, whenever \(n\ge2\), \(\varepsilon_{1,n}\le\varepsilon_0\), and \(\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\neq\varnothing\), \[ \frac cn \le \inf_{\widetilde\theta(O_1,\ldots,O_n;\bar g_n,\bar q_n)} \sup_{P\in\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}} E_P[(\widetilde\theta-\theta_0(P))^2] \le \sup_{P\in\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}} E_P[(\widehat\theta_{\mathrm{spec},n}-\theta_0(P))^2] \le\frac Cn, \] and \[ \sup_{P\in\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}} Q_{P,1-\gamma}(|\widehat\theta_{\mathrm{spec},n}-\theta_0(P)|) \le\sqrt{C/(\gamma n)}. \] No inclusion \(\mathcal P_{\mathrm{NG},n}\subseteq\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\) is asserted. The finite-order ACE and spectral generalized-quantile guarantees may therefore be compared on the full published ACE class, and that class itself has matched \(\Theta(n^{-1})\) minimax mean-squared error. The latter lower bound is over all estimators on the class; it is not a lower bound on \(\widehat\theta_{\mathrm{ACE},r,n}\) or on its cited generalized-quantile guarantee. The class \(\mathcal P_{\mathrm{NG},n}^{\mathrm{ACE}}\) remains the additional \(L^s(P_X)\)-restricted subclass and equals the published ACE class when \(s=r\) under the common clipped-code convention. The constants may deteriorate with fixed \(\delta\), and this theorem gives no useful shrinking-\(\delta_n\) rate, adaptive switching rule, uniform inference result, or matching local lower bound.

### T-block: t4 — For every \(n\),…
**Statement.** For every \(n\), \[ \mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}} \subseteq\mathcal P_{\mathrm{NG},n}. \] For \(s\ge r\), \[ \mathcal P_{\mathrm{NG},n}^{\mathrm{ACE}} \subseteq\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}, \] and, under the common clipped-code convention, \(\mathcal P_{\mathrm{NG},n}^{\mathrm{ACE}}=\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\) when \(s=r\). No inclusion \(\mathcal P_{\mathrm{NG},n}\subseteq\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\) is asserted. If \(\mathsf E_n^{\mathrm{JMS}}\) holds and \(\varepsilon_{1,n}>0\), then, for every \(\varepsilon_{2,n}\ge0\), with \(\varepsilon_{2,n}=0\) obtained by monotone boundary extension, \[ \begin{aligned} &\sup_{P\in\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}} Q_{P,1-\gamma}(|\widehat\theta_{\mathrm{ACE},r,n}-\theta_0(P)|)\\ &\quad\le B_{\mathrm{ACE},n,\gamma}\\ &\quad=C_{\gamma}r!16^r\delta^{-1} \Big[\varepsilon_{1,n}^r\varepsilon_{2,n} +C_{\theta}\varepsilon_{1,n}^{r+1} +64(C_g+\psi_{\eta})^r \{r^2(C_g+\psi_{\eta})+\psi_{\xi}+C_{\theta}\psi_{\eta}\} (\gamma n)^{-1/2}\Big]. \end{aligned} \] If also \(n\ge2\), \(\varepsilon_{1,n}\le\varepsilon_0\), and \(\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\neq\varnothing\), then \[ \sup_{P\in\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}} Q_{P,1-\gamma}(|\widehat\theta_{\mathrm{spec},n}-\theta_0(P)|) \le\sqrt{C/(\gamma n)}, \] where \(C\) is the fixed-separation constant in \(\text{thm:adaptive-rootn-minimax}\). Whenever \(\varepsilon_{1,n}^r\varepsilon_{2,n}+C_{\theta}\varepsilon_{1,n}^{r+1}\gg n^{-1/2}\), the spectral upper guarantee is strictly smaller in order than \(B_{\mathrm{ACE},n,\gamma}\) on the full published ACE class. This is a comparison of upper guarantees, not a lower bound for ACE.

### T-block: t5 — The following are separate facts on separately defined, i…
**Statement.** The following are separate facts on separately defined, incomparable classes. Fix the same deterministic code pair \((\widehat g_n,\widehat q_n)\), its clipped pair \((\bar g_n,\bar q_n)\), and the same nested radii \((\varepsilon_{1,n},\varepsilon_{2,n})\) as external inputs. Also fix once for all the exact primitive records \(\mathfrak p_{\star}\) and \(\mathfrak c_{\theta,\star}\) specified in \(\text{def:contour-bank-handle}\) and \(\text{def:adaptive-contour-estimator}\). Under this shared-record convention, apply and fix the once-for-all translated-dyadic bank and positive dyadic certificate specified by \(\text{lem:finite-contour-bank}\). Along every sequence of indices \(n\to\infty\) for which \(n\ge2\), \(\mathcal P_{\mathrm{NG},n}\neq\varnothing\), and \(\varepsilon_{1,n}\le\varepsilon_0\), \[ \mathfrak R_n^{\mathrm{NG}}\asymp n^{-1}. \] This includes fixed nonzero \(\varepsilon_{1,n}\). The witnessing total Borel statistic \(\widehat\theta_{\mathrm{spec},n}\), as well as membership in \(\mathcal P_{\mathrm{NG},n}\), imposes no restriction on and makes no use of \(\widehat q_n\), \(\bar q_n\), or \(\varepsilon_{2,n}\). Its ordinary-sample interpretation canonicalizes only observation-coordinate records and uses the same fixed primitive records. If the bounded-build substrate in \(\text{def:certified-contour-arithmetic-substrate}\) is concretely implemented and compiled, the statistic is additionally delivered by the represented-data transducer on the identical fixed primitive records and supplied canonical observation records. Its correspondence claim does not quantify over alternative primitive records, and trace determinism is asserted only for identical full input records. No computability claim from arbitrary alternative real names is made. Separately, every \(P\in\mathcal P_{\mathrm G,n}^{\mathrm{JMS}}\) in the simultaneous bounded-observed-outcome Gaussian intersection of \(\text{def:gaussian-class}\) satisfies \(\theta_0(P)=0\). Whenever that intersection is nonempty, \[ \mathfrak R_n^{\mathrm G} =\mathfrak M_{n,1-\gamma}^{\mathrm G} =0. \] Thus \(\text{prop:bounded-outcome-gaussian-degeneracy}\) is solely a diagnostic of the simultaneous exact-PLM, bounded-\(Y\), and nondegenerate conditional-Gaussian assumptions stated around Jin--Mackey--Syrgkanis Theorem 3.2; its positive lower-bound claim is not used. These conclusions neither form a Gaussian-versus-non-Gaussian minimax dichotomy nor determine a local-to-Gaussian frontier, and no equality or inclusion between the two classes is asserted.

### T-block: t6 — For every \(n\) such that \(\varepsilon_{1,n}\le 1/\pi\),…
**Statement.** For every \(n\) such that \(\varepsilon_{1,n}\le 1/\pi\), let \(O_1,\ldots,O_n\stackrel{\mathrm{iid}}{\sim}P\). Suppose that \(\eta\sim\tfrac12N(-1,1)+\tfrac12N(1,1)\), \(\eta\perp X\), \(E_P[\xi\mid X,T]=0\) almost surely, \(|\theta_0(P)|\le C_{\theta}\), \(\|g_0\|_{\infty}\le C_g\), \(\|q_0\|_{\infty}\le C_q\), \(\|\xi\|_{\psi_2\mid X}\le\psi_{\xi}\) almost surely, and \(\|\bar g_n-g_0\|_{L^1(P_X)}\le\varepsilon_{1,n}\). Then \(\kappa_4(\eta)=-2\), \(M(i\pi/2)=0\), and \[ E_P\!\left[Z_n\sin\!\left(\frac{\pi Z_n}{2}\right)\right] =e^{-\pi^2/8}E_P\!\left[\cos\!\left(\frac{\pi D_n}{2}\right)\right] \ge \frac12e^{-\pi^2/8}. \] There is a finite constant \(C\), depending only on \(C_{\theta},C_g,C_q,\psi_{\xi}\), such that, uniformly over all laws \(P\) satisfying the preceding conditions, \[ E_P\!\left[(\widehat\theta_{\sin,n}-\theta_0(P))^2\right]\le \frac{C}{n}. \] No condition on the supplied outcome code or on \(\varepsilon_{2,n}\) is required.

### T-block: t7 — The following two conclusions are valid, but they do not…
**Statement.** The following two conclusions are valid, but they do not determine the full-class minimax phase diagram. First, for each \(n\), let \(\mathcal C_n(\delta_n)\) be the class obtained from \(\mathcal P_{\mathrm{ACE},n}^{\mathrm{JMS}}\) in \(\text{def:jms-ace-class}\) by replacing the fixed restriction \(\lvert\kappa_{r+1}(\eta)\rvert\ge\delta\) by \(\lvert\kappa_{r+1}(\eta)\rvert\ge\delta_n\), without changing any other restriction. Define \[ \widetilde b_{2,n} =200\min\{1,C_{\theta}\}\delta_n \max\!\left\{ \varepsilon_{1,n},\varepsilon_{2,n}, (\gamma n)^{-1/2}(\psi_{\xi}+C_{\theta}\psi_{\eta}) \right\}^{-1}. \] Suppose \(\varepsilon_{1,n},\varepsilon_{2,n}>0\), all logarithms and denominators in the following display are defined, and the exact local eligibility condition \[ r\le \min\!\left\{ \frac{b_{1,n}}{a_{1,n}} -\frac{1}{a_{1,n}}\log\!\left(\frac{b_{1,n}}{a_{1,n}}\right), \frac{\widetilde b_{2,n}}{a_2\log(a_2\widetilde b_{2,n})} \right\} \] holds. Then the order-\(r\) ACE estimator obeys the local oracle benchmark \[ \begin{aligned} &\sup_{P\in\mathcal C_n(\delta_n)} Q_{P,1-\gamma} \!\left(\left|\widehat\theta_{\mathrm{ACE},r,n}-\theta_0(P)\right|\right)\\ &\quad\le C_{\gamma}r!16^r\delta_n^{-1} \Bigl[ \varepsilon_{1,n}^r\varepsilon_{2,n} +C_{\theta}\varepsilon_{1,n}^{r+1}\\ &\hspace{42mm} +64(C_g+\psi_{\eta})^r \{r^2(C_g+\psi_{\eta})+\psi_{\xi}+C_{\theta}\psi_{\eta}\} (\gamma n)^{-1/2} \Bigr]. \end{aligned} \] Denote the right-hand side by \(U_{\mathrm{ACE},n}^{\mathrm{loc}}\). If \(U_{\mathrm{DML},n}\) denotes a separately valid ordinary-DML upper bound under its own hypotheses, then \(\min\{U_{\mathrm{DML},n},U_{\mathrm{ACE},n}^{\mathrm{loc}}\}\) is only an oracle upper envelope; this comparison neither asserts a minimax rate nor constructs a data-driven selector. Second, specialize to \(k=4\), hence \(r=3\). For \(0<a\le1\), let \[ \eta_a=\sqrt{1-a^2}\,G_{\circ}+aS, \] where \(G_{\circ}\sim N(0,1)\), \(S\) is symmetric Rademacher, and \(G_{\circ}\perp S\). Its moment generating function, fourth cumulant, and first positive characteristic zero are \[ M_a(z)=\exp\!\left(\frac{1-a^2}{2}z^2\right)\cosh(az), \qquad \kappa_4(\eta_a)=-2a^4, \qquad t_a=\frac{\pi}{2a}. \] For every real \(d\), \[ E\!\left[\sin\{t_a(\eta_a+d)\}\right]=0. \] Consider any law satisfying \(O_1,\ldots,O_n\stackrel{\mathrm{iid}}{\sim}P\), \(\eta=\eta_a\perp X\), \(E_P[\xi\mid X,T]=0\) almost surely, \(\lvert\theta_0(P)\rvert\le C_{\theta}\), \(\lVert g_0\rVert_{\infty}\le C_g\), \(\lVert q_0\rVert_{\infty}\le C_q\), \(\lVert\xi\rVert_{\psi_2\mid X}\le\psi_{\xi}\) almost surely, and \(\lVert\bar g_n-g_0\rVert_{L^1(P_X)}\le\varepsilon_{1,n}\). Put \[ A_a=a\exp\!\left\{-\frac{(1-a^2)\pi^2}{8a^2}\right\}. \] If \(t_a\varepsilon_{1,n}\le1/2\), then \[ E_P\!\left[Z_n\sin(t_aZ_n)\right] =A_aE_P[\cos(t_aD_n)] \ge\frac{A_a}{2}. \] Writing \(\mathbb P_n\) for the empirical average, define the computable clipped ratio \[ \widehat\theta_{a,n} = \begin{cases} \displaystyle \Pi_{[-C_{\theta},C_{\theta}]} \frac{\mathbb P_n[Y\sin(t_aZ_n)]} {\mathbb P_n[Z_n\sin(t_aZ_n)]}, &\displaystyle \mathbb P_n[Z_n\sin(t_aZ_n)]\ge A_a/4,\\[3mm] 0,&\text{otherwise}. \end{cases} \] There is a finite constant \(C\), depending only on \(C_{\theta},C_g,C_q,\psi_{\xi}\), such that \[ \sup_P E_P\!\left[(\widehat\theta_{a,n}-\theta_0(P))^2\right] \le C\min\!\left\{ 1,\frac{1}{n}a^{-2} \exp\!\left(\frac{(1-a^2)\pi^2}{4a^2}\right) \right\}, \] where the supremum is over the just-displayed path laws. Equivalently, with \(\Delta_a=2a^4=\lvert\kappa_4(\eta_a)\rvert\), the condition \(t_a\varepsilon_{1,n}\le1/2\) is \[ \varepsilon_{1,n}\le\frac{\Delta_a^{1/4}}{2^{1/4}\pi}, \] and the preceding risk is at most \[ C\min\!\left\{ 1,\frac{1}{n}\Delta_a^{-1/2} \exp\!\left(\frac{\pi^2\sqrt2}{4}\Delta_a^{-1/2}\right) \right\}. \] This last display is a construction-specific upper bound with \(\varepsilon_{1,n}=O(\Delta_a^{1/4})\); it is not asserted to be sharp for the class specified only by a fourth-cumulant lower bound.

### T-block: t8 — For every \(n\) and every…
**Statement.** For every \(n\) and every \(P\in\mathcal P_{\mathrm G,n}^{\mathrm{JMS}}\), the simultaneous restrictions in \(\text{def:gaussian-class}\) imply \(\theta_0(P)=0\). If \(\mathcal P_{\mathrm G,n}^{\mathrm{JMS}}\neq\varnothing\), then the zero statistic is minimax and \[ \mathfrak R_n^{\mathrm G} =\mathfrak M_{n,1-\gamma}^{\mathrm G} =0. \]
