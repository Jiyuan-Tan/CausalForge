The bank is now an actual finite computation on positive certified primitive-name records: a coarse enclosure produces a rational zero-count budget, and closed integer formulas produce the grid, analytic lower certificate, and dyadic modulus without an unbounded or proof-selected search.