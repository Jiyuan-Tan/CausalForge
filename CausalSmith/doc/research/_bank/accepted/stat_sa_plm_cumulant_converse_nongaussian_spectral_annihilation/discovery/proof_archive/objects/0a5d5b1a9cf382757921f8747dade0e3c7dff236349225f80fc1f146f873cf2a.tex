Apply \(\text{thm:adaptive-rootn-minimax}\).  Its canonical algorithm internally computes the once-for-all bank from the certified primitive names, and on every index covered by the hypotheses,
\[
 \frac cn\le\mathfrak R_n^{\mathrm{NG}}\le\frac Cn.
\]
Hence \(\mathfrak R_n^{\mathrm{NG}}\asymp n^{-1}\) along every stated sequence, including one with a fixed nonzero treatment-code radius not exceeding \(\varepsilon_0\).  Inspection of \(\text{def:non-gaussian-class}\) and the corrected \(\text{def:adaptive-contour-estimator}\) shows that both the class and the statistic use \(\bar g_n\), while neither imposes a restriction involving nor uses \(\widehat q_n\), \(\bar q_n\), or \(\varepsilon_{2,n}\).

Separately, \(\text{prop:bounded-outcome-gaussian-degeneracy}\) proves that every member of \(\mathcal P_{\mathrm G,n}^{\mathrm{JMS}}\) has \(\theta_0(P)=0\).  When that class is nonempty, the identically zero statistic has zero squared-error and generalized-quantile losses.  Nonnegativity and \(\text{def:minimax-risks}\) therefore give
\[
 \mathfrak R_n^{\mathrm G}
 =\mathfrak M_{n,1-\gamma}^{\mathrm G}=0.
\]
That proposition uses only the simultaneous exact partially linear conditional mean, bounded observed outcome, and nondegenerate Gaussian treatment-noise restrictions; it does not use the source's positive lower-bound claim.  The two conclusions concern separately defined classes, and no inclusion or equality between them has been proved.  They therefore establish neither a Gaussian-versus-non-Gaussian minimax dichotomy nor a local-to-Gaussian frontier.  This proves all asserted scope qualifications.