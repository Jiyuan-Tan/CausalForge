Complex zeros of the treatment-noise moment generating function yield exact shift-annihilating instruments.  With \(Z_n=T-\bar g_n(X)\), the identities \(F_n=M H_n\) and \(G_n=\theta_0F_n'+M B_n\) lead to an observable contour formula.  A non-effective compactness argument supplies a once-for-all finite contour bank, and the measurable split-sample contour statistic attains uniform MSE \(\Theta(n^{-1})\) on the fixed-separated non-Gaussian class whenever \(\varepsilon_{1,n}\le\varepsilon_0\), including fixed nonzero treatment-code error and arbitrary outcome-code error.  The formerly advertised Gaussian lower arm is invalid for the frozen class: bounded \(Y\), Gaussian residual support, and the global partially linear conditional mean force \(\theta_0=0\), so both Gaussian minimax losses are zero.  The contribution is therefore the proved non-Gaussian exact-annihilation and minimax result, with the local-to-Gaussian frontier left open.