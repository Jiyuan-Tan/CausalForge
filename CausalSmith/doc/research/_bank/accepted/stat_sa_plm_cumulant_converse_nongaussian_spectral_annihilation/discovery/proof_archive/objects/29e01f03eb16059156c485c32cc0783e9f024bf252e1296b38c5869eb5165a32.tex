Treatment is continuous with residual \(\eta=T-g_0(X)\).  The spectral class imposes independent cumulant-separated sub-Gaussian noise.  The frozen Gaussian branch additionally combines nondegenerate Gaussian residual support with bounded observed outcomes and the global partially linear conditional mean, which forces the coefficient to vanish.