Claim matched fixed-separation \(\Theta(n^{-1})\) MSE for the ordinary-sample Borel statistic on the broad direct-\(L^1(P_X)\) spectral class and on the full published ACE class. State represented-data computability only conditionally on a concrete compiled implementation of the displayed bounded-build substrate, using exactly the fixed primitive records shared with the Borel interpretation; canonicalize only observations and assert trace determinism only for identical full input records. Keep the ACE comparison upper-versus-upper and distinguish its class-minimax lower bound from a lower bound on the ACE estimator. Treat the local-to-Gaussian results as partial upper benchmarks and the bounded-outcome Gaussian calculation solely as a diagnostic. Do not claim alternative-name extensionality, a sharp local phase diagram, adaptive switching, uniform inference, or matching local lower bounds.