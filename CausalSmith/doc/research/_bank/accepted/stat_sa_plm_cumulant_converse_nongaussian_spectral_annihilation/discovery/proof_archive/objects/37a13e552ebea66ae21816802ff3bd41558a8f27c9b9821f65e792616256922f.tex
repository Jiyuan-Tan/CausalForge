Fix the explicit bank and put \(m_a=|I_a|\).  For \(n\ge2\), \(m_a\ge n/3\).  Given \(P\in\mathcal P_{\mathrm{NG},n}\), \(\text{lem:finite-contour-bank}\) supplies \(j_\star\) with positive zero count and \(\inf_{C_{j_\star}}|M|\ge a_\star\).  If \(\varepsilon_{1,n}\le\varepsilon_0\), \(\text{lem:l1-nuisance-zero-free}\) gives \(|H_n|\ge3/4\) on the bank disk and preserves zeros, hence
\[
 N_{C_{j_\star}}(F_n)\ge1,
 \qquad \inf_{C_{j_\star}}|F_n|\ge3a_\star/4. \tag{1}
\]
Let
\[
 d_a=\sup_{|z|\le R_1}|\widehat F_{a,n}-F_n|,
 \quad e_a=\sup_{|z|\le R_1}|\widehat G_{a,n}-G_n|,
 \quad \mathcal A_a=\{d_a\le a_\star/8\}.
\]
The certified modulus interval has width at most \(a_\star/64\).  On \(\mathcal A_0\), its lower endpoint at \(j_\star\) is at least \(39a_\star/64>a_\star/2\).  Rouch\'e's theorem and the winding error below \(1/4\) make the rounded integer equal to \(N_{C_{j_\star}}(F_n)\).  The selected index therefore satisfies
\[
 \inf_{C_{\widehat j_n}}|F_n|\ge31a_\star/64,
 \qquad \widehat N_{0\widehat j_n,n}=N_{C_{\widehat j_n}}(F_n)\ge1. \tag{2}
\]
On \(\mathcal A_0\cap\mathcal A_1\), the evaluation lower endpoint exceeds \(a_\star/4\), so no fallback occurs.

The algorithm is total because every modulus mesh is given by a finite ceiling; winding and ratio meshes are invoked only with a certified positive denominator, making their displayed Lipschitz constants finite and their ceiling calculations terminating.  Elementary interval refinement to a prescribed positive width terminates, while every failed certification triggers zero.  Each operation is Borel measurable; finite tie-breaking and fallback preserve measurability.

Uniformly over the class, \(\sup_{|z|\le R_1}|G_n(z)|\le C_G<\infty\), because \(|D_n|\le2C_g\), \(Z_n=\eta+D_n\), and
\[
 |G_n(z)|\le e^{2C_gR_1}\|Y\|_2\{E[e^{2R_1|\eta|}]\}^{1/2},
\]
whose factors are controlled by the class bounds.  On the good event, (2), \(\text{thm:exact-contour-identification}\), subtraction of ratios, and the certified numerical error at most \(n^{-1}\) give
\[
 |\widehat\theta_{\mathrm{spec},n}-\theta_0|
 \le L_\star(d_1+e_1)+n^{-1}. \tag{3}
\]
Real parts and projection cannot increase distance from \(\theta_0\).  By \(\text{lem:empirical-transform-uniform-l2}\),
\[
 E[d_a^2+e_a^2]\le K_1/n,
 \qquad P(\mathcal A_0^c\cup\mathcal A_1^c)\le K_2/n. \tag{4}
\]
On the complement, clipping bounds squared error by \(4C_\theta^2\).  Equations (3)--(4) prove the uniform \(C/n\) upper bound with one data-driven estimator.

For the converse, use \(\text{lem:non-gaussian-hard-submodel}\).  Choose \(0<h_0\le\min\{c_0,1,(8C_0)^{-1/2}\}\) and \(\vartheta_\pm=\pm h_0/(2\sqrt n)\).  Their target separation is \(h_0/\sqrt n\), their product Kullback--Leibler divergence is at most \(1/8\), and Pinsker gives total variation at most \(1/4\).  The sign test induced by any estimator yields
\[
 \max_{v\in\{-,+\}}E_{P_{n,\vartheta_v}}[(\widetilde\theta-\vartheta_v)^2]
 \ge\frac{h_0^2}{8n}(1-\|P_-^{\otimes n}-P_+^{\otimes n}\|_{\mathrm{TV}})
 \ge\frac{3h_0^2}{32n}.
\]
Taking the infimum proves \(c/n\).  Markov's inequality applied to the MSE bound proves the displayed generalized-quantile claim.  The formulas use no outcome code; the separate class definitions give the ACE disclaimer.  No overlap, propensity positivity, matrix rank, or inverse condition is used.  The only divisions occur after the certified positive boundary modulus and positive integer zero count have been established.