The causal target is \(\theta_0(P)\); the contour ratio is an observed-law identifying functional only under the proved zero-free, positive-count conditions.  At fixed separation, the concrete certified-name computation turns that identity into one measurable sample statistic with matched parametric MSE on the broad direct-\(L^1(P_X)\) class.  The same order on the full published ACE class is a minimax statement over all estimators, not a lower bound for the ACE statistic.  The Gaussian bounded-outcome proposition is a consistency diagnostic on a separate class, not a Gaussian-versus-non-Gaussian minimax dichotomy.