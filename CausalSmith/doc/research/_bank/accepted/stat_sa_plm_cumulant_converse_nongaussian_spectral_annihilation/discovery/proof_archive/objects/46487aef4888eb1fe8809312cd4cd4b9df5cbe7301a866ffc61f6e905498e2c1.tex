The first display follows directly from \(\text{thm:adaptive-rootn-minimax}\), whose constants are uniform in \(n\).  The condition \(\varepsilon_{1,n}\le\varepsilon_0\) allows a fixed positive sequence.  Inspection of \(\text{def:non-gaussian-class}\) and \(\text{def:adaptive-contour-estimator}\) shows that only \(\bar g_n\), not an outcome code or \(\varepsilon_{2,n}\), enters this branch.  The bank and all numerical certification are effective functions of the fixed class constants.

For the separate Gaussian intersection, \(\text{prop:bounded-outcome-gaussian-degeneracy}\) gives \(\theta_0(P)=0\) for every member.  If the class is nonempty, the zero statistic has zero squared and generalized-quantile loss, so nonnegativity and \(\text{def:minimax-risks}\) give both zero minimax criteria.  The definitions assert no relation between the two classes.  Thus these are separate conclusions, and the shrinking-separation and inference questions remain untouched.