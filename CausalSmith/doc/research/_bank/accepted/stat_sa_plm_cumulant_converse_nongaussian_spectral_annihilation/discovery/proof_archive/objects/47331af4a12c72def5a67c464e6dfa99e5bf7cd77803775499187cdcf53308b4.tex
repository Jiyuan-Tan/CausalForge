For each \(n\), the deterministic code pair is fixed before \(O_1,\ldots,O_n\stackrel{\mathrm{iid}}{\sim}P\) is drawn. Before \(n\), the law, or the sample varies, the experiment also fixes one actual positive primitive-name record for \((\delta,\psi_{\eta},R_0,R_1)\) and one positive represented record for \(C_{\theta}\). The ordinary-sample Borel statistic canonicalizes only \(T_i\), \(Y_i\), and \(\bar g_n(X_i)\), while retaining those exact primitive records. If the bounded-build substrate is compiled, the represented transducer receives the same full records. No alternative primitive-name record is substituted or quantified over in the correspondence claim.