Apply \(\text{thm:adaptive-rootn-minimax}\) under the fixed shared records
\(\mathfrak p_{\star}\) and \(\mathfrak c_{\theta,\star}\).  Its bank is
specified once from \(\mathfrak p_{\star}\) by
\(\text{def:contour-bank-handle}\) and
\(\text{lem:finite-contour-bank}\), without access to the treatment-noise
law.  Along every displayed eligible sequence, it gives constants
independent of \(n\) such that
\[
 \frac cn\le\mathfrak R_n^{\mathrm{NG}}\le\frac Cn.
\]
Hence \(\mathfrak R_n^{\mathrm{NG}}\asymp n^{-1}\).  The only restriction
on the treatment-code radius is \(\varepsilon_{1,n}\le\varepsilon_0\), so
eligible fixed nonzero radii are included.

The definition of \(\mathcal P_{\mathrm{NG},n}\) imposes the direct
\(L^1(P_X)\) treatment-code restriction and no condition involving
\(\widehat q_n\), \(\bar q_n\), or \(\varepsilon_{2,n}\).  The Borel
statistic likewise uses \(T_i,Y_i,\bar g_n(X_i)\), but no supplied outcome
code.  Its mathematical layer uses the fixed primitive records and
canonicalizes only observation coordinates.  If the bounded-build substrate
is implemented and compiled, the representation conclusion of the minimax
theorem supplies the additional transducer on exactly the same fixed
primitive records.  The correspondence does not range over alternative
primitive records, and trace identity is asserted only for identical full
input records.

Separately, \(\text{prop:bounded-outcome-gaussian-degeneracy}\) proves that
every \(P\in\mathcal P_{\mathrm G,n}^{\mathrm{JMS}}\) has
\(\theta_0(P)=0\).  If this class is nonempty, the identically zero statistic
has zero squared error and zero absolute-error generalized quantile under
every member.  Nonnegativity of the losses in \(\text{def:minimax-risks}\)
therefore gives
\[
 \mathfrak R_n^{\mathrm G}
 =\mathfrak M_{n,1-\gamma}^{\mathrm G}=0.
\]
This calculation uses only the simultaneous exact conditional-mean
restriction, bounded observed outcome, and nondegenerate Gaussian treatment
noise.  It does not use any positive lower-bound claim from the cited source.

Finally, the model restrictions are structurally incompatible.  Since
\(k=r+1\ge3\), Gaussian treatment noise has \(\kappa_k(\eta)=0\), whereas
every law in \(\mathcal P_{\mathrm{NG},n}\) has
\(|\kappa_k(\eta)|\ge\delta>0\).  Thus the two classes are disjoint; whenever
both are nonempty, neither contains the other.  No equality or inclusion is
needed for either preceding conclusion.  In particular, the two displays do
not compare a common local experiment and do not determine a
shrinking-separation frontier.