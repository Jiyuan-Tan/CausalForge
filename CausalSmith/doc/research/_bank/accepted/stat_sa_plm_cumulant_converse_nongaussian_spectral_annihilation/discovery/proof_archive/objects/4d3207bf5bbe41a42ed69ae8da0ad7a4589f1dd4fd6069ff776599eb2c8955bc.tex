For each \(n\), the deterministic supplied codes are fixed before \(O_1,\ldots,O_n\stackrel{\mathrm{iid}}{\sim}P\) is drawn.  The executable spectral algorithm accepts the supplied canonical certified-real names of \(T_i\), \(Y_i\), and \(\bar g_n(X_i)\), plus positive certified names of \(\delta,\psi_{\eta},R_0,R_1\) and exact \(k\).  It constructs residual names, circle nodes, complex-exponential rectangles, empirical transforms, winding enclosures, and the final contour enclosure compositionally through the concrete paper-independent substrate.  Every mesh and precision has explicit rational fuel; the algorithm receives neither exact-real values, an externally selected bank, an outcome code, nor the treatment-noise law.