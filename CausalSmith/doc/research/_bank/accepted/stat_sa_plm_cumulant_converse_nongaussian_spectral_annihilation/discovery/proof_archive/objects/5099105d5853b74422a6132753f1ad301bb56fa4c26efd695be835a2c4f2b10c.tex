The two branches share only the deterministic external inputs named in the statement; their law classes are distinct.

Along the stated sequence of non-Gaussian experiments, \(\text{thm:adaptive-rootn-minimax}\) gives constants \(0<c<C<\infty\), independent of \(n\), such that
\[
 \frac{c}{n}
 \le \mathfrak R_n^{\mathrm{NG}}
 \le \frac{C}{n}.
\]
Therefore \(\mathfrak R_n^{\mathrm{NG}}\asymp n^{-1}\).  The witnessing estimator uses \(\bar g_n\) only through \(Z_n=T-\bar g_n(X)\).  Neither membership in \(\mathcal P_{\mathrm{NG},n}\) nor the estimator uses \(\widehat q_n\), \(\bar q_n\), or \(\varepsilon_{2,n}\).  The result consequently includes any fixed nonzero \(\varepsilon_{1,n}\le\varepsilon_0\).

For the separate Gaussian branch, the repaired \(\text{thm:gaussian-quantile-to-mse}\) proves, for every \(n\),
\[
 \theta_0(P)=0\quad\text{for every }P\in\mathcal P_{\mathrm G,n}^{\mathrm{JMS}},
 \qquad
 \mathfrak R_n^{\mathrm G}
 =\mathfrak M_{n,1-\gamma}^{\mathrm G}=0.
\]
This conclusion uses no asymptotic nuisance-radius condition.  Since a positive lower bound is impossible on a zero-risk class, the published Gaussian lower theorem cannot be transferred to this frozen simultaneous-hypothesis class.  Combining the two conclusions neither requires nor establishes an inclusion or equality between the classes.