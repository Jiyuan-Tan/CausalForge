The frozen assumptions give only a lower bound on one cumulant.  After replacing \(\delta\) by \(\delta_n\), zero localization controls how far a zero may lie from the origin but supplies no quantitative lower bound on the treatment transform along a usable contour.  Fixed-separation compactness yields a non-effective modulus \(a_\star(\delta)\) for each fixed \(\delta>0\), with no rate as \(\delta\downarrow0\).  Consequently the current primitives do not control contour variance, selector errors, a weak-identification limit law, Wald coverage, or a matching local lower experiment.  The repaired Gaussian branch is degenerate and cannot supply the missing converse.