For each \(n\), the externally supplied deterministic code pair \((\widehat g_n,\widehat q_n)\) is fixed, or conditioned upon as fixed, before \(O_1,\ldots,O_n\stackrel{\mathrm{iid}}{\sim}P\) are drawn.  The spectral rule splits this inference sample into the fixed halves \(I_0\) and \(I_1\).  Independently of \(n\), \(P\), and the sample, one finite contour bank furnished existentially by the class constants is fixed once and for all before the statistic is defined.