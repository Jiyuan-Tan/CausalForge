For each \(n\), the same externally supplied deterministic code pair \((\widehat g_n,\widehat q_n)\) is clipped and offered to both formal branches before the independent inference sample is drawn.  This common-input convention does not make the law classes equal: the separated non-Gaussian branch is nontrivial, whereas the frozen bounded-observed-outcome Gaussian intersection has \(\theta_0=0\).