The two branches are separate law classes that share the displayed
deterministic inputs.  Along every indicated non-Gaussian sequence of
indices, \(\text{thm:adaptive-rootn-minimax}\) gives constants
\(0<c<C<\infty\), independent of \(n\) and of
\(\varepsilon_{2,n}\), such that
\[
 \frac cn\le\mathfrak R_n^{\mathrm{NG}}\le\frac Cn.
\]
The only radius restriction used there is
\(\varepsilon_{1,n}\le\varepsilon_0\), which permits a fixed nonzero
treatment radius.  This proves
\(\mathfrak R_n^{\mathrm{NG}}\asymp n^{-1}\).

Separately, for all sufficiently large indices satisfying the Gaussian
hypotheses, \(\text{thm:gaussian-quantile-to-mse}\) gives
\[
 \mathfrak R_n^{\mathrm G}\ge\gamma L_{\mathrm G,n,\gamma}^2.
\]
Substituting the definition of \(L_{\mathrm G,n,\gamma}\) yields
\[
 \mathfrak R_n^{\mathrm G}\ge
 \gamma c_{\gamma}^2\left[
 \sigma^{-2}\varepsilon_{1,n}\varepsilon_{2,n}
 \left\{\frac{\log(1/\varepsilon_{1,n})}{\log n}\right\}^{3}
 +\sigma^{-1}n^{-1/2}\right]^2.
\]
The common-code premise is respected because both definitions use the
same deterministic supplied codes, clipped codes, and radii.  No equality
or inclusion between the Gaussian and non-Gaussian law classes is used or
asserted.