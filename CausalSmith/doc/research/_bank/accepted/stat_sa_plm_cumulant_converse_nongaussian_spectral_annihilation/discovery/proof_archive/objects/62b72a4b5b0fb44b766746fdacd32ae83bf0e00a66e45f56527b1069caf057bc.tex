For each \(n\), the externally supplied deterministic code pair is fixed before \(O_1,\ldots,O_n\stackrel{\mathrm{iid}}{\sim}P\) is drawn.  The spectral algorithm receives canonical dyadic names of \(T_i\), \(Y_i\), and \(\bar g_n(X_i)\), internally computes the bank from canonical certified names of \(k,\delta,\psi_{\eta},R_0,R_1\), and derives every empirical transform, derivative, Lipschitz, winding, and final-integrand enclosure by finite outward rational refinement.  It receives neither an externally selected bank nor the treatment-noise law.