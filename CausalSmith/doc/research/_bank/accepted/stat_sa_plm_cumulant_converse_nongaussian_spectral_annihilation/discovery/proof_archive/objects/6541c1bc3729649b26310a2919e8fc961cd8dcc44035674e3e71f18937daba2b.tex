For fixed nonzero cumulant separation, a once-for-all certified translated-dyadic contour bank and a total split-sample statistic identify and estimate the partially linear causal coefficient with uniform matched \(\Theta(n^{-1})\) mean-squared error using only a sufficiently accurate direct \(L^1(P_X)\) treatment code, no outcome learner, and no knowledge of the treatment-noise law. The full published Jin--Mackey--Syrgkanis ACE class is contained in this broad spectral class, so the exact finite-order ACE upper bound and the spectral upper guarantee can be compared on the full published ACE class. The stronger \(L^s(P_X)\) comparison subclass remains useful for \(s>r\) and equals the published ACE class when \(s=r\). The bounded-outcome Gaussian calculation is only a source-assumption diagnostic: the simultaneous stated restrictions force \(\theta_0=0\).