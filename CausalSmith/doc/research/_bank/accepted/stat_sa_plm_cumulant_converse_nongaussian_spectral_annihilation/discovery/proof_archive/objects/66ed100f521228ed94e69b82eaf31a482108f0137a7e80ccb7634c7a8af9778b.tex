At every fixed positive separation, the effective bank has the explicit zero-count and modulus in \(\text{def:contour-bank-handle}\), and the certified statistic has \(C/n\) MSE for \(\varepsilon_{1,n}\le\varepsilon_0\).  Substituting a shrinking \(\delta_n\) into \(R_{0,n}\) yields an explicit but rapidly deteriorating contour upper bound.  Independently, \(\text{thm:local-to-gaussian-partial-benchmarks}\) supplies the exact local ACE upper benchmark and the Gaussian--Rademacher-path sine bound.  None matches a full-class local converse or yields coverage-valid adaptive switching.