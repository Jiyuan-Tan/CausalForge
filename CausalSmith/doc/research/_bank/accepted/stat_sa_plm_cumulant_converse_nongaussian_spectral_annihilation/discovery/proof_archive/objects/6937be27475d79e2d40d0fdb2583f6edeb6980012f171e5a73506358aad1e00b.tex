The lower bound uses an explicit Gaussian-outcome-innovation path that preserves the law of \((X,T)\), both regression functions, and the deterministic supplied codes; on the published ACE branch this preservation keeps both \(L^r(P_X)\) code bounds intact.