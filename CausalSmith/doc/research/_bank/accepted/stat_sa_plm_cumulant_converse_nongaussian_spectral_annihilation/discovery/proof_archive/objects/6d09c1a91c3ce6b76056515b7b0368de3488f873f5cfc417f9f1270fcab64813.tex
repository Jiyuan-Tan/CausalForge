Use the explicit bank from \(\text{def:contour-bank-handle}\).  On split \(I_a\), circle \(C_j\), and angular mesh \(t_v=2\pi v/L\), set \(z_v=\rho_je^{it_v}\).  The observable bounds
\[
 A_{F,p}^{(a,j)}=|I_a|^{-1}\sum_{i\in I_a}|Z_{n,i}|^p e^{\rho_j|Z_{n,i}|},
 \quad
 A_{G,p}^{(a,j)}=|I_a|^{-1}\sum_{i\in I_a}|Y_i||Z_{n,i}|^p e^{\rho_j|Z_{n,i}|}
\]
bound \(\sup_{C_j}|\widehat F_{a,n}^{(p)}|\) and \(\sup_{C_j}|\widehat G_{a,n}^{(p)}|\).  With \(\Delta_L=2\pi/L\), define the certified boundary-modulus interval
\[
 \left[\min_v|\widehat F_{a,n}(z_v)|-\rho_jA_{F,1}^{(a,j)}\Delta_L/2,
       \min_v|\widehat F_{a,n}(z_v)|\right].
\]
Choose the least \(L\) making its width at most \(a_{\star}/64\).  This is a finite explicit ceiling calculation, with \(L=1\) when the Lipschitz bound is zero.

If the lower endpoint is \(b>0\), approximate the winding average of
\(w(t)=\rho_je^{it}\widehat F'_{a,n}(\rho_je^{it})/\widehat F_{a,n}(\rho_je^{it})\) by the periodic trapezoid rule.  Its angular Lipschitz constant is bounded observably by
\[
 L_w=\rho_jA_{F,1}^{(a,j)}/b+
 \rho_j^2\{A_{F,2}^{(a,j)}/b+(A_{F,1}^{(a,j)})^2/b^2\}.
\]
Choose the least \(L\) with \(L_w\Delta_L/2\le1/8\), enclose each elementary evaluation to make the additional average error at most \(1/8\), and round to the unique integer within distance \(1/4\); if there is no such integer, record failure.  This defines \(\widehat N_{aj,n}\).

Declare \(j\) pilot-admissible when its pilot lower endpoint is at least \(a_{\star}/2\), winding certification succeeds, and \(\widehat N_{0j,n}\ge1\).  Select the smallest index among admissible maximizers of the certified lower endpoint.  Return zero if none exists or if the evaluation lower endpoint on the selected circle is below \(a_{\star}/4\).  Otherwise approximate the contour-ratio average with integrand \(v(t)=\rho_je^{it}\widehat G_{1,n}(\rho_je^{it})/\widehat F_{1,n}(\rho_je^{it})\).  If \(b>0\) is its certified denominator, an observable angular Lipschitz bound is
\[
 L_v=\rho_jA_{G,0}^{(1,j)}/b+
 \rho_j^2\{A_{G,1}^{(1,j)}/b+A_{G,0}^{(1,j)}A_{F,1}^{(1,j)}/b^2\}.
\]
Choose the least mesh size making the trapezoid error at most \(1/(2n)\), and use deterministic interval evaluation until the complex midpoint has additional error at most \(1/(2n)\).  Divide by \(\widehat N_{0j,n}\), take the real part, round to the midpoint of a rational interval of radius at most \(1/n\), and project onto \([-C_{\theta},C_{\theta}]\).  Every mesh size is an explicit finite ceiling once a contour is admitted; nonadmitted cases use the displayed deterministic fallback.  Thus the algorithm is total, every refinement terminates, and finite sums, interval endpoints, integer rounding, finite tie-breaking, rational midpoint selection, and projection define a measurable sample map.