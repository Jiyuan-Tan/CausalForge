The available routes were exhausted as follows: substitute \(\delta_n\) into the zero-localization and nuisance-zero-free arguments; specialize the known-zero construction to an explicit Gaussian--Rademacher path; and import the local finite-order ACE guarantee under its exact eligibility condition.  These yield the benchmarks below but do not relate the non-effective contour modulus to \(\delta_n\), construct a uniformly valid adaptive switch, or produce a full-class lower bound.