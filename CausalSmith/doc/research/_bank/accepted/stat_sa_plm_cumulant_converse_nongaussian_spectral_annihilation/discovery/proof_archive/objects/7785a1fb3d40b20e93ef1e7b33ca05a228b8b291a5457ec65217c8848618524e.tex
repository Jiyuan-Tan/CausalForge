The deterministic radii \(\varepsilon_{1,n}\) and \(\varepsilon_{2,n}\) are nonincreasing, so the uncertainty balls are nested.  The spectral estimator uses only \(\bar g_n\), and its bank is computed from certified-real names of primitive constants without the unknown treatment-noise law; the Gaussian diagnostic uses the same clipped pair and radii.