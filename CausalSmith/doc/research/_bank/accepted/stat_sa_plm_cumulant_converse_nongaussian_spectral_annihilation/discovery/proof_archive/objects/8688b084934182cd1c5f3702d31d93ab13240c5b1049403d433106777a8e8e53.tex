Use the full published ACE class for the exact ACE and spectral upper-guarantee comparison, and use the auxiliary \(L^s(P_X)\) subclass only when that stronger supplied-code regime is itself relevant.