Independent cumulant-separated sub-Gaussian treatment noise creates an exact null space for every location shift induced by treatment-code error. A sufficiently small fixed direct \(L^1(P_X)\) residual contamination preserves observable zero geometry. One fixed supplied primitive-name record determines the same law-blind bank for all \(n\); both the ordinary Borel interpretation and the conditional future transducer use that identical record and canonicalize only observation coordinates.