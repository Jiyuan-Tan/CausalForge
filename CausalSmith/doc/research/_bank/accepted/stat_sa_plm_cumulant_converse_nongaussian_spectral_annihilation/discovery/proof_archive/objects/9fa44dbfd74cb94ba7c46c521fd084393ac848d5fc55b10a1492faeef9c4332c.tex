The treatment is continuous with \(\eta=T-g_0(X)\).  The fixed-separated spectral class imposes independent cumulant-separated sub-Gaussian treatment noise.  The separate Gaussian intersection reproduces simultaneous known-variance and bounded-observed-outcome assumptions used only for the source-assumption diagnostic.