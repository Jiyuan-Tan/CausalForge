The causal target is \(\theta_0(P)\); the observable contour ratio is its identifying functional only under the proved boundary, zero-free, and positive-count conditions. At fixed separation, the ordinary-sample Borel statistic has matched parametric MSE on the broad direct-\(L^1(P_X)\) class and the full published ACE class. The represented-data computability statement is strictly conditional on compiling the explicit bounded-build substrate and is relative to the same fixed primitive records used by the Borel interpretation. The ACE lower bound is class-level over all estimators, not a lower bound for the ACE statistic. The Gaussian proposition is only an assumption-consistency diagnostic.