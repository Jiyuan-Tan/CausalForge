The matched \(n^{-1}\) MSE result is an effective fixed-separation theorem under the certified-real input convention.  It allows sufficiently small fixed nonzero direct \(L^1(P_X)\) treatment-code error, uses no supplied outcome code, and does not know the treatment-noise law.  The ACE comparison is only an upper-guarantee comparison on \(\mathcal P_{\mathrm{NG},n}^{\mathrm{ACE}}\) under the exact published eligibility condition and constants.  The bounded-observed-outcome Gaussian intersection forces \(\theta_0=0\) and is used only to diagnose the simultaneous source assumptions, not as a positive Gaussian lower-bound arm or a Gaussian-versus-non-Gaussian dichotomy.  The local-to-Gaussian frontier, adaptive switching, uniform inference, and matching local lower bounds remain open.