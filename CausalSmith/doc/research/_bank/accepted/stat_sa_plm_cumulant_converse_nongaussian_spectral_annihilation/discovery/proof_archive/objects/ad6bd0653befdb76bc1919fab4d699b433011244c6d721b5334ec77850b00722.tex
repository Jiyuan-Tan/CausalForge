The current lemma records only the broad-class branch. The same outcome tilt preserves the published ACE class because it leaves the entire treatment law, both regression functions, both clipped codes, and both \(L^r(P_X)\) code errors unchanged; adding this branch exposes the transferable two-point converse required by the maximality directive.