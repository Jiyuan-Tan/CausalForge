Use the effective dyadic bank and the explicit value \(a_{\star}=a_{\star}^{\mathrm{eff}}\) in \(\text{def:contour-bank-handle}\).  On each split \(I_a\), form \(\widehat F_{a,n}\) and \(\widehat G_{a,n}\).  For every bank circle, deterministic interval quadrature on its fixed angular parameterization computes certified intervals of width at most \(a_{\star}/64\) for \(\inf_{C_j}|\widehat F_{a,n}|\).  Such intervals are obtained from a uniform angular mesh and the observable derivative bound \(\sup_{C_j}|\widehat F'_{a,n}|\le |I_a|^{-1}\sum_{i\in I_a}|Z_{n,i}|e^{\rho_j|Z_{n,i}|}\).  Whenever the certified lower endpoint is positive, the same deterministic refinement computes the winding integral to error less than \(1/4\), and its unique integer rounding is denoted \(\widehat N_{aj,n}\).

Declare \(j\) pilot-admissible when the certified lower endpoint for \(\inf_{C_j}|\widehat F_{0,n}|\) is at least \(a_{\star}/2\) and \(\widehat N_{0j,n}\ge1\).  Let \(\widehat j_n\) be the smallest pilot-admissible index maximizing that certified lower endpoint.  Return zero if no such index exists or if the certified evaluation-fold lower endpoint on \(C_{\widehat j_n}\) is below \(a_{\star}/4\).  Otherwise compute the contour ratio by certified deterministic quadrature with absolute error at most \(n^{-1}\), and set
\[
 \widehat\theta_{\mathrm{spec},n}
 =\Pi_{[-C_{\theta},C_{\theta}]}
 \operatorname{Re}\!\left[
 \frac{1}{\widehat N_{0\widehat j_n,n}2\pi i}
 \oint_{C_{\widehat j_n}}
 \frac{\widehat G_{1,n}(z)}{\widehat F_{1,n}(z)}\,dz
 \right],
\]
where the displayed integral denotes the certified approximation just described.  Interval refinement, integer rounding, smallest-index tie breaking, and the two fallback rules make this a total measurable and computable sample map.  It uses \(Y\) and \(Z_n=T-\bar g_n(X)\), but no supplied outcome code and no knowledge of the law of \(\eta\).