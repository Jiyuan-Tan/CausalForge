At fixed cumulant separation, zeros of the treatment-noise moment generating function give exact shift-annihilating instruments, and the observable factorization \(F_n=M H_n\) yields an explicit contour identity for the causal coefficient \(\theta_0(P)\).  Compactness proves the existence of a finite uniformly conditioned contour bank depending only on class constants.  After one such bank is fixed once and for all, the prescribed split-sample contour rule is a measurable statistic and has uniform mean-squared error \(\Theta(n^{-1})\) whenever \(\varepsilon_{1,n}\le\varepsilon_0\), including fixed nonzero treatment-code error and without any supplied outcome code.  The bank is nonconstructive: no effective enumeration, numerical radii, conditioning modulus, or implementable bank-finding algorithm is claimed.  The Gaussian comparison remains a separate-class benchmark, and the local-to-Gaussian frontier remains open.