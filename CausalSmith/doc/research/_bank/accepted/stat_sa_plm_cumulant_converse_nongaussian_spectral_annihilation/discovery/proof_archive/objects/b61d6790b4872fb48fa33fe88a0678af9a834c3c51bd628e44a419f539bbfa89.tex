Apply \(\text{thm:adaptive-rootn-minimax}\).  The certified translated-dyadic bank is computed once from the primitive-constant names, its statistic is a total measurable computable sample map, and on every stated index
\[
 c/n\le\mathfrak R_n^{\mathrm{NG}}\le C/n.
\]
This is exactly \(\mathfrak R_n^{\mathrm{NG}}\asymp n^{-1}\), including when the nonincreasing sequence \(\varepsilon_{1,n}\) is fixed at any positive value not exceeding \(\varepsilon_0\).  Inspection of \(\text{def:non-gaussian-class}\) and \(\text{def:adaptive-contour-estimator}\) shows that the class and statistic use \(\bar g_n\), but impose no restriction involving and make no use of \(\widehat q_n\), \(\bar q_n\), or \(\varepsilon_{2,n}\).

Separately, \(\text{prop:bounded-outcome-gaussian-degeneracy}\) proves that \(\theta_0(P)=0\) for every member of the simultaneous Gaussian intersection and, when the intersection is nonempty, computes both its minimax squared-error and generalized-quantile losses as zero.  The two conclusions concern separately defined classes, and no inclusion or equality between them has been established.  They therefore imply neither a Gaussian-versus-non-Gaussian minimax dichotomy nor a shrinking-separation frontier.  The fixed-separation theorem explicitly supplies neither a useful rate when \(\delta_n\downarrow0\) nor uniform inference, which proves the final scope statement.