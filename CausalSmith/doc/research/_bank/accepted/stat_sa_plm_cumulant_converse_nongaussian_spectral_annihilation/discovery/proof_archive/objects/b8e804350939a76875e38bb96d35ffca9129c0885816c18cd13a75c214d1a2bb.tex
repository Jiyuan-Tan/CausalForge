Put \(m_a=|I_a|\).  For \(n\ge2\), \(m_a\ge n/3\).  Fix \(P\in\mathcal P_{\mathrm{NG},n}\).  The certified bank from \(\text{lem:finite-contour-bank}\) supplies an index \(j_{\star}\) such that
\[
 N_{C_{j_{\star}}}(M)\ge1,
 \qquad \inf_{C_{j_{\star}}}|M|\ge a_{\star}.
\]
If \(\varepsilon_{1,n}\le\varepsilon_0\), \(\text{lem:l1-nuisance-zero-free}\) gives \(|H_n|\ge3/4\) throughout the bank disk and preserves every zero and its multiplicity.  Thus
\[
 N_{C_{j_{\star}}}(F_n)\ge1,
 \qquad \inf_{C_{j_{\star}}}|F_n|\ge3a_{\star}/4. \tag{1}
\]
Let
\[
 d_a=\sup_{|z|\le R_1}|\widehat F_{a,n}(z)-F_n(z)|,
 \qquad
 e_a=\sup_{|z|\le R_1}|\widehat G_{a,n}(z)-G_n(z)|,
 \qquad
 \mathcal A_a=\{d_a\le a_{\star}/8\}.
\]
The certified modulus interval loses at most \(a_{\star}/128\) to its mesh and \(a_{\star}/128\) to node evaluation.  On \(\mathcal A_0\), its lower endpoint at \(j_{\star}\) is therefore at least
\[
 3a_{\star}/4-a_{\star}/8-a_{\star}/64
 =39a_{\star}/64>a_{\star}/2. \tag{2}
\]
Rouch\'e's theorem makes the empirical and population zero counts equal there, and the strict winding enclosure error below \(1/4\) certifies that integer.  Hence \(j_{\star}\) is admissible.  Since the selector maximizes the certified lower endpoint, the selected contour obeys
\[
 \inf_{C_{\widehat j_n}}|F_n|
 \ge39a_{\star}/64-a_{\star}/8
 =31a_{\star}/64,
 \qquad
 \widehat N_{0\widehat j_n,n}=N_{C_{\widehat j_n}}(F_n)\ge1. \tag{3}
\]
On \(\mathcal A_0\cap\mathcal A_1\), (3) and the same outward errors make the evaluation lower endpoint larger than \(a_{\star}/4\), so no fallback occurs.  Rouch\'e's theorem also makes the evaluation-fold zero count equal the population and pilot counts.

Every numerical subroutine is total.  The modulus mesh has a prescribed finite ceiling.  Winding and ratio refinement are invoked only after a certified positive denominator; their displayed observable derivative bounds are then finite, so uniform-mesh quadrature error tends to zero.  Outward evaluation intervals for the finite exponential sums can likewise be refined to every prescribed positive width.  Thus the winding enclosure reaches strict radius below \(1/4\), and the single enclosure of \((2\pi\widehat N_{0\widehat j_n,n})^{-1}\int_0^{2\pi}V(t)\,dt\) reaches radius at most \(1/n\).  Its one rational midpoint has complex error at most \(1/n\) and is the sole output before taking the real part and projecting.  Finite sums, finite refinement, fixed parameterizations, smallest-index tie-breaking, rational midpoints, projection, and fallback are Borel operations.  This proves totality, measurability, and computability from the stated inputs.

Uniformly over the class,
\[
 \sup_{|z|\le R_1}|G_n(z)|\le C_G<\infty, \tag{4}
\]
because \(|D_n|\le2C_g\), \(Z_n=\eta+D_n\), and
\[
 |G_n(z)|
 \le e^{2C_gR_1}\|Y\|_2
      \{E_Pe^{2R_1|\eta|}\}^{1/2},
\]
whose factors are controlled by the bounded regressions, bounded coefficient, and the two sub-Gaussian envelopes.  On the good event, (3), \(\text{thm:exact-contour-identification}\), the identity
\[
 \frac{\widehat G}{\widehat F}-\frac G F
 =\frac{\widehat G-G}{\widehat F}
   +G\frac{F-\widehat F}{{\widehat F}F},
\]
the fixed contour lengths, and (4) give a class-constant \(L_{\star}<\infty\) such that the sole numerical enclosure contributes no more than \(1/n\) and
\[
 |\widehat\theta_{\mathrm{spec},n}-\theta_0|
 \le L_{\star}(d_1+e_1)+n^{-1}. \tag{5}
\]
Taking real parts and projecting cannot increase distance from \(\theta_0\in[-C_{\theta},C_{\theta}]\).  By \(\text{lem:empirical-transform-uniform-l2}\),
\[
 E_P[d_a^2+e_a^2]\le K_1/n,
 \qquad
 P(\mathcal A_0^c\cup\mathcal A_1^c)\le K_2/n. \tag{6}
\]
On the failure event clipping bounds the squared error by \(4C_{\theta}^2\).  Squaring (5), using (6), and enlarging a class-uniform constant proves
\[
 \sup_{P\in\mathcal P_{\mathrm{NG},n}}
 E_P[(\widehat\theta_{\mathrm{spec},n}-\theta_0(P))^2]
 \le C/n.
\]
Since the minimax infimum ranges over all measurable sample maps, the middle inequality follows from this explicit witness.

For the converse, invoke \(\text{lem:non-gaussian-hard-submodel}\).  Choose
\[
 0<h_0\le\min\{c_0,1,(8C_0)^{-1/2}\},
 \qquad \vartheta_{\pm}=\pm h_0/(2\sqrt n).
\]
Their target separation is \(h_0/\sqrt n\), and their product Kullback--Leibler divergence is at most \(1/8\).  Pinsker's inequality gives total variation at most \(1/4\).  The sign test induced by any estimator then yields
\[
 \max_{v\in\{-,+\}}
 E_{P_{n,\vartheta_v}}[(\widetilde\theta-\vartheta_v)^2]
 \ge\frac{h_0^2}{8n}
 \{1-\|P_-^{\otimes n}-P_+^{\otimes n}\|_{\mathrm{TV}}\}
 \ge\frac{3h_0^2}{32n}.
\]
Taking the infimum proves the lower bound.  Finally, Markov's inequality applied to the MSE bound gives
\[
 P\left(|\widehat\theta_{\mathrm{spec},n}-\theta_0|>\sqrt{C/(\gamma n)}\right)
 \le\gamma,
\]
which is the asserted generalized-quantile bound under the definition of \(Q_{P,1-\gamma}\).  No outcome code, overlap, propensity positivity, matrix rank, or inverse is used; every division is licensed by a certified positive boundary modulus and a positive integer winding number.