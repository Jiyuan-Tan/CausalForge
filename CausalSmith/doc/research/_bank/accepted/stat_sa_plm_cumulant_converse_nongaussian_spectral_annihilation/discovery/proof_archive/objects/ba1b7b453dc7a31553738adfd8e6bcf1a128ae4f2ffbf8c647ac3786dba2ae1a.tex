The causal target is \(\theta_0(P)\).  For every \(P\in\mathcal P_{\mathrm{NG},n}\), the observable contour ratio is its identifying functional only on contours satisfying the proved boundary zero-free, nuisance-transform zero-free, and positive-count conditions.  At fixed separation, certification turns that identity into a computable sample statistic with matched parametric mean-squared error on the broad direct-\(L^1(P_X)\) class.  Because the lower-bound outcome tilt also preserves both published \(L^r(P_X)\) code restrictions, the full published ACE class has the same matched class-minimax order.  This is a statement over all estimators, not a lower bound for the particular ACE statistic or its generalized-quantile upper guarantee.  The Gaussian bounded-outcome proposition does not establish a Gaussian-versus-non-Gaussian minimax dichotomy: it shows only that one simultaneous source-stated intersection has the identically zero target.