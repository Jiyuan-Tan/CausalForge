Fix once for all the actual positive primitive-name record \(\mathfrak p_{\star}\) for \((\delta,\psi_{\eta},R_0,R_1)\) and the represented range record \(\mathfrak c_{\theta,\star}\). Relative to these same fixed records, the certified translated-dyadic bank and the ordinary-sample Borel statistic define an observed-sample map that canonicalizes only observation coordinates and satisfies, for every \(n\ge2\) with \(\mathcal P_{\mathrm{NG},n}\neq\varnothing\) and \(\varepsilon_{1,n}\le\varepsilon_0\), \(\sup_{P\in\mathcal P_{\mathrm{NG},n}}E_P[(\widehat\theta_{\mathrm{spec},n}-\theta_0(P))^2]\le C/n\). If the bounded-build substrate is concretely implemented and compiled, the represented-data transducer on those identical fixed primitive records returns a certified-real name with the same value; no correspondence over alternative primitive records is claimed.