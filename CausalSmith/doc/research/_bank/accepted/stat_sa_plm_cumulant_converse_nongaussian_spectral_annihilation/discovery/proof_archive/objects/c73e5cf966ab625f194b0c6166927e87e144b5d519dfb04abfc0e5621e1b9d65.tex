For every fixed \(n\), the exact factorization and contour identity remain valid.  The zero-localization proof gives the explicit radius bound
\[
 R_{0,n}\le A_k(\psi_{\eta}^2/\delta_n)^{1/(k-2)}.
\]
If, in addition, the treatment-code radius satisfies
\[
 \varepsilon_{1,n}\le\{4(R_{0,n}+1)e^{2C_g(R_{0,n}+1)}\}^{-1},
\]
then \(H_n\) is zero-free on the corresponding disk and the observable transform inherits the latent zeros.  Thus the remaining problem is quantitative conditioning and minimax matching, not point identification.