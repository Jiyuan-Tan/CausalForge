For each \(n\), the same externally supplied deterministic code pair \((\widehat g_n,\widehat q_n)\) is clipped and presented to both formally separate branches; the pair may vary with \(n\) but is fixed before the independent inference sample is drawn. Sharing these inputs does not imply any inclusion or equivalence of the two law classes.