For fixed nonzero cumulant separation and one experiment-wide certified primitive-name record, a translated-dyadic contour bank and a total ordinary-sample Borel statistic estimate the partially linear causal coefficient with uniform matched \(\Theta(n^{-1})\) mean-squared error using only a sufficiently accurate direct \(L^1(P_X)\) treatment code, no outcome learner, and no knowledge of the treatment-noise law. If the explicit bounded-build complex interval substrate is implemented and compiled, the same value is additionally returned by a represented-data transducer using the identical fixed primitive records and canonical observation records. The full published ACE class has the same matched class-minimax order, while the estimator comparison remains upper-versus-upper. The bounded-outcome Gaussian calculation is only a source-assumption diagnostic forcing \(\theta_0=0\).