For fixed nonzero cumulant separation, a once-for-all certified translated-dyadic contour bank and a total split-sample statistic estimate the partially linear causal coefficient with uniform matched \(\Theta(n^{-1})\) mean-squared error using only a sufficiently accurate direct \(L^1(P_X)\) treatment code, no outcome learner, and no knowledge of the treatment-noise law.  The population contour identity is asserted only for \(P\in\mathcal P_{\mathrm{NG},n}\), where the learned-residual factorization holds.  An ACE-preserving outcome-tilt converse and the spectral upper bound give the same matched class-minimax order on the full published ACE class, while the ACE-versus-spectral generalized-quantile comparison remains strictly upper-versus-upper.  The bounded-outcome Gaussian calculation is solely a source-assumption diagnostic: the simultaneous stated restrictions force \(\theta_0=0\).