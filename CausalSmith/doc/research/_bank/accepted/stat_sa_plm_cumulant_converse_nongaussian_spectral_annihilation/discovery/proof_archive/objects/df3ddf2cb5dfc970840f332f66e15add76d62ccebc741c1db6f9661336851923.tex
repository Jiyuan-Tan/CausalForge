The proved statistical contribution is the fixed-separated non-Gaussian minimax theorem: global transform zeros eliminate the entire location-shift nuisance rather than finitely many Taylor terms, so a small fixed treatment-code error and an arbitrary outcome-code error are compatible with root-\(n\) estimation. The bank is a once-for-all existence witness depending only on class constants, not an effectively enumerable algorithm. The displayed Gaussian intersection supplies no opposing nuisance-product barrier because it is degenerate at \(\theta_0=0\); accordingly, the result should not be advertised as a proved nontrivial Gaussian-versus-non-Gaussian minimax dichotomy. The novelty claim is confined to the PLM annihilator, observable factorization and contour functional, unknown-noise adaptation, and the fixed-separated minimax consequence, not to classical location-family incompleteness itself.