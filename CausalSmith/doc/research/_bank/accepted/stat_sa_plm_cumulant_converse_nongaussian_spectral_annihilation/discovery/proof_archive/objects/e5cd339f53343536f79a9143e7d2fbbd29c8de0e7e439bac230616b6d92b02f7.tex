\textbf{Non-proof; established partial results only.}  At every fixed \(\delta>0\), \(\text{thm:adaptive-rootn-minimax}\) supplies an unconditional total ordinary-sample Borel statistic and constants \(0<c<C<\infty\) such that, whenever the displayed nonemptiness and direct \(L^1(P_X)\) treatment-code conditions hold,
\[
 \frac{c}{n}
 \le \mathfrak R_n^{\mathrm{NG}}
 \le \sup_{P\in\mathcal P_{\mathrm{NG},n}}
 E_P\!\left[(\widehat\theta_{\mathrm{spec},n}-\theta_0(P))^2\right]
 \le \frac{C}{n}.
\]
Thus the fixed-separation ordinary-sample result has matched \(\Theta(n^{-1})\) mean-squared error.  The represented-data executable transducer is not unconditional: it exists only if a future concrete implementation satisfying the certified-contour arithmetic substrate's bounded-build obligation \(\mathsf{CCIA}_{\mathrm{build}}\) is added and compiled.

Along \(\delta_n\downarrow0\), \(\text{thm:local-to-gaussian-partial-benchmarks}\) establishes only two upper benchmarks.  First, if \(\varepsilon_{1,n},\varepsilon_{2,n}>0\), every logarithm and denominator in its displayed local eligibility condition is defined, and that exact eligibility inequality holds, then the order-\(r\) ACE estimator satisfies its displayed generalized-quantile upper bound \(U_{\mathrm{ACE},n}^{\mathrm{loc}}\), whose leading separation factor is \(\delta_n^{-1}\).  Second, on the Gaussian--Rademacher path, with \(\Delta_a=2a^4\), it gives the path-specific mean-squared-error upper bound
\[
 C\min\!\left\{
  1,
  n^{-1}\Delta_a^{-1/2}
  \exp\!\left(\frac{\pi^2\sqrt2}{4}\Delta_a^{-1/2}\right)
 \right\}
\]
provided \(\varepsilon_{1,n}\le\Delta_a^{1/4}/(2^{1/4}\pi)\).  Neither benchmark is called computable here absent a separate represented implementation.  Neither constructs a data-driven selector or inference procedure, determines a sharp full-class phase diagram, or proves a matching local minimax lower bound.  Consequently this node remains an unresolved, nonassertive research-question carrier.