The deterministic radii \(\varepsilon_{1,n}\) and \(\varepsilon_{2,n}\) are nonincreasing. The spectral estimator uses only \(\bar g_n\) and is uniform over the supplied outcome code. On the simultaneous Gaussian intersection the zero statistic ignores both codes because the target is identically zero; no nuisance-product lower bound is claimed there.