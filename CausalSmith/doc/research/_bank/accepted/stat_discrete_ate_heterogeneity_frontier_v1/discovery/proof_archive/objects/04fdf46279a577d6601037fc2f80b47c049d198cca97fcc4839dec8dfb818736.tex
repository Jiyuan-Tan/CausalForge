The existing radius channel was rescaled and still produced the proved product separation.  Mixing unrestricted priors with a common center again multiplied the source separation by \(\sigma\).  A paired construction with masses \(p_+=Pr\), \(p_-=P(1-r)\) and effects \(t_+=\theta+\sigma(1-r)\), \(t_-=\theta-\sigma r\) was checked to satisfy exact centering and realization-wise radius control; translating \(\theta\) and \(r\) preserves the two cell effects, but deterministic \(P\) fails label-intensity first-moment matching.  Convex aggregation of the two frozen estimators was optimized and remained constant-equivalent to the selector.  Finally, a denominator-free polynomial collision-score correction achieved product-radius population bias, but absolute coefficient bounds did not yield the required cancellation-aware stochastic bound.