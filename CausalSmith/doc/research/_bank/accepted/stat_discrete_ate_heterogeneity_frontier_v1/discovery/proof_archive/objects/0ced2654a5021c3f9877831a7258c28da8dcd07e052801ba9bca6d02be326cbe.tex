The all-alphabet results establish
\[
 c_{\epsilon}M^2\left\{n^{-1}+\min(1,d/n^2)+\sigma^2\min(1,u)\right\}
 \le \mathsf R_{n,d,\epsilon,M,\sigma}
 \le C_{\epsilon}M^2\left\{n^{-1}+\min(1,u,\sigma^2+d/n^2)\right\}.
\]
Inside the residual wedge \(b\ll u\ll1\), \(b\ll\sigma^2\ll1\), the caps are inactive and these reduce to the previously localized benchmarks.  They match at both endpoints, every fixed positive radius, saturation, \(u\lesssim b\), and \(\sigma^2\lesssim b\).  The hypothesis-independent Bernoulli channel still yields only target separation of order \(M\sigma\min\{1,d/[n\log(en)]\}\), whose square is the product lower term.