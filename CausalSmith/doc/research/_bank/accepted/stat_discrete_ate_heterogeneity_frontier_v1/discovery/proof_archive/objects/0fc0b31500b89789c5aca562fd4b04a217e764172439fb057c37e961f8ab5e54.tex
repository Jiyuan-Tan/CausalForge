The frozen lower construction is a hypothesis-independent contraction of the unrestricted binary experiment.  It necessarily multiplies every source separation by \(\sigma/2\), so it yields \(M\sigma\min\{1,d/[n\log(en)]\}\), not \(M\min\{\sigma,d/[n\log(en)]\}\).  The frozen estimators, on the other hand, yield only the minimum of the polynomial and collision guarantees.  No declared node supplies the required constrained moment-matching family or a sharper estimator.