Begin with scaled two-point outcomes supported on \(\{-M/2,M/2\}\) and propensity values in \([\epsilon,1-\epsilon]\). Use duality between best polynomial approximation and moment matching to choose two Poissonized priors for the marked cell intensities. Couple a common-effect coordinate with centered heterogeneous coordinates bounded by \(\sigma M\), match observable intensity moments through a degree proportional to \(\log(en)\), and transfer the separation to \(Q_P^{(n)}\). Determine the priors, degree constant, centering, normalization, and de-Poissonization in \(\mathrm{oeq:sigma\text{-}indexed\text{-}converse}\).