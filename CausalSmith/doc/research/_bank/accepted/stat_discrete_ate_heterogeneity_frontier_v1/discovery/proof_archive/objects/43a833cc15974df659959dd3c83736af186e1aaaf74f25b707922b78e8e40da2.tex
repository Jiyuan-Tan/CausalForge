The hypothesis-independent Bernoulli channel necessarily multiplies the unrestricted separation by \(\sigma\), yielding only \(M\sigma\min\{1,d/[n\log(en)]\}\).  For the upper route, the frozen assumptions control only conditional second moments; the unresolved step is a covariance bound for a centered mixed-arm factorial lift, followed by fixed-sample totalization, without an extra logarithmic factor.  For the lower route, deterministic paired-cell masses expose the common-effect translation through a degree-two collision moment, so a valid fuzzy experiment needs correlated moment matching in the pair mass and share, exact normalization, realization-wise radius control, and de-Poissonization.  None of the established nodes supplies either missing construction.