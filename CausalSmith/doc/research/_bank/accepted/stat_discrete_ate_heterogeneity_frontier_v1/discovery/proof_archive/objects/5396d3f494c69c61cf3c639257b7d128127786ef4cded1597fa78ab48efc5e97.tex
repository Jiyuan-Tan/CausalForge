Split the observations into independent blocks for occupancy classification, coefficient estimation, and marked-outcome evaluation. On light cells, approximate the two reciprocal arm intensities over the overlap cone with a coefficient-controlled polynomial of degree proportional to \(\log(en)\), and evaluate its monomials using cross-block falling-factorial counts paired with robustly truncated or median-aggregated marked sums. On heavy cells, use robust arm-mean ratios. Sum the contributions and apply \(\operatorname{clip}_{[-M,M]}(z):=\max\{-M,\min(M,z)\}\). Determine the degree constant, block proportions, truncation levels, and fixed-sample correction in \(\mathrm{oeq:robust\text{-}upper\text{-}construction}\).