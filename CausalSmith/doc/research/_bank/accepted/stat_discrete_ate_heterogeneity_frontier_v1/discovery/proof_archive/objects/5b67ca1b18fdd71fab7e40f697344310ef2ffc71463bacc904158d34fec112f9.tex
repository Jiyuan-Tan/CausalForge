The theorem is a two-sided minimax bracket, not a uniform matched frontier throughout the residual wedge \(b\ll u,\sigma^2\ll1\).  The explicit sequence \(d=n\), \(\sigma^2=1/\log(en)\) leaves a logarithmic numerical gap between the current benchmarks.  Closing it requires either a radius-constrained paired moment-matching experiment or a cancellation-aware factorial estimator.  The note does not claim inference, adaptation to unknown \(M\) or \(\sigma\), deteriorating overlap, sharp constants, or preferred finite-sample tuning.