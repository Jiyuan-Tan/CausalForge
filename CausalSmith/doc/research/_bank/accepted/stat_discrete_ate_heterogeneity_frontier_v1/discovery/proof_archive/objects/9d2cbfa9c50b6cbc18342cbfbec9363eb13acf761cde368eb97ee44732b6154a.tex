The frozen lower-bound inputs do not contain the required local radius-constrained experiment.  The exact-homogeneity binary construction yields only the collision scale, while the unrestricted one-arm construction has order-one cell-effect variation.  The hypothesis-independent radius channel therefore contracts both that variation and its ATE separation by \(\sigma\), producing only \(M\sigma\min\{1,d/(nL)\}\).  A paired parametrization enforces the radius constraint without this algebraic contraction, but fixed pair mass fails observable first-moment matching; the missing load-bearing fact is a correlated bivariate moment-matching modulus for pair mass and within-pair share, including normalization concentration and fixed-sample de-Poissonization.  On the upper side, the available factorial covariance lemma controls a linear-mark ATE functional but not the cancellation-aware heterogeneous score needed to retain exact-homogeneity degeneracy.  Assuming either missing modulus or variance bound would assume the crux.