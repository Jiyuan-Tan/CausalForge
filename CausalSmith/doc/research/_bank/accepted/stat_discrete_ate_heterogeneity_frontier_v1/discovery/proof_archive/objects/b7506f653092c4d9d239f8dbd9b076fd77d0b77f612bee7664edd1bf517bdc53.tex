The upper attempt normalized \(V=Y/M\), introduced an independent pilot \(t\), expanded a centered cross-arm identity, and bounded its deterministic reciprocal-polynomial approximation error.  The lower attempt used radius-preserving paired cells and tested a simultaneous common-effect translation and share shift; it failed because fixed pair mass leaves an observable quadratic collision moment.  Reapplying the existing radius channel was also checked and gives only the already-proved product separation.