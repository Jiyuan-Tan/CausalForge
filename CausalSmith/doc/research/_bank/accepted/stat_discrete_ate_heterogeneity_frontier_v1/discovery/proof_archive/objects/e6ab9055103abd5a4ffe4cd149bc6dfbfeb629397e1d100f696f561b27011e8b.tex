The established hypothesis-independent channel multiplies the source separation by \(\sigma\), yielding only \(M\sigma\min\{1,d/[n\log(en)]\}\).  On the upper side, the deterministic centering identity below reduces the problem to a mixed-arm factorial-moment covariance and totalization estimate that is not supplied by any frozen lemma.  On the lower side, deterministic paired-cell mass leaves a degree-two collision moment that reveals a common-effect translation, so a valid fuzzy construction must correlate and moment-match the pair mass and share while preserving normalization, the realization-wise radius constraint, and fixed-sample total variation.