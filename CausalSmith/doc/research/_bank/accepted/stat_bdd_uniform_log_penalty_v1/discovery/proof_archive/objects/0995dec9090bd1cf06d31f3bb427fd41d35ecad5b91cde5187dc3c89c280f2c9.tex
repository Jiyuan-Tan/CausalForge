The logarithm is an information-loss phenomenon for simultaneous boundary recovery from coordinatewise outcome--distance samples: many disjoint angular perturbations remain individually hard after radial compression, so at least one boundary coordinate fails with probability bounded away from zero.  The common law-independent map \(T_n\) is the inherited Cattaneo--Titiunik--Yu estimator architecture, not an extra restriction imposed by this note.  Minor envelope-normalization remark: \(1=\int_{\mathcal X_P}f_P\geq|\mathcal X_P|/L\) implies \(|\mathcal X_P|\leq L\), so using the support \([-1,1]^2\) would require \(L\geq4\).  This is only the normalization built into the density envelope.