The support-boundary construction proves the point-indexed \((\log n/n)^{1/4}\) converse on the full \(\mathcal P_{\mathrm{NP}}(L,q)\) and transfers it to CTY's strict common-map subclass.  On \(\mathcal P_{12}(p,\nu,L)\), the all-order rectangle hypercube includes \(p=0\) by taking \(A>2C\), while the winsorized score has conditional truncation bias at most \(LB_n^{-3}\) and expected centered fluctuation at most \(C\{\sqrt{\log(1/h)/(nh^2)}+B\log(1/h)/(nh^2)\}\).  With \(h_n=a_n\) and \(B_n=a_n^{-1/3}\), these terms yield the matching upper rate for every \(\nu\geq2\).