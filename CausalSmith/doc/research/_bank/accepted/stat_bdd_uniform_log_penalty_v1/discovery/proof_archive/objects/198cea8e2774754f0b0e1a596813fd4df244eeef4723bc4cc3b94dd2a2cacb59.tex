The upper-bound calculation in the rectifiable theorem applies on the trimmed smooth boundary verbatim.  The chart conditions and the smooth-boundary-length lemma give uniform covering constants; the stability and bias bounds are unchanged.  Hence \(R_{n,\delta}^{\mathrm{dist}}(\mathcal P_{\mathrm{sm}})\leq C_{\mathrm{sm},\delta}a_n\).  The narrowed angular packing belongs to \(\mathcal P_{\mathrm{sm}}\), places all its centers in a fixed trimmed straight segment, and has target separation \(c_1a_n\) with adjacent compressed divergence at most \(\alpha\log M_n\).  Applying the coordinatewise overlap direct-product lemma and integrating the finite-maximum tail as above yields \(R_{n,\delta}^{\mathrm{dist}}(\mathcal P_{\mathrm{sm}})\geq c_{\mathrm{sm},\delta}a_n\).  The two estimates prove the displayed limits.