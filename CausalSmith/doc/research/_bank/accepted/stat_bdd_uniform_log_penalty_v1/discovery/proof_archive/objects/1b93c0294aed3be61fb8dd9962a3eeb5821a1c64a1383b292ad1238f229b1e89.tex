In both law classes, the observed units are independently and identically distributed.  In \(\mathcal P_{12}(p,\nu,L)\), the full vectors \((Y_i(0),Y_i(1),X_i)\) are independently and identically distributed and the observed outcome obeys consistency under the deterministic assignment rule.