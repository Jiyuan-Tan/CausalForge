For coordinatewise distance releases, the primitive two-sided-thickness rectifiable class has the sharp uniform frontier \(R_n^{\mathrm{dist}}(\mathcal P_{\mathrm{rect}})\asymp(\log n/n)^{1/4}\).  The same first-order saturation holds on the endpoint-trimmed smooth class when bounded continuous score densities share no angular modulus.  On the canonical straight design, full locations attain \((\log n/n)^{q/(2q+2)}\); distance releases remain at the first-order rate when \(L>4\), but attain the full-location rate when \(L=4\) forces the uniform score density.  More generally, for every fixed integer \(m\geq1\), an order-\(m\) normalized-coarea remainder yields the explicit upper rate \((\log n/n)^{m/(2m+2)}\).  A contained uniform-design bump submodel proves this rate exact for every \(m\geq q\); for \(m<q\), the proposed height-\(h^m\) bump violates the frozen \(q\)-H\"older envelope, so only the upper bound is asserted.  No same-class resolution of the Cattaneo--Titiunik--Yu conjecture is claimed.