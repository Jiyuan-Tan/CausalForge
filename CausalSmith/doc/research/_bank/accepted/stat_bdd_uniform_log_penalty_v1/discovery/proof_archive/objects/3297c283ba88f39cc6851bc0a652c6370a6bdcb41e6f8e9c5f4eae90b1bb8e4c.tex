The result separates three sources of regularity.  Smooth boundary charts control indexing and covering, H\"older outcome regressions control the available bump scale, and the normalized coarea ratio controls radial bias after angular information has been discarded.  When only first-order control is uniform, distance compression saturates at \((\log n/n)^{1/4}\).  An order-\(m\) ratio expansion lets the explicit stabilized polynomial rule improve the exponent to \(m/(2m+2)\).  The matching construction is certified at \(m=q\); for \(m<q\), the upper rate is usable but its minimax tightness remains unresolved.