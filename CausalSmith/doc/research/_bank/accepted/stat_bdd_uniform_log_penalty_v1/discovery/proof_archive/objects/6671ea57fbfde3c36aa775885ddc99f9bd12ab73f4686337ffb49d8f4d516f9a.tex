Density smoothness by itself does not encode the interaction of angular design weights, outcome regression, and boundary geometry.  The contained-bump converse now closes every \(m\geq q\); the residual unproved matching regime is exactly \(m<q\), where a height-\(h^m\) bump violates the frozen \(q\)-H\"older envelope.