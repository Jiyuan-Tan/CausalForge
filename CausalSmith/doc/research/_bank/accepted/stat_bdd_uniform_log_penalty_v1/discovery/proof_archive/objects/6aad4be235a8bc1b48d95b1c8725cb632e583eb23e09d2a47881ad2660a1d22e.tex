The note constructs a hypercube of laws on the square \([-1/2,1/2]^2\), verifies all four defining conditions of \(\mathcal P_{\mathrm{NP}}(L,q)\), exposes the construction's cellwise bit locality and common cell mass, proves adjacent compressed Kullback--Leibler divergence at most \(\alpha\log M_n\), and combines a marked-Poisson cell decomposition with the coordinatewise overlap direct-product lemma.  This proves the same-class lower rate \((\log n/n)^{1/4}\) for the exact inherited common-map decision class.  It makes no full-class upper-bound or matched-frontier claim.