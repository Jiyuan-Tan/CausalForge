The full-\(\mathcal P_{\mathrm{NP}}(L,q)\) result is converse-only: rectifiability alone does not supply the additional local-mass and Gram stability imposed in the signed-distance upper class, so no matching upper bound is claimed there.  The \(\mathcal P_{12}(p,\nu,L)\) upper bound holds for \(L\geq4\), but its matching converse uses the constructed fixed-geometry subclass and requires \(L\geq L_0(p)\).  The bias input is cited only in its source-supported sequential regime, and the upper proof verifies that regime at \(h_n=a_n\).  Outer expectation is retained wherever an arbitrary point-indexed family may produce a nonmeasurable full-boundary supremum.