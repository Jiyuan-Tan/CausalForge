Write \(q=p+1\).  Fix the displayed rectangle geometry and choose bottom-edge sites \(x_j=(s_j,0)\) separated by \(4w\), where \(w=A\Delta^{1/q}\); then \(M\asymp w^{-1}\).  With smooth cutoffs \(\phi,\eta\) as above, define
\[
 b_{\Delta,w}(r)={2\Delta\over ar}\phi(r/w)\eta(r/(C\Delta)),\quad b_{\Delta,w}(0)=0,
\]
\[
 f_\omega(x)={1\over36}\left[1+\sum_j\omega_jb_{\Delta,w}(r_j)\cos\alpha_j\right],
\quad \mu_{0,\omega}(x)=\tfrac12,
\quad \mu_{1,\omega}(x)=\tfrac12+ax_1-\Delta\sum_j\omega_j\phi(r_j/w).
\]
Take conditionally independent Bernoulli potential outcomes with these means.  The disks are disjoint, each angular perturbation integrates to zero, and \(|b_{\Delta,w}|\leq2/(aC)\).  Choosing \(C\) large gives \(1/48\leq f_\omega\leq5/144\), so a fixed \(L_0\geq48\) supplies both density bounds.  Continuity follows because the perturbation vanishes for \(r\leq C\Delta\) and at \(r=w\).  A bump derivative of order \(k\leq q\) is bounded by
\[
 C_k\Delta w^{-k}=C_kA^{-k}\Delta^{1-k/q};
\]
thus derivatives through order \(p\) and the Lipschitz constant of order \(p\) are uniformly bounded after choosing \(A\) large.  Small \(a,\Delta\) keep the Bernoulli means in \([1/4,3/4]\), giving uniform variance and every conditional moment bound.

The boundary is an interior rectangle of length eight and distance one from the support boundary.  The metric equals Euclidean distance.  At a smooth edge or a corner, each relevant arm contains a sector of opening at least \(\pi/2\).  For the uniform kernel, scaling such a sector by \(h^{-1}\) gives the kernel-mass lower bound.  For every nonzero polynomial coefficient vector \(v\),
\[
 v^\top\Psi_{t,P,x}(h)v\geq c\int_0^1(v^\top r_p(u))^2u\,du>0.
\]
Compactness of the unit sphere in \(\mathbb R^{p+1}\) makes the lower bound uniform.  The corresponding circular sectors have arc length between positive multiples of \(s\), proving the radial Hausdorff condition, including at corners.  Enlarging \(L_0(p)\) therefore verifies every class clause for every vertex and every \(n\).

At \(x_j\), the treatment contrast changes by \(\Delta\).  Semicircular integration shows that the signed-radius marginal is identical under adjacent vertices and that the treated conditional Bernoulli mean changes by
\[
 -\Delta\phi(r/w)\{1-\eta(r/(C\Delta))\},
\]
which vanishes outside \(0<r<2C\Delta\); the control law is identical.  Conditional cell divergence is at most \(C\Delta^4/w^2\), while the common cell probability is \(\pi w^2/36\).  Tensorization therefore gives \(Cn\Delta^4\).  This proves all assertions.