The note constructs a hypercube of laws on the square \([-1/2,1/2]^2\), verifies all four defining conditions of \(\mathcal P_{\mathrm{NP}}(L,q)\), exposes the construction's cellwise bit locality and common cell mass, and proves adjacent compressed Kullback--Leibler divergence at most \(\alpha\log M_n\).  A marked-Poisson cell decomposition and the coordinatewise overlap direct-product lemma then give the exact error lower bound \(\frac12[1-\exp\{-M_n^{1-2\alpha}/2\}]\), which is \(1/2-o(1)\) because the packing proves \(M_n\to\infty\).  This establishes the same-class lower rate \((\log n/n)^{1/4}\) for the inherited common-map decision class.  No full-class upper-bound or matched-frontier claim is made.