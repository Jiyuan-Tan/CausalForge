The logarithm in both converses comes from simultaneous recovery of many boundary coordinates after distance compression, not from requiring a common estimator map.  At the causal fourth-moment endpoint, the earlier logarithmic gap was an artifact of using the raw unbounded score: winsorization at \(B_n=a_n^{-1/3}\) makes its deterministic tail bias exactly order \(a_n\), while the bounded-envelope linear term is smaller than \(a_n\).  Identification remains a separate cited design result; the contribution is the exact outer-expected minimax rate and its attaining estimator on the displayed class.