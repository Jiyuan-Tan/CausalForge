In both models the observations are independently and identically distributed.  In \(\mathcal P_{12}(p,\nu,L)\), the latent vectors \((Y_i(0),Y_i(1),X_i)\) are independently and identically distributed and the observed outcome follows consistency.