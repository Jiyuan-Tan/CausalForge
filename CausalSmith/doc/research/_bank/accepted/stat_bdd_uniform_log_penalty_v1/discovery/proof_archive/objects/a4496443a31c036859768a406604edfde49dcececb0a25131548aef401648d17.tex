The score density is bounded and continuous but has no class-uniform derivative or continuity modulus.  Exact-distance regressions use the canonical coarea ratio on positive-denominator radii and the H\"older representative on zero-denominator radii.  First-order upper bounds use the fixed triangular local-constant rule.  Order-\(m\) subclasses use the promoted stabilized degree-\((m-1)\) local-polynomial rule with an explicit Gram matrix, outcome vector, eigenvalue cutoff, inverse branch, zero branch, and bandwidth \(h_n\asymp(\log n/n)^{1/(2m+2)}\).  The comparison experiment permits estimators to use full score locations jointly.