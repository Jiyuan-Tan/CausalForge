The logarithm is an information-loss phenomenon for simultaneous boundary recovery from coordinatewise outcome--distance samples: many disjoint angular perturbations remain individually hard after radial compression, so at least one boundary coordinate fails with probability bounded away from zero.  The exact certificate is finite-sample, and its \(1/2-o(1)\) form is invoked only along the packing sequence \(M_n\to\infty\).  The common law-independent map \(T_n\) is the inherited Cattaneo--Titiunik--Yu estimator architecture, not an extra restriction imposed by this note.  The normalization \(1=\int_{\mathcal X_P}f_P\geq|\mathcal X_P|/L\) implies \(|\mathcal X_P|\leq L\); this is only the density-envelope normalization.