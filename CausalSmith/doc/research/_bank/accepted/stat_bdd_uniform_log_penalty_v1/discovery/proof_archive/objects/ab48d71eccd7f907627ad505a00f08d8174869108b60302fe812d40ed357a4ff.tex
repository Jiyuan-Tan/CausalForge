The inherited information restriction applies the same measurable map \(T_n\) to the outcome--distance sample at each queried boundary point.  A local angular perturbation of the score density can compensate a regression bump in the radial outcome law except on radii of order \(a_n\), while disjoint boundary cells turn the uniform loss into a simultaneous decentralized testing problem.  The direct-product step is finite-sample: under coordinatewise Kullback--Leibler bounds \(\kappa\log M\), its error certificate is \(\frac12[1-\exp\{-M^{1-\kappa}/2\}]\).