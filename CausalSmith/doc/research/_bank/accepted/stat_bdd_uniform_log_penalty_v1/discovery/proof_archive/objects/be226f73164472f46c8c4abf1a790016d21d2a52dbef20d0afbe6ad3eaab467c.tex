The relevant information restriction applies the same measurable map \(T_n\) to the outcome--distance sample at each queried boundary point.  A local angular perturbation of the score density can compensate a regression bump in the radial outcome law except on radii of order \(a_n\), while disjoint boundary cells turn the uniform loss into a simultaneous decentralized testing problem.