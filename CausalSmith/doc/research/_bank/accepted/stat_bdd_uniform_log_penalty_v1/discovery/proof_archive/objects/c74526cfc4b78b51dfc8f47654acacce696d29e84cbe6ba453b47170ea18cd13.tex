The proved contribution is the same-class lower bound \(R_n^{\mathrm{NP}}\gtrsim(\log n/n)^{1/4}\) for the exact inherited common-map coordinatewise distance class.  The matched order \(R_n^{\mathrm{NP}}\asymp(\log n/n)^{1/4}\) uses the published uniform upper bound; no exact asymptotic constant is claimed.  The scope is exactly the displayed CTY law class and decision architecture.