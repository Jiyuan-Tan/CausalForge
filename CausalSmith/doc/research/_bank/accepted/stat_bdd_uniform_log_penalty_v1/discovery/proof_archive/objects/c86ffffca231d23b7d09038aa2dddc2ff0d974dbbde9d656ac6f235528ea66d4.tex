For the Cattaneo--Titiunik--Yu coordinatewise distance experiment, Theorem~6 itself places one rule \(T_n\) outside the supremum over laws and applies it as \(T_n(U_n(x))\) at every evaluation point.  On that published decision architecture and the same \(\mathcal P_{\mathrm{NP}}(L,q)\), the minimax risk obeys \(R_n^{\mathrm{NP}}\gtrsim(\log n/n)^{1/4}\).  An explicit square-support angular packing and a simultaneous-coordinate testing bound prove the logarithmic converse.  This is deliberately a one-sided same-class result: CTY Assumption~2 concerns an interior assignment boundary, whereas Theorem~6 concerns the support boundary, so its upper theorem cannot be transferred to the full Theorem~6 class by merely checking the square witness.