For arbitrary law-independent point-indexed distance families \(\{T_{n,x}\}\) whose fixed-point sections are Borel measurable, a decision class strictly larger than the inherited Cattaneo--Titiunik--Yu one-common-map class, the outer-expectation minimax risk satisfies \(R_n^{\mathrm{PI}}\gtrsim(\log n/n)^{1/4}\).  The proof works with a measurable finite packing maximum and therefore needs no joint regularity in \(x\).  Since \(\mathcal T_n^{\mathrm{NP}}\subsetneq\mathcal T_n^{\mathrm{PI}}\), the CTY common-map logarithmic converse follows by inclusion and resolves their printed conjecture.  No Assumption~2 result, full-\(\mathcal P_{\mathrm{NP}}\) upper bound, or matched two-sided frontier is claimed.