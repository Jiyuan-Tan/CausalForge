The regression \(\mu_P\) lies in \(\mathcal H^q(\mathcal X_P,L)\), and the continuous conditional variance satisfies \(L^{-1}\leq\sigma_P^2(x)\leq L\).  At each boundary point the estimator observes only \(V_{n,P}(x)\), and one common law-independent map \(T_n\) must be used at every point.