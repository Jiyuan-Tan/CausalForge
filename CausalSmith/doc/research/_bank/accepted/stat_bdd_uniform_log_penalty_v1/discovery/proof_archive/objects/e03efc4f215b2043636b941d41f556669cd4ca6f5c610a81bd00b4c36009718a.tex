We verify the substitutions in the cited supplement argument.  On the kernel support, \(|D^{\pm}_{P,x}/h|\leq1\), so every coordinate of \(r_p(D^{\pm}_{P,x}/h)\) is bounded by one.  Clause \(\mathrm{(viii)}\) gives \(\|\Psi_{t,P,x}(h)^{-1}\|\leq L\).  Moreover, the density upper bound and the area of a radius-\(h\) disk imply
\[
 {1\over h^2}P\{|D^{\pm}_{P,x}|\leq h\}\leq \pi L.
\]
Conditional Jensen and clause \(\mathrm{(v)}\) give \(\mathbb E_P[Y(t)^2\mid X]\leq L^{2/(2+\nu)}\leq L\).  Hence the original population score vector is uniformly bounded, and therefore
\[
 \sup_{P,t,x,h}\|\beta^*_{t,P,x}(h)\|\leq C(p,L).
\]

For each coordinate of the displayed score, the centered summand before division by \(h^2\) consequently has envelope at most \(C(p,L)B\), while its second moment is at most \(Ch^2\): winsorization is a contraction in absolute value, \(|\psi_B(Y)|\leq|Y|\), and the summand vanishes unless \(|D^{\pm}_{P,x}|\leq h\).  After the factor \(h^{-2}\), the variance proxy is thus \(C/h^2\) and the uniform envelope is \(CB/h^2\).

The indexing sets in \(x\) are intersections of a fixed arm with Euclidean balls, and \(\mathrm{lem:euclidean\text{-}balls\text{-}vc}\) gives VC index at most four.  Multiplication by one of the finitely many bounded monomials \(u^k\), \(0\leq k\leq p\), and centering changes only constants in the VC-type covering bound.  Repeating the symmetrization, VC covering, and Bernstein integration steps used for the expected displays in \(\mathrm{lem:cty\text{-}expected\text{-}local\text{-}polynomial\text{-}maximal\text{-}bounds}\), with variance proxy \(C/h^2\) and envelope \(CB/h^2\), gives for each coordinate
\[
 \mathbb E_P^*\sup_{t,x}
 \left|\widehat O^B_{t,x,k}(h)-\mathbb E_P\widehat O^B_{t,x,k}(h)\right|
 \leq C\left\{
 \sqrt{{\log(1/h)\over nh^2}}+{B\log(1/h)\over nh^2}
 \right\}.
\]
There are \(p+1\) coordinates.  Equivalence of Euclidean norms in this fixed dimension absorbs their sum into \(C(p,L)\), proving the asserted vector inequality.  The stated condition is the small-bandwidth regime needed by the same Bernstein integration; no joint measurability of an arbitrary decision rule is used.