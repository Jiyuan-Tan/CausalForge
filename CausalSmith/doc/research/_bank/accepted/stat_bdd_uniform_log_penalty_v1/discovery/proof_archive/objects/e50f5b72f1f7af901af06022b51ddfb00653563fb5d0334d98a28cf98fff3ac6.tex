A generic CTY rule generated by one common law-independent measurable map \(T_n\) through \(\widehat\mu_n(x)=T_n(V_{n,P}(x))\) at every boundary point; \(x\) affects the rule only through the compressed sample.