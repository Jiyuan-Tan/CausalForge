The note constructs a hypercube of laws on the square \([-1/2,1/2]^2\), verifies all four defining conditions of \(\mathcal P_{\mathrm{NP}}(L,q)\), proves adjacent compressed Kullback--Leibler divergence at most \(\alpha\log M_n\), and combines a marked-Poisson cell decomposition with the coordinatewise overlap direct-product lemma.  This yields the same-class lower rate \((\log n/n)^{1/4}\) for the exact common-map decision class and matches the published uniform upper rate.