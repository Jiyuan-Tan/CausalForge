A downstream analyst can verify radial mass and remainder constants, choose any fixed \(m\geq1\), set \(h_n\asymp(\log n/n)^{1/(2m+2)}\), and obtain the stated uniform distance-estimation rate; exactness follows when the subclass contains the valid bump submodel and \(m\geq q\).