The published information restriction uses a single law-independent map \(T_n\) applied separately to each outcome--distance sample.  A local angular score-density perturbation can compensate a regression bump in the radial outcome law except on radii of order \(a_n\), and disjoint boundary cells convert uniform loss into a decentralized simultaneous testing problem.