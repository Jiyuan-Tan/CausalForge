# Accepted Bank

Stage -0.5 ACCEPT + Stage 0.5 ACCEPT at the requested `novelty_target`.

Entries here are publishable as-is (modulo polish) and are the candidate set
for promotion into AutoID. Promotion is a deliberate human step — see
`../README.md` § "Authoring conventions".

See `../README.md` for the per-entry metadata schema.
