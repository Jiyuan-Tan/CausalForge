The pair-swap, orbit, balance, and selector-legality conclusions of \(\texttt{prop:s2-reduction}\) use only the audited hypotheses \(\mathcal V_b=\{v_{b,0},v_{b,1}\}\), \(\langle s_b\rangle\le\mathcal G\), the arm-preserving action, and \(\texttt{ass:observable-selector-kernel}\). They do not involve \(\boldsymbol\varepsilon\), so they remain unchanged.

If the target arm-one lift is supported on \(O_{b,1}\) and \(q^{\mathrm{obs}}_{w,1}(O_{b,1})=0\), the unique target-supported arm-one orbit has zero calibration mass. Applying \(\texttt{prop:asymmetric-disjoint-orbit-witness}\) with \(a=1\) gives
\[
\begin{array}{c|c|c}
\text{rank regime}&\mathcal C^{\boldsymbol\varepsilon}_w&\text{conditional coverage}\\ \hline
k_0,k_1\le M&\{0\}&0\\
\text{exactly one }k_c=M+1&[-K,K]&1\\
k_0=k_1=M+1&[-2K,2K]&1.
\end{array}
\tag{1}
\]
On the diagonal \(\varepsilon_0=\varepsilon_1=\alpha/2\), \(\texttt{lem:armwise-finite-rank-staircase}\) gives \(k_0=k_1=k\). The middle row of (1) is then impossible, while the other two rows are exactly the regimes of \(\texttt{prop:s2-reduction}\).