Specialize \(\texttt{prop:asymmetric-s2-reduction}\) to \(\varepsilon_0=\varepsilon_1=\alpha/2\). The pair action, its arm-specific orbits, the differing mate-control orbit vector, the LP failure, and the arm-\(1\) selector illegality do not depend on the error budget. The two ranks coincide: when \(k\le M\), the generalized disjoint witness gives zero coverage; when \(k=M+1\), both arms are at the boundary and \(\texttt{prop:rank-boundary}\) gives coverage one. This is the stated reduction.