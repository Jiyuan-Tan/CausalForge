The declared group \(S_2\times S_2\) contains the product of the two relevant local swaps, so the corrected pair definition identifies the four global arm-specific orbits. The orbit-vector mismatch and arm-one selector illegality are therefore exactly those proved in \(\texttt{prop:two-pair-sanity-check}\), and they do not involve the armwise error budget. If \(q^{\mathrm{obs}}_{w,1}(O_{1,1})=0\), the only target-supported arm-one orbit has zero calibration mass. Applying \(\texttt{prop:asymmetric-disjoint-orbit-witness}\) gives, respectively, \(\mathcal C^{\boldsymbol\varepsilon}_w=\{0\}\) with coverage zero, \(\mathcal C^{\boldsymbol\varepsilon}_w=[-K,K]\) with coverage one, and \(\mathcal C^{\boldsymbol\varepsilon}_w=[-2K,2K]\) with coverage one in the two-finite, exactly-one-boundary, and two-boundary regimes. The diagonal identity for the ranks recovers the symmetric calculation.