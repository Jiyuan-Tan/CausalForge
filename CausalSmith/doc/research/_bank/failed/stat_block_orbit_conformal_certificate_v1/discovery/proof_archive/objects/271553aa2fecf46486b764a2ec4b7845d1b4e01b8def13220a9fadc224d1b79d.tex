Set \(\varepsilon_0=\varepsilon_1=\alpha/2\). By \(\texttt{lem:armwise-finite-rank-staircase}\),
\[
k_0=k_1=k,
\qquad
\Gamma_{M,\alpha,\boldsymbol\varepsilon}(w)=\Gamma_{M,\alpha}(w).
\tag{1}
\]
Thus the exactness, finite preorder attainment, coherent probability objects, binary counterexample, and disjoint-support conclusion follow from \(\texttt{thm:asymmetric-sharp-orbit-coverage-frontier}\).

Assume \(k\le M\). Specializing its exact-key partition gives
\[
\Pi_\star=\Pi_{\mathrm{vert}}\mathbin{\dot\cup}\Pi_{\mathrm{edge}}
\mathbin{\dot\cup}\Pi_{\mathrm{roll}}\mathbin{\dot\cup}
\Pi_{\mathrm{MS}}\mathbin{\dot\cup}\Pi_+.
\tag{2}
\]
The width parameters become \(\bar k_0=\bar k_1=\bar k\), so its rolling charge, branch sum, and workspace specialize exactly to \(\mathsf C_{M,k,k}\), \(\mathsf B_{\mathrm{sym}}\), and \(\mathsf W_{\mathrm{sym}}\). Substitution in the two asymmetric resource formulas proves both diagonal time bounds and
\[
O(m_0m_1+L+U_{\max}+G+\mathsf W_{\mathrm{sym}})
\]
memory. Support-two evaluation remains constant-workspace, and factorials are built only when a support-at-least-three key lies in \(\Pi_{\mathrm{MS}}\).

For a diagonal strict witness, let \(M\ge2\), take \(\alpha=2/(M+1)\), so that
\[
k=\left\lceil(M+1)\left(1-\frac1{M+1}\right)\right\rceil=M,
\]
and retain the two audited pair blocks and key
\[
\pi=(1/2,1/2,0,0)
\]
from the asymmetric audit, but take \(k=M\) in both coordinates. If \(N\sim\operatorname{Bin}(M,1/2)\), then
\[
D_{M,M,M}(\pi)=\Pr\{0<M,\ N<M\}=1-2^{-M}.
\tag{3}
\]
The edge interval \([0,M-1]\) is evaluated from its one-point complement in \(\Theta(\log M)\) exact arithmetic and constant workspace. Here \(\bar k=1\); the former rolling and multinomial charges are \(M\) and \(M+1\), while the least positive-corner orientation still costs \(\Theta(M)\). Hence the old least charge is \(\Theta(M)\). The off-diagonal values \(M=2n\), \(k_0=n\), \(k_1=M\) and their complete budget audit remain valid for the common asymmetric evaluator but are not falsely described as equal-split values.

If \(k=M+1\), \(\texttt{prop:rank-boundary}\) makes both rank events certain, so the defining deficit is zero. At \(M=3\), \(\alpha=1/2\), equation (1) gives the equal split used in the exact calculation (4)--(5) of the asymmetric theorem, proving the four-level value, binary upper bound, and strict gap. Its disjoint-support construction also lies on the diagonal and proves \(\Gamma_{M,\alpha}(w)=1-\alpha\) for \(k\le M\). Since every computational branch evaluates the same strict-prefix key probability, all common-target, selector, active-arm, pair, empty-library, ordinary-rank, and weighted-orbit semantics are unchanged.