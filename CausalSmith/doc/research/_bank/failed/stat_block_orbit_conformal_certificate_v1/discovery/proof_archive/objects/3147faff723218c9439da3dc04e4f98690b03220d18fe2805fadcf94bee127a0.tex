Set \(\varepsilon_0=\varepsilon_1=\alpha/2\). By \texttt{lem:armwise-finite-rank-staircase},
\[
k_0=k_1=k,\qquad \rho_{M,\varepsilon_0}=\rho_{M,\varepsilon_1}=\rho_{M,\alpha},
\tag{1}
\]
and \texttt{def:coverage-frontier} gives \(\Gamma_{M,\alpha,\boldsymbol\varepsilon}(w)=\Gamma_{M,\alpha}(w)\). Apply \texttt{thm:asymmetric-sharp-orbit-coverage-frontier} on this diagonal. When \(k\le M\), its strict-below identity shows that total-preorder enumeration is exact, and \texttt{lem:coherent-two-finite-rank-frontier-evaluator} gives each preorder objective as
\[
\sum_{\pi\in\Pi_C}\omega_C(\pi)D_{M,k,k}(\pi).
\tag{2}
\]
Every \(\pi\) in (2) comes from the unchanged full \(R_w\), every weight from the coherent pushforward of the one common target, and the global key is \((M,k,k,\pi_{00},\pi_{01},\pi_{10})\). Substituting \(k_0=k_1=k\) in the instance-adaptive time bound, uniform time bound, and memory bound of that lemma gives exactly the three displays in this theorem. The multinomial, corner-oriented rolling, and positive-base coefficient evaluators remain exact per-key alternatives; choosing the rolling branch proves the displayed coarser charges \(GMk^2\) and \(O(k^2)\).

If \(k=M+1\), both rank events are certain, so
\[
\Gamma_{M,\alpha}(w)=[(1-\alpha)-1]_+=0.
\tag{3}
\]
The four-pair counterexample in \texttt{thm:asymmetric-sharp-orbit-coverage-frontier} already lies on the diagonal and uses \(\mathcal G=S_2^4\), so every corrected local pair condition is satisfied. It gives a four-level objective \(24957/160000\), a largest binary objective \(759109/5000000\), and the strict positive gap \(83189/20000000\). The same theorem's disjoint-support construction gives \(\Gamma_{M,\alpha}(w)=1-\alpha\) whenever \(k\le M\). These conclusions concern the declared finite orbit-constant score class only and make no claim of equality with a supremum over \(\mathcal P_{\mathcal G,K}\).