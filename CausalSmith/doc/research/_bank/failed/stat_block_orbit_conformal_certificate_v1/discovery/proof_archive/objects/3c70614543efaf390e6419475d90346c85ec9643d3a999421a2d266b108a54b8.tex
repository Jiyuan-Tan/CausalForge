Set
\(\varepsilon_0=\varepsilon_1=\alpha/2\). By
\(\texttt{lem:armwise-finite-rank-staircase}\),
\[
k_0=k_1=k,\qquad
\Gamma_{M,\alpha,\boldsymbol\varepsilon}(w)
=\Gamma_{M,\alpha}(w).
\tag{1}
\]
Therefore the exact score-class interpretation, finite-preorder attainment, coherent laws \(R_w\) and \(P_T^C\), local weights, memo keys, binary counterexample, and disjoint-support value follow from
\(\texttt{thm:asymmetric-sharp-orbit-coverage-frontier}\).

Assume \(k\le M\). Specializing the asymmetric partition gives
\[
\Pi_\star=\Pi_{\mathrm{vert}}\mathbin{\dot\cup}
\Pi_{\mathrm{edge}}\mathbin{\dot\cup}
\Pi_{\mathrm{face}}\mathbin{\dot\cup}
\Pi_{\mathrm{roll}}\mathbin{\dot\cup}
\Pi_{\mathrm{MS}}\mathbin{\dot\cup}\Pi_+.
\tag{2}
\]
Now \(\bar k_0=\bar k_1=\bar k=\min\{k,M-k+1\}\), so
\[
\mathsf C_{M,k,k}
=\sum_{i=0}^{M-1}\min\{\bar k,i+1\}^2.
\]
Substitution in the asymmetric branch sum gives exactly
\(\mathsf B_{\mathrm{sym}}\), including
\(\sum_{\pi\in\Pi_{\mathrm{face}}}\mathsf F_M(\pi)\).
The same substitution gives \(\mathsf W_{\mathrm{sym}}\); the face branch has constant workspace and adds no term. It also does not trigger factorial preprocessing. Substitution in the adaptive, uniform, and memory formulas proves all displayed diagonal resource bounds.

The earlier diagonal edge witness remains valid. For every \(M\ge2\), taking
\(\alpha=2/(M+1)\) gives \(k=M\), and for
\(\pi=(1/2,1/2,0,0)\),
\[
D_{M,M,M}(\pi)=1-2^{-M}.
\]
Its edge cost is \(\Theta(\log M)\) and the former dispatcher cost is
\(\Theta(M)\). The new face witness uses the same diagonal ranks and
\[
\pi=(1/3,1/3,1/3,0),\qquad
D_{M,M,M}(\pi)=1-2\cdot3^{-M}.
\]
The admissible trivial-group two-pair construction is proved in
\(\texttt{lem:coherent-two-finite-rank-frontier-evaluator}\). Its zero-corner face branch costs \(\Theta(\log M)\) and constant workspace, whereas the rolling and multinomial charges and every allowed positive-base coefficient charge are \(\Theta(M)\).

If \(k=M+1\), \(\texttt{prop:rank-boundary}\) makes both rank events certain and therefore \(\Gamma_{M,\alpha}(w)=0\). At
\(M=3\), \(\alpha=1/2\), equation (1) imports the exact four-level value
\(24957/160000\), binary upper bound \(759109/5000000\), and gap
\(83189/20000000\). The disjoint-support construction likewise gives
\(\Gamma_{M,\alpha}(w)=1-\alpha\) when \(k\le M\). Since every prepass evaluates the same strict-prefix probabilities without altering their laws or weights, all common-target, active-arm, pair-audit, empty-library, ordinary-rank, and weighted-orbit semantics are preserved.