Set \(\varepsilon_0=\varepsilon_1=\alpha/2\). By \(\texttt{lem:armwise-finite-rank-staircase}\), \(k_0=k_1=k\), and \(\texttt{def:coverage-frontier}\) gives \(\Gamma_{M,\alpha,\boldsymbol\varepsilon}(w)=\Gamma_{M,\alpha}(w)\). Therefore \(\texttt{thm:asymmetric-sharp-orbit-coverage-frontier}\) applies on this diagonal. Its total-preorder argument proves finite attainment and exactness over the declared score-map class, while preserving the unchanged \(P_T^C\), \(R_w\), local coherent weights, common target, and common selector.\n\nWhen \(k\le M\), put\n\[h=M-k+1,\qquad \bar k=\min\{k,h\},\qquad \mathsf C_{M,k,k}=\sum_{i=0}^{M-1}\min\{\bar k,i+1\}^2.\]\nThe asymmetric theorem's exact-key partition becomes\n\[\Pi_\star=\Pi_{\mathrm{roll}}\mathbin{\dot\cup}\Pi_{\mathrm{MS}}\mathbin{\dot\cup}\Pi_+,\]\nwhere each key is deterministically assigned to the least certified charge among \(\mathsf C_{M,k,k}\), \(M+\mathsf T_{M,\bar k,\bar k}\), and \(\mathsf C_+(\pi)\). Consequently its terminal charge is\n\[\begin{aligned}\mathsf B_{\mathrm{sym}}={}&M\mathbf 1\{\Pi_{\mathrm{MS}}\ne\varnothing\}+\sum_{\pi\in\Pi_{\mathrm{roll}}}\mathsf C_{M,k,k}\\&+\sum_{\pi\in\Pi_{\mathrm{MS}}}\{M+\mathsf T_{M,\bar k,\bar k}\}+\sum_{\pi\in\Pi_+}\mathsf C_+(\pi),\end{aligned}\]\nwith empty sums zero, and its selected-branch workspace is\n\[\mathsf W_{\mathrm{sym}}=\max\!\left(\{0\}\cup\{\bar k^2:\Pi_{\mathrm{roll}}\ne\varnothing\}\cup\{M:\Pi_{\mathrm{MS}}\ne\varnothing\}\cup\left\{\min\{t_0^{\sigma(\pi)},t_1^{\sigma(\pi)}\}:\pi\in\Pi_+\right\}\right).\]\nSubstitution in the asymmetric instance-adaptive and uniform displays proves the proposed symmetric displays and memory \(O(m_0m_1+L+U_{\max}+G+\mathsf W_{\mathrm{sym}})\). The first term in \(\mathsf B_{\mathrm{sym}}\) is present exactly once if the multinomial branch is used. At \(k=M\), \(\bar k=1\) and \(\mathsf C_{M,M,M}=M\), so rolling costs \(\Theta(M)\) per key rather than \(\Theta(M^3)\), with a potentially smaller positive-corner charge. If \(k=M+1\), both rank events are certain and the frontier is zero. Finally, the asymmetric theorem's four-pair construction is already on this diagonal and yields the four-level value \(24957/160000\), the binary upper value \(759109/5000000\), and the strict gap \(83189/20000000\). Its disjoint-support construction gives \(\Gamma_{M,\alpha}(w)=1-\alpha\) when \(k\le M\).