In the two-pair instance, \(\mathcal G=S_2\times S_2\) contains both required local swaps. Consequently \texttt{def:pair-action} identifies the four global cells as the four orbits, and the orbit-vector mismatch and arm-one selector illegality proved in \texttt{prop:two-pair-sanity-check} are unaffected by the choice of \(\boldsymbol\varepsilon\).

If \(q^{\mathrm{obs}}_{w,1}(O_{1,1})=0\), the sole target-supported arm-one orbit has zero calibration mass. Apply \texttt{prop:asymmetric-disjoint-orbit-witness} with \(a=1\). When both ranks are finite, it gives \(\mathcal C^{\boldsymbol\varepsilon}_w=\{0\}\) and coverage zero. When exactly one rank is at the boundary, it gives \(\mathcal C^{\boldsymbol\varepsilon}_w=[-K,K]\) and coverage one. When both ranks are at the boundary, it gives \(\mathcal C^{\boldsymbol\varepsilon}_w=[-2K,2K]\) and coverage one. Finally, \texttt{lem:armwise-finite-rank-staircase} gives \(k_0=k_1=k\) under \(\varepsilon_0=\varepsilon_1=\alpha/2\), so the exactly-one-boundary regime disappears and the symmetric two-pair calculation is recovered.