Assume \(\mathcal V_b=\{v_{b,0},v_{b,1}\}\) and \(\langle s_b\rangle\le\mathcal G\), as required by the proposed statement and the corrected \(\texttt{def:pair-action}\). The nonidentity element interchanges the two members, preserves the arm label, and fixes outside members. Since every element of \(\mathcal G\) preserves blocks and block \(b\) has only these two members, the \(\mathcal G\)-orbit of either \((v_{b,r},a)\) is exactly \(O_{b,a}=\{(v_{b,0},a),(v_{b,1},a)\}\). The complete-record action is \(\texttt{def:group-action}\). If the target arm-one lift is supported on \(O_{b,1}\), its arm-one orbit vector puts mass one there. An exclusive mate-control state lies in the distinct arm-zero orbit \(O_{b,0}\), so it puts zero mass on the \((1,O_{b,1})\) row. The joint-balance equations therefore cannot hold for that analogue. Moreover, \(\texttt{ass:observable-selector-kernel}\) permits an arm-one selector to choose only a member observed in arm one; a control-labelled mate is illegal. Separately, if a legal arm-one selector has \(q^{\mathrm{obs}}_{w,1}(O_{b,1})=0\), the sole target-supported arm-one orbit has zero calibration mass. The premise of \(\texttt{prop:disjoint-orbit-witness}\) holds. That proposition gives zero coverage when \(k\le M\), while \(\texttt{prop:rank-boundary}\) gives coverage one when \(k=M+1\).