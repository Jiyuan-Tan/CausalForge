Set \(\varepsilon_0=\varepsilon_1=\alpha/2\). By \(\texttt{lem:armwise-finite-rank-staircase}\),
\[
k_0=k_1=k,\qquad \Gamma_{M,\alpha,\boldsymbol\varepsilon}(w)=\Gamma_{M,\alpha}(w).
\tag{1}
\]
Therefore the exact score-class interpretation, finite-preorder attainment, coherent laws \(R_w\) and \(P_T^C\), local weights, exact memo key, binary counterexample, and disjoint-support value follow from \(\texttt{thm:asymmetric-sharp-orbit-coverage-frontier}\).

Assume \(k\le M\). Specializing the asymmetric five-way exact-key partition gives
\[
\Pi_\star=\Pi_{\mathrm{vert}}\mathbin{\dot\cup}\Pi_{\mathrm{edge}}\mathbin{\dot\cup}\Pi_{\mathrm{roll}}\mathbin{\dot\cup}\Pi_{\mathrm{MS}}\mathbin{\dot\cup}\Pi_+.
\tag{2}
\]
Here \(\bar k_0=\bar k_1=\bar k=\min\{k,M-k+1\}\), so the rolling charge becomes
\[
\mathsf C_{M,k,k}=\sum_{i=0}^{M-1}\min\{\bar k,i+1\}^2.
\tag{3}
\]
Substitution into the asymmetric branch sum and live-workspace maximum gives exactly \(\mathsf B_{\mathrm{sym}}\) and \(\mathsf W_{\mathrm{sym}}\). The same substitution in its adaptive and uniform time formulas proves both displayed diagonal bounds, and its memory formula becomes
\[
O(m_0m_1+L+U_{\max}+G+\mathsf W_{\mathrm{sym}}).
\tag{4}
\]
The support-two affine-binomial evaluator uses constant workspace, and the shared factorial table is built only if a support-at-least-three key belongs to \(\Pi_{\mathrm{MS}}\).

For the diagonal strict witness, fix any \(M\ge2\), use the same two audited pair blocks, invariant randomization, legal selector, target, and preorders as in \(\texttt{lem:coherent-two-finite-rank-frontier-evaluator}\), and take
\[
\alpha=\frac{2}{M+1},\qquad \varepsilon_0=\varepsilon_1=\frac1{M+1}.
\tag{5}
\]
Then \(k=\lceil(M+1)(1-1/(M+1))\rceil=M\). For the same key \(\pi=(1/2,1/2,0,0)\), if \(N\sim\operatorname{Bin}(M,1/2)\),
\[
D_{M,M,M}(\pi)=\Pr\{0<M,\ N<M\}=1-2^{-M}.
\tag{6}
\]
The edge branch evaluates the one-point complement in \(\Theta(\log M)\) exact arithmetic and constant workspace. Since \(\bar k=1\), the former rolling and multinomial charges are respectively \(M\) and \(M+1\); the only positive-base orientations also have \(\Theta(M)\) charge. Thus the former least-charge dispatcher costs \(\Theta(M)\). The off-diagonal construction \(M=2n\), \(k_0=n\), \(k_1=M\), and its exact levels remain the strict asymmetric certificate already audited in that lemma.

If \(k=M+1\), \(\texttt{prop:rank-boundary}\) makes both rank events certain, so \(\Gamma_{M,\alpha}(w)=0\) and evaluation is constant. At \(M=3\) and \(\alpha=1/2\), (1) gives \(\varepsilon_0=\varepsilon_1=1/4\), so the asymmetric theorem's exact four-level value \(24957/160000\), binary upper bound \(759109/5000000\), and positive gap \(83189/20000000\) are diagonal values. Its disjoint-support score construction also lies on the diagonal and proves \(\Gamma_{M,\alpha}(w)=1-\alpha\) whenever \(k\le M\). Finally, every prepass computes the same strict-prefix probability without changing any selector, common-target, active-arm, pair-audit, empty-library, ordinary-rank, or weighted-orbit object, which proves the remaining semantic assertion.