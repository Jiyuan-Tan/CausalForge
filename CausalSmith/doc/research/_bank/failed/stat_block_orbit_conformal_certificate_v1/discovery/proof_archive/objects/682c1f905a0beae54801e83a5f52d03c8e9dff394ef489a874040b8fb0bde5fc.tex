Complementing a coordinate maps \((B_{i0},B_{i1})\) bijectively to another four-cell draw and therefore only permutes \(\pi\). Under the prescribed orientation, the transformed retained thresholds are \(\bar k_0\) and \(\bar k_1\). The feasible-count formula in \(\texttt{lem:four-cell-multinomial-sum-alternative}\), applied to the permuted law, consequently costs \(O(M+\mathsf T_{M,\bar k_0,\bar k_1})\) operations and \(O(M)\) memory. The corner identities in \(\texttt{lem:corner-oriented-four-cell-rank-evaluator}\) reconstruct the original lower--lower event from this corner and at most two marginal binomial probabilities. By \(\texttt{lem:exact-binomial-shorter-tail-evaluator}\), their total cost is \(O(M)\) and constant memory, so it is absorbed by the preceding charge. The same corner lemma evaluates the chosen corner by recurrence in \(O(C_{M,k_0,k_1})\) operations and \(O(\bar k_0\bar k_1)\) memory. Selecting the cheaper time branch proves the per-key claim. For multiple keys, share the probability-independent factorial table, rebuild probability-dependent powers for each multinomial key, reuse the rolling recurrence arrays, and retain one scalar per key. Adding these charges proves the aggregate time and maximum-workspace formulas.