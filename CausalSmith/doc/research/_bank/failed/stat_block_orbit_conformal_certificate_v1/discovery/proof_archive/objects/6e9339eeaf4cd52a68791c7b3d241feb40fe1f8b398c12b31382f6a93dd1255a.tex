Set \(\varepsilon_0=\varepsilon_1=\alpha/2\). By \(\texttt{lem:armwise-finite-rank-staircase}\), \(k_0=k_1=k\), and \(\texttt{def:coverage-frontier}\) gives \(\Gamma_{M,\alpha,\boldsymbol\varepsilon}(w)=\Gamma_{M,\alpha}(w)\). Apply \(\texttt{thm:asymmetric-sharp-orbit-coverage-frontier}\) on this diagonal. When \(k\le M\), each event is the strict-below count event, total-preorder enumeration is exact, and the coherent objective is \(\sum_{\pi\in\Pi_C}\omega_C(\pi)D_{M,k,k}(\pi)\). Every \(\pi\) comes from the unchanged full \(R_w\), every weight comes from the coherent pushforward of the one common target, and the global key is \((M,k,k,\pi_{00},\pi_{01},\pi_{10})\). Substituting \(k_0=k_1=k\) into the two resource displays of \(\texttt{lem:coherent-two-finite-rank-frontier-evaluator}\) gives exactly both time bounds and the memory bound in this theorem. The zero-cell-safe multinomial, corner-oriented rolling, and positive-base coefficient branches remain exact alternatives for each key; selecting among them preserves the law, and the statement's coarser \(GMk^2\) and \(k^2\) charges follow by the rolling branch. If \(k=M+1\), both rank events are certain and the frontier is zero. The four-pair specialization in the asymmetric theorem uses \(\mathcal G=S_2^4\), so it satisfies every corrected local pair condition and gives the stated four-level and binary values and their positive gap. Its disjoint-support construction gives frontier \(1-\alpha\) whenever \(k\le M\). No step identifies this finite score-map frontier with a supremum over all invariant latent laws.