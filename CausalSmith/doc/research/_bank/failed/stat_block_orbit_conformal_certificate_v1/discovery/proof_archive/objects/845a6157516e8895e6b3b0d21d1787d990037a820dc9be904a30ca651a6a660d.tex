Here \(\mathcal G=S_2\times S_2\) contains both local swap subgroups, so the corrected \(\texttt{def:pair-action}\) applies to each pair. The action preserves blocks and arms, and each block has two members; hence the four global orbits are exactly \(O_{1,0},O_{1,1},O_{2,0},O_{2,1}\). If \(q_{\mathrm{tar}}\) concentrates on \(v_{1,0}\), then \(T^1\in O_{1,1}\) almost surely, so the target arm-one orbit vector has mass one on \(O_{1,1}\). The mate-control state \((v_{1,1},0)\) lies in \(O_{1,0}\), and its vector has zero mass in the \((1,O_{1,1})\) row. Thus the relevant stacked balance equation fails. The control-labelled state is also excluded by \(\texttt{ass:observable-selector-kernel}\) from any legal arm-one selector. If a legal selector instead has \(q^{\mathrm{obs}}_{w,1}(O_{1,1})=0\), the disjoint-orbit premise holds. For \(k\le M\), \(\texttt{prop:disjoint-orbit-witness}\) gives \(\mathcal C_w=\{0\}\) and coverage zero. For \(k=M+1\), \(\texttt{prop:rank-boundary}\) gives \(I_{0,w}=I_{1,w}=[-K,K]\), \(\mathcal C_w=[-2K,2K]\), and coverage one.