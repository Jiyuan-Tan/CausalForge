Set \(\varepsilon_0=\varepsilon_1=\alpha/2\). By \(\texttt{lem:armwise-finite-rank-staircase}\), \(k_0=k_1=k\), and by \(\texttt{def:coverage-frontier}\),
\[
\Gamma_{M,\alpha,\boldsymbol\varepsilon}(w)=\Gamma_{M,\alpha}(w).
\]
Apply \(\texttt{thm:asymmetric-sharp-orbit-coverage-frontier}\). Its total-preorder reduction proves exact attainment over the same bounded orbit-constant class, with every preorder represented by at most \(m_a\) levels in \([0,K]\), and its common-target formula remains one sum over \(v\in\mathcal V\). When \(k\le M\), substitute \(k_0=k_1=k\) into the exact four-cell evaluator to obtain
\[
O\!\left(F_{m_0}F_{m_1}|\mathcal V|\{m_0m_1+Mk^2\}\right)
\]
arithmetic operations and \(O(k^2)\) working memory after the joint calibration orbit law is tabulated. When \(k=M+1\), both arm events are certain, so the objective is \([(1-\alpha)-1]_+=0\) and is returned in constant time.

The four-pair construction in the asymmetric theorem already lies on this diagonal. Its four-level objective, largest binary objective, and strict gap are respectively
\[
\frac{24957}{160000},\qquad \frac{759109}{5000000},\qquad \frac{83189}{20000000}>0.
\]
Thus binary attainment is false. The asymmetric disjoint-support conclusion gives \(\Gamma_{M,\alpha}(w)=1-\alpha\) when the common finite rank satisfies \(k\le M\). If \(k=M+1\), both arm events are certain and \(\Gamma_{M,\alpha}(w)=0\). These are exactly the asserted equal-split conclusions, including the dynamic-program time and memory bounds and the constant boundary reduction.