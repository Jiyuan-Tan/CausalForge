In the two-pair instance, \(\mathcal G=S_2\times S_2\) contains both pure local swaps. The sufficient audit in \(\texttt{def:pair-action}\) therefore identifies the four pair cells as the four global arm-specific orbits. The orbit-vector mismatch and arm-one selector illegality proved in \(\texttt{prop:two-pair-sanity-check}\) do not involve \(\boldsymbol\varepsilon\), so they are unchanged.

If \(q^{\mathrm{obs}}_{w,1}(O_{1,1})=0\), the sole target-supported arm-one orbit has zero calibration mass. Applying \(\texttt{prop:asymmetric-disjoint-orbit-witness}\) with \(a=1\) gives
\[
\begin{array}{c|c|c}
\text{rank regime}&\mathcal C^{\boldsymbol\varepsilon}_w&\text{conditional coverage}\\ \hline
k_0,k_1\le M&\{0\}&0\\
\text{exactly one }k_c=M+1&[-K,K]&1\\
k_0=k_1=M+1&[-2K,2K]&1.
\end{array}
\tag{1}
\]
When \(\varepsilon_0=\varepsilon_1=\alpha/2\), \(\texttt{lem:armwise-finite-rank-staircase}\) gives \(k_0=k_1=k\). Thus the exactly-one-boundary regime disappears and (1) reduces to the symmetric two-pair calculation.