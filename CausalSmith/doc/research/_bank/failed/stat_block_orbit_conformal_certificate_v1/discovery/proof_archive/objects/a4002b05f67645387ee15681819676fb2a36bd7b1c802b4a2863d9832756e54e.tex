Set \(\varepsilon_0=\varepsilon_1=\alpha/2\). Then \(k_0=k_1=k\) by \(\texttt{lem:armwise-finite-rank-staircase}\), and \(\texttt{def:coverage-frontier}\) identifies \(\Gamma_{M,\alpha,\boldsymbol\varepsilon}(w)=\Gamma_{M,\alpha}(w)\). Apply \(\texttt{thm:asymmetric-sharp-orbit-coverage-frontier}\) on this diagonal. Its total-preorder reduction proves finite attainment and exactness over the declared score class. When \(k\le M\), substituting \(k_0=k_1=k\) gives the exact memo key \((M,k,k,\pi_{00},\pi_{01},\pi_{10})\), the displayed instance-adaptive bound with terminal charge \(GMk^2\), its uniform specialization, and memory \(O(m_0m_1+L+k^2+U_{\max}+G)\). Keys are formed from the unchanged \(R_w\) and local weights from the unchanged coherent \(P_T^C\), preserving shared-assignment dependence and the common target. If \(k=M+1\), both events are certain and the frontier is zero. The asymmetric theorem's four-pair certificate lies on this diagonal and gives the four-level value \(24957/160000\), binary upper value \(759109/5000000\), and strict gap \(83189/20000000\). Its disjoint-support score construction gives \(\Gamma_{M,\alpha}(w)=1-\alpha\) when \(k\le M\). No equality with a supremum over \(\mathcal P_{\mathcal G,K}\) is used or claimed.