Apply \(\texttt{prop:active-arm-finite-rank-orbit-gap-bound}\). Since \(\mathcal A_{\boldsymbol\varepsilon}\subseteq\{0,1\}\) and every \(\rho_{M,\varepsilon_a}\) and \(\Delta_a(w)\) is nonnegative,
\[
1-\sum_{a\in\mathcal A_{\boldsymbol\varepsilon}}\{\rho_{M,\varepsilon_a}+\Delta_a(w)\}
\ge
1-\rho_{M,\varepsilon_0}-\rho_{M,\varepsilon_1}-\Delta_0(w)-\Delta_1(w).
\]
Monotonicity of \(x\mapsto\max\{0,x\}\) proves the displayed frozen lower bound. The coordinate elbows and the inequality \(|\delta_{a,O}(w)|\le\Delta_a(w)\) are also conclusions of that proposition. On the equal-split diagonal, the two ranks coincide. If \(k\le M\), both arms are active and the active-arm formula is exactly the stated symmetric formula. If \(k=M+1\), both arms are boundary arms and the active-arm proposition gives coverage one, which is at least the stated symmetric lower bound. Thus every assertion of the frozen proposition follows, with the boundary regime sharpened rather than weakened.