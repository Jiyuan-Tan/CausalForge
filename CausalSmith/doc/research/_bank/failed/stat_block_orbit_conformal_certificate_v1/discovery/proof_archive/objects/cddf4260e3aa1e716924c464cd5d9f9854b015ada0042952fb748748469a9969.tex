The pair and mate-control conclusions are those of the conditionally valid \(\texttt{prop:s2-reduction}\), hence they inherit its explicit requirement \(\mathcal V_b=\{v_{b,0},v_{b,1}\}\) and \(\langle s_b\rangle\le\mathcal G\). Neither orbit-mass mismatch nor selector illegality depends on \(\boldsymbol\varepsilon\). If the legal implemented arm-one selector assigns zero mass to \(O_{b,1}\), the hypotheses of \(\texttt{prop:asymmetric-disjoint-orbit-witness}\) hold with \(a=1\). That proposition yields \(\{0\}\) and zero coverage when both ranks are finite, \([-K,K]\) and coverage one when exactly one rank equals \(M+1\), and \([-2K,2K]\) and coverage one when both ranks equal \(M+1\). On the diagonal, \(\texttt{lem:armwise-finite-rank-staircase}\) gives \(k_0=k_1=k\), so exactly the two symmetric regimes remain.