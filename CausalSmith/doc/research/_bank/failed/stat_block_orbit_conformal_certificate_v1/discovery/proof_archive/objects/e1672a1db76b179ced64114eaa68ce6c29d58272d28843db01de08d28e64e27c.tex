The full-row portion of \(\texttt{thm:active-arm-minimum-imbalance-selector-certificate}\) proves attainment of \(D_*\) and \(\mathcal W_*\), equality with the absolute-value program, and the attained dual identity. If \(y^*\) is any maximizing dual vector, then for \(x^*=-y^*\), \[(x^*)^\top b_{\mathrm{LP}}-\max_h(x^*)^\top A_{\cdot h}=D_*.\] Thus \(D_*>0\) gives the stated separator and margin. The same theorem gives \[D_*=0\quad\Longleftrightarrow\quad Aw=b_{\mathrm{LP}}\text{ for some }w\in\Delta(\mathcal H),\] which is exact joint-balance feasibility. The entries of \(A\) and \(b_{\mathrm{LP}}\) are fixed by the known design, selector library, and target rule; off-support kernel extensions have zero \(\nu\)-weight. A deterministically chosen optimizer is therefore prespecified.
For every \(w\), the row identities give \[\frac12\sum_{a=0}^1\sum_O|(Aw-b_{\mathrm{LP}})_{a,O}|=\Delta_0(w)+\Delta_1(w).\tag{1}\] The full-row coverage consequence of the active theorem, applied to \(w_*\), proves \[\Pr_P(\tau_T\in\mathcal C^{\boldsymbol\varepsilon}_{w_*}\mid\mathcal T)\ge\max\{0,1-\rho_{M,\varepsilon_0}-\rho_{M,\varepsilon_1}-D_*\}.\tag{2}\] Optimality and (1) give \(D_*\le\Delta_0(w)+\Delta_1(w)\) for every fixed \(w\). Monotonicity after truncation at zero proves weak dominance, and strict inequality together with positivity of the optimized untruncated bound proves strict dominance. On the equal-split diagonal, \(\texttt{def:rank-inversion}\) gives \(\mathcal C^{\boldsymbol\varepsilon}_{w_*}=\mathcal C_{w_*}\), while \(\texttt{def:finite-rank-error}\) gives \(\rho_{M,\varepsilon_0}=\rho_{M,\varepsilon_1}=\rho_{M,\alpha}\). Substitution in (2) yields the asserted symmetric certificate.