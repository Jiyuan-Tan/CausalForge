---
qid: stat_block_orbit_conformal_certificate
spec: v1
topic: "sound-and-complete orbit-balance characterization for exact conformal cluster-effect prediction under finite matched-block randomization: for each known block permutation group and assignment support, prove equality of calibration- and target-selector mass on every member orbit iff selected scores are universally exchangeable over all bounded invariant block laws and equivariant learners; give a finite selector-feasibility LP and block-level rank-inversion algorithm, quantitative bounded undercoverage from every orbit gap, zero-coverage disjoint-orbit and paired-mate-leakage witnesses, the S2 matched-pair reduction, and the Shamba Maisha eight-pair facility consumer"
novelty_target: field
banked_novelty_tier: unknown
tier_at_proposal: ACCEPT
tier_at_derivation: NA
proposal_promise_gap: "tier_genuinely_below"
reusable: not_reusable
reraise_status: re-raise
gap_reasons:
  - "thm:orbit-balance-iff — novelty-comparator-gap: adjacent group-symmetry and causal-conformal classes are named, but the note lacks a theorem-level comparison establishing what they fail to cover."
  - "thm:asymmetric-sharp-orbit-coverage-frontier — novelty-floor-below-field: the exact restricted evaluator was not established as a recognized Stat frontier advance over the closest exact-rank or causal-conformal computation."
reusable_artifacts:
  - "discovery/core.json — fully discharged D0 theorem graph; math referee passed."
  - "discovery/writeup.tex — rendered 46-page derivation note with exact orbit-balance, selector-LP, active-arm, witness, and finite-frontier results."
  - "discovery/d0_working.json — carried proofs and source-attested comparator nodes."
  - "reviews/review_math.json — final passing mathematical review."
  - "reviews/review_rubric.json — exact remaining novelty/comparator findings."
  - "orchestrator/decision_log.jsonl — maximality, source, repair, and stop-rule receipts."
seeds_burned: []
proof_attempt_summary: |
  Discovery reached a fully discharged D0 core and a NO_ROOM maximality verdict; the
  final math referee passed. The single authorized D0.5 decision review did not clear
  the field novelty floor because two theorem-level comparator gaps remained, so the
  revise loop was stopped before F and the verified D-stage artifact was banked.
banked_on: "2026-07-22"
---

# stat_block_orbit_conformal_certificate / v1 — Failed

**Topic.** sound-and-complete orbit-balance characterization for exact conformal cluster-effect prediction under finite matched-block randomization: for each known block permutation group and assignment support, prove equality of calibration- and target-selector mass on every member orbit iff selected scores are universally exchangeable over all bounded invariant block laws and equivariant learners; give a finite selector-feasibility LP and block-level rank-inversion algorithm, quantitative bounded undercoverage from every orbit gap, zero-coverage disjoint-orbit and paired-mate-leakage witnesses, the S2 matched-pair reduction, and the Shamba Maisha eight-pair facility consumer

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** NA

**Banking reason.** Math review passed, but the single authorized D0.5 decision review found unresolved theorem-level comparator positioning and novelty below the field floor.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

The mathematical artifact is reusable, but a future re-raise should begin with a
primary-source theorem comparison for the orbit-balance iff and the exact-rank
frontier. It should not repay the proof search or weaken the scoped deterministic
frontier claims merely to obtain a different review outcome.
