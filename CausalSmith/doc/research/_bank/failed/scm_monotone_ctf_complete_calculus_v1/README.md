---
qid: scm_monotone_ctf_complete_calculus
spec: v1
topic: "Complete signed-monotone counterfactual identification for finite binary semi-Markovian causal diagrams. Given a signed local-monotonicity set M, any finite menu Z of observational/interventional population laws, and any positive-denominator unnested conditional counterfactual query Q, construct a signed order-annotated Ancestral Multi-world Network and canonical order-normal event form; define a terminating M-CTFID procedure that factors order-normal cells over districts and uses ctf-calculus transformations; prove it returns a finite identifying functional iff Q is identified, and otherwise compiles its FAIL trace into two legal finite order-respecting response-type SCMs agreeing on all Z but disagreeing on Q. Replace the ordinary AMWN XOR converse with composable signed isotone threshold gadgets, recover ordinary CTFID when M is empty and all successful M-ID outputs, give the explicit response-type-count termination bound, and add empirical-cell plug-in inference under fixed positive denominators. Use Plecko–Bareinboim's Causal Fairness Analysis toolkit and Maiti's M-ID implementation as consumers."
novelty_target: field
banked_novelty_tier: subfield
tier_at_proposal: ACCEPT
tier_at_derivation: NA
proposal_promise_gap: "kernel_substituted"
reusable: solver_blocked
reraise_status: retry
gap_reasons:
  - "The proposal's district-compositional signed order-AMWN/ctf-calculus with a terminal local-trace compiler is not delivered."
  - "Its if-and-only-if proof uses global response-profile enumeration and real algebraic elimination; it is not a district-compositional order-AMWN or ctf-calculus completeness theorem and does not supply a compiler from a terminal local graphical failure trace."
  - "Districtwise atom probabilities cannot simply be selected independently: the common-menu conditions are globally coupled polynomial constraints, and the current algebraic sample certificate contains no canonical terminal district obstruction. Resolving this requires a genuinely new construction."
reusable_artifacts:
  - "discovery/core.json — typed finite signed-response canonical reduction, global QE/CAD identifier, exact algebraic failure certificates, and M-ID comparison results"
  - "discovery/writeup.tex — full sound subfield derivation, including the explicit limitation separating the oracle from the promised graphical calculus"
  - "orchestrator/d0_5_final_boundary_audit.txt — terminal boundary audit and the missing district-interface/gluing obligations"
  - "reviews/review_general.json — final tier assessment and bounded upgrade diagnosis"
seeds_burned: []
proof_attempt_summary: |
  Discovery derived a sound finite signed-isotone canonical reduction and a terminating global real-algebraic identification oracle with exact countermodels on failure. It could not replace the oracle with the promised district-compositional order-AMWN/ctf-calculus or compile terminal local failure traces into monotone twin SCMs; repeated bounded attempts discharged none of that load-bearing obligation. The remaining work is a genuinely new local-to-global realizability/gluing theorem and failure-backpointer construction, so the achieved subfield result does not meet the field floor.
token_usage:
  complete: false
  orchestrator_tokens: null
  pipeline_codex_tokens: 57776175
  pipeline_claude_tokens: 0
  total_tokens_consumed: null
banked_on: "2026-08-28"
---

# scm_monotone_ctf_complete_calculus / v1 — Failed

**Topic.** Complete signed-monotone counterfactual identification for finite binary semi-Markovian causal diagrams. Given a signed local-monotonicity set M, any finite menu Z of observational/interventional population laws, and any positive-denominator unnested conditional counterfactual query Q, construct a signed order-annotated Ancestral Multi-world Network and canonical order-normal event form; define a terminating M-CTFID procedure that factors order-normal cells over districts and uses ctf-calculus transformations; prove it returns a finite identifying functional iff Q is identified, and otherwise compiles its FAIL trace into two legal finite order-respecting response-type SCMs agreeing on all Z but disagreeing on Q. Replace the ordinary AMWN XOR converse with composable signed isotone threshold gadgets, recover ordinary CTFID when M is empty and all successful M-ID outputs, give the explicit response-type-count termination bound, and add empirical-cell plug-in inference under fixed positive denominators. Use Plecko–Bareinboim's Causal Fairness Analysis toolkit and Maiti's M-ID implementation as consumers.

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** NA

**Banking reason.** The district-compositional signed order-AMWN/ctf-calculus with a terminal local-trace compiler was not delivered; the sound global QE/CAD oracle achieved only subfield tier.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

<!-- Free-form context: what makes this entry interesting, what should be
re-derived vs. re-used, links to follow-on runs. Fill in by hand after the
scaffold is generated. -->
