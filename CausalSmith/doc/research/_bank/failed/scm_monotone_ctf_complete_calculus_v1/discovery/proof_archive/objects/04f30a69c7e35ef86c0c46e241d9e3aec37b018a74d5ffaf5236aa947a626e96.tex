The counting and representation part is proved directly.  If \(M=\varnothing\), then \(S_v=\varnothing\), so
\[
 m_v=0,
 \qquad
 r_v=|\operatorname{pa}_G(v)|.
\]
The Boolean lattice \(\{0,1\}^0\) has one element and exactly two up-sets, namely the empty set and the whole singleton, whence \(D_0=2\).  Therefore
\[
 T_v=D_0^{2^{r_v}}
 =2^{2^{|\operatorname{pa}_G(v)|}}.
\]
There are no signed coordinates to compare, so every order annotation is vacuous.  In the corrected exact response-type atom basis, the order-normal construction is precisely the ordinary AMWN event normalization.  The remaining claim about the completed procedure cannot be discharged until \(\mathrm{oeq:complete\text{-}mctfid}\) is resolved.