The exact order-normal atom identity was combined with district independence and the canonical response bound.  A first attempt treated district response-cell probabilities as free local kernels; this fails because arbitrary local cell laws need not be realizable by independent private and edge latents passed through shared signed-isotone tables.  A second attempt projected the global semialgebraic pair system district by district; its projection steps require the same real quantifier elimination and retain global menu-equality interfaces, so it is not a ctf-calculus completeness proof or a uniform local failure compiler.  A third attempt tried to use each global countermodel pair as a trace; that certificate is indexed by whole profiles and algebraic mass vectors and supplies no composable local gadget.  No parity, multi-bit, or assumed-gluing shortcut was used.