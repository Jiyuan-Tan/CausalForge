Starting handle: normalize \(Q\) with \(\mathsf O_{G,M}(Q)\) and \(\mathsf N_{G,M}(Q)\), memoize requested district order cells, apply the three ctf-calculus rules and signed M\"obius differences before any elimination, and use district-local finite response-type elimination only for a residual requested cell; derive a terminating procedure that returns either an expression in \(\mathcal R(p)\) or the explicit pair \((\mathsf{FAIL},\tau_{\mathsf F})\).