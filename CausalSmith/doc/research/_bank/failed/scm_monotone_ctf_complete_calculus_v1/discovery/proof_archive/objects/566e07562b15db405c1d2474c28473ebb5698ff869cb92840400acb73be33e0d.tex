On common binary inputs, sign normalization turns each restricted response table into an up-set of a Boolean lattice.  This yields exact Dedekind counts, a one-sided transfer of decoded nonidentification certificates to the broader published class, and finite order-closure entailments that expose a systematic antichain/join failure family for the published \(\mathsf{MID}\) control flow.