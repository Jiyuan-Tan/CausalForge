It remains open whether the global response-profile oracle can be replaced, on the same finite-binary recursive semi-Markovian signed-local-monotonicity class, by a terminating district-local rewrite system on \(\mathsf O_{G,M}(Q)\) and \(\mathsf N_{G,M}(Q)\) whose success invariant is sound and complete and whose every terminal local failure compiles into two legal binary signed-isotone district-factorized SCMs agreeing on all specified hard-intervention menu laws, having positive query denominators, and separating \(Q\).