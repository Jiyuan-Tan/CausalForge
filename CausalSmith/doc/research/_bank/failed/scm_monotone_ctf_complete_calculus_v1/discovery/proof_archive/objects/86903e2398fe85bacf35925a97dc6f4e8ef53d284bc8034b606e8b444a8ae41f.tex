MaitiPleckoBareinboim2025 defines the broader signed-monotone CGMA/general-SCM setting and proves \(\mathsf{MID}\) sound.  On the common binary input, the present antichain/join theorem proves that isotonicity makes an entire family of conditional queries equal to one even though the published Algorithms 1--2 delete the entailed upper literal and then return \(\mathsf{FAIL}\); the finite \(\mathsf{MID}^{+}\) order-closure precheck repairs exactly this pattern.  The global \(\mathsf{MCTFID}\) oracle is complete only on the narrower finite-binary recursive semi-Markovian class, but each finite positive-support pair decoded from its failure certificate is also legal in the broader published class and therefore transfers nonidentification upward.  CorreaLeeBareinboim2021 supplies conditional \(\mathsf{CTFID}\) completeness without signed restrictions, and the empty-sign theorem matches that success set only on the common deterministic-hard-intervention binary domain.  ZhangTianBareinboim2022 motivates canonical finite response representations; the present reduction filters the local tables to signed-isotone up-sets and proves the Dedekind count.  CorreaBareinboim2025 motivates AMWN and ctf-calculus structure, while the signed district-local completeness and gluing theorem remain open.  DuarteEtAl2024 is the nearest polynomial-program substrate.  PleckoBareinboim2024 and Maiti's implementation are downstream consumers of the identified functionals, order-closure precheck, and regression witnesses.