The order-normal theorem factors each event indicator into district response-table atoms, but it does not characterize which probability assignments to those atoms are jointly realizable by the semi-Markovian latent incidence structure.  Within a district, response tables at several vertices can share edge latents while every private and edge latent must retain a product law; across directed district interfaces, all regimes must arise from the same local mechanisms.  Consequently a collection of district-local equalities or local failure witnesses need not glue to one global model, much less to two models with exactly equal full menu laws.  The global oracle enforces realizability, common-menu equalities, positive denominators, and query separation simultaneously through whole structural profiles and product-simplex masses.  No declared invariant localizes that nonconvex product-image constraint, and no signed-isotone threshold gadget is proved closed under the required gluing.  Using the global quantifier-elimination sample point as the purported local trace would merely rename the forbidden global kernel.  The frozen primitives therefore prove neither the requested positive compression nor its impossibility.