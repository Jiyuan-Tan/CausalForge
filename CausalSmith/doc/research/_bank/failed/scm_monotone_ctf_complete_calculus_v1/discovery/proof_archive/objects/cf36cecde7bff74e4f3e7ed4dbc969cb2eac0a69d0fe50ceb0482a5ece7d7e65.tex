For each \(E\in\left\lbrace A_Q\cap B_Q,B_Q\right\rbrace\), Theorem \(\mathrm{thm:order\text{-}normal\text{-}factorization}\) gives the unique finite identity
\[
 \mathbf 1_E
 =\sum_{t:e_E(t)=1}\prod_{D\in\mathcal D(G)}
   \mathbf 1_{\left\lbrace (R_v)_{v\in D}=t_D\right\rbrace},
\]
and every deleted order-inconsistent cell is null in every \(\mathfrak m\in\mathfrak M(G,M)\). Theorems \(\mathrm{thm:finite\text{-}decision}\) and \(\mathrm{thm:finite\text{-}oracle\text{-}complete\text{-}mctfid}\) give
\[
 \operatorname{GID}_{G,M,\mathcal Z}(Q)=1
 \quad\Longleftrightarrow\quad
 \mathsf{MCTFID}(G,M,\mathcal Z,Q)=F\in\mathcal R(p),
\]
with \(F(P_{\mathcal Z}(\mathfrak m))=\theta_Q(\mathfrak m)\) for every compatible positive-denominator model. On the complementary branch, the exact global algebraic certificate decodes into finite binary district-factorized signed-isotone models \(\mathfrak m_0,\mathfrak m_1\) with equal hard-intervention menu laws, positive denominators, and unequal query values. Thus both semantic endpoints are proved; only their realization by a terminating district-compositional graphical recursion and a uniform local failure compiler remains unresolved.