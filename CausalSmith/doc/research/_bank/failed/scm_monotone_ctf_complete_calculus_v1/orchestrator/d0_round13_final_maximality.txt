Verdict: ADVANCE-TO-D0.5

The paper is now whole-paper maximal relative to the frozen results. No further material bounded improvement is available. The only remaining FIELD-level advance is the open district-local recursion together with a signed-isotone local-to-global gluing/failure-compiler theorem.

1. Correctness and maximal statement relative to the anchor

The new results are correct and properly scoped against the published Maiti–Plecko–Bareinboim class.

The published paper assumes a general SCM with a strictly positive exogenous law and defines edge monotonicity pointwise while fixing all other observed parents and the exogenous assignment. Its CGMA does not impose the manuscript’s finite-support, independent private/edge-latent, or semi-Markovian restrictions. The manuscript therefore correctly treats its finite-binary recursive semi-Markovian class as a subclass of the relevant common-input binary published class.

The resulting transfer is exactly one-sided. A decoded oracle-failure pair lies in the narrow class and hence also in the broader published binary class. Equal menu laws and unequal positive-denominator query values remain a valid nonidentification witness after class enlargement. Oracle success proves fiber constancy only in the narrow class. Constancy on a subclass does not imply constancy after additional models are admitted. The manuscript explicitly states that success and completeness do not transfer upward. This is the strongest conclusion justified by class inclusion.

The strictness witness and family match the anchor’s actual Algorithms 1–2. Algorithm 1 constructs the joint numerator-and-conditioning ctf-factor and invokes M-REDUCE before forming the conditional ratio. Algorithm 2 applies the binary simplification rule first. For equal-one literals at ordered assignments, that rule deletes the upper literal. The residual lower assignments in the collider are incomparable, so Lemma 6(2), checked at Algorithm 2 line 10, triggers FAIL; Algorithm 1 line 11 propagates it. Independently, isotonicity gives Y_10(u) ≤ Y_11(u) and Y_01(u) ≤ Y_11(u) pointwise. Thus the query is one throughout the broader published binary class whenever its denominator is positive.

The two-scope theorem is therefore exact: broader-class pointwise identification and published-procedure failure; narrow-class GID = 1, MCTFID success, and strict success-set containment.

The antichain/join family and parent-fixed MID+ closure are also correct. Parent-fixedness is essential because only then do t,s specify the complete observed-parent assignment needed for a pointwise order implication. The fallthrough to published MID preserves soundness, and the stated finite literal bound is valid.

2. No further material bounded upgrade

Several immediate corollaries could be written, but none is material enough to justify another D0 round: replace the incomparable pair by a larger finite antichain; allow any common upper bound rather than displaying the coordinatewise join; extend the constant precheck to conjunctive numerators; state a work-list implementation or shave the insertion bound; or add a coarse generic CAD/QE complexity estimate.

These merely repackage the same order-cone argument. The pair is the minimal witness needed to activate Lemma 6’s incomparability branch, the join gives the canonical least upper bound, and the existing closure definition already exposes the reusable computational content. A conventional CAD bound would be enormous, encoding-dependent, and would not improve the semantic theorem or field position. No sharper latent-support constant, statistical rate, branch-boundary result, or meaningful oracle-complexity theorem follows from the frozen proofs without substantial new work.

Thus the new comparator results are now maximally stated in the materially relevant sense.

3. Remaining FIELD-level improvement

The only remaining material FIELD-level route is genuinely oeq:graphical-oracle-compression: a terminating district-compositional order-AMWN/ctf-calculus recursion; a realizability-preserving invariant across recursion; a signed-isotone gluing theorem preserving all cross-regime menu equalities and strict target separation; and a uniform compiler from every terminal local graphical failure trace to two legal signed-isotone SCMs.

The frozen global response-atom factorization does not imply this. Districtwise atom probabilities cannot simply be selected independently: the common-menu conditions are globally coupled polynomial constraints, and the current algebraic sample certificate contains no canonical terminal district obstruction. Resolving this requires a genuinely new construction, not another bounded corollary or prose reframe.

4. PROSE-OPEN-OVERCLAIM audit

The warning on project_justification.fill is a false positive. The sentence claims decoded countermodels, which the completed global oracle theorem proves: a satisfiable global QE system produces exact profile/mass data, and the decoder checks it and returns two finite signed-isotone models.

The open obligation is different. It asks for countermodels compiled from a terminal district-local graphical failure trace through a uniform signed-isotone gluing construction. The current fill immediately distinguishes the two by saying that the full district-local graphical calculus and local failure compiler remain open.

Therefore there is no true overclaim and no prose repair is warranted. Replacing decoded countermodels merely to silence the lexical warning would make the proved result less precise.

The paper may—and likely will—still fail the FIELD floor at D0.5 because its complete procedure remains a global response-enumeration/QE/CAD oracle rather than the promised graphical complete calculus. That is a novelty-tier limitation, not a remaining bounded maximality defect.
