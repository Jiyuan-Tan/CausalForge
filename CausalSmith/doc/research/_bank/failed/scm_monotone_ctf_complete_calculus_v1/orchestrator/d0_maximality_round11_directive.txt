Whole-paper maximality strengthening grounded in the published Maiti–Plecko–Bareinboim M-ID hypotheses and Algorithms 1–2. Add `thm:published-class-failure-transfer`: formally define the common binary input inclusion from this paper's finite-binary recursive semi-Markovian signed-isotone class into the published broader signed-monotone CGMA/general-SCM class, and prove that every finite positive-support decoded MCTFID failure pair remains a legal separating pair in that broader class, hence certifies nonidentification there. Explicitly state that oracle success/completeness does not transfer upward. Strengthen `thm:mid-success-strictness-witness` without broadening MCTFID: its MCTFID success remains subclass-scoped, but prove that the pointwise constant-one identification and the published M-ID Algorithm 1/2 FAIL trace hold on the broader published binary CGMA class. Add `thm:mid-antichain-join-failure-family`: for binary W, incomparable monotone-parent assignments t1,t2 with a common nonmonotone-parent assignment s, and coordinatewise join t∨, prove the positive-denominator query P(W_{t∨,s}=1 | W_{t1,s}=1,W_{t2,s}=1)=1 pointwise, prove published M-ID deletes the upper literal then fails on the incomparable residual pair, and define/prove sound an M-ID+ order-closure precheck returning conditional constants 1 or 0 when the conditioning event pointwise entails the query or its complement. Preserve `oeq:graphical-oracle-compression` unchanged and open: these bounded strengthenings do not supply the missing district-local recursion or signed-isotone gluing theorem and do not satisfy the FIELD floor alone. Treat the current PROSE-OPEN-OVERCLAIM warning as lexical: global algebraic countermodels are proved; only a local graphical failure compiler is open. SYNC THE PROSE FIELDS (`tldr`, `project_justification.{gap,niche,fill}`, `related_work`, honest scope, theorem justification/gap/consumer text, and the Maiti comparator row) to the two-scope result. Never describe the global oracle as complete for the anchor's broader ordered-variable/general-SCM class.
