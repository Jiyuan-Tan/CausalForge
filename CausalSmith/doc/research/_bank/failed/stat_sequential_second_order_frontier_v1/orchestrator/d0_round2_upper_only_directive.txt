Adjust the headline to the strongest honest result under the same frozen assumptions: a log-free unrestricted upper frontier, not an unproved two-sided minimax frontier. Do not attempt another Brown--Low, Duchi--Ruan, two-point, Assouad, or ordinary Bayes-risk transport for the centered lower bound. The cited inequalities control total risk; after subtracting the law-dependent V*(P)/n their leading 1/n terms swallow the desired residual. The coupled density--mean hypercube has the correct n^{-1-2 alpha_d} target scale, but its mean perturbation changes V*(P)/n at exactly that scale, so target separation does not prove positive centered excess.

Re-author thm:nuisance-reduction-frontier as the established unrestricted upper theorem R_n <= C max(Q_mu,Q_sigma)/n, and thm:explicit-holder-frontier as the corresponding explicit log-free upper rate C max(n^{-1-2 alpha_d}, n^{-1-2 beta_d}). Move the unrestricted centered lower bound into a clearly marked OEQ that may be reopened only after a first-principles centered Bayes-risk decomposition of the form inf_T E_pi MSE >= E_pi V*/n + c h^{4s+d} for every fixed law-independent adaptive design, with all vertex-dependent V* terms explicitly calculated. Never add that decomposition as an assumption or definition. If useful, state a separate estimator-restricted comparator theorem only for exactly/local-unbiased estimators and only at the strength actually justified by Li--Simchi-Levi; do not present it as unrestricted MSE.

Repair lem:predictable-aipw-holder-upper before using it. Work with the smaller sequential-reveal filtration generated by observations actually revealed before time t, or explicitly restrict the information order so future baseline covariates are excluded; then re-establish the conditional-centering and cross-fit orthogonality step. Prove the cross-evaluated allocation-regret inequality F_z(u*(z_tilde)) - F_z(u*(z)) <= C ||z-z_tilde||^2 itself, using the clipped strongly convex/piecewise-smooth allocation objective and controlling active-set changes; piecewise quadraticity of only the optimized value is insufficient. Do not response-clip squared residuals while invoking E[(Y-muhat)^2|X]=sigma^2+(muhat-mu)^2; clip only the fitted variance or quantify clipping bias. Fix all malformed TeX in the superseding proof payloads, including missing backslashes in mu, <=, and sqrt expressions.

Synchronize tldr, project_justification.gap/niche/fill, related_work, comparator_promise_table, honest_scope, technical_internal_limitation, estimand_functional, theorem justifications/gaps/consumers, and title-level positioning. Lead with the constructive log-free sequential upper bound over every measurable estimator/design pair and the exact standalone nuisance maximum under the disclosed strict variance range. State plainly that a matching unrestricted centered converse remains open, so do not call the result exact, minimax-matching, a frontier settled in both directions, or equality even for s <= r. Keep the allocation-sensitivity and smoothness-adaptation questions separate and honest.
