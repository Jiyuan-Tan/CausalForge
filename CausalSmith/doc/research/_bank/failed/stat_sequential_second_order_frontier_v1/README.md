---
qid: stat_sequential_second_order_frontier
spec: v1
topic: "Exact second-order minimax excess-risk frontier for a linear causal functional in a sequential (non-anticipating) experiment, over the UNRESTRICTED estimator class. Setting: K >= 2 arms, X in [0,1]^d, latent units (X, Y(1),...,Y(K)) i.i.d., observed data generated by any non-anticipating design (Li and Simchi-Levi arXiv:2606.31190 Definition 2.1) with positivity floor eps; no interference; target theta(P) = E[sum_a omega_a(X) mu_a(X)] with fixed known bounded weights (contains ATE, weighted/subgroup ATE, policy value). Model class P(s,r): fixed n-independent Holder class -- covariate density bounded above and below on [0,1]^d, Gaussian conditional outcomes, mu_a in H^s(L_mu) with sup-norm bound M, sigma_a^2 in H^r(L_sig) with values in [v_min, v_max]; so the nuisance L2 minimax exponents are alpha = s/(2s+d) for the outcome regressions and beta = r/(2r+d) for the design-relevant second moments m_a = omega_a^2 sigma_a^2, both automatically in (0,1/2). Define R_n(s,r) := inf over pairs (non-anticipating design, ANY F_n-measurable estimator T) of sup over P in P(s,r) of { E_P[(T - theta(P))^2] - V*(P)/n }, where V*(P) is the oracle generalized-Neyman semiparametric benchmark inf_e V(e) at P; the loss is MEAN-SQUARED ERROR and NO unbiasedness, regularity, asymptotic-linearity or bias-rate restriction is imposed on T. CONJECTURE: R_n(s,r) is of exact order max( n^{-1-2alpha}, n^{-1-2beta} ) with matching bounds up to constants only, with NO logarithmic factor on either side. This settles two things the anchor leaves open: (i) its Theorem 4.6 converse holds only for EXACTLY unbiased estimators, because its whole proof runs through the Cauchy-projection identity Var_P(T) - V*(P)/n = E_P[(T - theta_oracle)^2] (Lemma E.5), which substitutes E_P[T] = theta(P) exactly and whose variance criterion is vacuous for biased T -- and the anchor's o(n^{-1/2}) bias relaxation (Remark 3.2) is a FIRST-order relaxation, useless at the second-order scale n^{-1/2-alpha}; (ii) both halves are stated only up to logarithmic factors. Mechanics DELEGATED to the solver: the converse mechanism, all constants, the batching schedule, sample-splitting scheme and nuisance learners, whether the attaining pair is the anchor's batched cross-fitted procedure or a modification, any log-removal argument, and whether Gaussianity can be relaxed to sub-Gaussian."
novelty_target: field
banked_novelty_tier: unknown
tier_at_proposal: ACCEPT
tier_at_derivation: NA
proposal_promise_gap: "A future proposal must supply an explicit sequential Bayes information-regret lower construction uniform over every non-anticipating design and show cancellation of every law-dependent term in V*(P)/n; fixed-design or ordinary total-risk lower bounds do not suffice."
reusable: unknown
reraise_status: retry
gap_reasons:
  - "The advertised exact unrestricted minimax frontier is replaced by a one-sided log-free upper bound; retain the upper result only after retitling and positioning it as such, or supply the missing centered converse."
  - "fixing variances freezes only Phi; zero-target mean bumps still vary Var_P(h_P(X))/n at the target second-order scale."
  - "A lower bound for one fixed D cannot bound R_n=inf_D L(D)."
reusable_artifacts:
  - "discovery/writeup.tex — proved predictable AIPW and smoothness-adaptive log-free upper constructions, with explicit scope disclaimers."
  - "discovery/core.json — formal dependency graph, single-arm variance obstruction, standalone nuisance results, and the open centered-converse node."
  - "discovery/gaps.json — literature map for sequential efficiency, adaptive allocation, variance-function estimation, and constrained-risk lower bounds."
  - "reviews/review_general.json — subfield assessment and the coarse-fine Bayes-prior research direction, explicitly not a proof."
seeds_burned: []
proof_attempt_summary: |
  Discovery derived a law-independent, smoothness-adaptive sequential construction giving the claimed log-free two-nuisance upper rate, plus sharper mean-only upper regimes. The exact frontier collapsed because ordinary Assouad/constrained-risk and fixed-design arguments do not survive subtraction of the law-dependent oracle benchmark or the infimum over all non-anticipating designs. A future proposal must begin with an explicit uniform-over-design sequential Bayes information-regret converse rather than reuse the present lower-bound carrier.
banked_on: "2026-08-09"
---

# stat_sequential_second_order_frontier / v1 — Failed

**Topic.** Exact second-order minimax excess-risk frontier for a linear causal functional in a sequential (non-anticipating) experiment, over the UNRESTRICTED estimator class. Setting: K >= 2 arms, X in [0,1]^d, latent units (X, Y(1),...,Y(K)) i.i.d., observed data generated by any non-anticipating design (Li and Simchi-Levi arXiv:2606.31190 Definition 2.1) with positivity floor eps; no interference; target theta(P) = E[sum_a omega_a(X) mu_a(X)] with fixed known bounded weights (contains ATE, weighted/subgroup ATE, policy value). Model class P(s,r): fixed n-independent Holder class -- covariate density bounded above and below on [0,1]^d, Gaussian conditional outcomes, mu_a in H^s(L_mu) with sup-norm bound M, sigma_a^2 in H^r(L_sig) with values in [v_min, v_max]; so the nuisance L2 minimax exponents are alpha = s/(2s+d) for the outcome regressions and beta = r/(2r+d) for the design-relevant second moments m_a = omega_a^2 sigma_a^2, both automatically in (0,1/2). Define R_n(s,r) := inf over pairs (non-anticipating design, ANY F_n-measurable estimator T) of sup over P in P(s,r) of { E_P[(T - theta(P))^2] - V*(P)/n }, where V*(P) is the oracle generalized-Neyman semiparametric benchmark inf_e V(e) at P; the loss is MEAN-SQUARED ERROR and NO unbiasedness, regularity, asymptotic-linearity or bias-rate restriction is imposed on T. CONJECTURE: R_n(s,r) is of exact order max( n^{-1-2alpha}, n^{-1-2beta} ) with matching bounds up to constants only, with NO logarithmic factor on either side. This settles two things the anchor leaves open: (i) its Theorem 4.6 converse holds only for EXACTLY unbiased estimators, because its whole proof runs through the Cauchy-projection identity Var_P(T) - V*(P)/n = E_P[(T - theta_oracle)^2] (Lemma E.5), which substitutes E_P[T] = theta(P) exactly and whose variance criterion is vacuous for biased T -- and the anchor's o(n^{-1/2}) bias relaxation (Remark 3.2) is a FIRST-order relaxation, useless at the second-order scale n^{-1/2-alpha}; (ii) both halves are stated only up to logarithmic factors. Mechanics DELEGATED to the solver: the converse mechanism, all constants, the batching schedule, sample-splitting scheme and nuisance learners, whether the attaining pair is the anchor's batched cross-fitted procedure or a modification, any log-removal argument, and whether Gaussianity can be relaxed to sub-Gaussian.

**Novelty target.** field

**Stage -0.5 verdict.** ACCEPT

**Stage 0.5 verdict.** NA

**Banking reason.** D0.5 kernel_substituted: the advertised exact unrestricted minimax frontier was replaced by a one-sided log-free upper bound, while the unrestricted centered converse remained open.

## Key files

- `state.json` — pipeline state at banking (`banked: true`).
- `discovery/proposal.tex` — final proposal version.
- `discovery/writeup.tex` — derivation note (if Stage 0 ran).
- `reviews/reviews.jsonl` — per-round reviewer log (Stage -0.5 and Stage 0.5).
- `reviews/` — per-version reviewer JSON files (if present).

## Notes

The upper-only mathematics is potentially reusable as a separately titled subfield contribution, but it
must not be presented as the original exact minimax frontier. The cited Shen–Gao–Witten–Han variance upper
bound was verified from the complete arXiv source and attested before the terminal D0.5 review.
