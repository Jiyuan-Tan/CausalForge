The proved result is population mark completeness for Dai et al.'s full causally sufficient pre-treatment-selection compatibility class.  Regime zero is indicator-free on \(V\); each \(k\in\mathcal K_+\) is on \(V\cup\{\zeta_k\}\).  The computable functional enumerates basal edge states, ancestor-closed selected sets, and ordered unknown targets, then derives the residual two-section from the input observational skeleton.  It returns the canonical SI-PAG and paired compatible witnesses in singly exponential time in \(D^2+DK\), without claiming polynomial conditional-independence discovery, a tight complexity bound, or completeness of a local rule calculus.  When \(K=0\) and selection is absent, the output reduces extensionally to the ordinary CPDAG.