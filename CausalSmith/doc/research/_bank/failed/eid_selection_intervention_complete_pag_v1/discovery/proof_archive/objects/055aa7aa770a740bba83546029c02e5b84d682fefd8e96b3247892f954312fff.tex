For every \(\eta\in\mathfrak C_D(\mathbf M)\), let \(E=\mathcal G_\eta[V]\), let \(A=\operatorname{an}_E(\bigcup_{s\in S_\eta}\operatorname{pa}_{\mathcal G_\eta}(s))\), put \(\{i,j\}\in Q\) exactly when some \(s\in S_\eta\) has \(\{i,j\}\subseteq\operatorname{pa}_{\mathcal G_\eta}(s)\), and set \(T_k=I_\eta^{(k)}\).  Then \(\mathcal R_\eta=(E,A,Q,(T_k)_{k\in\mathcal K_+})\) is a normalized Dai two-section certificate.  Replacing the selection nodes by one binary childless node with parent set \(\{i,j\}\) for each \(\{i,j\}\in Q\) and one with singleton parent \(\{i\}\) for each \(Q\)-isolated \(i\in A\), while discarding empty-parent nodes, produces \(\bar\eta\in\mathfrak C_D(\mathbf M)\) with exactly the same projected MAG in every regime.  Conversely, every normalized certificate is the exact \((A,Q)\) summary of this canonical reconstruction, which uses at most \(\binom D2+D\) selection nodes.