Let \(\mathcal Q=\operatorname{Cl}_{\mathsf R_{\mathrm{SI}}}(\mathcal P_{\mathrm{init}}(\mathbf M))\).  Conditional on the soundness and paired-witness conclusions requested in \(\texttt{oeq:si-extension-calculus}\), one has \(\mathcal Q=\mathcal P_{\mathrm{SI}}(\mathbf M)\).  For if \(\mathcal Q\) fixes endpoint \(e\) to \(a\in\{\mathrm{tail},\mathrm{arrow}\}\), soundness gives \(\operatorname{mark}_e(\mathcal M_\eta^{(k)})=a\) for every \(\eta\in\mathfrak C_D(\mathbf M)\), so the canonical graph fixes \(a\).  If \(\mathcal Q\) leaves \(e\) circular, the two compatible witnesses give both possible marks, so the canonical graph also has a circle at \(e\).  The skeletons agree by construction, proving equality endpoint by endpoint and also proving the stated witness equivalence.