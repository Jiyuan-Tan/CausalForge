\textbf{Proof.}
The equality, population soundness and completeness, paired witness construction, certificate-count bound, and runtime bound are exactly \(\texttt{thm:enumerative-si-pag-completeness}\) applied to the encoded family.  The domain statement follows from the two branches of \(\texttt{def:dai-certificate-checker}\): \(\mathcal N^{(0)}\) is constructed on \(V\) without an indicator, while \(\mathcal N^{(k)}\) is constructed on \(V\cup\{\zeta_k\}\) only for \(k\in\mathcal K_+\).  The algorithm consumes the already supplied matrices from \(\texttt{def:oracle-input-encoding}\), so its proof neither invokes nor bounds any procedure for discovering those matrices.  Finally, it enumerates a generally doubly exponential global certificate space and never invokes the unresolved local calculus; hence no polynomial global-search or local-rule completeness claim follows. \(\square\)