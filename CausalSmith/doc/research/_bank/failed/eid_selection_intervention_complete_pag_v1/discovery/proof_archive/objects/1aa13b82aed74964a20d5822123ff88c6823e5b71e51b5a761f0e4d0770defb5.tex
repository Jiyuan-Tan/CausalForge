A tail or arrowhead in the output is invariant over every basal DAG, selection structure, and ordered target family compatible with the supplied oracle MAGs.  A circle is stronger than a failure of one orientation heuristic: the algorithm returns two accepted certificates whose reconstructed common models agree on every fixed output mark and differ at that endpoint.  In the sciPlex2 A549 workflow, these certificates support an auditable population-level distinction between identified orientation and genuine ambiguity over the full pre-treatment-selection class.  Dai et al.'s two-variable witness is the smallest sanity check: selection through \(1\to S\leftarrow2\) and the unselected direction \(2\to1\) force the endpoint at vertex \(1\) to remain circular.