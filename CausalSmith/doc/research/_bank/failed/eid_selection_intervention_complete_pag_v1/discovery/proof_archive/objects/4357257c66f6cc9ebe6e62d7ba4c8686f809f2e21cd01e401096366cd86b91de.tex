Population-oracle graphical input with no sampling randomness: one MAG matrix on \(V\) for observation and one MAG matrix on \(V\cup\{\zeta_k\}\) for each \(k\in\mathcal K_+\).  Completeness is obtained by finite normalized-certificate enumeration, not by an assumed local orientation closure.