At a fixed endpoint, the output tail or arrowhead is a structural feature shared by every model in the full Dai compatibility class.  A circle is not merely a failure of the enumerator: the algorithm returns one accepted common-basal certificate with a tail and another with an arrowhead, both agreeing with all fixed output marks.  Dai et al.'s two-variable witness is the smallest sanity check of this interpretation.  The residualization of \(Q\) removes only selection pairs whose observational adjacency is already generated by \(E\) or an observed collider child; it does not narrow the compatibility class.