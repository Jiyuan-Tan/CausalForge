The note proves a two-way selection two-section normalization, an exact finite checker, and endpointwise feasible-mark enumeration.  The resulting computable map equals the canonical SI-PAG, supplies actual opposing common-basal certificates at every residual circle, and has explicit complexity \(O\!\left(6^{\binom D2}2^{D+DK}[D^2+(K+1)(D+1)^3+N_{\mathrm{end}}]\right)\), while the fixed local extension calculus remains an open question.