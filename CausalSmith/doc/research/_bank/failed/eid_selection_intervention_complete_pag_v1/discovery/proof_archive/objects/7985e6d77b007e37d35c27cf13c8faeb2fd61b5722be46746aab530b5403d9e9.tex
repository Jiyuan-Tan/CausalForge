For every compatible encoded oracle family \(\mathbf M\), \(\operatorname{CDIS}^{\mathrm{cmp}}(\mathbf M)=\mathcal P_{\mathrm{SI}}(\mathbf M)\); the algorithm is population sound and complete, its orientation phase uses at most \(N_{\mathrm{end}}\) strict endpoint updates, and its total oracle-input closure runtime is \(T_{\mathrm{cl}}(D,K)\). The observational output is on \(V\), each nonobservational certificate component is on \(V\cup\{\zeta_k\}\) for \(k\in\mathcal K_+\), and no bound is asserted for the conditional-independence or invariance queries needed to obtain \(\mathbf M\).