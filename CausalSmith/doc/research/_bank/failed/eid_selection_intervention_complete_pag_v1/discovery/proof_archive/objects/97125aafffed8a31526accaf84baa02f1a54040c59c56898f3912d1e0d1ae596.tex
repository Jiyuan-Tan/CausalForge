For every separately supplied fixed finite monotone rule list, \(\texttt{lem:oracle-closure-runtime}\) proves termination after at most \(N_{\mathrm{end}}\) strict endpoint updates and a polynomial mark-propagation runtime.  Independently, \(\texttt{thm:enumerative-si-pag-completeness}\) computes the exact canonical SI-PAG and supplies globally compatible opposing certificates for every circle.  What remains open is whether one fixed local rule list produces that same graph on the full Dai class.