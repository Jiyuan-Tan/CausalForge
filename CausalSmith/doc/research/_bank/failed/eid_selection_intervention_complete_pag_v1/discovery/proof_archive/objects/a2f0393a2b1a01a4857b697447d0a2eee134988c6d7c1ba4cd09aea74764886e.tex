If a finite sound rule system with the requested paired witness property is supplied, its closure is automatically the canonical SI-PAG.  Indeed, soundness makes every fixed closure mark invariant over \(\mathfrak C_D(\mathbf M)\), while the tail and arrowhead witnesses for each residual circle make that endpoint noninvariant.  Together with the common skeletons, these two facts determine every endpoint exactly as in \(\texttt{def:canonical-si-pag}\).  The proved runtime lemma then gives the requested polynomial oracle-input closure certificate for that system.