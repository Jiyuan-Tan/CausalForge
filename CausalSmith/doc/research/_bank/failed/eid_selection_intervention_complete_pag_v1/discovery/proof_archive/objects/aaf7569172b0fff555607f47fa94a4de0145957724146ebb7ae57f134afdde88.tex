The missing object can be recovered without assuming the unresolved local extension lemma by searching finite certificates that encode one common basal DAG, the distinct selection-parent sets, and the ordered unknown intervention targets.