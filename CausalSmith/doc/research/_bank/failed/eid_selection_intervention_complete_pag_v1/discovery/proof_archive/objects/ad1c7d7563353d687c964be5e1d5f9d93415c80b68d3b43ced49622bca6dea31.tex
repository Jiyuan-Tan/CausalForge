The paper proves that the finite checker accepts exactly the normalized members of the full compatibility class, then enumerates all such certificates to compute the canonical SI-PAG and to retain opposing compatible witnesses for every residual circle, with a conservative explicit exponential runtime certificate.