Let \(\mathcal R=(E,H,(T_k)_{k\in\mathcal K_+})\) be a normalized certificate, let \(A=\operatorname{an}_E(\bigcup H)\), and let \(F_k=\operatorname{de}_E(T_k)\).  For the causally sufficient, childless-selection, pre-treatment twin construction, Dai et al.'s MAG formulas give exactly the observational adjacencies, interventional path-induced adjacencies, indicator adjacencies, and endpoint orientations used to construct \(\mathbf N(\mathcal R)\) in \(\texttt{def:dai-certificate-checker}\).  Moreover, two such basal-DAG and ordered-target families are Markov equivalent across all regimes if and only if their corresponding regime MAGs have the same adjacencies and unshielded collider triples in every regime.