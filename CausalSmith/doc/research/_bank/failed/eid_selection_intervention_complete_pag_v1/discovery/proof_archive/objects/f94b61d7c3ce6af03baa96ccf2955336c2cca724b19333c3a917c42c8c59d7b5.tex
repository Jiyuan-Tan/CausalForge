Let \(\mathcal R=(E,A,Q,(T_k)_{k\in\mathcal K_+})\) be a normalized Dai two-section certificate and let \(F_k=\operatorname{de}_E(T_k)\).  In the causally sufficient, childless-selection, pre-treatment twin construction, Dai et al.'s MAG formulas depend on the selection structure only through the ancestrally selected set \(A\) and the two-section graph \(Q\): they give exactly the observational adjacencies, interventional path-induced adjacencies, indicator adjacencies, and endpoint orientations used to construct \(\mathbf N(\mathcal R)\) in \(\texttt{def:dai-certificate-checker}\).  Moreover, two such basal-DAG and ordered-target families are Markov equivalent across all regimes if and only if their corresponding regime MAGs have the same adjacencies and unshielded collider triples in every regime.