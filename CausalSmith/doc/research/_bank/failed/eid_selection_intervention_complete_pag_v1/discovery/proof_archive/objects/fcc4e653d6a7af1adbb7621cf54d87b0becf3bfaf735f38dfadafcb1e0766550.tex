For every \(\eta\in\mathfrak C_D(\mathbf M)\), deleting all but one childless selection node for each distinct parent set produces a candidate \(\bar\eta\in\mathfrak C_D(\mathbf M)\) with exactly the same observational and nonobservational projected MAGs.  The candidate \(\bar\eta\) has a normalized certificate \(\mathcal R_{\bar\eta}=(E,H,(T_k)_{k\in\mathcal K_+})\) with \(|H|\le 2^D\).