For any declared fixed positive contrast-spectrum and pre-Gram lower bounds, a deterministic singular-value threshold tending to zero can separate exact rank one from the corresponding fixed separated rank-two class once its stochastic singular-value error is below that threshold, and sample splitting can charge the selection error to \(\varepsilon_{N_0,K_T,K_D}(\alpha)\). Uniformity over all unknown positive lower bounds also includes triangular rank-two sequences approaching the rank-one boundary, so a complete result requires an explicit separation sequence and a normalization-preserving testing pair that links testing error to excess expected width under the common coverage schedule.