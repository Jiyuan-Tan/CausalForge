\textbf{Proof.}
At \(r=1\), \(d=0\), and normalization collapse gives \(\theta=s_1\). The corrected definition pools the \(n_D=N_0K_D\) donor-post records, forms their ordinary aggregate-risk product-limit curve, and then interpolates it:
\[
 \widehat\theta_1=\mathcal L_{\mathcal G}\widehat S_D^{\mathrm{PL}}. \tag{1}
\]
It attaches one multiplier to each pooled patient increment and uses the same aggregate risk denominator. The pooled non-identically censored lemma proves the required oracle and empirical martingale bounds even with donor-specific censoring. The deterministic guard is a fixed multiple of \(n_D^{-1/2}\), the higher-rank \(\eta_D\) term vanishes, the interpolation proposition supplies the stated allowance, and the attaining theorem supplies simultaneous honesty. This is exactly the asserted pooled Kaplan--Meier guarded multiplier band. \(\square\)