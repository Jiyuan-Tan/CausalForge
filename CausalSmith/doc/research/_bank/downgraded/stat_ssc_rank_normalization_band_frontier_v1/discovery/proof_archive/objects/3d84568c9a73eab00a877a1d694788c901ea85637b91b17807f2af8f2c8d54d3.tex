\textbf{Proof.}
At \(r=1\), \(d=0\).  The normalization-collapse lemma gives
\(\theta=s_1\), and neither the target-pre cohort nor a heterogeneous
loading enters the estimand.  The corrected estimator definition first
pools all \(n_D=N_0K_D\) donor-post records, forms their c\`adl\`ag
product-limit curve \(\widehat S_D^{\mathrm{PL}}\), and then applies the
deterministic grid interpolator:
\[
 \widehat\theta_1=\mathcal L_{\mathcal G}
                    \widehat S_D^{\mathrm{PL}}.             \tag{1}
\]
Thus the declared estimator is continuous rather than a raw c\`adl\`ag
curve.

The same definition attaches one independent multiplier to each pooled
patient counting-process increment and applies the same interpolation
operator.  The pooled non-identically censored Kaplan--Meier lemma proves
that this is the conditional multiplier for the first-order influence sum,
even when the donor censoring survivals differ.  It also supplies the
conditional covariance consistency and random-quantile inequality.  The
higher-rank deterministic bias term vanishes because
\(\mathbf1\{r\ge2\}\eta_D=0\).  Finally, the interpolation proposition
adds only \(c_{\mathrm{int}}h^{\min\{\beta+1,2\}}\), and the attaining-band
theorem proves simultaneous honesty.  These identities are exactly the
pooled donor-post Kaplan--Meier estimator and its patient-martingale
multiplier band, up to the displayed interpolation allowance. \(\square\)