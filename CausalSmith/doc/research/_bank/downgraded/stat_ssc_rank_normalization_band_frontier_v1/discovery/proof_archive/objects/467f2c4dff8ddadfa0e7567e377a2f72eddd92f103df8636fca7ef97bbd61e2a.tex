\textbf{Proof.}
At \(r=1\), normalization collapse gives \(d=0\) and \(\theta=s_1\).  The corrected construction explicitly bypasses stabilization and every factor, projector, loading, and pre-Gram operation: it pools the \(N_0K_D\) donor-post records, forms their ordinary aggregate-risk product-limit estimator, and interpolates its grid values.  Its branchwise multiplier coefficients are exactly the ordinary pooled patient martingale coefficients.  The deterministic risk-exhaustion bias and centered remainder, including non-identical donor censoring, are those proved in \(\mathrm{lem:pooled\mbox{-}noniid\mbox{-}km\mbox{-}linearization}\).  The pointwise continuous band uses the conditional quantile of this pooled multiplier plus the finite-tail and interpolation allowances validated in the attaining theorem and \(\mathrm{prop:grid\mbox{-}interpolation}\).  Thus both the estimator and band reduce to the stated pooled Kaplan--Meier construction, up to the displayed interpolation allowance. \(\square\)