Put \(\nu=\min\{\beta+1,2\}\).  Let \(c_{\mathrm{int}}=c_{\mathrm{int}}(L,\bar\tau)<\infty\) be a deterministic uniform upper bound for the piecewise-linear interpolation error of a survival curve whose hazard has \(C^\beta(\mathcal I)\)-norm at most \(L\).  Choose a deterministic grid \(\mathcal G_{N_0,K_T,K_D}\) containing zero and \(\bar\tau\) whose mesh obeys \(c_{\mathrm{int}}h_{N_0,K_T,K_D}^{\nu}\le\rho_r(N_0,K_T,K_D)\), and use piecewise-linear interpolation between adjacent grid points.