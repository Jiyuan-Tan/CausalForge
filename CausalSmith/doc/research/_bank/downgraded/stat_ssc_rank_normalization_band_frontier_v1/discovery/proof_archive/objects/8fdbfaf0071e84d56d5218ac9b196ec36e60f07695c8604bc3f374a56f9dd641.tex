\textbf{Proof.}
At \(r=1\), \(d=0\).  By normalization collapse, every factor and loading product is an empty sum and no target-pre record enters either the target or the estimator.  The corrected estimator definition explicitly sets \(\widehat\theta_1\) equal to the Kaplan--Meier curve formed by pooling the \(N_0K_D\) donor-post patients.  Its empirical derivative is therefore the ordinary pooled product-limit martingale derivative, and attaching one independent multiplier to each patient gives exactly the patient-level simultaneous multiplier process.  The additional risk-exhaustion allowance is multiplied by \(\mathbf 1\{r\ge2\}\), so it vanishes in this case.  Finally, the interpolation proposition supplies the stated continuous-time allowance. \(\square\)