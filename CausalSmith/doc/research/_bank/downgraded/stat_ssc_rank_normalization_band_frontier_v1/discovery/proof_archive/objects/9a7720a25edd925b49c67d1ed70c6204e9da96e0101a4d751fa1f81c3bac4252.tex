The primitive centered donor survival operator has exactly \(r-1\) bounded nonzero singular directions. Stabilized cellwise product-limit curves and odd--even cross-products feed a covariance-normalized functional-PCR map. Patient-level martingale derivatives estimate both target-pre and donor covariance blocks; a conditional Gaussian maximum quantile is inflated in level and supplemented by an explicit \(o(\rho_r)\) additive comparison slack, with total measurable fallback branches.