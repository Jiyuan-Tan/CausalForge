Normalization \(S(0)=1\) creates a rank-specific information geometry absent from scalar synthetic-control inference.  The fixed-rank problem separates a donor-patient channel from a target-pre loading channel, while the unknown-rank problem additionally separates donor-rank detectability from target-loading estimability.