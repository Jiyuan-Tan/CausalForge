The closed claims concern known fixed total rank defined by a primitive centered donor operator, exactly \(r-1\) nonzero contrast directions with uniform spectrum bounds, target-pre factor compatibility with a positive pre-period Gram, independent right censoring, a fixed horizon, smooth hazards, and independent patient sampling. Honesty is imposed at every evaluated triple with a deterministic coverage-error allowance vanishing uniformly on three-dimensional tails; no balanced-growth condition or coordinate-variance floor is imposed. The rank-one result is exact at order \((N_0K_D)^{-1/2}\). For fixed higher rank, only the displayed \(\rho_r\)-to-\(\rho_r+\eta_D\) bracket is unrestricted. On the larger no-common-pre-Gram donor-visible known-rank-two class, the minimax order is one, so donor-rank detection alone cannot license shrinking rank-adaptive bands. Covariate-dependent censoring and approximate-rank bias remain outside scope.