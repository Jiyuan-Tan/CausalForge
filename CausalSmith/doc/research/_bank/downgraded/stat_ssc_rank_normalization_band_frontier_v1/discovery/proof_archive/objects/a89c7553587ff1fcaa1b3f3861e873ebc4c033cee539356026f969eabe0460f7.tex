The rank-one frontier is donor-patient limited because survival normalization collapses every heterogeneous loading coordinate. At fixed higher rank, target-pre patients and donor-post patients form two independent first-order information blocks. The multiplier quantile estimates the joint Gaussian radius even when some times have zero variance, while the additive slack pays only for Gaussian approximation and estimated covariance. The separate \(\eta_D(K_D)\) term is deterministic product-limit risk-exhaustion bias and is not removed by bootstrap validity.