\textbf{Proof.}
At \(r=1\), \(d=0\). By normalization collapse, every factor and loading product is an empty sum and no target-pre record enters either the causal target or the estimator. The corrected estimator definition sets \(\widehat\theta_1\) exactly equal to the Kaplan--Meier curve formed by pooling the \(N_0K_D\) donor-post patients. Its empirical derivative is the ordinary pooled product-limit martingale derivative, and attaching one independent multiplier to each patient gives exactly the patient-level simultaneous multiplier process. The correction \(\mathbf 1\{r\ge2\}\eta_D(K_D)\) vanishes, while the channel-separated calculation gives \(\omega_1=o((N_0K_D)^{-1/2})\). Finally, the interpolation proposition supplies the stated continuous-time allowance. Thus the rank-one branch is precisely the pooled donor-post patient-level construction asserted in the proposition. \(\square\)