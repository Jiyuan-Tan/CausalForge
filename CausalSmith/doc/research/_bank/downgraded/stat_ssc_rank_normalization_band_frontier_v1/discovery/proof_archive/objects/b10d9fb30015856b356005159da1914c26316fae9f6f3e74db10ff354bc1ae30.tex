At \(r=1\), \(d=0\), so every contrast and loading product is an empty sum and no target-pre observation enters the estimator.  Under the proposed estimator definition, the rank-one branch is explicitly the pooled Kaplan--Meier curve from the \(N_0K_D\) donor-post records; differentiating that branch attaches one martingale multiplier to each pooled patient.  Piecewise-linear interpolation and the corrected allowance then give exactly the claimed reduction.  The argument is complete against the proposed construction but cannot be attached before that definition correction is adjudicated.