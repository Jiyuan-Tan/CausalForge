Compute every \(\widehat S^{\mathrm{KM}}_{p,m}\) on \(\mathcal G_{N_0,K_T,K_D}\). Center the donor curves, retain the first \(d=r-1\) functional principal contrasts, estimate \(z_\star\) by minimum-norm least squares from \(\widehat S^{\mathrm{KM}}_{0,\star}-\widehat s_0\), and set \(\widehat\theta_r=\widehat s_1+\widehat b_1^\top\widehat z_\star\), with \(\widehat\theta_1=\widehat s_1\). Linearize this map jointly in all cellwise Kaplan--Meier counting-process martingales. Replace each patient martingale increment by \(\xi_{p,m,j}\) times its estimated increment, recompute the linearized target process, and take its conditional \((1-\alpha)\)-quantile in grid supremum norm. Define \(\widehat B_r\) by adding and subtracting that quantile and the deterministic interpolation allowance \(Lh_{N_0,K_T,K_D}^\beta\), interpolate linearly, clip to \([0,1]\), and impose the common endpoint value one at time zero.