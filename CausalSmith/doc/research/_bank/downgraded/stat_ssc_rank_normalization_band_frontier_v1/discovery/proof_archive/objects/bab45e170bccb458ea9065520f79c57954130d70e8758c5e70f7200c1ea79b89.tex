For every \(P\in\mathfrak P_1(N_0,K_T,K_D)\), the canonical contrast dimension is \(d=0\), so \(S^0_{p,m}(t)=s_p(t)\) for all units and \(\theta(t)=s_1(t)\). Along every regular submodel that perturbs \(\theta\) through the post-control law, the score contribution of the target-pre records is identically zero. Thus target-pre data are \(\theta\)-ancillary at exact rank one, while the canonical target-loading derivative is nonzero on a normalization-preserving submodel of \(\mathfrak P_r\) for every fixed \(r\ge2\).