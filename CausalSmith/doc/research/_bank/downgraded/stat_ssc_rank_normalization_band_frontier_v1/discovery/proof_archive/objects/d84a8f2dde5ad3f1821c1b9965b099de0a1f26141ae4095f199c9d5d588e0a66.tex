Let \(n_D=N_0K_D\).  On the explicit baseline permitted by the proposed numerical condition, change only the common donor-post survival curve by \(\pm c n_D^{-1/2}\psi_\circ\), leaving the centered factor curves, donor scores, and every pre-period law fixed.  For small fixed \(c\), the numerical inequalities keep both laws inside the survival and hazard bounds, the centered operator is unchanged, and the product-law Kullback--Leibler divergence is bounded by a constant multiple of \(c^2\).  At a maximizer of \(|\psi_\circ|\), the target separation is \(2c\|\psi_\circ\|_\infty n_D^{-1/2}\).

For any two laws \(Q_0,Q_1\), target separation \(\Delta\), and a band with error \(\eta=\alpha+\varepsilon_{N_0,K_T,K_D}(\alpha)\),
\[
 Q_0\{\theta_0,\theta_1\in B\}
 \ge1-2\eta-\|Q_0-Q_1\|_{\mathrm{TV}}.
\]
On this event \(w(B)\ge\Delta\).  Pinsker's inequality and a sufficiently small \(c\) therefore give the donor lower channel.  The rank-two calculation in the preceding partial result gives the target channel, and the empirical-orthogonal polynomial scores plus smooth orthogonal factor curves retain it for fixed \(r>2\).  The maximum of the two lower bounds is at least one half their sum.  The rank-one donor argument and the two-point band inequality are complete; applying the rank-at-least-two part awaits adjudication of the explicit numerical compatibility condition.