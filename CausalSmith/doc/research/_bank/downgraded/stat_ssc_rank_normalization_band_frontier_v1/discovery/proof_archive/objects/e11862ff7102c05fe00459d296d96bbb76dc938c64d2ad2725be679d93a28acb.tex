Survival normalization and target-pre factor compatibility create a rank-specific information geometry: exact rank one removes the target-loading channel, positive pre-Gram strength makes higher-rank loading estimation regular, and donor-visible rank alone is insufficient when that pre-Gram degenerates.