Assume the proposed numerical compatibility condition and corrected fixed-r construction.  Put \(n_D=N_0K_D\).  For the donor channel, keep the empirical-orthonormal scores, the curves \(A_{p,d}\), every pre-period law, and target loading zero fixed, and replace only the post baseline by
\[
 s_1^{(u)}(t)=e^{-\lambda_\circ t}+u c n_D^{-1/2}\psi_\circ(t),
 \qquad u\in\{-1,+1\}.
\]
For fixed sufficiently small \(c>0\) and all sufficiently large triples, \(c n_D^{-1/2}\le v_\circ\); hence the compatibility inequalities verify survival, hazard, overlap, density, envelope, exact-rank, spectrum, Gram, and normalization conditions.  The laws differ only in the \(n_D\) donor-post records, their target curves are separated by \(2c\|\psi_\circ\|_\infty n_D^{-1/2}\), and the same density calculation as for the target pair bounds their product Hellinger distance, hence their total variation, by a constant multiple of \(c\).

For the target channel at rank \(r\ge2\), use \(P_{r,+}\) and \(P_{r,-}\) from the corrected construction.  Centering of every \(q_k\) is preserved by Gram--Schmidt, and
\[
 N_0^{-1}\sum_mz_mz_m^\top=I_d.
\]
The joint-period factor columns are orthogonal with common squared norm \((a_{\circ,0}^2+a_{\circ,1}^2)\|\psi_\circ\|_2^2\), so the centered operator has exactly \(d\) nonzero singular values, all equal to the permitted value.  Likewise,
\[
 \int_{\mathcal I}A_{0,d}(t)A_{0,d}(t)^\top dt
 =a_{\circ,0}^2\|\psi_\circ\|_2^2I_d.
\]
The row bound gives donor incoherence.  Because \(M_\star>0\) and \(R_d>0\), eventually
\[
 h_\circ K_T^{-1/2}\le\min\{M_\star,R_d\}.
\]
The pointwise compatibility conditions then verify all survival, hazard, envelope, and support requirements.  Only the first target-loading coordinate changes, so the target separation remains \(2|a_{\circ,1}|h_\circ\|\psi_\circ\|_\infty K_T^{-1/2}\), and the total-variation bound from the rank-two calculation is unchanged by the fixed orthogonal coordinates.

It remains to convert each pair into a width bound.  Let \(Q_-\) and \(Q_+\) be either pair, let their target separation at a maximizing time be \(\Delta\), and put \(\eta=\alpha+\varepsilon_{N_0,K_T,K_D}(\alpha)\).  Honesty under the two laws and the definition of total variation imply
\[
 Q_-\{\theta_-,\theta_+\in B\}
 \ge1-2\eta-\|Q_--Q_+\|_{\mathrm{TV}}.
\]
On this event \(w(B)\ge\Delta\).  Since
\[
 1-2\eta\ge\frac32(1/2-\alpha),
\]
the explicit smallness condition for \(h_\circ\), and a sufficiently small fixed donor amplitude \(c\), make the last probability uniformly positive.  Thus the donor and target pairs yield constants times \(n_D^{-1/2}\) and \(K_T^{-1/2}\), respectively.  Taking the larger of the two lower bounds and using \(\max\{a,b\}\ge(a+b)/2\) proves the displayed converse, conditional only on acceptance of the explicit numerical premise and corrected construction.