The project studies uniformly honest continuous-time bands for the missing post-control survival trajectory of a treated unit in an exact-factor Synthetic Survival Control panel. Canonical coordinates are derived from target-pre compatibility and a positive pre-period Gram. At exact rank one, normalization removes heterogeneous target-loading uncertainty and the minimax width is exactly of order \((N_0K_D)^{-1/2}\). At every fixed rank at least two, the proved unrestricted conclusion is the bracket \(c\rho_r\le W_r^*\le C\{\rho_r+\eta_D(K_D)\}\), which matches at order \(\rho_r\) only when \(\eta_D(K_D)=O(\rho_r)\). The explicit censoring-aware functional-PCR band now uses a genuinely valid conditional multiplier quantile with a degeneracy-robust additive slack, while normalization-preserving Le Cam pairs supply the minimax converses. Without a common positive pre-period contrast-Gram lower bound, honest minimax width remains of order one even on a donor-visible known-rank-two Han--Shah Theorem~1 subclass.