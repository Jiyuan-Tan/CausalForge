# stat_reversekl_two_coverage — formalization note (bridge; rendered from core + plan)

_Auto-generated from the typed core + F1 plan. The structural source of truth is the formalization graph; this note is the human-readable / causalsmith bridge._

## Environment (S)

**S-1 (Fixed public contextual-bandit experiment E=(X,A,phi,pi_r…).** Fixed public contextual-bandit experiment E=(X,A,phi,pi_ref,eta) over finite supports carrying their standing discrete/Borel measurable structures, with a bounded linear-reward one-observation law P over S x [0,1], i.i.d. sampling, and the extended reverse-KL objective J_{eta,P} valued in R u {-infinity} — DISCRETE-SUPPORT SYNC (this round, design decision f2.5-discrete-support + added assumption finite-support-discrete-measurability): this S-block is the CARRIER of the standing measurable structure on the finite supports. The world now fixes [MeasurableSpace X] [MeasurableSingletonClass X] [MeasurableSpace A] [MeasurableSingletonClass A] as ambient instances, matching the core's def:common-experiment ('Fix the finite supports X, A, carrying their intended discrete/Borel measurable structures') and sampling_model.design ('The finite supports carry their intended discrete/Borel measurable structures'). These are instance arguments of the world, NOT a new core assumption node: P1 forbids inventing nodes, and the added assumption is classified regularity-bookkeeping. Every downstream measurability claim in this plan (the finite-dimensional selector helper at def:prediction-polytope-learner, the support-repair operator at def:minimax-risk, and the cited separable-metrizable interface at lem:measurable-maximum-theorem) is discharged against THESE instances rather than re-deriving a measurable structure per node. EXTENDED-KL SYNC (this round): J_{eta,P} is bound here and its core type is now an EXTENDED-real functional into R u {-infinity}, under the standard convention p log(p/0)=+infinity for p>0 and 0 log(0/q)=0; a policy putting positive mass off supp pi_ref at a positive-rho-mass context has objective -infinity and regret +infinity. Its restriction to reference-supported policies is finite and real valued, which is the restriction every Gibbs learner in this plan lands in. MODULE-RESOLUTION FIX (round 3): dropped the non-resolving module path `Causalean.Stat.Concentration.Hoeffding` from this S-block. Verified on disk that no `Causalean/Stat/Concentration/Hoeffding.lean` exists — the Hoeffding tail lives one level deeper at `Causalean/Stat/Concentration/TailBounds/Hoeffding.lean` (module `Causalean.Stat.Concentration.TailBounds.Hoeffding`), alongside Bernstein.lean and BinomialCount.lean. It is also unused by this S-block: the emitted S1 carrier file Basic.lean imports exactly Causalean.Stat.Sample.PiTransport and Causalean.Stat.Minimax.MinimaxRisk (S2), and every tail-bound reuse this plan actually needs is already declared at node level under the resolving `Causalean.Stat.Concentration.TailBounds.*` paths (lem:multinomial-max-count -> TailBounds.BinomialCount, lem:uniform-gibbs-radius-comparison -> TailBounds.Bernstein). Removing the stale entry restores P6 (every module path resolves) and keeps the S-block faithful to the emitted Lean. SYNC-BACK (F2): the planned module Causalean.Stat.IIDSample does not exist at the current substrate path; the IIDSample structure is exported by Causalean.Stat.Sample and the finite product-law transport is Causalean.Stat.Sample.PiTransport. Trace: surveyed Causalean.PO (POManskiIVSystem/Backdoor/ATE/ATT), SCM.Model, and Stat for a contextual-bandit reverse-KL DGP world -> not found; the PO/SCM worlds are causal-identification abstractions at a different level than a finite decision-theoretic offline bandit. Reuse the finite i.i.d. sampling scaffold but build the Gibbs policy pi_P^star, KL-regularized welfare J_{eta,P}, and feature covariances Lambda over Mathlib. STANDARD-BOREL SYNC (round 5, required by the GENERAL-CONDITIONAL re-plan of lem:bounded-projection-concentration): this world also records that the sample space Omega = (S x [0,1])^n is NONEMPTY and STANDARD BOREL — a finite product of a finite discrete space with the unit interval — which is exactly the ambient hypothesis Mathlib's regular conditional distribution `ProbabilityTheory.condExpKernel mu m` and the conditional sub-Gaussian layer `ProbabilityTheory.HasCondSubgaussianMGF` require. This is a property of the world already fixed by the sampling model (it is derived, not assumed: it needs no new core node and no new added assumption), and it is what lets the conditional concentration lemma be proved for an ARBITRARY conditioning sub-sigma-field rather than only for the finite-cell partition.
**required modules.** Causalean.Stat.Sample.PiTransport

**S-2 (Minimax decision-theoretic frontier).** Minimax decision-theoretic frontier: prediction polytope, confidence set, deployed Gibbs learners, minimax regularized regret and sample complexity over the exact (C,D) shell — DECISION-CLASS SYNC (this round — SUPERSEDES the prior 'full simplex, no PolicySupportedOn conjunct' encoding, which now contradicts the core). The core's symbol L_n(E) declares its codomain as {pi in Delta(A)^X : supp pi(.|x) subseteq supp pi_ref(.|x) for every x}, and def:common-experiment states a learner 'is supported on pi_ref at every context'. So L_n(E) IS the reference-SUPPORTED finite-objective class: encode it with the PolicySupportedOn conjunct. This is law-independent (the condition depends only on the PUBLIC pi_ref), and it is WITHOUT LOSS, not a narrowing: def:minimax-risk supplies the public contextwise support repair S_ref, which is Borel measurable BECAUSE the supports carry the standing discrete/Borel structures of design decision f2.5-discrete-support, maps every measurable full-simplex learner into L_n(E), and never increases regret (off-support regret is +infinity under the extended convention, repaired regret is finite). Supported learners are themselves full-simplex learners, so the two infima are EQUAL and the common value is a nonnegative real. The plan therefore encodes the supported class as the definition and the full-simplex equality as a proved companion, exactly as the core states. J_underscore_n is likewise DIAGNOSTIC ONLY and its domain is exactly the reference-supported policies (core: 'no real-valued off-support evaluation is intended'); it does not define pi-hat. REDIRECT SYNC (F2 attempt 7): current covering-number module path is Causalean.Stat.Concentration.Covering.CoveringNumber. Trace: surveyed Causalean.Stat.Minimax.MinimaxRisk (real_two_point_lower_bound, iid_two_point_lower_bound, minimaxMiss packaging) and Estimation.MinimaxATE.Model (minimaxMiss/nMiss) -> those package a minimax MISS PROBABILITY of a scalar functional, not an infimum-over-learners of expected reverse-KL regularized REGRET; abstraction mismatch, so the decision class L_n(E), risk R_{n,E}, and sample complexity N* are define-local over Mathlib.
**required modules.** Causalean.Stat.Minimax.MinimaxRisk, Causalean.Stat.Concentration.Covering.CoveringNumber

**S-3 (Paired-Bernoulli exact-shell hard-family lower-bound cons…).** Paired-Bernoulli exact-shell hard-family lower-bound construction (orthonormal hard coordinates, Gibbs calibration, sign-cube packing) — Trace: the sign-cube packing / pairwise-neighbour testing argument reuses Causalean.Stat.Minimax.Assouad (hammingRisk_pair_ge, sum_decode_ge, assouad_average), Pinsker (pinskerBound_of_ac_of_ne_top, klForm_two_point_lower_bound), TotalVariation (one_sub_tvDist_le_test), and Mixture. The explicit hard-family construction (orthonormal e_0,u_j,w_j basis, Gibbs calibration T_gamma/beta, Bhattacharyya-affinity two-point bound) is novel and define-local over these reusable testing primitives.
**required modules.** Causalean.Stat.Minimax.Assouad, Causalean.Stat.Minimax.Pinsker, Causalean.Stat.Minimax.Mixture

## Assumptions (A)

**A-1 (assumption).** \(\mathcal X\) is finite.

**A-2 (assumption).** \(\mathcal A\) is finite.

**A-3 (assumption).** \(\sup_{(x,a)\in S}\|\phi(x,a)\|_2\le1\).

**A-4 (assumption).** \(\mathbb E_P[Y\mid X=x,A=a]=r_\theta(x,a)\) for every \((x,a)\in S\).

**A-5 (assumption).** \(P(A=a\mid X=x)=\pi_{\mathrm{ref}}(a\mid x)\) for every \((x,a)\in S\).

**A-6 (assumption).** \(\Lambda_{\mathrm{ref}}\succ0\).

**A-7 (assumption).** \(C_P=C\).

**A-8 (assumption).** \(D_P=D\).

## Definitions (P)

**P-1 (Fix the finite supports \(\mathcal X,\mathcal A\), carryi…).** Fix the finite supports \(\mathcal X,\mathcal A\), carrying their intended discrete/Borel measurable structures, the known feature map \(\phi\), the known reference and logging policy \(\pi_{\mathrm{ref}}\), and \(\eta\) as \(\mathfrak E=(\mathcal X,\mathcal A,\phi,\pi_{\mathrm{ref}},\eta)\). The context distribution \(\rho\) is not a component of \(\mathfrak E\); it remains law-specific. For every compatible one-observation law \(P\), \(\mathcal D_n\sim P^{\otimes n}\). A learner is an element of \(\mathcal L_n(\mathfrak E)\), so it receives \(\mathcal D_n\) and \(\mathfrak E\), returns a policy, and is supported on \(\pi_{\mathrm{ref}}\) at every context. Because \(\pi_{\mathrm{ref}}\) is public, this support requirement is law-independent and uses only public design inputs. By \(\text{def:minimax-risk}\), it is without loss relative to the class of all measurable full-simplex policy learners under the extended reverse-KL convention. Write \(\mathcal M_{\mathfrak E}(d,C,D,\eta)\), \(\mathfrak R_{n,\mathfrak E}\), and \(N^\star_{\mathfrak E}\) whenever public experiments are compared. The unindexed notation \(\mathcal M(d,C,D,\eta)\), \(\mathfrak R_n\), and \(N^\star\) is an abbreviation only after one \(\mathfrak E\) has been fixed and no cross-experiment comparison is being made.

**P-2 (\(\mathcal M_{\mathfrak E}(d,C,D,\eta)=\{P:\ P\text{ is c…).** \(\mathcal M_{\mathfrak E}(d,C,D,\eta)=\{P:\ P\text{ is compatible with the fixed public experiment }\mathfrak E,\ \rho\text{ is the context distribution supplied by }P,\text{ and ass:finite-contexts, ass:finite-actions, ass:bounded-features, ass:linear-realizability, ass:reference-logging, ass:nonsingular-logging-geometry, ass:pointwise-exact-shell, and ass:feature-exact-shell hold for }P\}\). When \(\mathfrak E\) is fixed and no experiments are compared, write \(\mathcal M(d,C,D,\eta)=\mathcal M_{\mathfrak E}(d,C,D,\eta)\).

**P-3 (Let \(S=\mathcal X\times\mathcal A\) and \(\mathcal F=\{f…).** Let \(S=\mathcal X\times\mathcal A\) and \(\mathcal F=\{f\in[0,1]^S:\exists\theta\in\mathbb R^d,\ f(x,a)=\phi(x,a)^\top\theta\}\). Fix once and for all a lexicographic order of \(S\). Set \(L_n(f)=n^{-1}\sum_{i=1}^n(Y_i-f(X_i,A_i))^2\), and let \(\widehat f\) be the sequentially lexicographically selected minimizer of \(L_n\) over the compact polytope \(\mathcal F\). Define \(\|h\|_n^2=n^{-1}\sum_{i=1}^nh(X_i,A_i)^2\) and \(\mathcal Q_n=\{f\in\mathcal F:\|f-\widehat f\|_n^2\le c_0[d\log(en)+\log(2n^2)]/n\}\). The set \(\mathcal Q_n\) is nonempty and compact because it contains \(\widehat f\). For every \((x,a)\in S\), define the unique minimum value \(\underline r_n(x,a)=\min_{f\in\mathcal Q_n}f(x,a)\); if an attaining witness is needed, select one sequentially lexicographically using the fixed order of \(S\). Define the deployed policy at every context, observed or unobserved, by \[ \widehat\pi(a\mid x)=\frac{\pi_{\mathrm{ref}}(a\mid x)e^{\eta\underline r_n(x,a)}}{\sum_{b\in\mathcal A}\pi_{\mathrm{ref}}(b\mid x)e^{\eta\underline r_n(x,b)}}. \] This Gibbs policy satisfies \(\operatorname{supp}\widehat\pi(\cdot\mid x)=\operatorname{supp}\pi_{\mathrm{ref}}(\cdot\mid x)\) for every context and therefore belongs to the finite-objective decision class. For reference-supported policies \(\pi\), retain \(\underline J_n(\pi)=\inf_{f\in\mathcal Q_n}[n^{-1}\sum_i\sum_a\pi(a\mid X_i)f(X_i,a)-(\eta n)^{-1}\sum_i\operatorname{KL}(\pi(\cdot\mid X_i)\Vert\pi_{\mathrm{ref}}(\cdot\mid X_i))]\) as the empirical pessimistic criterion; no real-valued off-support evaluation is intended. In any feature coordinates, \(\|f_\theta-f_{\widehat\theta}\|_n^2=(\theta-\widehat\theta)^\top\widehat\Sigma_n(\theta-\widehat\theta)\), where \(\widehat\Sigma_n=n^{-1}\sum_i\phi(X_i,A_i)\phi(X_i,A_i)^\top\); this equality is notation only and no coefficient representative is selected.

**P-4 (For a fixed public experiment \(\mathfrak E\) and every n…).** For a fixed public experiment \(\mathfrak E\) and every nonempty class \(\mathcal M\) of laws compatible with \(\mathfrak E\), use the standard extended reverse-KL convention in \(J_{\eta,P}\). Thus a policy that assigns positive mass outside \(\operatorname{supp}\pi_{\mathrm{ref}}(\cdot\mid x)\) at any context with positive \(\rho\)-mass has objective \(-\infty\) and regret \(+\infty\). For an arbitrary policy \(\pi\), define the public contextwise support repair \[ (\mathscr S_{\mathrm{ref}}\pi)(a\mid x)= \begin{cases} \pi(a\mid x),&\pi(b\mid x)=0\text{ for every }b\text{ such that }\pi_{\mathrm{ref}}(b\mid x)=0,\\ \pi_{\mathrm{ref}}(a\mid x),&\text{otherwise.} \end{cases} \] Because the supports have the standing finite discrete/Borel measurable structures, \(\mathscr S_{\mathrm{ref}}\) is Borel measurable and sends every measurable policy learner to an element of \(\mathcal L_n(\mathfrak E)\). For every law and every realized learner output, its repair leaves the objective unchanged when every offending context has zero \(\rho\)-mass, while otherwise the unrepaired regret is \(+\infty\) and the repaired regret is finite. Hence repair never increases regret. Since supported learners are themselves full-simplex learners, the two infima are equal, and their common value is \[ \mathfrak R_{n,\mathfrak E}(\mathcal M)=\inf_{\widetilde\pi\in\mathcal L_n(\mathfrak E)}\sup_{P\in\mathcal M}\mathbb E_{P^{\otimes n}}\left[J_{\eta,P}(\pi_P^\star)-J_{\eta,P}(\widetilde\pi(\mathcal D_n;\mathfrak E))\right]. \] Equivalently, the right-hand side equals the same infimum taken over all measurable full-simplex policy-valued learners with the displayed loss interpreted in \([0,+\infty]\). The Gibbs optimum has exactly the support of \(\pi_{\mathrm{ref}}\), and the public reference learner has finite regret, so the common minimax value is a nonnegative real number. When \(\mathfrak E\) is fixed and no experiments are compared, write \(\mathfrak R_n=\mathfrak R_{n,\mathfrak E}\).

**P-5 (For \(\mathcal M_{\mathfrak E}(d,C,D,\eta)\ne\varnothing\…).** For \(\mathcal M_{\mathfrak E}(d,C,D,\eta)\ne\varnothing\), define \[ N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)=\inf\left\{n\in\mathbb N:\mathfrak R_{n,\mathfrak E}\bigl(\mathcal M_{\mathfrak E}(d,C,D,\eta)\bigr)\le\epsilon\right\}, \] with the convention \(\inf\varnothing=\infty\). When \(\mathfrak E\) is fixed and no experiments are compared, write \(N^\star=N^\star_{\mathfrak E}\).

**P-6 (\(\mathfrak F_{\mathrm{hard}}^\circ(d,\eta)=\{(C,D):1<D\l…).** \(\mathfrak F_{\mathrm{hard}}^\circ(d,\eta)=\{(C,D):1<D\le C<e^\eta,\ 2D-1<e^\eta,\ d\ge4\}\)

**P-7 (\(\epsilon_0(C,D,\eta)=[64\eta]^{-1}\log\cosh(\eta\min\{g…).** \(\epsilon_0(C,D,\eta)=[64\eta]^{-1}\log\cosh(\eta\min\{g_0(D,\eta),1/4\})\), indexed by \(0<\epsilon\le\epsilon_0(C,D,\eta)\).

**P-8 (Handle).** Handle: derive the joint Gibbs feasibility constraints for \(C_P\) and \(D_P\), then construct covariance-preserving lower families or prove their nonexistence.

**P-9 (For \((C,D)\in\mathfrak F_{\mathrm{hard}}^\circ(d,\eta)\)…).** For \((C,D)\in\mathfrak F_{\mathrm{hard}}^\circ(d,\eta)\), put \(k=\lfloor(d-1)/2\rfloor\), take an orthonormal basis \(e_0,(u_j,w_j)_{j=1}^k\), and add \(z\) when \(d\) is even. Use actions \(\{+,-,0\}\). At \(x_C\), set \(q=(e^\eta/C-1)/(e^\eta-1)\), \(\pi_{\mathrm{ref}}(+\mid x_C)=q\), distribute \(1-q\) positively over \(-,0\), and set \(\phi(x_C,+)=e_0\), \(\phi(x_C,-)=\phi(x_C,0)=0\), and \(\theta_0=1\). Put \(A=q(C-D)/(D-1)\), add \(x_A\) when \(C>D\) with every action feature equal to \(e_0\), give \(x_C\) raw mass \(\tau=[4(1+A)]^{-1}\) and \(x_A\) raw mass \(\tau A\), and give the hard contexts total raw mass one. When \(z\) is present, add \(x_z\) with raw mass \(1/4\), every action feature \(z\), and coefficient zero. Normalize these raw masses to define \(\rho\). At each \(x_j\), of raw mass \(1/k\), put \(p=(4D)^{-1}\), \(\pi_{\mathrm{ref}}(+\mid x_j)=\pi_{\mathrm{ref}}(-\mid x_j)=p\), \(\pi_{\mathrm{ref}}(0\mid x_j)=1-2p\), \(\phi(x_j,+)=(u_j+w_j)/\sqrt2\), \(\phi(x_j,-)=(u_j-w_j)/\sqrt2\), and \(\phi(x_j,0)=0\). For \(v\in\{-1,1\}^k\), let \(T_\gamma=D(1-2p)/[e^{\eta\gamma}-2pD\cosh(\eta\gamma)]\), \(\beta=\eta^{-1}\log T_\gamma\), and \(\theta_{u_j}=\sqrt2\beta\), \(\theta_{w_j}=\sqrt2\gamma v_j\), so \(r_v(x_j,\pm)=\beta\pm\gamma v_j\). Define \[ g_0(D,\eta)=\sup\left\{g>0:\text{ for every }s\in[0,g],\ e^{\eta s}-2pD\cosh(\eta s)>0\text{ and }s\le\eta^{-1}\log T_s\le1-s\right\}, \] and set \(\gamma(\epsilon,d,C,D,\eta)=\eta^{-1}\operatorname{arcosh}(e^{64\eta\epsilon})\). The family \(\mathcal H_{d,C,D,\eta,\epsilon}\) comprises these \(2^k\) Bernoulli laws whenever \(\gamma\le\min\{g_0(D,\eta),1/4,\beta,1-\beta\}\).

**P-10 (Using the sequentially lexicographically selected empiric…).** Using the sequentially lexicographically selected empirical-risk minimizer \(\widehat f\in\mathcal F\) from \(\text{def:prediction-polytope-learner}\), define, for every \(x\in\mathcal X\) and \(a\in\mathcal A\), \[ \widehat\pi_{\mathrm{ERM}}(a\mid x) =\frac{\pi_{\mathrm{ref}}(a\mid x)e^{\eta\widehat f(x,a)}}{\sum_{b\in\mathcal A}\pi_{\mathrm{ref}}(b\mid x)e^{\eta\widehat f(x,b)}}. \] The learner receives only \((\mathcal D_n,\mathfrak E)\); it does not receive the shell indices \(C\) or \(D\).

## Lemmas (L)

**L-1 (Under every invertible reparameterization \(\phi\mapsto T…).** Under every invertible reparameterization \(\phi\mapsto T\phi\) and \(\theta\mapsto T^{-\top}\theta\), the prediction polytope \(\mathcal F\), the selected function \(\widehat f\), the confidence set \(\mathcal Q_n\), the criterion \(\underline J_n\), and the selected policy \(\widehat\pi\) are unchanged.

**L-2 (Let \(\varepsilon_1,\ldots,\varepsilon_n\) be conditional…).** Let \(\varepsilon_1,\ldots,\varepsilon_n\) be conditionally independent given a sigma-field \(\mathcal G\), with \(\mathbb E[\varepsilon_i\mid\mathcal G]=0\) and \(|\varepsilon_i|\le1\). If \(\Pi\) is a \(\mathcal G\)-measurable orthogonal projector of rank at most \(d\), then, conditionally with probability at least \(1-\zeta\), \[ \|\Pi\varepsilon\|_2^2 \le8\left\{d\log5+\log\frac2\zeta\right\}. \]

**L-3 (Let \(N_1,\ldots,N_d\) be the cell counts from \(d^2\) in…).** Let \(N_1,\ldots,N_d\) be the cell counts from \(d^2\) independent draws from the uniform distribution on \(\{1,\ldots,d\}\). Then \[ \Pr\left\{\max_{1\le j\le d}N_j>3d\right\} \le d\exp\{-d(3\log3-2)\}=o(1). \]

**L-4 (For every \(P\in\mathcal M(d,C,D,\eta)\) and every \(h\in…).** For every \(P\in\mathcal M(d,C,D,\eta)\) and every \(h\in\operatorname{span}(\phi)\), \(\mathbb E_{\rho,\pi_P^\star}h(X,A)^2\le D_P\mathbb E_{\rho,\pi_{\mathrm{ref}}}h(X,A)^2\).

**L-5 (Fix \(P\in\mathcal M(d,C,D,\eta)\)).** Fix \(P\in\mathcal M(d,C,D,\eta)\). If \(M\succeq0\) and a function \(q:S\to\mathbb R\) satisfies \(q(x,a)^2\le\phi(x,a)^\top M\phi(x,a)\) on \(S\), then \[ \mathbb E_{\rho,\pi_P^\star}q^2 \le\operatorname{tr}(M\Lambda_P^\star) \le D_P\operatorname{tr}(M\Lambda_{\mathrm{ref}}) =D_P\mathbb E_{\rho,\pi_{\mathrm{ref}}}\phi^\top M\phi. \]

**L-6 (For every \(P\in\mathcal M(d,C,D,\eta)\) and every policy…).** For every \(P\in\mathcal M(d,C,D,\eta)\) and every policy \(\pi\) supported on \(\pi_{\mathrm{ref}}\), \(J_{\eta,P}(\pi_P^\star)-J_{\eta,P}(\pi)=\eta^{-1}\mathbb E_\rho\operatorname{KL}(\pi(\cdot\mid X)\Vert\pi_P^\star(\cdot\mid X))\).

**L-7 (Fix \(c_0=64\)).** Fix \(c_0=64\). For every \(P\in\mathcal M(d,C,D,\eta)\) and every \(n\in\mathbb N\), with \(\delta_n=n^{-2}\), there is an event \(\mathcal E_n\) satisfying \(P^{\otimes n}(\mathcal E_n)\ge1-\delta_n/2\) such that \[ r_\theta\in\mathcal Q_n \quad\text{and}\quad \sup_{f\in\mathcal Q_n}\mathbb E_{X\sim\rho,A\sim\pi_{\mathrm{ref}}(\cdot\mid X)}[(f(X,A)-r_\theta(X,A))^2] \le600\frac{d\log(en)+\log(2n^2)}{n}. \] More generally, for any fixed universal \(c_0\ge64\), the constant \(600\) may be replaced by \(4c_0+291\).

**L-8 (Let \(r,g:S\to[0,1]\), put \(\Delta=g-r\), and let \(\pi_…).** Let \(r,g:S\to[0,1]\), put \(\Delta=g-r\), and let \(\pi_r\) and \(\pi_g\) be their Gibbs policies relative to \(\pi_{\mathrm{ref}}\). Write \[ J_{\eta,r}(\pi)=\mathbb E_{\rho,\pi}r(X,A)-\eta^{-1}\mathbb E_\rho\operatorname{KL}(\pi(\cdot\mid X)\Vert\pi_{\mathrm{ref}}(\cdot\mid X)), \] and define \[ \kappa_\eta=\eta\int_0^1t e^{2\eta t}\,dt =\frac{e^{2\eta}(2\eta-1)+1}{4\eta}. \] Then \[ J_{\eta,r}(\pi_r)-J_{\eta,r}(\pi_g) \le\min\left\{ \kappa_\eta\mathbb E_{\rho,\pi_r}[\Delta^2], (1+e^{2\eta})\sqrt{\mathbb E_{\rho,\pi_r}[\Delta^2]} \right\}. \]

**L-9 (Fix \(d\ge4\), \(\eta>0\), and any finite public experime…).** Fix \(d\ge4\), \(\eta>0\), and any finite public experiment \(\mathfrak E\) for which \(\mathcal M_{\mathfrak E}(d,C,D,\eta)\ne\varnothing\). Define \[ b_\eta=\frac12\min\{2,\eta/2\},\qquad Q_{d,D,\eta}=\max\{600\kappa_\eta D(d+2),b_\eta,1\}. \] Then, for every \(0<\epsilon<1\), \[ N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta) \le \left\lceil\frac{8Q_{d,D,\eta}}{\epsilon} \log\!\left(\frac{8eQ_{d,D,\eta}}{\epsilon}\right)\right\rceil . \] Consequently, \[ \lim_{\epsilon\downarrow0} \frac{\epsilon^2}{d}N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)=0. \] Thus no specified finite public experiment with a nonempty exact shell admits constants \(c(C,D,\eta)>0\) and \(\epsilon_0(C,D,\eta)>0\) for which \(N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)\ge c(C,D,\eta)d/\epsilon^2\) for every \(0<\epsilon\le\epsilon_0(C,D,\eta)\). This is an asymptotic conclusion and does not exclude slower preasymptotic behavior on a restricted accuracy interval.

**L-10 (Fix the public experiment \(\mathfrak E\), and write \(\m…).** Fix the public experiment \(\mathfrak E\), and write \(\mathcal A_x^+=\{a\in\mathcal A:\pi_{\mathrm{ref}}(a\mid x)>0\}\). For \(\vartheta\in\mathbb R^d\), define \[ Z_\vartheta(x)=\sum_{a\in\mathcal A}\pi_{\mathrm{ref}}(a\mid x)e^{\eta\phi(x,a)^\top\vartheta},\qquad w_\vartheta(x,a)=\frac{e^{\eta\phi(x,a)^\top\vartheta}}{Z_\vartheta(x)}, \] \[ B_x=\sum_{a\in\mathcal A}\pi_{\mathrm{ref}}(a\mid x)\phi(x,a)\phi(x,a)^\top, \qquad G_x(\vartheta)=\sum_{a\in\mathcal A}\pi_{\mathrm{ref}}(a\mid x)w_\vartheta(x,a)\phi(x,a)\phi(x,a)^\top. \] Then \(\mathcal M(d,C,D,\eta)\ne\varnothing\) if and only if the public feature map has Euclidean norm at most one at every cell and at least one of the following finitely many feasibility problems has a solution. Enumerate a nonempty set \(I\subseteq\mathcal X\) and a cell \((x_0,a_0)\) with \(x_0\in I\) and \(a_0\in\mathcal A_{x_0}^+\). The variables are \(\vartheta\in\mathbb R^d\), \((\rho_x)_{x\in I}\), and \(u\in\mathbb R^d\), and the constraints are \[ 0\le\phi(x,a)^\top\vartheta\le1\quad((x,a)\in S),\qquad \rho_x>0\quad(x\in I),\qquad \sum_{x\in I}\rho_x=1, \] \[ B(\rho):=\sum_{x\in I}\rho_xB_x\succ0, \] \[ e^{\eta\phi(x,a)^\top\vartheta}\le C Z_\vartheta(x)\quad(x\in I\text{ and }a\in\mathcal A_x^+), \qquad e^{\eta\phi(x_0,a_0)^\top\vartheta}=C Z_\vartheta(x_0), \] \[ G(\vartheta,\rho):=\sum_{x\in I}\rho_xG_x(\vartheta),\qquad DB(\rho)-G(\vartheta,\rho)\succeq0, \] \[ \{DB(\rho)-G(\vartheta,\rho)\}u=0,\qquad u^\top u=1. \] There are at most \((2^{|\mathcal X|}-1)|S|\) branches, and a branch has \(2d+|I|\) real variables and only displayed exponential, linear, quadratic, and semidefinite constraints.

**L-11 (Let \(r,g:S\to[0,1]\), put \(\Delta=g-r\), and let \(\pi_…).** Let \(r,g:S\to[0,1]\), put \(\Delta=g-r\), and let \(\pi_r\) and \(\pi_g\) be their Gibbs policies relative to \(\pi_{\mathrm{ref}}\). Define \[ A=\mathbb E_{\rho,\pi_r}\Delta^2, \qquad B=\mathbb E_{\rho,\pi_g}\Delta^2. \] Then \[ J_{\eta,r}(\pi_r)-J_{\eta,r}(\pi_g) \le\min\left\{\eta(A+B),\sqrt A+\sqrt B\right\}. \]

**L-12 (For \(g,f\in\mathcal F\), let \[ H_{g,f}(x)=\sum_{a\in\ma…).** For \(g,f\in\mathcal F\), let \[ H_{g,f}(x)=\sum_{a\in\mathcal A}\pi_g(a\mid x)\{g(x,a)-f(x,a)\}^2, \] where \(\pi_g\) is the Gibbs policy generated by \(g\). Let \(X_1,\ldots,X_n\) be independent with law \(\rho\), write \(P_\rho h=\mathbb E_\rho h(X)\) and \(P_nh=n^{-1}\sum_{i=1}^nh(X_i)\), and fix \(0<\zeta<1\). With probability at least \(1-\zeta\), simultaneously for every \(g,f\in\mathcal F\), \[ P_\rho H_{g,f}\le2P_nH_{g,f}+b_n(\zeta), \qquad P_nH_{g,f}\le2P_\rho H_{g,f}+b_n(\zeta), \] where \[ b_n(\zeta)=\frac{8}{3n}\left[\log\frac2\zeta+2d\log\{1+4n(\eta+2)\}\right]+\frac3n. \]

**L-13 (Let \(r,g:S\to[0,1]\), put \(\Delta=g-r\), and suppose \(…).** Let \(r,g:S\to[0,1]\), put \(\Delta=g-r\), and suppose \(\Delta\le0\) pointwise. Write \[ J_{\eta,r}(\pi)=\mathbb E_{\rho,\pi}r(X,A)-\eta^{-1}\mathbb E_\rho\operatorname{KL}(\pi(\cdot\mid X)\Vert\pi_{\mathrm{ref}}(\cdot\mid X)). \] For \(\gamma\in[0,1]\), let \(\pi_\gamma(\cdot\mid x)\) be the Gibbs policy generated by \(r+\gamma\Delta\), so \(\pi_0=\pi_r\) and \(\pi_1=\pi_g\). Then \[ J_{\eta,r}(\pi_r)-J_{\eta,r}(\pi_g) \le\min\left\{\mathbb E_{\rho,\pi_r}[-\Delta],\frac\eta2\mathbb E_{\rho,\pi_r}\Delta^2\right\}. \]

## Theorems (T)

### T-block: t1 — If the shell \(\mathcal M(d,1,1,\eta)\) is nonempty, then…
**Statement.** If the shell \(\mathcal M(d,1,1,\eta)\) is nonempty, then every \(P\) in it has \(\pi_P^\star=\pi_{\mathrm{ref}}\) \(\rho\)-almost surely, \(D_P=1\), and \(J_{\eta,P}(\pi_P^\star)-J_{\eta,P}(\pi_{\mathrm{ref}})=0\). Hence \(N^\star(\epsilon;d,1,1,\eta)=1\) for every \(\epsilon>0\).

### T-block: t2 — There are universal constants such that, after choosing \…
**Statement.** There are universal constants such that, after choosing \(c_0\) sufficiently large and whenever \(\mathcal M(d,C,D,\eta)\ne\varnothing\), the lower-envelope Gibbs learner \(\widehat\pi\in\mathcal L_n(\mathfrak E)\) uses neither \(C\) nor \(D\) as input and satisfies \[ \sup_{P\in\mathcal M(d,C,D,\eta)}\mathbb E_{P^{\otimes n}}[J_{\eta,P}(\pi_P^\star)-J_{\eta,P}(\widehat\pi)] \le\widetilde O\left(\min\left\{\frac{\eta d^2D}{n},\sqrt{\frac{d^2D}{n}}\right\}\right)+\delta_n. \] The proof derives a distribution-free \(d/n\) logging prediction radius for every member of \(\mathcal Q_n\), a pointwise leverage majorant for the nonlinear lower envelope, the trace inequality \(D_{2,P}\le dD_P\), the Gibbs regret identity, and the endpoint-monotonicity fast and slow conversions. Consequently \[ N^\star(\epsilon;d,C,D,\eta) \le\widetilde O\left(d^2D\min\{\eta/\epsilon,\epsilon^{-2}\}\right) \] when the right-hand side is at least two. The additional factor \(d\) is unavoidable for the empirical-ball lower-envelope learner in general: \(\text{prop:empirical-ball-dimension-witness}\) gives exact shells on which the original \(dD\) upper rate fails.

### T-block: t3 — For every \((C,D)\in\mathfrak F_{\mathrm{hard}}^\circ(d,\…
**Statement.** For every \((C,D)\in\mathfrak F_{\mathrm{hard}}^\circ(d,\eta)\), the construction in \(\text{def:explicit-hard-family}\) gives a nonempty shell \(\mathcal M(d,C,D,\eta)\), has \(g_0(D,\eta)>0\), and has a positive admissible accuracy interval on which \(\mathcal H_{d,C,D,\eta,\epsilon}\subset\mathcal M(d,C,D,\eta)\). Its members satisfy exactly \(C_P=C\), \(D_P=D\), and \(\Lambda_{\mathrm{ref}}\succ0\). There are constants \(\epsilon_1(C,D,\eta)\in(0,\epsilon_0(C,D,\eta)]\) and \(c(C,D,\eta)>0\) such that, for every \(0<\epsilon\le\epsilon_1(C,D,\eta)\), \(N^\star(\epsilon;d,C,D,\eta)\ge c(C,D,\eta)dD\eta/\epsilon\). Thus the displayed paired-Bernoulli family proves the local fast branch; it does not prove the \(\epsilon^{-2}\) branch.

### T-block: t4 — Under the canonical tabular specialization \(d=|S|\) and…
**Statement.** Under the canonical tabular specialization \(d=|S|\) and \(\phi(x,a)=e_{(x,a)}\), \(\Lambda_{\mathrm{ref}}^{-1/2}\Lambda_P^\star\Lambda_{\mathrm{ref}}^{-1/2}\) is diagonal with entries \(\pi_P^\star(a\mid x)/\pi_{\mathrm{ref}}(a\mid x)\). Hence \(D_P=C_P\), and the exact shell \(\mathcal M(d,C,C,\eta)\) reduces to the canonical tabular reverse-KL shell.

### T-block: t5 — For \(d=5\), every \((C,D)\in\mathfrak F_{\mathrm{hard}}^…
**Statement.** For \(d=5\), every \((C,D)\in\mathfrak F_{\mathrm{hard}}^\circ(5,\eta)\), and every admissible \(\epsilon\), each law in \(\mathcal H_{5,C,D,\eta,\epsilon}\) is a non-tabular shared-feature member: at every hard context \(x_j\), the two action features \(\phi(x_j,+)=(u_j+w_j)/\sqrt2\) and \(\phi(x_j,-)=(u_j-w_j)/\sqrt2\) share both latent directions and are not canonical coordinate indicators.

### T-block: t6 — For the lower-envelope Gibbs completion of the empirical…
**Statement.** For the lower-envelope Gibbs completion of the empirical ball in \(\text{def:prediction-polytope-learner}\), the claimed \(\widetilde O(dD/n)\) fast rate is false. Specifically, fix \(\eta_0=4\log3\), \(C_0=3/2\), and \(D_0=13/10\). There is a sequence of public experiments and deterministic-reward laws \(P_d\in\mathcal M(d,C_0,D_0,\eta_0)\) such that, for \(n_d=\lceil d^2\rceil\), \[ \mathbb E_{P_d^{\otimes n_d}}[J_{\eta_0,P_d}(\pi_{P_d}^\star)-J_{\eta_0,P_d}(\widehat\pi)] \ge \frac{\log(4/3)}{8\log3}\{1-o(1)\}. \] In contrast, both \(dD_0/n_d\) and \(\sqrt{dD_0/n_d}\) tend to zero up to polylogarithmic factors.

### T-block: t7 — For every nonempty exact shell, \(1\le D\le C\le e^\eta\)
**Statement.** For every nonempty exact shell, \(1\le D\le C\le e^\eta\); because the supports are finite, either \((C,D)=(1,1)\) or \(1<D\le C<e^\eta\). Conversely, for every \(d\ge4\) and every pair \(1<D\le C<e^\eta\), there is a finite public experiment with bounded features and nonsingular logging covariance for which \(\mathcal M(d,C,D,\eta)\ne\varnothing\); the pair \((1,1)\) is also realizable. Thus the feasible index region when the public experiment may vary is exactly \(\{(1,1)\}\cup\{(C,D):1<D\le C<e^\eta\}\). Feasibility for a fixed public experiment can be strictly smaller: canonical tabular features force \(D=C\), whereas shared features realize off-diagonal pairs \(D<C\). This theorem answers the feasibility part of the question; it does not assert a minimax lower frontier outside the proved local hard regime.

### T-block: t8 — For every \(d\ge4\), \(\eta>0\), and \(1<D\le C<e^\eta\),…
**Statement.** For every \(d\ge4\), \(\eta>0\), and \(1<D\le C<e^\eta\), there is a fixed finite public experiment \(\mathfrak E_1\) with bounded features and a nonempty exact shell \(\mathcal M_{\mathfrak E_1}(d,C,D,\eta)\), and there are constants \(c(C,D,\eta)>0\) and \(\epsilon_0(C,D,\eta)>0\) such that \[ N^\star_{\mathfrak E_1}(\epsilon;d,C,D,\eta)\ge c(C,D,\eta)\frac{dD\eta}{\epsilon} \qquad(0<\epsilon\le\epsilon_0(C,D,\eta)). \] No condition \(2D-1<e^\eta\) is required. The constants may depend on \((C,D,\eta)\); hence this is a pointwise local \(d/\epsilon\) lower bound with displayed factor \(D\eta\), not a uniform assertion about coverage or temperature dependence.

### T-block: t9 — Fix \(c_0=64\), let \(a_n=d\log(en)+\log(2n^2)\), and sup…
**Statement.** Fix \(c_0=64\), let \(a_n=d\log(en)+\log(2n^2)\), and suppose \(\mathcal M_{\mathfrak E}(d,C,D,\eta)\ne\varnothing\). Let \(\widehat\pi_{\mathrm{ERM}}\) be the Gibbs policy generated by \(\widehat f\) as in \(\text{def:erm-gibbs-plugin-learner}\). This learner belongs to \(\mathcal L_n(\mathfrak E)\), uses neither \(C\) nor \(D\) as input, and satisfies \[ \sup_{P\in\mathcal M_{\mathfrak E}(d,C,D,\eta)} \mathbb E_{P^{\otimes n}}[J_{\eta,P}(\pi_P^\star)-J_{\eta,P}(\widehat\pi_{\mathrm{ERM}})] \le \min\left\{ 600\kappa_\eta\frac{Da_n}{n}, \sqrt{600}(1+e^{2\eta})\sqrt{\frac{Da_n}{n}} \right\} +\frac{\delta_n}{2}\min\{2,\eta/2\}. \] Consequently, whenever the displayed sample-size order is at least two, \[ N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta) \le\widetilde O\left( dD\min\left\{ \frac{\kappa_\eta}{\epsilon}, \frac{(1+e^{2\eta})^2}{\epsilon^2} \right\} \right). \] Together with \(\text{thm:localized-upper}\), this yields the complementary general envelope \[ N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta) \le\widetilde O\left( dD\min\left\{ \frac{d\eta}{\epsilon}, \frac d{\epsilon^2}, \frac{\kappa_\eta}{\epsilon}, \frac{(1+e^{2\eta})^2}{\epsilon^2} \right\} \right). \] No clean-temperature \(\widetilde O(dD\min\{\eta/\epsilon,\epsilon^{-2}\})\) conclusion is asserted.

### T-block: t10 — Fix \(d\ge4\), \(\eta>0\), \(1<D<C<e^\eta\), and any fini…
**Statement.** Fix \(d\ge4\), \(\eta>0\), \(1<D<C<e^\eta\), and any finite public experiment \(\mathfrak E\) for which \(\mathcal M_{\mathfrak E}(d,C,D,\eta)\ne\varnothing\). Define \[ b_\eta=\frac12\min\{2,\eta/2\},\qquad Q_{d,D,\eta}=\max\{600\kappa_\eta D(d+2),b_\eta,1\}. \] Then, for every \(0<\epsilon<1\), \[ N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta) \le \left\lceil\frac{8Q_{d,D,\eta}}{\epsilon} \log\!\left(\frac{8eQ_{d,D,\eta}}{\epsilon}\right)\right\rceil . \] Consequently, \[ \lim_{\epsilon\downarrow0} \frac{\epsilon^2}{d}N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)=0. \] Thus no specified finite public experiment, genuinely shared-feature or otherwise, admits constants \(c(C,D,\eta)>0\) and \(\epsilon_0(C,D,\eta)>0\) for which \(N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)\ge c(C,D,\eta)d/\epsilon^2\) for every \(0<\epsilon\le\epsilon_0(C,D,\eta)\). The proposed shared-feature slow branch is therefore false.

### T-block: t11 — For every \(d\ge4\), \(\eta>0\), and \(1<D\le C<e^\eta\),…
**Statement.** For every \(d\ge4\), \(\eta>0\), and \(1<D\le C<e^\eta\), there is a finite public experiment with bounded features and a nonempty exact shell \(\mathcal M(d,C,D,\eta)\) such that every member of the shell has the same publicly computable Gibbs optimum. For this experiment, \(\mathfrak R_n(\mathcal M(d,C,D,\eta))=0\) for every \(n\in\mathbb N\), and \(N^\star(\epsilon;d,C,D,\eta)=1\) for every \(\epsilon>0\).

### T-block: t12 — For a fixed public experiment \(\mathfrak E\), nonemptine…
**Statement.** For a fixed public experiment \(\mathfrak E\), nonemptiness of \(\mathcal M_{\mathfrak E}(d,C,D,\eta)\) is equivalent to the explicit finite exponential nonlinear-semidefinite feasibility system in \(\text{lem:fixed-experiment-shell-certificate}\). Nevertheless, the indices \((d,C,D,\eta)\) do not determine the fixed-experiment minimax frontier. For every \(d\ge4\), \(\eta>0\), and \(1<D\le C<e^\eta\), there is a finite public experiment \(\mathfrak E_0\) with a nonempty exact \((C,D)\)-shell on which \[ \mathfrak R_{n,\mathfrak E_0}\bigl(\mathcal M_{\mathfrak E_0}(d,C,D,\eta)\bigr)=0 \] for every \(n\), and \(N^\star_{\mathfrak E_0}(\epsilon;d,C,D,\eta)=1\) for every \(\epsilon>0\). There is also a finite public experiment \(\mathfrak E_1\) at the same indices and constants \(c(C,D,\eta)>0\) and \(\epsilon_0(C,D,\eta)>0\) for which \[ N^\star_{\mathfrak E_1}(\epsilon;d,C,D,\eta)\ge c(C,D,\eta)\frac{dD\eta}{\epsilon} \qquad(0<\epsilon\le\epsilon_0(C,D,\eta)). \] These conclusions include pairs satisfying \(2D-1\ge e^\eta\). Consequently, any sample-complexity lower bound that is uniform over all fixed public experiments with a nonempty shell at the given indices is at most \(1\); no nontrivial such bound can exceed the unavoidable inequality \(N^\star_{\mathfrak E}\ge1\), and no such bound can diverge as \(\epsilon\downarrow0\). In particular, no uniform matching \(\Omega(dD\epsilon^{-2})\) branch can hold as a function only of \((d,C,D,\eta)\).

### T-block: t13 — Fix \(c_0=64\)
**Statement.** Fix \(c_0=64\). For every positive integer \(n\) and \(g,f\in\mathcal F\), let \(\pi_g\) be the Gibbs policy generated by \(g\), and define \[ H_{g,f}(x)=\sum_{a\in\mathcal A}\pi_g(a\mid x)\{g(x,a)-f(x,a)\}^2, \qquad \widehat U_n(g)=\sup_{f\in\mathcal Q_n}\frac1n\sum_{i=1}^nH_{g,f}(X_i). \] Let \(\widetilde g_n\) be the sequentially lexicographically selected member of \(\operatorname*{arg\,min}_{g\in\mathcal Q_n}\widehat U_n(g)\), and let \(\widehat\pi_{\mathrm{stab}}=\pi_{\widetilde g_n}\). This is a measurable member of \(\mathcal L_n(\mathfrak E)\), uses only \((\mathcal D_n,\mathfrak E)\), and uses neither \(\rho\), \(C\), nor \(D\) as input. Put \[ a_n=d\log(en)+\log(2n^2), \] \[ b_n=\frac{8}{3n}\left[\log(4n^2)+2d\log\{1+4n(\eta+2)\}\right]+\frac3n, \qquad V_n=D\left(3000\frac{a_n}{n}+3b_n\right). \] For every nonempty exact shell and every positive integer \(n\), \[ \sup_{P\in\mathcal M_{\mathfrak E}(d,C,D,\eta)} \mathbb E_{P^{\otimes n}}\left[J_{\eta,P}(\pi_P^\star)-J_{\eta,P}(\widehat\pi_{\mathrm{stab}})\right] \le \min\left\{\eta V_n,\sqrt{2V_n}\right\} +\delta_n\min\{2,\eta/2\}. \] Moreover, for every \(\epsilon\in(0,1)\), the following explicit inversion holds. If \(\eta\le2\epsilon\), then \[ N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)=1. \] If \(\eta>2\epsilon\), define \[ q_\epsilon=dD\min\{\eta/\epsilon,\epsilon^{-2}\}, \qquad \ell_\epsilon=1+\log(eq_\epsilon)+\log\{1+(1+\eta)q_\epsilon\}, \] \[ n_0=\left\lceil10^8q_\epsilon\ell_\epsilon\right\rceil. \] Then \[ \sup_{P\in\mathcal M_{\mathfrak E}(d,C,D,\eta)} \mathbb E_{P^{\otimes n_0}}\left[J_{\eta,P}(\pi_P^\star)-J_{\eta,P}(\widehat\pi_{\mathrm{stab}})\right] \le\epsilon, \qquad N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)\le n_0. \] Consequently, with universal constants and whenever the displayed sample-size order is at least two, \[ N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta) \le\widetilde O\left(dD\min\{\eta/\epsilon,\epsilon^{-2}\}\right). \] Thus the clean feature-coverage upper bound holds at every temperature; pointwise coverage \(C\) is not needed by the learner or by this upper bound.

### T-block: t14 — Fix \(d\ge4\), \(\eta>0\), and a public experiment \(\mat…
**Statement.** Fix \(d\ge4\), \(\eta>0\), and a public experiment \(\mathfrak E=(\mathcal X,\mathcal A,\phi,\pi_{\mathrm{ref}},\eta)\), where the context law \(\rho\) remains law-specific. For every \(1<D\le C<e^\eta\), nonemptiness of \(\mathcal M_{\mathfrak E}(d,C,D,\eta)\) is equivalent to the explicit finite exponential nonlinear-semidefinite system in \(\text{lem:fixed-experiment-shell-certificate}\). Whenever that shell is nonempty, the explicit measurable idealized global-optimization learner of \(\text{thm:clean-temperature-feature-upper}\) uses only \((\mathcal D_n,\mathfrak E)\), not \(\rho\), \(C\), or \(D\). For every \(0<\epsilon<1\), the following inversion holds. If \(\eta\le2\epsilon\), then \(N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)=1\). If \(\eta>2\epsilon\), put \[ q_\epsilon=dD\min\{\eta/\epsilon,\epsilon^{-2}\}, \qquad \ell_\epsilon=1+\log(eq_\epsilon)+\log\{1+(1+\eta)q_\epsilon\}. \] Then \[ N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta) \le\left\lceil10^8q_\epsilon\ell_\epsilon\right\rceil =\widetilde O\!\left(dD\min\{\eta/\epsilon,\epsilon^{-2}\}\right). \] On a nonempty boundary shell, \(N^\star_{\mathfrak E}(\epsilon;d,1,1,\eta)=1\) for every \(\epsilon>0\). The tuple \((d,C,D,\eta)\) does not determine the fixed-design minimax frontier: for every \(1<D\le C<e^\eta\), there are two finite public experiments \(\mathfrak E_0\) and \(\mathfrak E_1\) having nonempty exact shells at those same indices such that \[ N^\star_{\mathfrak E_0}(\epsilon;d,C,D,\eta)=1\quad(\epsilon>0), \] whereas, for constants \(c(C,D,\eta)>0\) and \(\epsilon_0(C,D,\eta)>0\), \[ N^\star_{\mathfrak E_1}(\epsilon;d,C,D,\eta)\ge c(C,D,\eta)\frac{dD\eta}{\epsilon} \quad\text{for }0<\epsilon\le\epsilon_0(C,D,\eta). \] The informative construction applies without the condition \(2D-1<e^\eta\), but its constants are pointwise in \((C,D,\eta)\); the invariant lower-rate content is \(\Omega_{C,D,\eta}(d/\epsilon)\), not uniform dependence on \(D\) or \(\eta\). Consequently, uniformly over public experiments, no lower bound based only on \((d,C,D,\eta)\) can exceed one or diverge as \(\epsilon\downarrow0\). For every specified finite experiment with a nonempty exact shell and fixed \((d,C,D,\eta)\), \[ \lim_{\epsilon\downarrow0}\frac{\epsilon^2}{d}N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)=0, \] so no positive \(d/\epsilon^2\), or fixed-\(D\) \(dD/\epsilon^2\), lower branch can hold for all sufficiently small \(\epsilon\).

### T-block: t15 — Fix \(d\ge4\), \(\eta>0\), and a public experiment \(\mat…
**Statement.** Fix \(d\ge4\), \(\eta>0\), and a public experiment \(\mathfrak E\). Nonemptiness of \(\mathcal M_{\mathfrak E}(d,C,D,\eta)\) is equivalent to feasibility of one of the finitely many explicit exponential nonlinear-semidefinite systems displayed in \(\text{lem:fixed-experiment-shell-certificate}\). If the boundary shell \(\mathcal M_{\mathfrak E}(d,1,1,\eta)\) is nonempty, then \(N^\star_{\mathfrak E}(\epsilon;d,1,1,\eta)=1\) for every \(\epsilon>0\). Whenever a nontrivial fixed-design shell is nonempty, the explicit measurable idealized global-optimization learner of \(\text{thm:clean-temperature-feature-upper}\) uses only \((\mathcal D_n,\mathfrak E)\), not \(\rho\), \(C\), or \(D\). For every \(0<\epsilon<1\), the following inversion holds. If \(\eta\le2\epsilon\), then \(N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)=1\). If \(\eta>2\epsilon\), define \[ q_\epsilon=dD\min\{\eta/\epsilon,\epsilon^{-2}\}, \qquad \ell_\epsilon=1+\log(eq_\epsilon)+\log\{1+(1+\eta)q_\epsilon\}, \] \[ n_0=\left\lceil10^8q_\epsilon\ell_\epsilon\right\rceil. \] Then the learner has worst-case expected regularized regret at most \(\epsilon\) at sample size \(n_0\), and \[ N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)\le n_0 =\widetilde O\!\left(dD\min\{\eta/\epsilon,\epsilon^{-2}\}\right). \] The tuple \((d,C,D,\eta)\) does not, however, determine the fixed-design minimax frontier. For every \(1<D\le C<e^\eta\), there are finite public experiments \(\mathfrak E_0\) and \(\mathfrak E_1\) with nonempty exact shells at the same indices such that \[ N^\star_{\mathfrak E_0}(\epsilon;d,C,D,\eta)=1\qquad(\epsilon>0), \] whereas, for constants \(c(C,D,\eta)>0\) and \(\epsilon_0(C,D,\eta)>0\), \[ N^\star_{\mathfrak E_1}(\epsilon;d,C,D,\eta)\ge c(C,D,\eta)\frac{dD\eta}{\epsilon} \qquad(0<\epsilon\le\epsilon_0(C,D,\eta)). \] The informative construction includes pairs with \(2D-1\ge e^\eta\), but its constant is pointwise in \((C,D,\eta)\); its invariant content is \(\Omega_{C,D,\eta}(d/\epsilon)\), not uniform lower dependence on \(D\) or \(\eta\). Consequently every lower bound asserted uniformly over all public experiments and based only on \((d,C,D,\eta)\) is at most one. Moreover, for each specified finite experiment with a nonempty exact shell and fixed \((d,C,D,\eta)\), \[ \lim_{\epsilon\downarrow0}\frac{\epsilon^2}{d}N^\star_{\mathfrak E}(\epsilon;d,C,D,\eta)=0, \] so no positive quadratic lower branch can hold throughout all sufficiently small accuracies. Thus the clean upper bound is universal over every nonempty exact shell, while lower matching is only pointwise and local in the \(d/\epsilon\) rate on the constructed informative experiment.
