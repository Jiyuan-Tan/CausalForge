D0_DIRECTIVE — atomic field-tier D0.5 repair after primary-source verification

[STRUCTURED CORE CHANGES REQUIRED]
[REQUIRED TARGETS: thm:same-shell-lower, prop:informative-fast-shell-all-indices, metadata:comparator-promise-table]

Implement one atomic solver-authored bundle. Change no theorem statement, definition, assumption, status, dependency, domain, formula, cited-source leaf, or accepted F artifact.

A. Proof-only Assouad repair for exactly thm:same-shell-lower and prop:informative-fast-shell-all-indices.

For sign v and data Z define R_j(v,Z)=rho(x_j)/eta KL(hat pi_Z(.|x_j)||s_{v_j}). By the Gibbs identity total regret R(v,Z) is a sum of nonnegative context contributions, hence R(v,Z)>=sum_j R_j(v,Z). If v^(j) flips coordinate j, prove pointwise
R_j(v,z)+R_j(v^(j),z)>=rho(x_j) Delta,
where Delta=t^2/(8 eta) for thm:same-shell-lower and Delta=alpha t^2/(2 eta) for prop:informative-fast-shell-all-indices. Integrate against the smaller neighboring data density. Under the already proved neighbor TV<=1/2, average the 2^(k-1) pairs separately for each j and then sum:
B:=2^-k sum_v E_v R(v,Z)
 >=sum_j 2^-k sum_v E_v R_j(v,Z)
 >=sum_j 2^-k 2^(k-1) rho(x_j)Delta/2
 =Delta/(4H).
Explicitly state that pairing full regret cannot be summed over j because it counts the same full loss k times.

For thm:same-shell-lower propagate B>=t^2/(32H eta)>=t^2/(48 eta), since H<=3/2. From log cosh t=64 eta epsilon and log cosh t<=t^2/2, obtain B>=8epsilon/3>epsilon. From log cosh t>=t^2/3 obtain gamma^2<=192epsilon/eta. With k>=d/4 and p=1/(4D), k/(p gamma^2)>=dD eta/(192epsilon); take c2<=c1/192 if needed.

For prop:informative-fast-shell-all-indices propagate B>=alpha t^2/(8H eta). Existing t^2=16H eta epsilon/alpha gives B>=2epsilon. Also p gamma^2=16H epsilon/(D eta) and k/(p gamma^2)>=dD eta/(64H epsilon).

Emit complete authoritative proof_tex for both nodes; statement and depends_on bytes stay identical.

B. Zhao closest-comparator correction.

Primary provenance checked by main: https://arxiv.org/html/2502.06051v3. Sec. 2.1 lines 114–124 gives offline iid reverse-KL contextual bandits, reference=behavior, and general realizable G. Def. 2.4 lines 133–137 defines C^{pi*}. Def. 2.5 and Remark 2.6 lines 139–149 define D_G^2 and qualify the linear leverage identity by mild parameter-space conditions. Assumptions 2.7–2.8 lines 151–159 distinguish all-policy D^2 and single-policy D_{pi*}^2. Theorem 2.10 lines 193–196 gives sufficiently-small-epsilon high-probability O~(eta D_{pi*}^2 epsilon^-1 log N_G(epsilon)). Theorem 2.11/Remark 2.12 lines 198–204 gives the existential C* lower. Appendix B lines 460–489 gives linear separations.

Use only this qualified algebra. For G_theta=phi^T theta,
D_G^2((s,a);pi_ref)=sup_{delta in Theta-Theta}(phi^T delta)^2/(delta^T Lambda_ref delta).
Only if Theta-Theta contains a nonzero multiple of every needed direction does this equal phi^T Lambda_ref^-1 phi. Then D_{pi*}^2=tr(M), M=Lambda_ref^-1/2 Lambda_P^star Lambda_ref^-1/2, and D_P=lambda_max(M)<=tr(M)<=dD_P. The bounded prediction polytope alone does not imply directional richness; without it claim only D_G^2<=phi^T Lambda_ref^-1 phi, not trace equality or D_P<=D_{pi*}^2.

Replace related_work's first paragraph with a direct Zhao comparison: Zhao is the closest general-function reverse-KL comparator, not tabular-only. State Theorem 2.10's exact criterion/rate and the qualified trace relation above. Concede that Zhao is pointwise, sufficiently-small-accuracy and high probability, whereas this note is bounded shared-linear, all-temperature, expected minimax and uniform over a fixed exact shell. Say the spectral factor may be up to d smaller than the trace factor, but do not claim wholesale domination. State that Theorem 2.11 is existential over density-ratio upper bound C*, not an exact-(C,D_P) shell lower. Retain Ji as the complementary tabular frontier. Add Zhao v3 and exact locators to the primary-source-basis sentence.

Replace project_justification.gap so it begins by conceding Zhao's single-policy fast upper and existential density-ratio lower, explains qualified trace versus largest-eigenvalue coverage, and claims only the missing bounded shared-linear exact-shell combination: all-temperature expected-minimax dD_P upper, fixed-experiment exact-(C_P,D_P) feasibility certificate, and same-index zero-versus-local-hard experiment comparison.

Emit comparator-promise-table-replace for metadata:comparator-promise-table, preserving every non-Zhao entry byte-for-byte. Zhao entry must have matched_by thm:clean-temperature-feature-upper and match_kind downgraded_to_informed_by, and must record Theorem 2.10's exact high-probability rate, the directionally-rich trace identity and D_P<=D_{pi*}^2<=dD_P qualification, boundedness caveat, and Theorem 2.11's existential-not-exact-shell status.

Replace honest_scope to concede the Zhao comparison: the lawful sharpening is only the bounded shared-linear spectral specialization and does not dominate Zhao's general-function/high-probability theorem. Preserve the Li--Ma--Srebro comparison, broader coverage-insufficiency concessions, local-only lower matching, zero-risk contrast, no-quadratic-converse limitation, and idealized-global-optimization limitation.

The bibliography entry is already accurate and stays byte-identical. The structured source/attestation on lem:zhao-uniform-square-comparison covers Appendix D.1 Lemma D.1 and stays byte-identical; this comparison provenance supplements it.

C. Preserve all other content.

Do not change tldr, any statement/dependency/domain, the accepted 0<epsilon<1 repair, all-positive boundary/zero-risk claims, formulas/constants beyond the displayed proof algebra, citations/attestations, or accepted feasibleFrontierHandle/PredictionLexLT/MeasurableSingletonClass work. Emit only the two proof payloads, complete prose replacements, and complete comparator-table replacement.
