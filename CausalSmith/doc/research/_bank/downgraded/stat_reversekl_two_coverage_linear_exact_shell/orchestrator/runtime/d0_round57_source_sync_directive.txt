D0 EXTENDED-KL FINITE-OBJECTIVE SOURCE SYNCHRONIZATION — solver-authored, no rate or theorem weakening.

The main-owned formal repair is now fixed and elaborates: `IsMeasurableLearner` requires every bounded-sample output to be both a policy and supported on the public reference policy. `ExactShell` has exactly the source's eight assumptions. `NoSlowBranch.lean` now carries `MeasurableSingletonClass` for both finite coordinates.

Synchronize the authoritative paper/core with the mathematically equivalent finite-objective convention.

1. State explicitly that the paper uses the standard extended reverse-KL convention: for any context with positive law mass, if a policy gives positive mass to an action where `pi_ref` is zero, then `KL(pi || pi_ref)=+infinity`, so `J_{eta,P}(pi)=-infinity` and its regret relative to the Gibbs optimum is `+infinity`.

2. Since every Gibbs optimum has exactly the support of `pi_ref`, policies outside reference support can never improve the minimax infimum. Any measurable learner can be replaced on every unsupported output by the public reference policy, measurably and without increasing worst-law regret. Therefore the full-simplex extended-KL minimax problem has exactly the same value as the infimum over measurable reference-supported, finite-objective learners.

3. Author complete synchronized replacements for the relevant typed-core leaves, not informal patches:
   - `sym:J_{eta,P}`: use an extended-real semantic signature/convention, or explicitly distinguish the extended objective from its finite real restriction on reference-supported policies.
   - `sym:mathcal L_n(mathfrak E)`: define the effective minimax decision class as measurable reference-supported policy learners, and state its exact WLOG equivalence to the advertised full-simplex extended-KL class.
   - `def:minimax-risk`: include the equivalence in the construction immediately adjacent to the infimum.
   - `def:common-experiment`: when introducing `mathcal L_n(mathfrak E)`, state that support is relative to the public `pi_ref`, hence the class remains law-independent and uses only public design inputs.
   - `sym:mathfrak R_{n,mathfrak E}` and `sym:mathfrak R_n`: preserve their nonnegative real risk value because the public reference learner is always finite and the supported restriction is WLOG.
   - `sym:underline J_n` and `def:prediction-polytope-learner`: clarify that the diagnostic empirical criterion is evaluated only on reference-supported policies; all Gibbs learners constructed in the note are automatically supported.
   - Synchronize any direct explanatory note, proof sentence, or limitations prose needed to prevent a real-valued off-support interpretation. Do not alter the statistical headline or novelty positioning.

4. Preserve all theorem statements, proof payloads, rates, constants, assumptions, dependencies, epsilon domains, citations/attestations, comparator table, Zhao qualification, coordinatewise Assouad repairs, exact-shell definition, feasibleFrontierHandle, PredictionLexLT, MeasurableSingletonClass work, and disclosed `sorry` scaffold status. Do not change TL;DR except if a minimal convention note is mechanically required; no rate or quantifier changes are authorized.

5. Emit only complete authoritative core edits/prose replacements necessary for this synchronization. Every other byte remains unchanged. The resulting paper must make clear that reference support is a finite-objective representation choice, not a restriction that changes the original full-simplex extended-KL minimax value.
