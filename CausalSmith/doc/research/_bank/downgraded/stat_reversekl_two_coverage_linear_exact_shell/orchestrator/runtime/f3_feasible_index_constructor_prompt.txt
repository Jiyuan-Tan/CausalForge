You are the F3 Lean proof worker for qid stat_reversekl_two_coverage, spec linear_exact_shell.

Goal: discharge the sole remaining `sorry` in
`CausalSmith/Stat/STAT_ReverseklTwoCoverage_Research/Feasibility/IndexRegion.lean`,
the frozen theorem `feasible_index_region`.

Hard constraints:
- Work in `/<repo-root>/CausalSmith`.
- Do not mutate pipeline state, graph.json, discovery/core.json, formalization plan, reviews, or theorem statements.
- Preserve the exact frozen theorem statement byte-for-byte. Replace only its proof body after an exact disposable wrapper compiles.
- First build the missing finite public-experiment constructor in `.../tmp/` and prove an exact wrapper for the frozen theorem there. Promote only after it compiles.
- No `sorry`, `admit`, `axiom`, `native_decide`, or hand-waving. No production import may depend on tmp.
- Keep production modules acyclic and <=600 lines each; split constructor/algebra/law helpers as needed.
- Reuse the newly proved `fixed_experiment_shell_certificate` machinery and existing helpers where useful, but the theorem needs an actual experiment and feasible witness, not merely a characterization.
- Do not weaken assumptions or alter F1/F2 artifacts.
- After promotion, build the focused target, downstream research barrel, run forbidden-token/import scans, and `#print axioms` on every promoted declaration and `feasible_index_region`.

Mathematical construction from the frozen discovery proof:
- For 1<K<exp eta, q_K=(exp eta/K-1)/(exp eta-1), so q_K∈(0,1).
- Use two actions and coordinate contexts. At a K-context, action-1 feature is basis vector e_j, action-0 feature is 0, reference action-1 probability q_K, theta is all ones, deterministic reward is the linear reward. Gibbs ratios are K and K/exp eta.
- Use x_C in coordinate 0 and x_j calibrated by D for j>=1.
- If C>D, add x_A where both action features equal e_0 and reference probabilities are positive; raw context mass A0=q_C(C-D)/(D-1). With raw x_C mass 1, x_A mass A0, and each other coordinate mass 1, the e_0 covariance ratio is (q_C*C+A0)/(q_C+A0)=D. All other ratios are D. This also supplies distinct x_C,x_A with the same nonzero feature direction and proves non-tabularity.
- If C=D, omit x_A. For (1,1), use coordinate contexts with a trivial tilt.
- The necessity and canonical-tabular parts are already developed above the theorem in IndexRegion.lean; use those lemmas rather than reproving where possible.
- The theorem is polymorphic in d and E, but the assumptions imply existence of `Fin d` through positive-definite logging geometry for input laws in necessity; for sufficiency the outer project’s standing d>=4 assumption may be encoded in `CommonExperiment`/existing context. Check exact definitions and do not assume what Lean does not provide.

Operate autonomously for a 30-minute event-triggered window: continue while producing concrete proof/build events; checkpoint only on a precise mathematical or API blocker. Return a concise receipt with files, builds, theorem fingerprint, axiom audit, and remaining blockers.
