MAXIMALITY: ROOM

The paper’s statistical content is internally coherent and the latest required repairs are present:

- Both lower bounds use coordinatewise Assouad regret contributions; neither sums paired full regret \(k\) times.
- Zhao et al. is correctly treated as the closest general-function comparator. The trace identity is conditional on directional richness, and no wholesale domination is claimed. This agrees with [Definitions 2.5, Assumption 2.8, and Theorem 2.10](https://arxiv.org/html/2502.06051v3).
- The comparator table carries the same qualification.
- Clean/capstone inversion is restricted to \(0<\epsilon<1\); boundary and zero-risk results cover every \(\epsilon>0\).
- `feasibleFrontierHandle`, sequential context-then-action `PredictionLexLT`, and `MeasurableSingletonClass` premises remain intact.
- All core dependency identifiers resolve, IDs are unique, both OEQs are resolved, and every proposal array is empty.
- No further statistical strengthening follows lawfully without new research: the universal \(dD\) upper result is closed, while uniform \(D,\eta\)-dependent converses and a persistent quadratic lower branch are correctly ruled out or disclaimed.

However, two repairable Lean–paper synchronization defects prevent NO-ROOM:

1. `Basic.lean` defines `policyKL` in `ℝ`. If \(\pi(x,a)>0\) but \(\pi_{\rm ref}(x,a)=0\), Lean’s totalized division/logarithm makes that summand finite—effectively zero—instead of \(+\infty\). Consequently `regularizedWelfare`, `learnerRisk`, and `minimaxRisk` do not presently formalize the paper’s reverse-KL decision problem for the advertised full simplex.

2. `ExactShell` contains the additional field `traceCoverage_ge_one`, although the paper defines membership through exactly eight assumptions. The inequality is mathematically derivable from positive-definite logging geometry and \(D_P\ge1\), but encoding it as an extra premise makes the Lean shell definition strictly stronger until that derivation is formalized.

All 30 substantive Lean declarations also currently end in `sorry`; thus these modules are proof scaffolds, not a machine-checked certificate.

Exact D0 directive:

> **D0-FORMAL-SYNC-REPAIR — no statistical weakening authorized.**
>
> Target `CausalSmith/Stat/STAT_ReverseklTwoCoverage_Research/Basic.lean` and only transitively necessary consumers.
>
> 1. Replace the real-totalized unsupported-action KL semantics with a faithful extended reverse-KL semantics through `policyKL`, `regularizedWelfare`, `learnerRisk`, and `minimaxRisk`; alternatively restrict the formal learner class to reference-supported policies only after proving that this restriction is exactly WLOG for the paper’s extended-KL minimax problem. Do not narrow the paper’s effective decision class, alter its minimax quantifiers, or weaken any lower theorem.
> 2. Remove `traceCoverage_ge_one` as a field of `ExactShell`. Prove it as a derived lemma from the existing eight shell premises and use that lemma wherever needed.
> 3. Recheck the statements in `Gibbs/RegretIdentity.lean`, `Feasibility/IndexRegion.lean`, `Upper/{Localized,ERMPlugin,CleanTemperature}.lean`, `NoSlowBranch.lean`, and `FrontierNonuniqueness.lean` against the repaired semantics.
> 4. Preserve verbatim in substance: `feasibleFrontierHandle`; context-then-action `PredictionLexLT`; all `MeasurableSingletonClass` premises; coordinatewise Assouad decompositions; Zhao’s directional-richness qualification; and the \(0<\epsilon<1\) domain of clean/capstone inversion.
> 5. Do not claim machine verification while any theorem in the advertised formal chain remains `sorry`.

Receipts:

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| `writeup.tex` | 192,128 | `129975e94cf4f0bdc59f498347e0282b13e10b23a5d07dcd96a32f2f22dfeb4c` |
| `core.json` | 205,614 | `6c76826d12d4df7e6c9d482e83e4e2d5dc5267c178fa5a4635c386372826f4fd` |
| `d0_working.json` | 345,701 | `5e956ddf83437c82942af183004b853d9f8862a5f7c6840bf0b6c73a09f62187` |
| `proto_core.json` | 67,656 | `d34338f28c9d434797ade59dab182b1c8794876439c9a186a4fce6e3e8363d5e` |

Lean files read completely:

- `Basic.lean`
- `Coverage/FeatureDomination.lean`
- `Feasibility/IndexRegion.lean`
- `FrontierNonuniqueness.lean`
- `Gibbs/RegretIdentity.lean`
- `Learner/PredictionPolytope.lean`
- `Localization/Concentration.lean`
- `Lower/DimensionWitness.lean`
- `Lower/HardFamily.lean`
- `Lower/InformativeShell.lean`
- `NoSlowBranch.lean`
- `Upper/CleanTemperature.lean`
- `Upper/ERMPlugin.lean`
- `Upper/Localized.lean`

`tmp/Main.lean` was excluded as directed. No files were edited.
