The note isolates a design-based posterior obstruction: within every clique block, any binary assignment reveals at most one of two independent target-layer signs.  This converts a supplied clique partition into an exact finite Bayes lower certificate and exposes how dense local structure changes the lower benchmark.