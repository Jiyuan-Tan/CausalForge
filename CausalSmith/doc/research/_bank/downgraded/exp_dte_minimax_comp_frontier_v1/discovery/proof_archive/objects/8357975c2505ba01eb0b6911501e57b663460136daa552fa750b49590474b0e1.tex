A binary assignment is observed together with the realized outcomes.  On a clique-partition subgraph, a maximal target-exposure observation reveals either the control-layer sign or one isolated-treatment-layer sign in each block, never both.