For any explicitly listed finite prior and any explicitly listed realizable procedure, every orbitwise posterior risk and every upper quadratic risk are finite computations, so their ratio is auditable.  On the equal block-clique family the corrected construction uses at most \(10(b+1)|\mathfrak G_{b,s}|\) atoms.  For arbitrary graphs, however, the frozen handle gives neither a polynomial-size atom dictionary nor a polynomial-time maximal-compatible-exposure separation oracle, and it supplies no approximation analysis.