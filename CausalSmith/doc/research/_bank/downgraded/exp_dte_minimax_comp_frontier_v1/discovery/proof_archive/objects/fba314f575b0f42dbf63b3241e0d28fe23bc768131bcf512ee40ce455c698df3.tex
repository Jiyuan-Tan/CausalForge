There are no estimated nuisance functions.  The arbitrary-graph lower result is uniform over all measurable estimators; the equal-block upper certificate uses the displayed linear estimator and makes no claim that every unrestricted estimator can be symmetrized into that linear class.