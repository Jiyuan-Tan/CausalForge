\textbf{Proof.}
For \(e\in\{L,U\}\), the empirical endpoint matrix \(\widehat Q_e\) is the second-moment matrix of the signed outcome stack under the empirical countermonotone or comonotone coupling.  Thus
\[
\widehat Q_e\succeq0.
\tag{1}
\]
Every coefficient of \(\widehat{\mathfrak I}_{m,\mathrm{alg}}^+\) is rational or real algebraic.  By ass:exposure-positivity, each diagonal entry of \(\Pi\) is positive, so \(\Pi^{-1}\) exists and has real-algebraic entries.  Consequently
\[
\widehat w_m^+(B)=
\max_{e\in\{L,U\}}n^{-2}\operatorname{tr}\!\left[
\left\{\Pi^{-1}\Omega\Pi^{-1}
-\Pi^{-1}\Omega B(B^{\top}\Omega B)^+B^{\top}\Omega\Pi^{-1}\right\}
\widehat Q_e\right]
\tag{2}
\]
has exactly the rank-stratified semialgebraic form used in thm:zhao-pseudoinverse-rank-stratified-epsilon-solver, with \(Q(c_e)\) replaced by \(\widehat Q_e\).

To make the reduction explicit, write \(S=S_B=B^{\top}\Omega B\).  Since \(\Omega\succeq0\), also \(S\succeq0\).  The three exhaustive rank cases are
\[
\begin{array}{c|c|c}
\operatorname{rank}(S)&\text{exact condition}&S^+\\ \hline
0&\operatorname{tr}(S)=0&0,\\[1mm]
1&\operatorname{tr}(S)>0,\ \det(S)=0&S/\{\operatorname{tr}(S)\}^2,\\[1mm]
2&\det(S)>0&\operatorname{adj}(S)/\det(S).
\end{array}
\tag{3}
\]
Every denominator in (3) has a certified positive sign on the cell where it is used.  After clearing that positive denominator, the two endpoint losses and their maximum are described by polynomial equalities and weak or strict inequalities.  The normalized zero-or-sphere arm constraints and the signed-pivot quotient charts are likewise semialgebraic.  Thus the empirical objective graph over all ranks is a finite semialgebraic set over a real-algebraic coefficient field.

Apply thm:zhao-pseudoinverse-rank-stratified-epsilon-solver to this empirical graph.  Its proof invokes lem:real-closed-field-quantifier-elimination to eliminate the basis coordinates, isolate the finite algebraic infimum, and lift an algebraic sample within the positive tolerance.  Because \(\varepsilon_m>0\) is exactly encoded, root isolation terminates and gives
\[
\widehat L_{m,\varepsilon_m}^{\mathrm{CAD},+}
\le\widehat v_m^+
\le\widehat U_{m,\varepsilon_m}^{\mathrm{CAD},+},
\qquad
\widehat U_{m,\varepsilon_m}^{\mathrm{CAD},+}
-\widehat L_{m,\varepsilon_m}^{\mathrm{CAD},+}
\le\varepsilon_m,
\tag{4}
\]
and an algebraic normalized class for which
\[
\widehat w_m^+(\widehat B_{m,\varepsilon_m}^{\mathrm{CAD},+})
\le\widehat v_m^++\varepsilon_m.
\tag{5}
\]
Rank-boundary points are not lost: they are handled by the lower-rank row of (3).

It remains to verify the augmented rank and spectral fields.  Put
\[
\widehat S_m^{\mathrm{CAD}}
=(\widehat B_{m,\varepsilon_m}^{\mathrm{CAD},+})^{\top}
\Omega\widehat B_{m,\varepsilon_m}^{\mathrm{CAD},+}.
\]
This is a positive-semidefinite \(2\times2\) matrix with real-algebraic entries.  Its rank is zero exactly when its trace is zero, is one exactly when its trace is positive and its determinant is zero, and is two exactly when its determinant is positive.  Exact ordered real-algebraic arithmetic decides those alternatives, proving \(\widehat r_{m,\varepsilon_m}^{\mathrm{CAD}}\in\{0,1,2\}\).

The eigenvalues, with multiplicity, are the roots of
\[
\chi_m(t)=t^2-\operatorname{tr}(\widehat S_m^{\mathrm{CAD}})t
+\det(\widehat S_m^{\mathrm{CAD}}).
\tag{6}
\]
Exact algebraic arithmetic decides equality with zero and root multiplicity.  For each distinct positive root, rational bisection with exact sign tests terminates at a rational interval contained in \((0,\infty)\), disjoint from the intervals of the other distinct roots, and containing precisely that root.  The retained CAD projection polynomials, sign table, algebraic samples, value isolations (4)--(5), rank signs, and root isolations (6) are the claimed finite locally checkable record.  The construction never evaluates the countable infimum and supplies neither a zero-tolerance selector nor a finite algorithm for arbitrary real pilot values. \(\square\)