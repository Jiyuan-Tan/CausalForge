\textbf{Proof.}
Thm:triangle-completion-separation-certificate identifies the complete rank-two projective optimizer sets and their values.  Thm:zhao-ordinary-domain-quotient-transfer makes each criterion constant on the fibers of \(q_Z\), so the unquotiented optimizer sets are exactly \(q_Z^{-1}(\mathcal A_0)\) and \(q_Z^{-1}(\mathcal A_{\mathrm{mm}})\).  The fiber invariance preserves the values, and subtraction gives \(521/1200-2613/8432=4912/39525\).  This is the positive-definite rank-two restriction; the published ranks zero and one are handled in the full-class triangle theorem. \(\square\)