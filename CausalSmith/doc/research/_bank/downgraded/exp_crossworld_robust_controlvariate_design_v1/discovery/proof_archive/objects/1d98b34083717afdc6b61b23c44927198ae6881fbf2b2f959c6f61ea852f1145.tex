Compute \(\widehat Q_L,\widehat Q_U\) from the empirical means, variances, and quantile-coupling covariance endpoints of \(\widehat F_1,\widehat F_0\).  For every \([B]_+\in\mathcal B_Z^+\), substitute \(\widehat Q_e\) into Zhao's pseudoinverse formulas and define
\[
\widehat w(B)=\max_{e\in\{L,U\}}\left\{\widehat h_e-\widehat g_B(c_e)\right\},
\qquad
\widehat v_{\mathrm{mm}}=\inf_{[B]_+\in\mathcal B_Z^+}\widehat w(B).
\]
For every \(\varepsilon>0\), the primary empirical problem selects any \([B]_+\) satisfying \(\widehat w(B)\le\widehat v_{\mathrm{mm}}+\varepsilon\).  On the secondary fixed-\(\kappa\), rank-two restriction, the same function uses \(S_B^+=S_B^{-1}\); denote its exact minimum and argmin locally by \(\min_{B\in\mathcal B_\kappa}\widehat w(B)\) and \(\operatorname*{argmin}_{B\in\mathcal B_\kappa}\widehat w(B)\).  Retain \(\widehat r(B)\), \(\widehat v\), and \(\widehat{\mathcal A}_R\) only as secondary diagnostics.