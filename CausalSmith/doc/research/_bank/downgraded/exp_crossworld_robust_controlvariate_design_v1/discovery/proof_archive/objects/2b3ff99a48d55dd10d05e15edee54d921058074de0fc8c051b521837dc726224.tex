Start from a three-vertex network under independent Bernoulli assignment with probability \(1/3\), enumerate the mutually exclusive exposure truth tables exactly, derive exact \(\Pi\) and \(\Omega\), use Bernoulli \(1/2\) pilot marginals, and run the certified projective interval handle simultaneously over the full robust and zero-covariance argmax correspondences. Search for rational certificates \(\delta>0\) and \(\eta>0\) that bound the correspondence-level loss separation and the optimizer-wide strict \(\kappa\)-slack.