\textbf{Proof.}
Assumption ass:kappa-feasibility and prop:kappa-exhaustion make \(\mathcal B_\kappa\) nonempty and compact.  Exposure positivity and \(S_B\succeq n\kappa I_2\) make the endpoint gains continuous; lem:ht-affine-drift makes \(w\) continuous.  Thus \(\mathcal A_{\mathrm{mm}}\) and \(\mathcal A_0\) are nonempty.  Every minimizer has loss \(v_{\mathrm{mm}}\), and the defining minimum gives
\[
\sup_{B_{\mathrm{mm}}\in\mathcal A_{\mathrm{mm}}}w(B_{\mathrm{mm}})
=v_{\mathrm{mm}}
\le\inf_{B_0\in\mathcal A_0}w(B_0).
\]
Strictness is not universal: if \(F_1=F_0=\delta_0\), both covariance endpoints and every endpoint moment matrix vanish, so every feasible basis has gain and loss zero. \(\square\)