Under ass:coordinate-bounded-kappa-feasibility, use only the independent unpaired pilot and choose
\[
\widehat B_{\mathrm{mm}}
\in\operatorname*{argmin}_{B\in\mathcal B_{\kappa,\overline a}}\widehat w(B),
\qquad
\widehat B_0
\in\operatorname*{argmax}_{B\in\mathcal B_{\kappa,\overline a}}\widehat g_B(0).
\]
The named condition makes \(\mathcal B_{\kappa,\overline a}\) nonempty.  Its normalized representatives form a closed subset of the product of two radius-\(\sqrt n\) spheres, because the coordinate-envelope and eigenvalue constraints are closed; the finite projective quotient is therefore compact.  The empirical criteria are continuous on this uniformly positive-definite set, so both optimizer correspondences are nonempty.  Their measurable dependence on the finite pilot statistics, followed by a fixed lexicographic signed-pivot-chart rule, defines measurable selectors \(\widehat B_{\mathrm{mm}}\) and \(\widehat B_0\).  In the independent main experiment deploy
\[
\widehat\tau_{\widehat B_{\mathrm{mm}}}^{\mathrm{CV}}
\quad\text{and}\quad
\widehat\tau_{\widehat B_0}^{\mathrm{CV}}
\]
through def:observable-coefficient-map, using \(\widehat\gamma^{\mathrm{HT}}\) and the ordinary inverse.  The pilot chooses the basis, while the main assignment and main potential-outcome array enter only the displayed estimator; no out-of-class fallback is used.