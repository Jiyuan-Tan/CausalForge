The robust basis does not identify a missing cross-world covariance.  It minimizes the largest residual design variance over every covariance compatible with the pilot-identified marginals.  On the triangle witness, every zero-covariance optimizer has worst loss \(521/1200\), whereas every minimax optimizer has loss \(2613/8432\), an absolute reduction \(4912/39525\).  Independent column rescaling changes matrix coordinates but neither the estimator span nor any gain, loss, value, or attained optimizer after quotienting.