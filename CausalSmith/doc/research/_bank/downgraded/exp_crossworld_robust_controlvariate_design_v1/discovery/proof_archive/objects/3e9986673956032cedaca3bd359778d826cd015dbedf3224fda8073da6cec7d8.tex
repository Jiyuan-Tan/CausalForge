Compute empirical means, variances, and the two empirical quantile-coupling covariances from \(\widehat F_1,\widehat F_0\), and substitute them into the endpoint moment matrices and def:gain-loss.  Define
\[
 \widehat w(B)=\max\{\widehat\ell_B(\widehat c_L),\widehat\ell_B(\widehat c_U)\},
 \quad
 \widehat v_{\mathrm{mm}}=\min_{B\in\mathcal B_\kappa}\widehat w(B),
 \quad
 \widehat{\mathcal A}_{\mathrm{mm}}=\operatorname*{argmin}_{B\in\mathcal B_\kappa}\widehat w(B).
\]
For a reported measurable selection \(\widehat B\in\widehat{\mathcal A}_{\mathrm{mm}}\), deploy \(\widehat\tau_{\widehat B}^{\mathrm{CV}}\) using def:observable-coefficient-map.  Retain \(\widehat r\), \(\widehat v\), and \(\widehat{\mathcal A}_R\) only as secondary maximin-gain diagnostics.  The independent-pilot theory proves robust basis-value and outer-set consistency; it does not by itself supply a limit law for the estimated deployment coefficient.