The headline robust object is \(w(B)=\max\{\ell_B(c_L),\ell_B(c_U)\}\), optimized on a nonempty coordinate-bounded \(\kappa\)-regular ordinary-inverse class; \(r(B)=\min\{g_B(c_L),g_B(c_U)\}\) is secondary.  The note claims neither a universal minimax interchange, an SDP reduction, a uniform branch-and-bound complexity bound, nor universal strict improvement.  General completion statements use an infimum and a possibly empty equality set, while the triangle theorem independently certifies an attained rank-two optimum.  Plug-in point convergence is claimed only for a singleton minimizer, and finite-\(n\) feasible-estimator strictness additionally requires the displayed linear geometry bound, optimizer containment, and pilot/remainder diagnostic.
