Compute the empirical means, variances, and two empirical quantile-coupling covariances from \(\widehat F_1,\widehat F_0\), and let \(\widehat Q_L,\widehat Q_U\) be the resulting endpoint matrices.  On the note's projective quotient of Zhao's published unquotiented all-block-separable pseudoinverse domain, define the distinct full-class empirical objective
\[
\widehat w_m^+(B)=
\max_{e\in\{L,U\}}n^{-2}\operatorname{tr}\!\left[
\left\{\Pi^{-1}\Omega\Pi^{-1}
-\Pi^{-1}\Omega B S_B^+B^{\top}\Omega\Pi^{-1}\right\}\widehat Q_e
\right].
\]
When \(\mathcal B_Z\ne\varnothing\), fix independently of the pilot a countable dense sequence \(\mathcal D_Z=([C_j]_+)_{j\ge1}\) in the normalized positive-definite rank-two stratum of \(\mathcal B_Z^+\).  Concretely, order the pairs \((q_1,q_0)\in(\mathbb Q^n\setminus\{0\})^2\), replace each \(q_a\) by \(\sqrt n q_a/\lVert q_a\rVert_2\), impose the fixed signed-pivot convention, and retain precisely the pairs satisfying \(C_j^{\top}\Omega C_j\succ0\).  For every strictly positive deterministic or pilot-measurable tolerance \(\varepsilon_m\), put
\[
\widehat I_m=\inf_{j\ge1}\widehat w_m^+(C_j),
\qquad
J_{m,\varepsilon_m}
=\min\left\{j\ge1:
\widehat w_m^+(C_j)\le\widehat I_m+\varepsilon_m\right\},
\qquad
\widehat B_{m,\varepsilon_m}^+=[C_{J_{m,\varepsilon_m}}]_+.
\]
The empirical rank-two density lemma proves that \(\widehat I_m=\inf_{[B]_+\in\mathcal B_Z^+}\widehat w_m^+(B)\), that the displayed minimum is finite, and that the first-qualifying-index selector is pilot measurable.  No full-carrier attainment or zero-tolerance selector is built into the construction.  On the compact fixed-threshold rank-two restriction, separately retain
\[
\widehat w(B)=\max\{\widehat\ell_B(\widehat c_L),\widehat\ell_B(\widehat c_U)\},
\quad
\widehat v_{\mathrm{mm}}=\min_{B\in\mathcal B_\kappa}\widehat w(B),
\quad
\widehat{\mathcal A}_{\mathrm{mm}}=\operatorname*{argmin}_{B\in\mathcal B_\kappa}\widehat w(B),
\]
and retain \(\widehat r(B)\), \(\widehat v\), and \(\widehat{\mathcal A}_R\) only as secondary diagnostics.  The measurable full-class positive-tolerance selector and the regular rank-two exact argmin are not conflated.