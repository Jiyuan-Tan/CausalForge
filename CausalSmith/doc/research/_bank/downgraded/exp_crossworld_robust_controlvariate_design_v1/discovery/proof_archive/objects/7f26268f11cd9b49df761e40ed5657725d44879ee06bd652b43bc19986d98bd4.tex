For \(F_1=F_0=\operatorname{Bernoulli}(1/2)\), direct evaluation of the quantile formulas gives \(\mu_1=\mu_0=1/2\), \(\sigma_1^2=\sigma_0^2=1/4\), \(c_L=-1/4\), and \(c_U=1/4\).  After the objective correction, global minimization itself yields the weak inequality
\[
\inf_{B_0\in\mathcal A_0(\xi)}w_\xi(B_0)-\sup_{B_{\mathrm{mm}}\in\mathcal A_{\mathrm{mm}}(\xi)}w_\xi(B_{\mathrm{mm}})\ge0
\]
whenever both correspondences are nonempty: every minimax optimizer has the common value \(\min_{B\in\mathcal B_\kappa}w_\xi(B)\), which is no larger than \(w_\xi(B_0)\) for any zero-covariance optimizer \(B_0\in\mathcal B_\kappa\).  These facts verify the covariance endpoints and non-strict comparison, but not a positive margin or optimizer-wide regularity slack for the proposed triangle instance.