\textbf{Proof.}
Ass:kappa-feasibility and prop:kappa-exhaustion make \(\mathcal B_\kappa\) nonempty and compact.  Exposure positivity and \(S_B\succeq n\kappa I_2\) make the two endpoint gains continuous on this quotient; lem:ht-affine-drift then makes \(w\) continuous.  Hence \(\mathcal A_{\mathrm{mm}}\) and \(\mathcal A_0\) are nonempty.  Every minimizer has loss \(v_{\mathrm{mm}}\), so definition of a minimum gives
\[
\sup_{B_{\mathrm{mm}}\in\mathcal A_{\mathrm{mm}}}w(B_{\mathrm{mm}})
=v_{\mathrm{mm}}
\le\inf_{B_0\in\mathcal A_0}w(B_0).
\]
Strictness is not universal: for \(F_1=F_0=\delta_0\), \(Q(0)=0\), both endpoints are zero, and every feasible basis has zero loss and gain.  This is the unconditional certificate only on the regular rank-two restriction; the primary infimum on the note's quotient of Zhao's full published carrier may be unattained. \(\square\)