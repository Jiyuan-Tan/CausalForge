Under ass:known-exposure and ass:coordinate-bounded-kappa-feasibility, and using only the independent unpaired pilot, define the secondary regular rank-two selectors
\[
\widehat B_{\mathrm{mm}}
\in\operatorname*{argmin}_{B\in\mathcal B_{\kappa,\overline a}}\widehat w(B),
\qquad
\widehat B_0
\in\operatorname*{argmax}_{B\in\mathcal B_{\kappa,\overline a}}\widehat g_B(0)
\]
by the fixed lexicographic quotient-chart tie-breaking rule.  In the independent main experiment deploy \(\widehat\tau_{\widehat B_{\mathrm{mm}}}^{\mathrm{CV}}\) and \(\widehat\tau_{\widehat B_0}^{\mathrm{CV}}\) through def:observable-coefficient-map.  Zhao's pseudoinverse equals the ordinary inverse on this class because \(S_B\succeq n\kappa I_2\).  The pilot chooses the basis, while the main assignment and fixed main potential-outcome array enter only the displayed estimator.  Existence and measurability follow from nonemptiness, compactness, continuity, and the fixed lexicographic rule; no out-of-class fallback is included.