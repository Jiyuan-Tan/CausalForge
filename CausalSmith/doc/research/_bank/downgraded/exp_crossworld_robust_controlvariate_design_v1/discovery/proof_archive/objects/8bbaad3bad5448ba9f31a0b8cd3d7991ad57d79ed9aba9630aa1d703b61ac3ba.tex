Start from a three-vertex network under independent Bernoulli assignment with probability \(1/3\), enumerate the mutually exclusive exposure truth tables exactly, derive exact \(\Pi\) and \(\Omega\), use Bernoulli \(1/2\) pilot marginals, and apply the moment-complete projective interval handle simultaneously to the full minimax-residual and zero-covariance optimizer correspondences. Search for rational certificates \(\delta>0\) and \(\eta>0\) that bound the correspondence-level residual-loss separation and the optimizer-wide strict \(\kappa\)-slack. The handle names the search and does not assert that the proposed triangle supplies either certificate.