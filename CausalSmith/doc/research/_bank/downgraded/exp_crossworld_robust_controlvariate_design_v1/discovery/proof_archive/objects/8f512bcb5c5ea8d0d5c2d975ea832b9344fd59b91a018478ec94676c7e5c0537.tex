\(\mathcal B_Z^+=\{[B]_+:B=\begin{bmatrix}b_1&0\\0&b_0\end{bmatrix},\ b_1,b_0\in\mathbb R^n\}\), where two representatives are equivalent when, separately for each arm, both corresponding columns are zero or one is a nonzero scalar multiple of the other.  Every class is represented by taking each nonzero arm column to have squared Euclidean norm \(n\), while a zero arm column remains zero.  This is the full block-separable basis class in Zhao2026OptimalControlVariates, Theorem 4 and Equation (8), modulo the note's exact independent arm-column rescaling quotient.  For every representative, \(S_B=B^{\top}\Omega B\succeq0\); Zhao's EC.2 convention interprets the inverse of this positive-semidefinite matrix by spectrally reciprocating its positive eigenvalues and mapping its zero eigenvalues to zero, namely the Moore--Penrose inverse \(S_B^+\).  Thus ranks zero, one, and two all belong to Zhao's published domain.