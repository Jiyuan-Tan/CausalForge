With the assignment law fixed, the note chooses a block-separable control-variate span to minimize worst Fr\'echet-endpoint residual variance, not to maximize an algebraically inequivalent gain diagnostic.  It derives endpoint reduction, compact \(\kappa\)-regular optimization, a finite CAD-backed algebraic certificate, uniform independent-pilot value and outer-argmin consistency, and point convergence exactly under a singleton minimizer.  A rank-stratified projector argument transfers the value to Zhao's published Moore--Penrose domain while retaining possible singular ties; the exact triangle certificate excludes those ties and yields relative-open strict separation.  The public Jiangxi PICC reanalysis is reproduced as an exploratory cross-fit application with an explicit deployed coefficient map and disclosed numerical gaps.