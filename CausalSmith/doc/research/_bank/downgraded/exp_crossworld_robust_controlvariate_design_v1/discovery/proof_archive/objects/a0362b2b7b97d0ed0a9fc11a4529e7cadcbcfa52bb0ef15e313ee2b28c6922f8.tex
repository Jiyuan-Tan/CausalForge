With a fixed network and known randomization, unpaired pilot marginals identify sharp Fr\'echet covariance endpoints but not the cross-world copula.  The primary design decision minimizes the larger endpoint residual \(w(B)\) over a nonempty normalized, coordinate-bounded, \(\kappa\)-regular projective class; the smaller endpoint gain \(r(B)\) is retained only as a secondary diagnostic.  The note gives affine endpoint reduction, certified algebraic \(\varepsilon\)-global optimization, an exact triangle separation from the zero-covariance surrogate, plug-in outer argmin consistency, and a finite-\(n\) bridge to the observable ordinary-inverse Horvitz--Thompson control-variate estimator.  General Moore--Penrose completion results are stated at the level of infima unless attainment is separately certified.