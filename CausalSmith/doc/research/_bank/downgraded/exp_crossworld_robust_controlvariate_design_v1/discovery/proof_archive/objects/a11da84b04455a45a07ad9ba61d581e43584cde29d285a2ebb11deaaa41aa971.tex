The exploratory record \(\mathcal J_{\mathrm{PICC}}\) is a reproducible but explicitly unexecuted procedure implemented by \(\texttt{discovery/jiangxi\_robust\_reanalysis.py}\).  Its input directory must contain \(\texttt{survey.tab}\) and \(\texttt{network.tab}\).  The procedure cleans identifiers, sentinel \(99\), self-loops, and duplicate directed nominations while retaining cross-village nominations; constructs the target populations for \(\tau_{10},\tau_{21},\tau_{32}\); and discloses the independent Bernoulli \((1/2)\) reconstruction necessitated by the unreleased original strata.  It splits connected components of the exposure-overlap graph without cutting components, estimates the two marginal laws and Fr\'echet endpoints on each training fold, and searches the full rank-stratified pseudoinverse robust and zero-covariance criteria under the same initialization list and iteration budget.  Each held-out basis is deployed through def:observable-coefficient-map using Zhao's pseudoinverse coefficient and additional \(\Pi^{-1}\) factor; on a reported \(\kappa\)-regular rank-two basis this equals the ordinary inverse.  The audit output reports point estimates, design-randomization standard errors, both endpoint losses, worst loss, basis rank, minimum positive-eigenvalue regularity, and a clearly labeled heuristic objective diagnostic, never a certified gap.  Authenticated replication data were unavailable, so the protocol was not executed and no numerical table is asserted.  Its internal cross-fitting is distinct from ass:unpaired-pilot and does not validate the independent-pilot asymptotic theory.