\textbf{Proof.}
Use the normalized representatives from def:zhao-published-pseudoinverse-domain: each arm column is either zero or lies on the radius-\(\sqrt n\) sphere.  This is a finite union of bounded basic semialgebraic support patterns.  Put \(S=S_B\).  Since \(S\succeq0\), its three ranks have the exact formulas
\[
S^+=0\quad(\operatorname{rank}S=0),
\qquad
S^+=\frac{S}{\{\operatorname{tr}S\}^2}quad(\operatorname{rank}S=1),
\qquad
S^+=\frac{\operatorname{adj}(S)}{\det S}quad(\operatorname{rank}S=2).
\tag{1}
\]
The rank conditions are respectively \(\operatorname{tr}S=0\), \(\operatorname{tr}S>0\) with \(\det S=0\), and \(\det S>0\).  Thus (1), the two endpoint traces, and an auxiliary variable encoding their maximum make the graph of \(w^+\) a finite union of rational semialgebraic sets over the real-algebraic input field.

For a real variable \(t\), the statement
\[
\exists [B]_+\in\mathcal B_Z^+\; w^+(B)\le t
\tag{2}
\]
is therefore a first-order formula over a real closed field.  Lem:real-closed-field-quantifier-elimination eliminates its basis variables.  The resulting one-dimensional semialgebraic set is the upper closure of the attainable-loss image; its infimum is a real-algebraic number because endpoints of one-dimensional semialgebraic sets over a real-algebraic field are real algebraic.  Exact root isolation returns rational or algebraic \(L_\varepsilon^+\le v_{\mathrm{mm}}^+\le U_\varepsilon^+\) with gap at most \(\varepsilon\).

By definition of an infimum, the strict formula \(w^+(B)<v_{\mathrm{mm}}^++\varepsilon\) is feasible.  CAD lifting applied to this formula returns a real-algebraic sample \(B_\varepsilon\); hence the weak displayed inequality follows.  The same quantifier-elimination calculation shows that its weak epsilon-sublevel set is nonempty and semialgebraic.  The finite projection, sign-invariant cell, rank-formula, root-isolation, and sample records are a checkable certificate.  Strict rank conditions are retained as strict sign cells, so a nonattained infimum on an open rank stratum causes no failure.  The proof supplies epsilon values and samples only, not an exact argmin or an outer argmin set. \(\square\)