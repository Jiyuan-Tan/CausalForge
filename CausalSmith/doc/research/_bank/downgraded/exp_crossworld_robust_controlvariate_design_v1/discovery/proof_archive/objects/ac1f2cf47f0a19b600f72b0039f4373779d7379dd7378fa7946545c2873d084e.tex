Given algebraic \(\mathfrak I_{\mathrm{cert}}\), cover each normalized projective factor by the signed pivot charts \(\{\pm b_j\ge n^{-1/2}\}\), retain all coordinates together with the sphere equations, and bisect rational chart boxes.  On every box, invoke lem:algebraic-box-feasibility-cad on the polynomial sphere, chart, box, and two-by-two positive-semidefinite constraints.  An infeasible box is discarded with its CAD certificate; a feasible box receives an exact algebraic sample.  Evaluate both endpoint residual losses at that sample by algebraic-number isolation and combine the result with a global feasible-set Lipschitz constant and the box diameter to obtain an outward rational lower bound.  Maintain the best sampled upper bound, prune only boxes whose certified lower bound exceeds it, and otherwise refine until both the value gap and retained mesh are at most \(\varepsilon\).  Return the finite local certificate \(\mathscr K_{\varepsilon}\); no feasibility oracle, inclusion-oracle incumbent, semidefinite-program reduction, or uniform complexity claim is used.