The exploratory record \(\mathcal J_{\mathrm{PICC}}\) is the reproducible but unexecuted procedure implemented by \(\texttt{discovery/jiangxi\_robust\_reanalysis.py}\).  Its input directory must contain \(\texttt{survey.tab}\) and \(\texttt{network.tab}\).  The procedure cleans identifiers, sentinel \(99\), self-loops, and duplicate directed nominations while retaining cross-village nominations; constructs the three target populations for \(\tau_{10},\tau_{21},\tau_{32}\); and uses the disclosed independent Bernoulli \((1/2)\) reconstruction necessitated by the unreleased original strata.  It splits connected components of the exposure-overlap graph without cutting components, estimates the two marginal laws and Fr\'echet endpoints on each training fold, and computes full published-class pseudoinverse epsilon-minimizers for the robust and zero-covariance criteria under a common deterministic optimization protocol.  It also records the regular rank-two restriction whenever the selected basis meets the declared \(\kappa\) and coordinate bounds.  Each held-out basis is deployed through def:observable-coefficient-map with Zhao's published pseudoinverse and the additional \(\Pi^{-1}\) factor.  The output fields report point estimates, design-randomization standard errors, both endpoint losses, worst loss, minimum regularity, and a clearly labeled heuristic optimization diagnostic; the diagnostic is not a certified upper-minus-lower gap.  The authenticated replication data were unavailable in the solve environment, so the protocol was not executed and no numerical table is asserted.  Its internal cross-fitting is distinct from the independent unpaired pilot of ass:unpaired-pilot, so the external-pilot first-order transfer theorem is not claimed for this application protocol.