For every rational or real-algebraic encoding of \(\Pi,\Omega,Q(c_L),Q(c_U)\) satisfying ass:exposure-positivity and the positive-semidefinite endpoint conditions in def:moment-matrix, the rank-stratified cylindrical-algebraic procedure determines the full Zhao-published pseudoinverse infimum
\[
v_{\mathrm{mm}}^+=\inf_{[B]_+\in\mathcal B_Z^+}w^+(B)
\]
as a real-algebraic number and returns a finite checkable quantifier-elimination certificate for that equality.  For every exactly encoded \(\varepsilon>0\), the normalized set
\[
\{[B]_+\in\mathcal B_Z^+:w^+(B)\le v_{\mathrm{mm}}^++\varepsilon\}
\]
is a nonempty semialgebraic set and the certificate contains a normalized real-algebraic member.  The procedure treats \(\operatorname{rank}(S_B)=0,1,2\) separately and certifies the global comparison across all ranks.  It claims neither existence of an exact minimizer, outer convergence to the possibly empty set \(\mathcal A_{\mathrm{mm}}^+\), a uniform complexity bound, nor a semidefinite-program reduction.