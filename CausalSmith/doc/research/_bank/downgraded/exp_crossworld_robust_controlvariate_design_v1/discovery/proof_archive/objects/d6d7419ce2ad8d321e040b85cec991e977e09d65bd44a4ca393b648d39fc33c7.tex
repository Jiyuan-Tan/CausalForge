\textbf{Proof.}
For \(e\in\{L,U\}\), the empirical endpoint matrix \(\widehat Q_e\) is the second-moment matrix of the signed vector obtained from the empirical comonotone or countermonotone coupling.  Therefore
\[
\widehat Q_e\succeq0.
\tag{1}
\]
Every coefficient entering \(\widehat{\mathfrak I}_{m,\mathrm{alg}}^+\) is rational or real algebraic, and exposure positivity makes \(\Pi^{-1}\) well defined.  Hence
\[
\widehat w_m^+(B)=
\max_{e\in\{L,U\}}n^{-2}\operatorname{tr}\!\left[
\left\{\Pi^{-1}\Omega\Pi^{-1}
-\Pi^{-1}\Omega B(B^{\top}\Omega B)^+B^{\top}\Omega\Pi^{-1}\right\}
\widehat Q_e\right]
\tag{2}
\]
has exactly the rank-stratified semialgebraic form treated by \(\texttt{thm:zhao-pseudoinverse-rank-stratified-epsilon-solver}\).  Applying that theorem with the empirical endpoint matrices in place of the population endpoint matrices proves finite termination and returns an algebraic basis and value-isolation record satisfying
\[
\widehat L_{m,\varepsilon_m}^{\mathrm{CAD},+}
\le\widehat v_m^+
\le\widehat U_{m,\varepsilon_m}^{\mathrm{CAD},+},
\qquad
\widehat U_{m,\varepsilon_m}^{\mathrm{CAD},+}
-\widehat L_{m,\varepsilon_m}^{\mathrm{CAD},+}
\le\varepsilon_m,
\tag{3}
\]
and
\[
\widehat w_m^+(\widehat B_{m,\varepsilon_m}^{\mathrm{CAD},+})
\le\widehat v_m^++\varepsilon_m.
\tag{4}
\]
The cited solver proof uses finite real-closed-field quantifier elimination, covers ranks zero, one, and two separately, and never evaluates the countable infimum in \(\texttt{def:pilot-plugin}\).

It remains to verify the augmented rank and spectral fields.  The entries of
\[
\widehat S_m^{\mathrm{CAD}}
=(\widehat B_{m,\varepsilon_m}^{\mathrm{CAD},+})^{\top}
\Omega\widehat B_{m,\varepsilon_m}^{\mathrm{CAD},+}
\]
are real algebraic, and this matrix is positive semidefinite.  For a positive-semidefinite \(2\times2\) matrix \(S\),
\[
\operatorname{rank}(S)=
\begin{cases}
0,&\operatorname{tr}(S)=0,\\
1,&\operatorname{tr}(S)>0\text{ and }\det(S)=0,\\
2,&\det(S)>0.
\end{cases}
\tag{5}
\]
Indeed, the two eigenvalues are nonnegative, their sum is the trace, and their product is the determinant.  Exact ordered real-algebraic arithmetic decides every sign and equality case in (5), so the selected-rank field is exact.

The eigenvalues are the roots of the displayed characteristic polynomial \(\chi_m\).  Exact square-free factorization and algebraic comparison determine the distinct roots, their multiplicities, and equality with zero.  For every positive distinct root, rational bisection with exact algebraic sign tests terminates at disjoint rational endpoints strictly inside \((0,\infty)\).  It therefore produces a rational isolating interval containing that root and no other distinct root.  These data form \(\widehat{\mathscr S}_{m,\varepsilon_m}^{\mathrm{CAD}}\); together with the CAD cell, sample, and value-isolation data they are locally checkable.  The construction uses the exact-input restriction essentially and proves no agreement with the nonconstructive countable selector, no zero-tolerance rule, no complexity bound, and no finite algorithm for arbitrary real pilot observations. \(\square\)