The exploratory record \(\mathcal J_{\mathrm{PICC}}\) is the reproducible procedure implemented by \(\texttt{discovery/jiangxi\_robust\_reanalysis.py}\).  It starts from the Cai--de Janvry--Sadoulet replication tables \(\texttt{0422survey.dta}\) and \(\texttt{0422allinforawnet.dta}\), cleans identifiers, sentinel \(99\), self-loops, and duplicate directed nominations while retaining cross-village nominations, constructs Zhao's three target populations for \(\tau_{10},\tau_{21},\tau_{32}\), and uses the disclosed independent Bernoulli \((1/2)\) reconstruction necessitated by the unreleased original strata.  It splits connected components of the exposure-overlap graph without cutting components, estimates the two marginal laws and Fr\'echet endpoints on each training fold, optimizes the robust and zero-covariance bases under the same initialization list and iteration budget, and deploys each held-out basis using def:observable-coefficient-map with the additional \(\Pi^{-1}\) factor.  It is required to report point estimates, design-randomization standard errors, both endpoint losses, worst loss, and numerical optimization gaps.  The AEA download redirected to authenticated access and returned HTTP \(403\) in this solve environment, so no corrected numerical table is asserted.  The protocol is exploratory because its internal cross-fitting is not the independent unpaired pilot of ass:unpaired-pilot.