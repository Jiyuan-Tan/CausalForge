Compute the empirical means, variances, and the two empirical quantile-coupling covariances from \(\widehat F_1,\widehat F_0\); substitute them into \(Q(c)\), \(g_B(c)\), and \(r(B)\); then set \(\widehat v=\max_{B\in\mathcal B_\kappa}\widehat r(B)\) and \(\widehat{\mathcal A}_R=\operatorname*{argmax}_{B\in\mathcal B_\kappa}\widehat r(B)\).