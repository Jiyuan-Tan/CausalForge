Given the moment-complete input \(\mathfrak I_{\mathrm{cert}}\), cover each normalized projective factor by the finite pivot-coordinate charts \(\{|b_j|\ge n^{-1/2}\}\), bisect retained boxes, handle the sphere equations and the exact \(\kappa\)-boundary through the feasibility interface, and form outward-rounded interval lower bounds for \(w(B)=\max\{\ell_B(c_L),\ell_B(c_U)\}\) together with interval-Hessian underestimators on each box. Maintain an exactly feasible incumbent giving an upper bound, prune only with a checkable infeasibility or lower-bound record, and, if the value gap and retained mesh both reach \(\varepsilon\), return \(\mathscr K_{\varepsilon}\). Termination and production of such a finite certificate for every moment-complete input are not part of this construction and remain to be proved.