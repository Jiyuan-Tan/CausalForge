Compute the empirical means, variances, and the two empirical quantile-coupling covariances from \(\widehat F_1,\widehat F_0\); substitute them into \(Q(c_L),Q(c_U)\), \(h(c)\), \(g_B(c)\), \(\ell_B(c)\), and \(w(B)\); then set \(\widehat v_{\mathrm{mm}}=\min_{B\in\mathcal B_\kappa}\widehat w(B)\) and \(\widehat{\mathcal A}_{\mathrm{mm}}=\operatorname*{argmin}_{B\in\mathcal B_\kappa}\widehat w(B)\).