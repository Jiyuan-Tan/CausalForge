The general finite-tolerance solver is instance dependent and supplies no uniform complexity bound or semidefinite-program reduction.  A fixed \(\kappa\)-class is always threshold restricted but need not be a strict subset for every design; the correct universal statement is exhaustion as \(\kappa\downarrow0\), together with exact value transfer through the rescaling quotient.  The Moore--Penrose completion is a novel comparison object, not Zhao's feasible domain.  The Jiangxi protocol uses internal cross-fitting rather than the independent unpaired pilot assumption, reports only a heuristic objective diagnostic rather than a certified optimization gap, and was not executed because the authenticated data were unavailable.