\textbf{Proof.}
If \([B]\in\mathcal B_Z\), then \(S_B\succ0\), so both columns are nonzero and \(\operatorname{rank}(S_B)=2\).  Independent nonzero rescaling is exactly the equivalence relation used by both the frozen ordinary projective class and the nonzero-column part of the published quotient.  Hence \([B]\mapsto[B]_+\) is well defined and injective.

Conversely, let \([C]_+\in\mathcal B_Z^+\) have \(\operatorname{rank}(C^{\top}\Omega C)=2\).  Its two columns are nonzero, and its normalized representative satisfies \(\lambda_{\min}(C^{\top}\Omega C)>0\).  The frozen def:zhao-basis-class therefore defines an ordinary class \([C]\in\mathcal B_Z\), and \(\iota([C])=[C]_+\).  This proves surjectivity onto the displayed rank-two stratum.

On that stratum, \(S_B^+=S_B^{-1}\).  Substitution into def:published-pseudoinverse-loss and def:gain-loss gives equality of the gains for every feasible \(c\); subtracting each from the same \(h(c)\) gives equality of the endpoint losses, and taking their maximum gives equality of \(w^+\) and \(w\). \(\square\)