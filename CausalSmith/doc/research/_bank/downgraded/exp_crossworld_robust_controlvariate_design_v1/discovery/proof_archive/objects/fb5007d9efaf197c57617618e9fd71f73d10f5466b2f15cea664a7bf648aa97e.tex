The measurable full-carrier pilot rule selects from the positive-definite rank-two stratum while benchmarking against the complete rank-zero, rank-one, and rank-two infimum.  Its proof therefore requires the minimal nonemptiness condition \(\mathcal B_Z\ne\varnothing\); exposure positivity by itself is insufficient, as a design whose exposure indicators are all functions of one Bernoulli variable can make every \(S_B\) rank at most one.  The one-sided projector argument proves equality of infima and objective regret, not cross-rank argmin convergence or attainment.  The first-order feasible-estimator bridge remains confined to the coordinate-bounded \(\kappa\)-regular ordinary-inverse class, and no such bridge is claimed for a singular class.  The generic certified solvers have instance-dependent finite termination but no uniform complexity or semidefinite reduction.  The Jiangxi PICC reconstruction uses internal splitting and a self-normalized heuristic implementation, not the independent external-pilot model, and authenticated replication data were unavailable, so no numerical reanalysis is asserted.