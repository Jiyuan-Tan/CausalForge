The certified solvers are finite and instance dependent.  The fixed-threshold interval procedure requires moment-complete exact algebraic data or effective inclusion oracles with exact feasibility decisions, and the full-carrier procedure uses rank-stratified cylindrical algebraic decomposition; neither result gives a uniform complexity bound or a semidefinite-program reduction.  The full-carrier infimum need not have an exact minimizer, so its pilot theorem gives value consistency and vanishing epsilon regret rather than outer argmin or point convergence.  The observable first-order variance bridge is proved only on a coordinate-bounded, \(\kappa\)-regular rank-two class under the stated dependency, exposure-rate, pilot-independence, and sensitivity conditions; its zero-surrogate first-order comparison additionally requires the displayed deterministic linear error bound.  The Jiangxi protocol uses local Adam optimization and a self-normalized coefficient, does not certify global optimality or an exact design-randomization standard error, was not executed without authenticated replication data, and is not covered by the independent-external-pilot bridge.