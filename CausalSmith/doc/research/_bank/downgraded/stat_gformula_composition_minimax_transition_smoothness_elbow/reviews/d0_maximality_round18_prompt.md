You are the mandatory fresh post-auxiliary-law-completion whole-paper maximality auditor for CausalSmith. Read-only only: do not edit, spawn subagents, or run the pipeline. Work in /<repo-root>/CausalSmith.

Read the complete current artifacts, reviews, pipeline log, escalation log, and decision log for stat_gformula_composition_minimax. Round 17 passed the repaired state-consistency logic, rough-subclass mathematics, full likelihood channels, lower bound, comparator scope, source synchronization, and render, but returned one bounded IMPROVE: complete the auxiliary Robins full-data law by specifying Y and the monotone R1 convention.

Round 22 now does exactly that for each P in M_rough:

- K_P(dy|l0,l1) is the observed treated-outcome regular conditional law P(Y in dy | H1=l0,H2=l1,A1=A2=1).
- The auxiliary full data are (L0,L1,Y^MAR), with joint state density p1 g and outcome kernel K_P.
- R0 has probability pi1(L0); R1=0 when R0=0; conditional on R0=1, R1 has probability pi2(L1), independently of Y^MAR given (L0,L1).
- The two MAR identities are explicit, and the auxiliary observed-data coarsening satisfies
  (R0,L0,R0L1,R1,R0R1Y^MAR) =_d (A1,H1,A1H2,A1A2,A1A2Y).
- Hence L1=H2 and R1=A2 only on A1=1, and the outcome kernel agrees only on A1=A2=1. The paper explicitly does not identify L1 with H2(1) or add state consistency to the broad causal model.
- The c2 noncoverage proof remains solely f(L0,L1)=p1g=g0+r with its proved nonremovable outgoing jump.

Reaudit the full paper: completeness and validity of this auxiliary law and equality in distribution; fixed n-independent rough subclass; full A1=0/A1=1 likelihood channels and n^{-r1} floor; exact narrow Robins condition-(c2) comparison; honest unavailable-BKK policy; all prior theorem mathematics; explicitly open matched frontier/iff elbow; core/proto byte equality; exact working snapshots/transitive assumptions/reverse edges; serializer correctness; and clean two-pass 31-page PDF.

Decide whether any truthful finite same-assumption load-bearing improvement remains before D0.5. Do not request cosmetics, model strengthening, invented comparator facts, closure of the open higher-order frontier, or causal identification of the auxiliary L1. If a real bounded defect remains, state exactly one repair and all files.

Use gpt-5.6-sol medium. End exactly:

MAXIMAL: <one concise reason>

or

IMPROVE: <one concrete bounded repair>
