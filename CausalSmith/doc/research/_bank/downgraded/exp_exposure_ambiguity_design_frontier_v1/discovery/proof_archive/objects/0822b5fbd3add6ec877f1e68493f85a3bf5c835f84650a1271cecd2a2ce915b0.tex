For fixed \(\bar p\in(0,1)\), every finite program and asymptotic sequence is indexed by \(n\in\mathcal N_{\bar p}\). The menu is finite with fixed cardinality, response arrays are arbitrary in \([-1,1]\), assignments obey one exact treated-count budget, and the estimator observes one realization. No graph locality, bounded degree, smoothness, linear response, common representer, endpoint support, assignment-level positivity, or estimator restriction is imposed. Accordingly the exact unrestricted frontier is indexed by \((n,\mathcal E,\bar p)\); \(G_n\) is ambient organizational information once the menu is fixed. Graph-local subclasses may use \(G_n\) to generate \(\mathcal E(G_n)\), but this is menu mediation, not a restored primitive graph argument. The collision sandwich gives opposite-side sufficient and necessary certificates, not an exhaustive classification. The persistent separation is engineered and nonlocal; polynomial compression and natural graph-local separation remain open.