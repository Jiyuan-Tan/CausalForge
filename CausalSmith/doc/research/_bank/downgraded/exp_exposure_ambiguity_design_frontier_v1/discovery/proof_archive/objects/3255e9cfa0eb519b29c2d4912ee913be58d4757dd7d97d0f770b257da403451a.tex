For every admissible exact-budget instance, the trivial group and one extremal pair per positive-diameter observation fiber give a fractional diameter cover with congestion at most \(|\Omega_n|\). Therefore
\[
 \inf_\pi U_n(\pi)
 \le |\Omega_n|^{1/2}\inf_\pi C_n(\pi),
\]
and
\[
 \frac12\inf_\pi C_n(\pi)
 \le L_n(G_n,\mathcal E,\bar p)
 \le \frac12|\Omega_n|^{1/2}\inf_\pi C_n(\pi).
\]
Under any supplied equivariant action and fractional cover with polynomial orbit tables and congestion \(\kappa_n\), \(\mathrm{thm{:}orbit\text{-}fractional\text{-}cover\text{-}compression}\) gives the exact quotient linear program, an explicit invariant design and midpoint estimator, and the sharper \(\sqrt{\kappa_n}\)-factor bracket. The unrestricted nonlocal construction in \(\mathrm{thm{:}persistent\text{-}nonrepresentability}\) also proves that mapping-wise labeled conflict-graph multisets do not determine the frontier without a locality restriction.