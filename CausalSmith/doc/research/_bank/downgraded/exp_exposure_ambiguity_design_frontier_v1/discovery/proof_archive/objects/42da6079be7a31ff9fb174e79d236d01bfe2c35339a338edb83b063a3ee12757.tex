The strongest unconditional result is the exact finite second-order-cone saddle together with
\[
 \frac12\inf_\pi C_n(\pi)
 \le L_n(G_n,\mathcal E,\bar p)
 \le\frac12\inf_\pi U_n(\pi).
\]
Whenever \(\inf_\pi U_n(\pi)\le K\inf_\pi C_n(\pi)\) with fixed \(K\), either optimized modulus is a factor-\(K\) certificate.  The nonlocal solved menu has \(C_n(\pi)=U_n(\pi)=2\) and \(V_n^\star=1\) for every design, so both universal constants are sharp.  The generalized persistent common-selector family has risks \(1\) and \(5/4\) along all sufficiently large admissible even sizes for every fixed \(\bar p\in(0,1)\), while preserving the mapping-wise conflict-graph multiset.  A general structural or polynomial-size compression and a natural local separation remain unresolved.