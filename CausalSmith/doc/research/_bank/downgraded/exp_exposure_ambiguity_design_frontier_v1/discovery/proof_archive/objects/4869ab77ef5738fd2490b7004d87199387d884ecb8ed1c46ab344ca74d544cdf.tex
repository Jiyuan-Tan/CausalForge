Prior design-based work either fixes a single exposure model, restricts the estimator or response law, studies specification testing or partial identification, or takes the assignment mechanism as fixed or externally supplied. None of the comparators supplies the present exact-count, design-inclusive minimax risk for the all-treated-versus-all-control causal contrast over an unknown finite union of arbitrary bounded exposure-measurable tables and arbitrary measurable estimators. The structural gap is now narrower than a blanket computational-open claim: an unconditional joint-signature reduction gives an exact collision linear program and a universal congestion-\(R_n\) cover with \(R_n\le|\Omega_n|\), while the remaining gap is to find broad primitive menu or graph conditions forcing small \(R_n\), bounded cover congestion, or another polynomial compression, especially on bounded-degree or sparse-natural graph-local families.