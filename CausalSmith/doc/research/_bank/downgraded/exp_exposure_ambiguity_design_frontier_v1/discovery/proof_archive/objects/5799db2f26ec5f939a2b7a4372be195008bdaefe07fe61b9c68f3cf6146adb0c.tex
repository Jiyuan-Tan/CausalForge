Fix \(\bar p\in(0,1)\) and \(n\in\mathcal N_{\bar p}\). For the graph-local two-mapping menu \(\mathcal E^{\mathrm{loc}}\), the finite primal and dual solve to \(L_n(G_n,\mathcal E^{\mathrm{loc}},\bar p)=1/n\). The estimator in \(\mathrm{def{:}local\text{-}solved\text{-}menu}\) is optimal under every exact-budget design. A dual certificate places independent symmetric sign mass on the two response coordinates of unit \(1\), so whichever value is observed, the other contributes posterior target variance \(1/n^2\).