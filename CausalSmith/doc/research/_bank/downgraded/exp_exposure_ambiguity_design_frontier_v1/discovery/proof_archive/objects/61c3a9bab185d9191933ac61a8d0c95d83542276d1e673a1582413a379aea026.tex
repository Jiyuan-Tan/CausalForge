Finite exposure ranges permit a lossless sign reduction, but the latent union makes cross-model observation collisions essential. The note treats the assignment distribution and unrestricted decision rule jointly, distinguishes the causal estimand \(\theta_n(y)\) from its minimax design risk \(L_n(\mathcal E,\bar p)\), and organizes computability through the exact sign experiment. Its structural niche is the joint exposure signature: collapsing identical assignment columns yields an unconditional \(R_n\)-variable collision program, while fractional diameter covers translate collision geometry into actionable upper rules.