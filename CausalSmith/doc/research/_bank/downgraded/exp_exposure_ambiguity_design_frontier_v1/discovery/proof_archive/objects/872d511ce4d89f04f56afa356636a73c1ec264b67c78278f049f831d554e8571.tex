Fix \(\bar p\in(0,1)\) and \(n\in\mathcal N_{\bar p}\). For the singleton menu \(\mathcal E^{\mathrm{NI}}\), complete randomization and difference in means satisfy \(\sup_{y\in\mathcal Y_n(\mathcal E^{\mathrm{NI}})}\operatorname{RMSE}_n(\Theta_n^{\mathrm{CR}},\widehat\theta_n^{\mathrm{DM}};y)\le\{n(n-1)^{-1}[m_n^{-1}+(n-m_n)^{-1}]\}^{1/2}\). Along \(n\in\mathcal N_{\bar p}\) with \(n\to\infty\), the finite-program value obeys \(L_n(G_n,\mathcal E^{\mathrm{NI}},\bar p)\asymp n^{-1/2}\).