Finite exposure ranges permit an exact sign-schedule reduction of arbitrary bounded responses and arbitrary measurable estimators. Cross-model observation fibers, rather than the ambient graph or candidate-wise summaries separately, are the operative finite geometry. Graph structure can enter a subclass by generating or organizing the menu whose sign experiment is then optimized.