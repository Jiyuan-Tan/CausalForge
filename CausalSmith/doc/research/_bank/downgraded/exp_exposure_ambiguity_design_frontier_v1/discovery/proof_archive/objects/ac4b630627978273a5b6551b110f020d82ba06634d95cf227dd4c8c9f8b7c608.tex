The experimenter can enumerate the menu-induced finite decision experiment, solve \(\mathsf P_n\), and implement its recovered exact-count randomization and multilinear estimator. The dual supplies a matching least-favourable-prior certificate. If a graph-local scientific model is desired, the analyst first uses \(G_n\) to specify \(\mathcal E(G_n)\); all subsequent risk and design effects pass through that menu. The local menu with frontier \(1/n\) and the nonlocal menu with frontier \(1\) make the remediable and design-inert regimes nonempty, but do not exhaust all sequences.