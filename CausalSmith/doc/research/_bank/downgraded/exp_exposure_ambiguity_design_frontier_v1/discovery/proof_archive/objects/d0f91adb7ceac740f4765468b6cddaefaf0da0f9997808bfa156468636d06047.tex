Fix \(\bar p\in(0,1)\) and \(n\in\mathcal N_{\bar p}\). One assignment \(Z\sim\Theta\) is drawn, where the experimenter chooses any \(\Theta\in\mathcal D_n(\bar p)\). The unrestricted design problem is indexed by \((n,\mathcal E,\bar p)\), not by the ambient graph.