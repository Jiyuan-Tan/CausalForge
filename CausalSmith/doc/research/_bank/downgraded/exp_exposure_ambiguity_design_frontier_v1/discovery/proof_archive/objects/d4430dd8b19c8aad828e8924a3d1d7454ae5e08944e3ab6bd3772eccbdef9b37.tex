The finite primal and least-favourable-prior dual compute the exact minimax root mean squared error and recover an attaining exact-budget design and lossless multilinear estimator. The collision sandwich supplies explicit necessary and sufficient certificates on opposite sides of asymptotic remediability. The joint-signature theorem strengthens this with an exact linear program, a universal congestion-\(R_n\) cover, and polynomial-time coefficient computation when \(R_n=O(\log n)\), the menu size is fixed, and exposure incidence is polynomially encoded. The no-interference boundary proves that graph locality and polynomial orbit tables alone do not bound congestion. The dense radius-one split-graph witness proves that even two signatures and a congestion-two cover do not make mapping-wise labeled conflict graphs sufficient: the exact frontiers remain \(1\) and \(5/4\). Classical finite minimax--maximin equality is acknowledged rather than claimed as novel; the contribution is its causal finite-menu reduction, computable saddle, recovered design--rule pair, and the sharp positive and negative compression boundaries.