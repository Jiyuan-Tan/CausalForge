The project gives an explicit numerical zero radius and translated-dyadic bank, proves localized shift factorization and a zero-free winding identity, replaces the unsafe global pilot by a fixed-reference localized winding test, derives selector, bias, Bahadur, multiplier, fallback, and moment controls, and obtains both an honest \(O(r_n)\) band and a matching lower bound over every measurable honest band.