At fixed separation the matched benchmark is \(r_n=(\log n/n)^{\beta/(2\beta+1)}\). For the natural class obtained by replacing fixed separation with \(|\kappa_k(\eta)|\ge\delta_n\), the exact-code many-bump subclass still gives a lower bound \(cr_n\), while the deterministic full-range band gives only the unconditional upper bound \(2C_\tau\).