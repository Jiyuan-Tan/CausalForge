The project reuses the accepted numerical zero radius and explicit translated-dyadic bank, proves the localized shift factorization and zero-free winding identity, replaces the unsafe global pilot by a fixed-reference localized winding test, derives selector, bias, Bahadur, multiplier, fallback, and moment controls, and obtains both an honest \(O(r_n)\) band and a matching lower bound over every measurable honest band.