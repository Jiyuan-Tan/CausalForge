A law-blind translated-dyadic circle bank is constructed explicitly from the fixed cumulant order, separation, and treatment-noise scale. The admissible fixed-code formulation contains the former direct conditional-\(L^1\) ball and allows the discrepancy \(g_0-\bar g_n\) to contain a persistent bounded H\"older modifier-only shift plus a conditionally small remainder. Local factorization removes that shift from denominator winding, a reference-localized pilot selects a contour even when the global contaminated transform has zeros, and the resulting no-outcome-learner band has uniform simultaneous coverage and minimax expected sup-width \((\log n/n)^{\beta/(2\beta+1)}\).