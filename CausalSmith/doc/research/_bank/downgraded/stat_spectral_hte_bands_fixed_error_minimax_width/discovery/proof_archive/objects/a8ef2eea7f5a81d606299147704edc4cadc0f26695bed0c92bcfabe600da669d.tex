Uniform deterministic bias is proved with the explicit constant \(B_{\mathrm{bias}}\), and the bandwidth algebra yields the advertised rate once the corrected stochastic moment and fallback bounds are supplied.