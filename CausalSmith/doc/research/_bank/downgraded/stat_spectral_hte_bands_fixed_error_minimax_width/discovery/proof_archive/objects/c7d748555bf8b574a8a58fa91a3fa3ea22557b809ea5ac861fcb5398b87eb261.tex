A law-blind translated-dyadic circle bank is now constructed explicitly from the fixed cumulant order, separation, and treatment-noise scale, using the accepted scalar anchor's numerical zero radius and dyadic boundary certificate.  The admissible fixed external-code class is enlarged: \(g_0-\bar g_n\) may contain a persistent bounded Hölder modifier-only shift plus a conditionally small \(L^1\) remainder.  Local factorization removes that shift from the denominator winding, a reference-localized pilot selects the contour even when the global contaminated transform has zeros, and the resulting no-outcome-learner band has uniform simultaneous coverage and minimax expected sup-width \((\log n/n)^{\beta/(2\beta+1)}\) over the enlarged pair class.