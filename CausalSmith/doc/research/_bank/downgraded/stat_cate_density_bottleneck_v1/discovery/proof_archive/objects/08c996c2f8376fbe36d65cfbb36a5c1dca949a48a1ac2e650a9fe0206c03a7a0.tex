I optimized the displayed \(h_n,k_n\) bound, which only reproduces its existing logarithmic inflation; swapped \(\alpha\) and \(\beta\), which is invalid because propensity and outcome roles in the \(R\)-normal equations are asymmetric; and used polynomial equivalence, which by definition cannot decide a logarithmic loss.  The same-cell collision audit of the lower handle also shows that a simple unequal-resolution Rademacher modification is insufficient without new moment matching.