The declared results yield only
\[
c n^{-q}\leq R_n^{\mathrm{band}}
\leq C n^{-t}=C r_n^{\mathrm{KBRW}},
\qquad t<q,
\]
on \(\mathcal R_{\mathrm{pair}}\).  The oracle lower theorem fixes the density
and therefore cannot supply the missing \(n^{-t}\) converse.  The candidate
density-coupled handle asserts no model membership, target separation, or
mixture-affinity bound, and the KBRW support-hole construction is not a member
of a model with \(f_P\geq f_->0\).  On the remainder of
\(\mathcal R_{\mathrm{low}}\), the rank-capped upper theorem retains an unmatched
logarithmic factor.  No frozen result closes any of these gaps.