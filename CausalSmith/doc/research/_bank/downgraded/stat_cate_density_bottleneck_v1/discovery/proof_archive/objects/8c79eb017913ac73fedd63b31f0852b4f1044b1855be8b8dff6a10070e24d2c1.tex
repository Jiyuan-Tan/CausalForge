If \(\pi_P\) and \(\mu_{a,P}\) are known, then the nuisance stage in \(\widehat{\tau}^{\mathrm{cap}}_n(x_0)\) vanishes and the estimator reduces to ordinary local-polynomial regression of an oracle unbiased CATE score. Its mean-absolute error is \(O(n^{-\gamma/(2\gamma+d)})\).