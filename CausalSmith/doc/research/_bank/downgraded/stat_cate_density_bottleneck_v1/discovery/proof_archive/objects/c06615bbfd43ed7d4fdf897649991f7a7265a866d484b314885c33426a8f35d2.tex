Construct two explicit priors supported on \(\mathcal P_{\mathrm{band},n}\) whose point targets are separated by order \(n^{-(\alpha+\beta)/d}\) while the Hellinger distance between their \(n\)-sample mixtures stays bounded away from its maximal value, with every covariate density normalized and satisfying the same fixed lower bound \(f_->0\).