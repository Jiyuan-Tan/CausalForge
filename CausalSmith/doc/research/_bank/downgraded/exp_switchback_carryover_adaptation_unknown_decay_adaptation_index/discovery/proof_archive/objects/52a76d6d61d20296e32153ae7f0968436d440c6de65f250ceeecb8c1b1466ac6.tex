Uniformly over \(\beta\in[\beta_-,\beta_+]\), the proved oracle bracket is \(B/\sqrt{3T}\le R_T(\beta;C)\le C_0T^{-\beta/(2\beta+1)}\), with an explicit beta-aware Markov switchback attaining the upper bound. Since every procedure's worst-case risk at each \(\beta\) is at least \(R_T(\beta;C)>0\), the definition of the adaptation index gives \(A_T(C)\ge1\). No divergence beyond one is proved.