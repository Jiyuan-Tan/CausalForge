The proved results are \(B/\sqrt{3T}\le R_T(\beta;C)\le C_0T^{-\beta/(2\beta+1)}\), an explicit beta-aware Markov switchback attaining the upper bound, endpoint-supported fixed-before-assignment point priors with normalized Bayes risk at least \(1-\varepsilon\) for every fixed procedure, and hence \(A_T(C)\ge1\). The strict finite-order inclusion and both explicit witnesses are also proved. No divergence beyond one follows.