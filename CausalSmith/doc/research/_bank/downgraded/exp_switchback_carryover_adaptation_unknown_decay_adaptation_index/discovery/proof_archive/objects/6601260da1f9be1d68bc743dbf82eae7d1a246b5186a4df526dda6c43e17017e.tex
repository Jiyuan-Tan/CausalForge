The strongest unconditional conclusion remains the exact computable bracket
+\[
+1\le A_T(C)\le\frac{N_T^{\mathrm{fin}}}{D_T^{\mathrm{bin}}}\le\frac{\sqrt{2}\,C_0}{B}T^{1/(4\beta_-+2)},
+\]
+where \(N_T^{\mathrm{fin}}\) and \(D_T^{\mathrm{bin}}\) are the finite numerator and exact-binomial oracle-risk lower denominator displayed in the question. Thus the presently certified two-class factor is only \(g_T=1\), uniformly over deterministic, history-dependent, and outcome-adaptive assignment kernels. In addition, if the rough-prior energy obeys
+\[
+\mathbb E_{P_{1,\pi}}\sum_{t=2b}^Tq_t(W_{1:t})^2\le\frac{3a^2}{64},
+\]
+then every estimator has rough-class risk at least \(s_b/25\). When \(\eta=1/2\) satisfies the amplitude condition,
+\[
+C2^{-(2+\beta)}b^{-\beta}\le s_b\le\frac{C}{2}b^{-\beta}.
+\]
+For every arm \(\alpha\) and \(1\le K<T\), the persistence construction separately gives
+\[
+\sup_{Y\in\mathcal P_{T,\beta}(C)}\mathbb E_\pi\left|\widehat\theta-\operatorname{GATE}_T(Y)\right|
+\ge\frac{(T-K)\delta_K}{2T}\left\{1-Q_\pi(E_{\alpha,K})\right\}.
+\]
+These results prove neither a divergent lower factor nor a subpolynomial common-procedure ratio; (K1) and (K2) remain logically compatible.