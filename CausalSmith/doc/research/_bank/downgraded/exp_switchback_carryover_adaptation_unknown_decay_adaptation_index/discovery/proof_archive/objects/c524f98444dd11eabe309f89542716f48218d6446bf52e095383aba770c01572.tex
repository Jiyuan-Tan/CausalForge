For \(U_{T,\beta}(b)=B\sqrt{V_{T,b}}/T+(2C/T)\sum_{\ell=b+1}^{T-1}(T-\ell)(1+\ell)^{-(1+\beta)}\), the oracle satisfies
\[
R_T(\beta;C)\le\min\left\{2B,\min_{1\le b\le T}U_{T,\beta}(b)\right\}
\le\left(\sqrt{20e+4}\,B+\frac{2C}{\beta}\right)T^{-\beta/(2\beta+1)}.
\]
With
\[
N_T^\star=\min\left\{2B,\min_{1\le b\le T}U_{T,\beta_-}(b)\right\},
\qquad
D_T^{\mathrm{bin}}=B2^{-(T-1)}\binom{T-1}{\lfloor(T-1)/2\rfloor},
\]
one common procedure fixed before assignment using \(\beta_-\) only satisfies
\[
1\le A_T(C)\le\frac{N_T^\star}{D_T^{\mathrm{bin}}}
\le\frac{N_T^{\mathrm{fin}}}{D_T^{\mathrm{bin}}}
\le\frac{\sqrt2\,C_0}{B}T^{1/(4\beta_-+2)}.
\]
Thus the certified two-class factor remains \(g_T=1\). Under the rough-prior low-energy condition, rough-class risk is at least \(s_b/25\); separately, the endpoint persistence construction gives
\[
\sup_{Y\in\mathcal P_{T,\beta}(C)}\mathbb E_\pi|\widehat\theta-\operatorname{GATE}_T(Y)|
\ge\frac{(T-K)\delta_K}{2T}\{1-Q_\pi(E_{\alpha,K})\}.
\]
These facts prove neither K1 nor K2, which remain logically compatible.