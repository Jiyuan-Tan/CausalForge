\textbf{Proof.} The lower bound is the positive-oracle-risk lemma. Its independent Rademacher direct-effect prior is supported on the no-carryover subclass of every \(\mathcal P_{T,\beta}(C)\), is sampled before assignment, and leaves one independent counterfactual sign hidden at every time under every sequential policy. Hence
\[
R_T(\beta;C)\ge \frac{B}{\sqrt{3T}}.
\]
This argument includes deterministic, history-dependent, and outcome-adaptive kernels and uses no positivity condition.

For the upper bound, put
\[
b_\beta=\left\lceil T^{1/(2\beta+1)}\right\rceil.
\]
For \(T=1\), one has \(b_\beta=1\). For integer \(T\ge2\), \(1<T^{1/(2\beta+1)}<T\), so again \(1\le b_\beta\le T\). Moreover,
\[
b_\beta\le T^{1/(2\beta+1)}+1\le2T^{1/(2\beta+1)}.
\]
Apply the Markov-washout oracle upper-bound lemma with \(b=b_\beta\). Its explicit stationary Markov switchback kernel belongs to \(\mathcal D_T\), every homogeneous-exposure denominator is positive, and its observed-data estimator satisfies
\[
\sup_{Y\in\mathcal P_{T,\beta}(C)}
\mathbb E_{\pi^{(b_\beta)}}
\left|\widehat\vartheta_{b_\beta}-\operatorname{GATE}_T(Y)\right|
\le 32B\sqrt{\frac{b_\beta}{T}}
+\frac{8C}{\beta_-}b_\beta^{-\beta}.
\]
The ceiling bounds imply
\[
\sqrt{\frac{b_\beta}{T}}
\le\sqrt2\,T^{-\beta/(2\beta+1)},
\qquad
b_\beta^{-\beta}\le T^{-\beta/(2\beta+1)}.
\]
Consequently,
\[
\sup_{Y\in\mathcal P_{T,\beta}(C)}
\mathbb E_{\pi^{(b_\beta)}}
\left|\widehat\vartheta_{b_\beta}-\operatorname{GATE}_T(Y)\right|
\le
\left(32\sqrt2 B+\frac{8C}{\beta_-}\right)
T^{-\beta/(2\beta+1)}.
\]
Taking the infimum over all designs and estimators gives the asserted upper bound with \(C_0=32\sqrt2 B+8C/\beta_-\), uniformly over \(\beta\in[\beta_-,\beta_+]\). The lower and upper exponents are different; no matching lower bound at the upper exponent is claimed. \(\square\)