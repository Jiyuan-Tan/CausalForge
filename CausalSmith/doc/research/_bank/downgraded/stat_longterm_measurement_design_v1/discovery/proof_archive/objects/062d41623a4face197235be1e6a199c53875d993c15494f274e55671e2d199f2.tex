Fix \(P\in\mathfrak P_{\mathcal D}^{\mathrm{obs}}\). Positive unit costs and Assumption~\(\mathrm{ass{:}nondegenerate\text{-}components}\) imply
\[
 0<\mathcal V_d^*(P)
 =\left(\sqrt{c_{E,d}V_{E,d}}+\sqrt{c_{O,d}V_{O,d}}\right)^2
 <\infty
\]
for every \(d\). Source-arm positivity likewise makes every IKM-normalized component finite and strictly positive, because \(0<1-p_{g,1}<1\). Since \(\mathcal D\) is finite and nonempty, \(\mathcal D_{\mathrm{CLW}}(P;B)\) is nonempty. Substitution into Definition~\(\mathrm{def{:}clw\text{-}linear\text{-}cost\text{-}selector}\) proves (1)--(2). In each case the numerator minimizes over a nonempty subset of the denominator's index set, giving a ratio at least one, while positivity and finiteness give a finite ratio. Dividing the selected inequality by the positive global minimum shows that the displayed ratio is its smallest constant.

For either positive profile \(w_d\), set
\[
 C_{\mathrm{CLW}}(P;B)
 =\max_{\substack{d,d'\in\mathcal D:\,
 \Psi_{\mathrm{CLW},d}(P;B)\le\Psi_{\mathrm{CLW},d'}(P;B)}}
 \frac{w_d}{w_{d'}}.
\]
The index set contains every diagonal pair and is finite. Any constant satisfying all CLW-order implications must dominate every indexed ratio, hence their maximum. Conversely, the maximum itself satisfies every implication. This proves (3) and makes the audit computable by at most \(|\mathcal D|^2\) comparisons after evaluating the finite list of risks and bridge values.

The generic lawwise identities follow from the two cited frozen theorems and are secondary here. The flagship named-workflow theorem proves (4), while the sharp-elbow and treatment-aware-elbow lemmas prove equality of all six center ratios and the expansion (5). Therefore no law-independent finite constant can bound the named CLW selector's bridge-design loss on the regular common-identification region, despite exact enumeration at every fixed law. This completes the proof.