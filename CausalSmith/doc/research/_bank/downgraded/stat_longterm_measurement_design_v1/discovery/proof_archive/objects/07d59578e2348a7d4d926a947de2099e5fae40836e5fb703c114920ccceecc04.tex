The actionable rule is to estimate each admissible design's two source-gradient variances from an independent full-catalogue pilot, insert the known costs, minimize the profiled value, and buy the two sources at the estimated square-root allocation. The exact curvature identity explains why first-order pilot variance error produces only second-order allocation loss; the separate floor remainder quantifies the price of integer purchasing. Prediction risk is not a defensible surrogate for the design value: a weak early proxy can be nearly irrelevant for predicting \(Y\) once a strong late proxy is present, yet its inverse selection bridge inflates the observational influence variance by order \(\epsilon^{-2}\). The failure survives uniformly over all finite \(\chi>1\), even though prediction selects the design that costs more in each source, and its sharp first-order worst-cost elbow is \(1/726\). On the full regular class the rule optimizes inference for the observed bridge contrast; it optimizes causal inference for \(\tau\) only on a common-identification subclass, with the IKM-normalized profile supplying the absolute causal-efficiency scale under the cited fixed-system premises.