At every binary center, \(p_{E,1}=p_{O,1}=1/2\).  The definitions immediately give
\[
 \psi_{E,d}^{\mathrm{IKM}}=\frac12\phi_{E,d},
 \qquad \psi_{O,d}^{\mathrm{IKM}}=\frac12\phi_{O,d},
 \qquad V_{E,d}^{\mathrm{IKM}}=\frac14V_{E,d},
 \qquad V_{O,d}^{\mathrm{IKM}}=\frac14V_{O,d}.                               \tag{1}
\]
Profiling multiplies every design value by one quarter.  Lemma~\(\mathrm{lem{:}sharp\text{-}uniform\text{-}binary\text{-}two\text{-}risk\text{-}elbow}\) shows that \(d_{\mathrm e}\) uniquely minimizes the arm-normalized profile, while both prediction rules uniquely select \(d_{\mathrm p}\).  The common factor cancels from each ratio, proving the center assertions.

Restrict first to the common regularity radius in Lemma~\(\mathrm{lem{:}binary\text{-}witness\text{-}two\text{-}risk\text{-}stability}\).  Within that finite-cell neighborhood, treatment probabilities, bridge coefficients, source moments, profiled values, and both prediction risks are continuous.  In particular,
\[
 V_{E,d}^{\mathrm{IKM}}=(1-p_{E,1})^2V_{E,d},
 \qquad V_{O,d}^{\mathrm{IKM}}=(1-p_{O,1})^2V_{O,d}
\]
are continuous.  The two prediction gaps and the normalized-profile optimizer gap are strict at the center, and both normalized center ratios are positive.  A further common radius reduction therefore preserves regularity, common identification, both prediction selectors, the normalized optimizer, and both half-ratio bounds.  Under the fixed-system causal-identification, local-model, canonical-gradient, and source-ratio premises recorded in Lemma~\(\mathrm{lem{:}ikmw\text{-}fixed\text{-}system\text{-}efficiency}\), that lemma identifies the unprofiled IKM variance as the local causal efficiency bound.  The positive two-source profiling identity from Theorem~\(\mathrm{thm{:}budget\text{-}profile}\) gives the stated conditional interpretation.