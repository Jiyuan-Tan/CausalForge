The impossibility concerns two precisely defined generic population squared-risk rules and their feasible saturated finite-cell implementation on the binary witness.  It does not claim that every predictive or surrogate-selection method fails, and it attributes neither rule to a published workflow.  The unbounded ratios hold on relatively open subsets of the constrained finite-cell model, not the ambient unrestricted simplex.  The arm-normalized profile is a source-score design value on the observable regular class and a gradient variance only along declared bridge-differentiable paths.  Absolute causal semiparametric efficiency is asserted only for the separately normalized IKM profile under the fixed-system canonical-gradient premises.  The general post-selection Wald theorem remains conditional on its declared learner, product-rate, multiplier, and variance conditions; primitive verification is supplied only on the stated fixed-dimensional subclass.