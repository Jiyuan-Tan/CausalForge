Lemma~\(\mathrm{lem{:}sharp\text{-}uniform\text{-}binary\text{-}elbow}\) proves at every center the normal equations for the CLW affine linear residual, the strict criterion chain, the source-score values, and the exact common arm-normalized and IKM-normalized ratio.  It therefore proves (1)--(7), including the eventual floor-budget choice.  Notice that this use is legitimate because the lemma derives \(r_d^{\mathrm{lin}}\) directly; it uses equality with \(R_d\) only at the symmetric centers.

For the open-region assertion, use the radii supplied by Lemma~\(\mathrm{lem{:}binary\text{-}witness\text{-}stability}\).  That lemma proves directly from
\[
 r_d^{\mathrm{lin}}(P)
 =\mathbb E_P[Y^2\mid G=O]
 -g_d(P)^\top\Gamma_d(P)^{-1}g_d(P)
\]
that the CLW criterion and its strict ordering are continuous throughout each smaller finite-cell ball.  Thus every member is CLW-admissible and regular, identifies the common contrast, and satisfies the first two assertions in (8).  Every center belongs to its defining ball, and the center ratio is bounded below by \((\epsilon^{-2}+14)/726\).  Letting \(\epsilon\downarrow0\) proves the supremum in (8).

On the IKM-normalized balls, Lemma~\(\mathrm{lem{:}binary\text{-}ikmw\text{-}normalized\text{-}stability}\) preserves the strict IKM optimizer and at least half the center ratio.  Intersecting its radius with the CLW-stability radius preserves the named selector as well.  Again the centers belong to the balls and their ratios diverge, proving (9).  Lemma~\(\mathrm{lem{:}ikmw\text{-}fixed\text{-}system\text{-}efficiency}\) supplies the absolute local causal-efficiency interpretation only after its fixed-system premises have been verified; the source-score statements themselves do not require that interpretation.

For a fixed law in either region, eventual agreement of the integer selector is the last conclusion of the binary stability lemma.  The generic unrestricted-risk center statements follow from the same sharp-elbow calculation, but no step substitutes \(R_d\) for \(r_d^{\mathrm{lin}}\) on a perturbation.

Finally, the prescribed primitive costs satisfy all cap constraints, their paired-cost ratio is \(5\chi/5=\chi\), and the sourcewise cost gaps equal \(5(\chi-1)/2>0\).  The cap-feasible cost manifold, the strict CLW criterion gap, and both bridge-value gaps vary continuously.  Hence a relatively open cost-manifold neighborhood preserves these strict inequalities for sufficiently small positive \(\epsilon\).  The construction transports only the population linear residual criterion and maps its support to \(M_d\); the qualifications in the statement therefore follow exactly from the objects proved and complete the proof.