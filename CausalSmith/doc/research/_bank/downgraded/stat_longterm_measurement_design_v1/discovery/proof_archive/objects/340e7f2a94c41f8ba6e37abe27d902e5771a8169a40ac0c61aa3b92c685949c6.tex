Fix \(\chi>1\).  The corrected binary stability lemma permits the radii in \(\mathcal U_{\mathrm{bin}}\) to be chosen so that every member is CLW-admissible and regular, identifies \(\tau\), and has the named CLW selector and arm-normalized optimizer shown in (1).  Lemma~\(\mathrm{lem{:}joint\text{-}predictive\text{-}binary\text{-}stability}\) shrinks the same radii while preserving those conclusions and adding both generic prediction-risk selector identities.  It follows also from the binary stability lemma that the floor-budget CLW selector agrees for every fixed law once \(B\) exceeds a finite law-specific threshold.

Every center \(P_{\chi,\epsilon}\) belongs to its ball.  At a center, Lemma~\(\mathrm{lem{:}sharp\text{-}uniform\text{-}binary\text{-}elbow}\) proves equality of the two CLW ratios and the two unadjusted generic ratios, and Lemma~\(\mathrm{lem{:}treatment\text{-}aware\text{-}sharp\text{-}binary\text{-}elbow}\) adds equality of both treatment-aware ratios.  Their common value exceeds \((\epsilon^{-2}+14)/726\).  Letting \(\epsilon\downarrow0\) proves all three arm-normalized suprema in (2).

On \(\mathcal U_{\mathrm{bin}}^{\mathrm{IKM}}\), Lemma~\(\mathrm{lem{:}binary\text{-}ikmw\text{-}normalized\text{-}stability}\) preserves the strict IKM optimizer, and joint stability preserves both generic rules while the corrected binary radius preserves CLW.  Each ratio on the ball exceeds half its diverging center value, proving (3).

The exact center formula from the sharp-elbow lemma gives (4).  Direct subtraction yields
\[
 \frac{5\chi+1}{363(10\chi-3)}-\frac1{726}
 =\frac5{726(10\chi-3)}>0,
\]
so \(1/726\) is not the fixed-\(\chi\) coefficient.  The same lemma proves the optimizing scale and expansion, and equality of all six center ratios transfers both conclusions without alteration.

Finally, the cost gaps and every strict objective gap are continuous in the relative cost-manifold and finite-cell topologies.  Hence the center has a relatively open neighborhood preserving the named selector, both generic secondary selectors, and the two bridge optimizers.  Since the CLW object is expressly the transported population affine-linear criterion, none of these calculations addresses the empirical equation~(3.6) procedure, its approximate solver, the original randomized-trial validity result, arbitrary prediction procedures, or bridge-role selection.  This completes the proof.