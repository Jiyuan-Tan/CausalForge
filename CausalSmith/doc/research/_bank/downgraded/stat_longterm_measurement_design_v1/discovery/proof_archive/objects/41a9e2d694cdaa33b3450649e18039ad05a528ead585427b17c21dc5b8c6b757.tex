The named negative result concerns the CLW population criterion only after the explicit linear per-unit cost specialization and support mapping. It does not analyze CLW's general nonlinear survey-cost model, their empirical equation~(3.6) implementation, their approximate orthogonal greedy algorithm, or their RCT estimator's validity. The witness is pointwise regular for every \(\epsilon>0\), but its inverse-bridge stability is not uniform as \(\epsilon\downarrow0\); that nonuniformity is the mechanism producing the divergence. Generic unadjusted and treatment-aware squared-risk selectors remain secondary corollaries, not claims about arbitrary predictive methods.