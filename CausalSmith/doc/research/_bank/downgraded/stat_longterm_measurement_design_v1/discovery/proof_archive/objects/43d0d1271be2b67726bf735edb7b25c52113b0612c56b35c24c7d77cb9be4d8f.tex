By Definition~\(\mathrm{def{:}common\text{-}identification\text{-}class}\), \(\theta_d(P)=\tau\) for every \(d\in\mathcal D\). Therefore \(\theta_{\widehat d_m}(P)=\tau\) for every pilot realization. Substitute this identity into the statistic and coverage conclusion of Theorem~\(\mathrm{thm{:}postselection\text{-}wald}\). The total event and zero extension are unchanged, proving both assertions.