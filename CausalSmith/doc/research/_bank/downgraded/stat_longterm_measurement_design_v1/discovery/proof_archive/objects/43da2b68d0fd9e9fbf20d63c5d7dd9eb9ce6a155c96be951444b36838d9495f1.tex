On the full frozen class, the bridge contrast \(\theta_d(P)\) need not equal \(\tau\), so the general Wald theorem is not causal coverage. The absolute fixed-system causal-efficiency interpretation also requires the external identification, unique-bridge, bijectivity, moment, and local-model premises in the cited IKM theorem. The finite-catalogue constant does not solve the residual primitive operator/generalized-eigenvalue comparability question.