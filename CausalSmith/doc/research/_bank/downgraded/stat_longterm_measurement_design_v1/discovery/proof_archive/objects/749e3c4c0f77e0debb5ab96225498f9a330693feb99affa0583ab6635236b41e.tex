A finite costed catalogue with preattached sequential roles creates nonnested conditional-moment inverse problems. The full affine prediction Gram may be perfectly regular while a weak early proxy makes the selection bridge and observational score variance explode, so a source-faithful predictive collection criterion and the bridge design value can rank the same subsets oppositely.