The note makes measurement design itself the argument of the two-source source-score and, under the published fixed-system premises, causal efficiency bound.  It profiles the budget allocation, identifies the finite-catalogue design-value gap from a full-catalogue pilot, supplies a feasible selected-design estimator and Wald law under explicit conditions, and proves a same-assumption negative result for two standard observational prediction rules.  On a regular common-identification binary region, both rules select the weak-early/strong-late system while the moderate-proxy system uniquely minimizes the arm-normalized and IKM-normalized profiles; the approximation ratio is unbounded with the exact sharp elbow.  A two-fold saturated finite-cell pilot estimator consistently implements the treatment-aware selector along triangular center laws, so the failure is empirically feasible rather than an oracle-only comparison.