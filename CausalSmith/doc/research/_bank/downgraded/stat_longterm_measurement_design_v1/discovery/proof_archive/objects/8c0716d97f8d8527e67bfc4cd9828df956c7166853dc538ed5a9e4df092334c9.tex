The cited CLW result establishes that, under their randomized-trial and homoskedasticity conditions with balanced treatment, their population design criterion is residual variance divided by the budget-feasible sample size. Definition~\(\mathrm{def{:}predictive\text{-}benchmark}\) specializes the cost to \(n(c_{E,d}+c_{O,d})\), and Definition~\(\mathrm{def{:}clw\text{-}linear\text{-}cost\text{-}selector}\) therefore makes the transported criterion exactly \(\Psi_{\mathrm{CLW},d}(P;B)\). Lemma~\(\mathrm{lem{:}sharp\text{-}uniform\text{-}binary\text{-}elbow}\) derives the regression coefficients and residual variance, so this mapping is equation-by-equation rather than an analogy.

Fix \(\chi>1\). By the updated stability lemma, every ball used in \(\mathcal U_{\mathrm{bin}}\) can be chosen so that every member is regular, both bridge contrasts equal \(\tau\), and
\[
 \mathcal D_{\mathrm{CLW}}(P;B)=\{d_{\mathrm p}\},
 \qquad D^*(P)=\{d_{\mathrm e}\}.                         \tag{1}
\]
The normalized stability lemma and the further shrinking used for \(\mathcal U_{\mathrm{bin}}^{\mathrm{IKM}}\) likewise give
\[
 D_{\mathrm{IKM}}^*(P)=\{d_{\mathrm e}\}.                \tag{2}
\]
The floor-budget assertion holds on every such ball by the final clause of the updated stability lemma. Positivity of the denominators follows once \(B\) exceeds both paired costs.

Every center \(P_{\chi,\epsilon}\) belongs to its defining ball. At a center, the sharp-elbow lemma gives
\[
 \rho_{\mathrm{CLW}}(P_{\chi,\epsilon};B)
 =\rho_{\mathrm{CLW}}^{\mathrm{IKM}}(P_{\chi,\epsilon};B)
 =\rho_{\mathrm{pred}}(P_{\chi,\epsilon})
 =\rho_{\mathrm{pred}}^{\mathrm{IKM}}(P_{\chi,\epsilon}). \tag{3}
\]
The treatment-aware sharp-elbow lemma proves that \(\mathcal D_{\mathrm{pred}}^A(P_{\chi,\epsilon})=\{d_{\mathrm p}\}\), so both treatment-aware ratios equal the common value in (3). The exact formula in the sharp-elbow lemma implies
\[
 \rho_{\mathrm{CLW}}(P_{\chi,\epsilon};B)
 >\frac{\epsilon^{-2}+14}{726}.                            \tag{4}
\]
Letting \(\epsilon\downarrow0\) at fixed \(\chi\), while using the center inclusions, proves both suprema in (1)--(2). Because all centers have positive cells, nonzero completeness determinants, bijective bridge operators, finite positive score variances, and common identification, the divergence is an efficiency failure, not a failure of identification or a boundary-law construction.

The same exact formula gives, at fixed \(\chi\),
\[
 \rho_{\mathrm{CLW}}(P_{\chi,\epsilon};B)
 =\frac{5\chi+1}{363(10\chi-3)}\epsilon^{-2}+O_\chi(\epsilon^{-1}). \tag{5}
\]
Algebra yields
\[
 \frac{5\chi+1}{363(10\chi-3)}-\frac1{726}
 =\frac5{726(10\chi-3)}>0.
\]
Thus \(1/726\) is not a fixed-\(\chi\) leading coefficient. The sharp-elbow optimization proves the asserted \(\epsilon^{-2/3}\) scale and second-order expansion. Equality of all six center ratios transfers that calculation without modification.

Finally, the center costs are \(5\chi\) and \(5\), so the disadvantage factor is exactly \(\chi\). The strict CLW criterion gap, sourcewise cost gaps, cap inequalities, and bridge-value gaps are continuous in the relative cost-manifold and finite-cell topologies. Hence the center has a relatively open neighborhood preserving the named selector and both efficiency orderings. This proves the exact named-workflow obstruction. The final limitations follow from the construction: only CLW's population criterion is transported; their original randomized-trial theorem, empirical equation~(3.6) selector, approximate algorithm, and support-to-role interpretation are not challenged.