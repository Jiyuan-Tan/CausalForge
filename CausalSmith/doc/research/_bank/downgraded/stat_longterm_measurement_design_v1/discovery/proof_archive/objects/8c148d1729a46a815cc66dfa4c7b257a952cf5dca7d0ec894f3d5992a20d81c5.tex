The deterministic finite-family inequality converts uniform pilot value error into a \(2\varepsilon_m\) regret certificate, while allocation error is second order.  The stochastic rate statement is conditional on the sanctioned pilot variance-consistency condition.