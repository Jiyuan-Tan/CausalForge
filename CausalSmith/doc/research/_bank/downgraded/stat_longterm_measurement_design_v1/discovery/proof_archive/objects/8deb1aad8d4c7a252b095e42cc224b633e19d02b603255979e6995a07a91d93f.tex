At the center, Lemma~\(\mathrm{lem{:}treatment\text{-}aware\text{-}sharp\text{-}binary\text{-}elbow}\) gives a strict treatment-aware risk gap. On the positive finite-cell model,
\[
 R_d^A(P)=\mathbb E_P\{\operatorname{Var}_P(Y\mid A,X,Z_{M_d},G=O)\mid G=O\}, \tag{33}
\]
which is a finite sum of cell masses and ratios with positive conditioning-cell denominators. Hence (33) is continuous in the relative finite-cell topology. The arm-normalized, named CLW, and IKM-normalized profiles are continuous on the smaller balls already supplied by the two stability lemmas, and their minima remain positive.

Intersect each existing ball with a sufficiently small open neighborhood on which the strict treatment-aware ordering persists. The best-tie set is then \(\{d_{\mathrm p}\}\), and each ratio is continuous. One further shrinking makes each treatment-aware ratio exceed one half of its center value. Decreasing a radius cannot destroy any conclusion valid on the larger ball, including full-Gram CLW admissibility, the named continuous ordering, or eventual integer-selector agreement. This proves simultaneous stability.