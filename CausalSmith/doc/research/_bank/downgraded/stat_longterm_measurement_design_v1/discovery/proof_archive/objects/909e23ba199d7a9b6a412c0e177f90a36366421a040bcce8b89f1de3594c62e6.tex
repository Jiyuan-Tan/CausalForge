The positive result is exact design-value selection and inference for the observed bridge contrast on the observable regular class, with causal ATE coverage only on the common-identification subclass. The negative result concerns two explicit population squared-risk rules, \(R_d(P)\) and \(R_d^A(P)\), plus a feasible saturated finite-cell implementation of the latter. It does not claim that every predictive method fails, does not attribute either rule to a named published workflow, and does not elevate the arm-normalized profile to an unconditional causal efficiency bound. The relatively open witness regions are open only within the constrained finite-cell model.