Use \(\mathsf H_{\mathrm{comp}}\) to compare the source-gradient quadratic forms with the residual-risk quadratic form underlying the newly defined paired-source diagnostic. The exact target is a lawwise constant bounding \(\rho_{\mathrm{pred}}(P)\); the handle does not assert that a generalized eigenvalue is sharp and does not represent a published proxy-selection workflow.