The causal target is \(\tau\). On the full regular class the design object is the observed bridge functional \(\theta_d(P)=\mu_{d,1}-\mu_{d,0}\), not \(\tau\) by default. The selector and allocation are \(\widehat d_m\in\arg\min_{d\in\mathcal D}\widehat{\mathcal V}_{d,m}^*\) and \(\widehat\alpha_m=\widehat\alpha_{\widehat d_m,m}\). On the common-identification subclass where every \(\theta_d(P)=\tau\), and under the exact deterministic-path, nuisance-rate, multiplier, total-denominator-event, and independent-pilot conditions of \(\texttt{thm:postselection-wald}\),
\[
 \frac{\sqrt B(\widehat\tau_B-\tau)}{\widehat s_B}
 \rightsquigarrow\mathcal N(0,1),
\]
with \(\widehat s_B^2\) and \(I_B\) defined by \(\texttt{def:selected-wald-variance}\).