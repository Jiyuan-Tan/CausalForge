The named CLW conclusion is confined to the source-faithful transported population rule on \(\mathfrak P_{\mathcal D}^{\mathrm{CLW}}\). Full-Gram nonsingularity is proved with a nonredundant affine encoding; no pseudoinverse or hidden rank-stability premise is used. The binary witness is relatively open only inside its constrained finite-cell model. The inverse-bridge envelope is deliberately nonuniform as \(\epsilon\downarrow0\), which generates the impossibility. The result does not cover CLW's empirical equation~(3.6), OGA, arbitrary predictive algorithms, or bridge-role choice, and it does not claim that CLW analyze persistent confounding. Generic unrestricted-risk results are mathematically distinct secondary comparisons. Post-selection inference remains conditional on the declared selected-path learner, multiplier, product-rate, pilot-variance, and main-study variance conditions outside the proved fixed-dimensional subclass.