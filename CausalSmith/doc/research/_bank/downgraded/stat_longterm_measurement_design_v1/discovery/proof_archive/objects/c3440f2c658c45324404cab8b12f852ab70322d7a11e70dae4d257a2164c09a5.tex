A finite catalogue of subset and sequential-role assignments creates design-specific conditional-moment operators and two source-specific score variances.  This makes measurement choice an argument of the profiled score variance, while still permitting a fully operational fixed-dimensional subclass in which every bridge is estimated by an explicit nonsingular sample-moment system.