Prior fixed-system and surrogate-efficiency results do not optimize measured proxy subsets and role assignments under source-specific costs, and no published workflow is claimed to use either benchmark rule.