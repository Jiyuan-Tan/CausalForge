The note derives the exact CLW specialization, computes the binary population regression coefficients and residual variance, and proves a regular open-region counterexample with an unbounded bridge-efficiency approximation ratio, including a floor-budget version and a sharp optimized envelope. It complements the negative result with computable finite-catalogue design-value enumeration, independent-pilot design selection, optimal source allocation, and conditional post-selection Wald inference.