Theorem~\(\mathrm{thm{:}predictive\text{-}impossibility}\) supplies regularity, common identification, the unadjusted selector, both profile optimizers, the exact center ratio, both original-region divergences, the sharp cost optimization, and the relatively open cost-neighborhood conclusion. Lemma~\(\mathrm{lem{:}treatment\text{-}aware\text{-}sharp\text{-}binary\text{-}elbow}\) proves at every center that the treatment-aware selector is the same singleton \(\{d_{\mathrm p}\}\) and that its arm-normalized and IKM-normalized ratios equal the two original ratios. Lemma~\(\mathrm{lem{:}joint\text{-}predictive\text{-}binary\text{-}stability}\) permits the defining radii to be chosen so that both strict prediction orderings and both profile orderings persist simultaneously throughout the displayed regions.

Every center remains in its own open ball. Therefore the center identity and the divergence from Lemma~\(\mathrm{lem{:}sharp\text{-}uniform\text{-}binary\text{-}elbow}\) imply
\[
 \sup_{P\in\mathcal U_{\mathrm{bin}}}\rho_{\mathrm{pred}}(P)
 =\sup_{P\in\mathcal U_{\mathrm{bin}}}\rho_{\mathrm{pred}}^A(P)=\infty
\]
and
\[
 \sup_{P\in\mathcal U_{\mathrm{bin}}^{\mathrm{IKM}}}
 \rho_{\mathrm{pred}}^{\mathrm{IKM}}(P)
 =\sup_{P\in\mathcal U_{\mathrm{bin}}^{\mathrm{IKM}}}
 \rho_{\mathrm{pred}}^{A,\mathrm{IKM}}(P)=\infty
\]
by letting \(\epsilon\downarrow0\) at fixed \(\chi\). The pointwise equality of all four center ratios transports the exact formula and both sharp-elbow constants without modification.

For the cost-neighborhood clause, at \(\epsilon=0\) the continuously extended treatment-aware risk of \(d_{\mathrm p}\) is no larger than its unadjusted risk \(3/(10\chi)<3/10\), while the treatment-aware risk of \(d_{\mathrm e}\) is \(585/1024\). Thus the treatment-aware gap is strict at the center cost vector. Finite-cell conditional risks and primitive costs vary continuously. Intersecting the cost neighborhood from the original impossibility theorem with a neighborhood preserving this additional strict gap proves simultaneous unique selection by both prediction rules. The source-score divergence and balanced-center one-quarter IKM scaling are unchanged because neither depends on which of the two coincident center selectors is used. This proves every assertion.