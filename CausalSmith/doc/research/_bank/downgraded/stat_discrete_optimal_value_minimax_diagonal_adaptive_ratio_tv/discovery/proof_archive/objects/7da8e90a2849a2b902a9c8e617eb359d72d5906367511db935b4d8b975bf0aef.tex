The published known-\(\mathbf Q\) theorem does not cover the entire dimension range through one unchecked invocation.  The proof now uses it only for \(d\gtrsim\log^2(en)\) after verifying its dominance condition and supplies the missing lower band by an explicit paired-sign fuzzy mixture.