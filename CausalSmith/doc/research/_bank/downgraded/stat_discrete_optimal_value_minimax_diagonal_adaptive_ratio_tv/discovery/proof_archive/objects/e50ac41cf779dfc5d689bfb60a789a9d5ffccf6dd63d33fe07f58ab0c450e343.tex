The unrestricted optimal-policy value over \(d\) discrete confounder cells has minimax mean-squared error \(n^{-1}+d/\{n\log(en)\}\) under fixed overlap.  A corrected computable estimator uses ratio plug-in only on pilot-heavy cells separated from the tie diagonal, a zero-anchored sparse-square Chebyshev selector on sparse overlap-cone boxes, and a centered local-stripe selector on heavy near-tie boxes, with factorial lifts and certified coefficient majorants.  The converse combines an exact equal-propensity \(L_1\) Markov embedding, a paired-sign fuzzy mixture below \(d\asymp\log^2(en)\), the published uniform-\(\mathbf Q\) lower theorem above that threshold, and a parametric two-point floor.