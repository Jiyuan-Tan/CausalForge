The oracle truncation comparison, separate regression and ratio-sieve approximation bounds, and nuisance-product calculation were completed. A cellwise spline-occupancy calculation was then applied to the first boundary knot supports. It identifies the strict inequality \(a<2t(2s+1)\) as the regime in which the expected source count per smallest boundary support diverges polynomially; outside it, comparison with a feasible oracle plan cannot be uniform. No available frozen result controls the normalized empirical dual potential or its continuation between source training atoms.