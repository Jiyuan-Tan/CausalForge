The proved contribution is the source-only three-regime minimax converse, including the multiscale critical logarithm, together with the matching truncated-oracle envelope and an optional strict-slack target-sample lower witness. The project also specifies a separate-sieve RiCOT plus boundary local-polynomial estimator and a corrected bias-aware interval, while isolating rather than assuming the empirical KKT localization, leverage, calibration, normal-approximation, and multiplier steps needed for attainment. These open steps are the bridge to a defensible precision analysis for the RiCOT cardiomyopathy consumer.