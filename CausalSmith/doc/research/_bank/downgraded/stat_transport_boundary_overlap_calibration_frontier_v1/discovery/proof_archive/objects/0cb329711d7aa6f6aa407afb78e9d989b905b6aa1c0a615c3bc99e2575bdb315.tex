Polynomial trial depletion \(f_S(x)\asymp x^a\) with target density positive at the boundary makes the transported causal mean regular below \(a=1\), critical at \(a=1\), and nonregular above it. The source-only converse and its boundary-bump witness remain valid even when the target-density bounds have no strict slack.