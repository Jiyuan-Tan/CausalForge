The critical source lower bound cannot be obtained from one boundary bump: a multiscale sign mixture is needed to accumulate the \(\sqrt{\log n}\) factor while keeping divergence bounded. The oracle benchmark depends on the population densities through \(G_h^{\circ}\), \(q_h^{\circ}\), and \(\omega_h^{\circ}\), so it is not measurable from \(\mathcal D_{n,m}\). For \(s>1\), the finite jet program is a certified outer relaxation and may be conservative; exact finite Whitney extension is not asserted. Proving a measurable same-class upper bound would additionally require stochastic held-out localization and coefficient control for the capped RiCOT continuation. Frontier-order expected interval length and an executable adaptive selector remain open. The discarded self-similarity handle is retained only as this prose limitation; \(\mathrm{thm:nonadaptation}\) is the substantive formal obstruction.