\(\tau=\mathbb E_{P_T}[\mu(X)]=\int_0^1\{\mu_1(x)-\mu_0(x)\}f_T(x)\,dx\), targeted by the unchanged \(\widehat\tau^{\mathrm{sp}}_{h,J_{\mu,n},J_{r,n}}\) using the capped downstream RiCOT weight. Conditional on the covariates, the estimator has the exact observable expansion \(\sum_i\Gamma_i Z_i\), which yields a finite-sample Hoeffding execution certificate with realized radius \(B_X+R_S+R_T\). The proved rate result is a one-sided minimax lower phase diagram accompanied by an algebraically rate-equivalent truncated-oracle bias--standard-deviation envelope; that oracle equivalence is not a data-driven minimax upper bound. Held-out capped-weight localization, empirical frontier attainment, and a frontier-order expected-length guarantee remain open.