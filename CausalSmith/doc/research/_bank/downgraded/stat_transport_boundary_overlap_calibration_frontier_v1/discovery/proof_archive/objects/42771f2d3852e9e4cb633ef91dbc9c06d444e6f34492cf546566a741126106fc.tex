\(\tau=\mathbb E_{P_T}[\mu(X)]=\int_0^1\{\mu_1(x)-\mu_0(x)\}f_T(x)\,dx\), targeted by \(\widehat\tau^{\mathrm{sp}}_{h_n,J_{\mu,n},J_{r,n}}\); the proved results are the source-only minimax lower phase diagram and the matching truncated-oracle envelope, while held-out empirical RiCOT attainment and coverage of the bias-aware interval, whose corrected radius contains \(|C_{n,m}(h_n)|\), remain open