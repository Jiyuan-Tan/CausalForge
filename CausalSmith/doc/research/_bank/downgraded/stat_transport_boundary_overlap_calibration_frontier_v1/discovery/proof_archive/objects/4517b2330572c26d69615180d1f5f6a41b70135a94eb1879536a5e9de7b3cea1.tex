The exact interior decomposition is
\[
\widehat\tau_{\mathrm{int}}-\tau_{\mathrm{int}}
=\frac1n\sum_iw_i\{Z_i-\mu(X_i)\}+(P_m^T-P_T)\{\mu\mathbf 1_{[h,1]}\}+C_{n,m}(h)-\Delta_{h,J_{r,n}}(\mu),
\]
with \(|\Delta(\mu)|\le2M\sup_{v\in\mathcal H^s(1)}|\Delta(v)|\). The four source entries of \(\widehat{\mathrm{se}}^2\) combine algebraically into the nonnegative within-fold sample variance of \(\widehat U^R+\widehat U^B\). Also,
\[
\operatorname{length}(\mathcal I)
\le2(\widehat B+\widehat E)+2z_{1-\gamma/4}\widehat{\mathrm{se}},
\]
and the one-dimensional Hölder entropy integral is finite exactly for \(s>1/2\). These facts reduce expected length to frontier-scale uniform moment bounds and reduce coverage to a valid capped-weight linearization plus Gaussian and multiplier approximations; none of those stochastic conclusions follows from the algebra alone.