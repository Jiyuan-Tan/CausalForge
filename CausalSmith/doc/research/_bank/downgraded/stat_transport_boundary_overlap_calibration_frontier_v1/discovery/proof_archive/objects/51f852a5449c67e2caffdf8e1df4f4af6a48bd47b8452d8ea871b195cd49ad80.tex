The boundary geometry \(f_S(x)\asymp x^a\) with \(f_T(0)>0\) changes the source observation norm exactly at \(a=1\). This yields a causal transported-mean lower phase diagram, a boundary-corrected oracle benchmark, and a realized-design coverage certificate in one coherent model without treating the oracle as an estimator.