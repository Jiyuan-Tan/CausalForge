Uniformly over the class at \(a=0\),
\[
\frac{c_{T,-}}{c_{S,+}}\le r(x)\le\frac{c_{T,+}}{c_{S,-}},
\qquad
h_n=n^{-1/\{2(s+1)\}},
\qquad
h_n^{s+1}=n^{-1/2},
\]
and
\[
\int_{h_n}^1\frac{f_T(x)^2}{f_S(x)}\,dx=O(1).
\]
When both training interior measures are nonzero, the defining dual sum is strictly positive at every held-out \(x\in[h_n,1]\), so the residual correction does not algebraically collapse to zero. Positivity alone gives neither a fitted-weight norm bound nor the claimed root-risk conclusion.