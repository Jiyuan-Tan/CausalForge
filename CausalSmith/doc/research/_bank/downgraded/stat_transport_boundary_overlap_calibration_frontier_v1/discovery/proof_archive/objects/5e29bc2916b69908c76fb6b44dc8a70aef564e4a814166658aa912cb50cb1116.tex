\[
\begin{aligned}
\widehat B_{h,J_{\mu,n},J_{r,n}}={}&2C_{\mathrm{LP}}Mh^{s+1}+2M\sup_{v\in\mathcal H^s(1)}|\Delta_{h,J_{r,n}}(v)|\\
&+\left|\frac1m\sum_{j=1}^m\widehat\mu_{h,J_{\mu,n}}^{(-k_T(j))}(X_j^T)\mathbf 1\{X_j^T\ge h\}
-\frac1n\sum_{i=1}^n\widehat r^{\mathrm{RICOT},(-k_S(i))}_{h,J_{r,n}}(X_i)\widehat\mu_{h,J_{\mu,n}}^{(-k_S(i))}(X_i)\mathbf 1\{X_i\ge h\}\right|.
\end{aligned}
\]
The last line is the observable cross-fitted calibration discrepancy of the fold-varying regression fits. The factor \(2M\) is the Hölder radius inherited by \(\mu=\mu_1-\mu_0\) from the two armwise radius-\(M\) balls. The regression dimension is displayed because the interval is attached to \(\widehat\tau^{\mathrm{sp}}_{h,J_{\mu,n},J_{r,n}}\), while the calibration support function itself depends on \(J_{r,n}\) through its fitted transport weight.