\textbf{Proof.}
By \(\mathrm{def:bias\text{-}aware\text{-}interval}\), membership of \(\tau\) in \(\mathcal I_{h,J_{\mu,n},J_{r,n}}\) is equivalent to
\[
\left|\widehat\tau^{\mathrm{sp}}_{h,J_{\mu,n},J_{r,n}}-\tau\right|
\le B_X(h,J_{\mu,n},J_{r,n})+R_S(h,J_{\mu,n},J_{r,n})+R_T(m).
\]
The probability of this event is at least \(1-\gamma\) for every member of \(\mathcal M_{n,m}(a,s,t)\) by \(\mathrm{thm:ricot\text{-}attainment}\). Taking the infimum over the same class proves the coverage display. The interval is symmetric with the displayed half-length, so its realized length is twice that quantity. The final implication follows by substituting \(B_X\le b\) and \(\sum_i\Gamma_i^2\le g^2\) into \(R_S\) and \(R_T\). These are deterministic substitutions on the realized covariate design and do not imply any probability or expected-length order for the diagnostics. \(\square\)