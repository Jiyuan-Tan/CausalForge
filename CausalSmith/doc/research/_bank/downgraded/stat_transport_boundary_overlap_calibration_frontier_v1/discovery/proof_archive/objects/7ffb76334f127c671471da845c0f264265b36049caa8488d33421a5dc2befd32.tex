Polynomial trial depletion with positive target boundary density creates an identified causal mean whose source information geometry changes at \(a=1\), while the target covariate sample contributes a separate parametric floor. The lower-bound geometry is present for every \(t>0\) and does not depend on nuisance-sieve rate conditions.