The established converse gives
\[
\mathfrak R_{n,m}(a,s,t)\ge c\{m^{-1/2}+\rho_n(a,s)\}
\]
under its stated normalization and target-slack restrictions. The oracle truncated calculation has the same three source exponents. A minimax upper bound, however, requires one measurable data-driven estimator uniformly over \(\mathcal M_{n,m}(a,s,t)\); it cannot use the oracle ratio \(r\). The repaired spliced estimator would supply that witness only after \(\mathrm{thm:ricot\text{-}attainment}\) is proved.