Use dyadic differences of adjacent untruncated boundary local-polynomial fits to diagnose persistent boundary approximation error, source empirical masses of dyadic boundary shells to diagnose \(a\), and the observable direct-series Gram event together with adjacent-scale changes in \(\widehat q^{\mathrm{DS}}_{h,J_{r,n}}\) to select one interval from \(\{\mathcal I_{h,J_{\mu,n},J_{r,n}}:h\text{ dyadic}\}\).  The handle still does not specify measurable exponent estimators, numerical thresholds, a selection rule, or tie breaking, so it remains non-executable.