\(\tau=\mathbb E_{P_T}[\mu(X)]=\int_0^1\{\mu_1(x)-\mu_0(x)\}f_T(x)\,dx\), targeted by the unchanged \(\widehat\tau^{\mathrm{sp}}_{h,J_{\mu,n},J_{r,n}}\) using the capped downstream RiCOT weight. Here \(J_{\mu,n}\) and \(J_{r,n}\) count regression and calibration knot intervals, giving \(J_{\mu,n}+q\) regression coefficients and a \(J_{r,n}+q\)-dimensional calibration spline space with \(J_{r,n}+q-1\) explicit nonconstant equations. Conditional on the covariates, the estimator has the exact observable expansion \(\sum_i\Gamma_i Z_i\). The finite-sample interval uses the computable certified jet-linear-program radius \(\overline B_X\), with \(B_X\le\overline B_X\), together with \(R_S+R_T\). The proved rate result is a one-sided minimax lower phase diagram paired with a boundary-corrected oracle local-polynomial linear-weight benchmark whose actual bias is at most \(Ch_n^{s+1}\) and whose deterministic class allowance and standard deviation have frontier order. This oracle benchmark is not a data-measurable minimax upper bound; held-out capped-weight localization, empirical frontier attainment, frontier-order expected length, and executable self-similar adaptation remain open.