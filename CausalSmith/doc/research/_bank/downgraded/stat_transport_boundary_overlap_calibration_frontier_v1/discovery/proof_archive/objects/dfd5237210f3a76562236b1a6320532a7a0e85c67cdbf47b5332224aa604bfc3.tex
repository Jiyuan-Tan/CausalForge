The executable statistic uses deterministic five-fold ridge boundary local polynomials of order \(p\), an interior regression spline of dimension \(J_{\mu,n}\), and a capped continuation from a semi-unbalanced entropic transport program with calibration dimension \(J_{r,n}\), fixed penalties \(\eta_0,\lambda_0\), and slack penalty \(\xi_n=n^2\). The proved finite-sample certificate needs only the exact observable coefficients \(\Gamma_i\), the Hölder design discrepancy \(B_X\), and bounded-outcome Hoeffding radii. The conditions \(t>1/2\) and \(s/(2s+1)+t/(2t+1)>1/2\) are retained only as candidate sufficient conditions for the still-open empirical capped-weight localization analysis; neither belongs to the proved theorem dependency graph.