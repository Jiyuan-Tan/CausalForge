A randomized source trial may have density \(f_S(x)=x^a g_S(x)\) near a clinically important boundary while the target density remains positive there. The causal target is identified as the observed-law functional \(\tau=\int_0^1\{\mu_1(x)-\mu_0(x)\}f_T(x)\,dx\). The proved minimax result is one-sided: the source root-risk lower rate is \(n^{-1/2}\) for \(a<1\), \(\sqrt{\log n/n}\) for \(a=1\), and \(n^{-(s+1)/(2s+a+1)}\) for \(a>1\), in addition to \(m^{-1/2}\). A boundary-corrected oracle local-polynomial weight has actual bias at most \(C h_n^{s+1}\), and its explicit class-sharp allowance for fixed \(a>0\) plus oracle standard deviation has the same order. Separately, the unchanged capped RiCOT estimator admits an exact realized-design linear expansion and a finite-sample interval whose bias radius is computed by two certified jet linear programs. No data-measurable minimax upper bound, RiCOT frontier attainment, frontier-order expected length, held-out localization, or executable adaptive interval is claimed.