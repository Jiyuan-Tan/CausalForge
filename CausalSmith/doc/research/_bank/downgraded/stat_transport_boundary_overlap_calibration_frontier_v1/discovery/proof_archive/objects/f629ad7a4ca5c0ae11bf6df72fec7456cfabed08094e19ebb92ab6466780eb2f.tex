The data-driven upper side is unresolved. The primitive assumptions do not imply quantitative localization of the calibrated transport optimizer or its held-out dual continuation. For \(a>1\), source counts on the first spline support can vanish for either declared sieve dimension, so a cellwise Bernstein--Gram argument does not close. The exact interior expansion also contains the observable term \(C_{n,m}(h)\), omitted by the old bias envelope; adding \(|C_{n,m}(h)|\) repairs evaluability but does not prove it has frontier order. Fixed-class honesty additionally requires a uniform linear expansion, maximum-leverage control, variance consistency including the source residual--boundary covariance, a weighted Lindeberg theorem, conditional multiplier approximation, and uniform integrability. None is promoted to an assumption.