A downstream reader can compute \(N^{\prime}\widehat\Omega\widehat b^{\mathrm{sep}}\) and \(\widehat\Delta\) from the fitted score covariance to diagnose equality versus potential gain, while treating the diagnostic as descriptive unless further gap-rate theory is supplied.