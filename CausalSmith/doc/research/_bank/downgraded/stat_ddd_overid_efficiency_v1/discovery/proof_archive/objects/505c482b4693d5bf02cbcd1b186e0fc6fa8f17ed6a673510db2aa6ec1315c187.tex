The paper has three closed headline results within the constant span of the retained pairwise staggered-DDD scores. First, the causal aggregate \(\tau_\omega(Q)\) is linked to an explicit observed-data functional and estimated by an everywhere-defined clipped, projected, and ridged cross-fitted joint estimator with a derived limit law and Wald coverage. Second, an exact joint-versus-componentwise frontier characterizes the efficiency loss from optimizing each group-event ATT separately, and the feasible quadratic diagnostic \(\widehat\Delta\) consistently estimates that loss. Third, the labelled shared-cell invariant gives a complete equality-versus-strict-gain classification, and a bounded two-parameter family with \(0<|\rho|<\sqrt7/3\) and small nonzero post-adoption effect \(\delta\) proves that strict gain is robustly attainable in the smallest three-period architecture. No classification or strict-gap OEQ remains.