Fix \(F\), \(T\), \(\varnothing\ne\mathcal K\subseteq\{(g,t):2\le g\le t\le T\}\), and \(M\). The aggregate formed from the Ortiz-Villavicencio--Sant'Anna componentwise-GMM ATT functionals using the prespecified weights is joint-optimal in the constant retained-score span if and only if \(N^{\prime}\Omega b^{\mathrm{sep}}=0\), equivalently if and only if each block of \(\Omega b^{\mathrm{sep}}\) is constant across the comparison cohorts in that block. For every \(Q\in\mathcal M\), with \(P=Q_O\), the population identity
\[
V_{\mathrm{sep}}-V_{\mathrm{joint}}
=(N^{\prime}\Omega b^{\mathrm{sep}})^{\prime}(N^{\prime}\Omega N)^{-1}(N^{\prime}\Omega b^{\mathrm{sep}})
=(b^{\mathrm{sep}}-b^{\mathrm{joint}})^{\prime}\Omega(b^{\mathrm{sep}}-b^{\mathrm{joint}})
\]
holds without learner conditions. Suppose in addition that the i.i.d. sampling, fixed-fold sample splitting, fitted-regression \(L_4(P_X)\) envelope, propensity and regression \(L_4(P_X)\) rates, product-rate condition, and deterministic ridge condition of Theorem \(\ref{thm:joint-system-limit}\) all hold. Then \(\widehat\tau_{\mathrm{joint}}\) and \(\widehat\tau_{\mathrm{sep}}\) have asymptotic variances \(V_{\mathrm{joint}}\) and \(V_{\mathrm{sep}}\), respectively, so the joint estimator has strictly smaller asymptotic variance exactly when the net cross-block covariance contribution is nonconstant across comparison cohorts within at least one group-event block. For \(g=2\), all covariance entries use the defined baseline period \(1\). If \(\mathcal K=\{k\}\), then \(N^{\prime}\Omega b^{\mathrm{sep}}=0\), \(b^{\mathrm{joint}}=b^{\mathrm{sep}}\), and \(V_{\mathrm{sep}}-V_{\mathrm{joint}}=0\), reproducing within-cell GMM optimality.