\(\mathfrak L_{0,n}^{\mathrm{orb}}(h,q)=(q^2/h)\,\mathfrak D_{\alpha}^{\mathrm{mix}}(h,q)\); for every normalization-legal finite grid, \(\kappa_N^*\) equals the explicit likelihood dual in Theorem~\ref{thm:finite-grid-simultaneous-dual}; over the continuum union, \(C_{\mathrm{prof}}\) is a \(K_\alpha\)-factor comparator rather than an exact continuum minimizer.