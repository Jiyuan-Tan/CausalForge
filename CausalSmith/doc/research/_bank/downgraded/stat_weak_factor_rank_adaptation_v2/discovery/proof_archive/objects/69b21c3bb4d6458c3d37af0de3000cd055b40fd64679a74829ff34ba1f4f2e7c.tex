The finite-grid problem is solved exactly on every normalization-legal grid: its simultaneous oracle factor equals the displayed \((\lambda,w)\)-dual, both primal and dual attain, and finite-action purification yields a nonrandomized connected interval.  Independently, the explicit continuum rule
\[
 C_{\mathrm{prof}}(y)=[\inf T(y),\sup T(y)]
\]
when \(T(y)\neq\varnothing\), and \(\{0\}\) otherwise, is data-only, uses no \(r,s,h,q\), is honest over \(\mathcal U\), has length at most \(2B_{\max}\), and satisfies
\[
 1\leq\frac{E_{P_0}\operatorname{len}C_{\mathrm{prof}}(Y)}
 {\mathfrak L_0^{\mathcal U}(n)}\leq\frac{2}{1-2\alpha}.
\]
Its endpoints are evaluable by fixed-dimensional real quantifier elimination and bisection.