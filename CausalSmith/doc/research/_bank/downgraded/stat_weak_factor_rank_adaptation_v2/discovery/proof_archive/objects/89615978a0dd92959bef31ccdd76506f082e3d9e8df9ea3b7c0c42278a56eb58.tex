Start from the finite-orbit dual acceptance regions in \(\mathrm{def:orbit-duals}\), replace the sixty-four dual weights by a measure over the rank-and-strength index \(r,s\) and its orientation fiber, and invert the resulting family of Gaussian tests while enforcing nested acceptance across target values.