The design is a fixed public four-by-four causal-panel mask: the two-by-two blocks \(A,F,G\) are observed with independent Gaussian sequence noise and \(D\) is the untreated treated-row posttreatment block.  Finite grids select legal mean--target pairs from this same frozen union; the continuum profile rule uses the closure of all such pairs.