For every \(n\ge2\) and \(l\ge3\), the explicit vertex \(\lambda^\star\) is the unique minimizer of a constructible rational objective \(g\), and every other vertex \(\mu\) has a deterministic polynomial-time computable adjacent parent \(P(\mu)\) satisfying \(g^{\mathsf T}P(\mu)<g^{\mathsf T}\mu\). Hence the parent graph is acyclic and every parent chain reaches \(\lambda^\star\). The tangent-cone and exact-line-search reductions isolate child generation to incident extreme-ray enumeration and parent filtering. At \((n,l)=(2,3)\), a certified adjacent vertex omitted by all canonical one-equation exchanges shows that a narrower exchange oracle cannot be complete. Thus only polynomial-delay child generation, traversal delay, and polynomial termination after the last output remain open.