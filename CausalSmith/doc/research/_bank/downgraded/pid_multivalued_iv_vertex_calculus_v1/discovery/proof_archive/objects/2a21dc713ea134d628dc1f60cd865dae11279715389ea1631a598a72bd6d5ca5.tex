Unconditionally, \(P\) is computable in polynomial bit time, has the known root \(\lambda^\star\), strictly decreases its rational objective, and reaches the root from every vertex. Given a rational candidate \(h\), one can decide in polynomial bit time whether \(h\) is a vertex of \(\mathcal K_\mu\), whether its tangent ray has a finite exact endpoint \(\nu\), and whether \(P(\nu)=\mu\), without expanding the \(2^l n^2\) type inequalities. Every simple vertex, characterized here by \(|\operatorname{Sig}(\mu)|=r\), therefore admits exact polynomial-total-time child generation with polynomial-time exhaustion. For arbitrary vertices, every normalized ray has a canonical encoding of length polynomial in \(n+l\), and \(\mathsf{NP}\)-oracle prefix search lists every child exactly once with polynomial delay and polynomial-time exhaustion. Coupling this iterator to the parent tree through the parity--Euler traversal lists every element of \(\mathcal V_{n,l}\) exactly once with global polynomial delay and polynomial-time final termination. Consequently the full requested enumerator exists relative to \(\mathsf{NP}\), and it exists deterministically if \(\mathsf P=\mathsf{NP}\); unconditional oracle removal at nonsimple vertices is the sole residual.