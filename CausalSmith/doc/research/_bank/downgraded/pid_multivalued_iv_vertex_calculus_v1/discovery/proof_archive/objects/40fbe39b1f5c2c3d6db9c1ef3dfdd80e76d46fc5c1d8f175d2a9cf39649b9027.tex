The residual is reduced exactly to facet enumeration for \(\mathcal A_\mu=\operatorname{conv}\{\alpha_\tau:\tau\in\operatorname{Sig}(\mu)\}\), a succinct convex hull of at most \(n^2\) Minkowski sums of \(l\) singletons or segments. A proposed normalized ray and its child endpoint are polynomial-time verifiable using at most \(n^2(l+1)\) compressed span generators. All children of a simple vertex with \(|\operatorname{Sig}(\mu)|=r\) are generated in polynomial total time by the rays \(R_\mu^{-1}e_i\). Relative to an \(\mathsf{NP}\) oracle, local and global polynomial-delay enumeration with polynomial exhaustion is proved. Conditional on any unrelativized local iterator, the parity Euler traversal supplies exact-once global polynomial delay and final termination even at exponential tree depth. Finally, \(|\operatorname{Sig}(\lambda^\star)|=(n+1)^2 2^{l-2}\), so full active-list expansion is provably inadmissible.