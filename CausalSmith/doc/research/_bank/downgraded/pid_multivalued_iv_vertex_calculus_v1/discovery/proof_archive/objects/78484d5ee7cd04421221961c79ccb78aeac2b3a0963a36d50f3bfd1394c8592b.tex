For every \(n\ge2\) and \(l\ge3\), \(\lambda^\star\) is an explicit vertex and unique minimizer of an explicitly constructed rational objective \(g\). Every other vertex has a deterministic polynomial-time computable adjacent parent of strictly smaller \(g\), so all parent chains reach \(\lambda^\star\). The tangent cone and its compact normalized base have polynomial-size extended formulations, and exact line search is polynomial. Separately, at \((2,3)\) an explicit adjacent vertex \(\mu\) proves that canonical one-equation exchanges miss a genuine edge. Thus the global-orientation half of reverse search is closed, and the residual is sharply isolated to polynomial-delay child or extreme-ray enumeration and polynomial termination after the last output.