The frontier is otherwise complete. There is an explicit polynomial-time parent map \(P\) with known root \(\lambda^\star\), strict descent, acyclicity, and reachability from every vertex. Given a rational candidate ray, its normalized tangent-ray extremality, finite exact endpoint, vertex status, and condition \(P(\nu)=\mu\) are decidable in polynomial bit time using at most \(n^2(l+1)\) compressed span generators. Every simple vertex admits polynomial-total-time exact child generation. With an \(\mathsf{NP}\) oracle, prefix self-reduction supplies polynomial-delay child generation with polynomial exhaustion for arbitrary vertices, and the parity-Euler traversal yields exact-once global polynomial delay and polynomial-time termination after the last output. Thus only removal of the oracle for nonsimple inverse-child generation remains unresolved.