For any finite \(J_{\mathrm{blk}}\) and positive block-specific squared loadings \(d_{j\ell k}\) in the rank-one-per-block menu, if \(b_j>\bar r_j\) for every active class, then \(\lambda_{\mathrm{blk}}^\star\) is the optimum of the explicit \(3q+1\)-variable LP in \(\mathrm{thm:block-specific-finite-class-frontier}\), and an independent-direction, independent-symmetric-sign product law from any LP optimizer attains it. The homogeneous two-class specialization recovers \(\lambda^\star=(a-2b)/(a+b)\) for \(a\ge2b\) and \(\lambda^\star=(2b-a)/\{2(a+b)\}\) for \(a\le2b\), with the stated regime-specific attaining laws and strict improvement over block complete randomization exactly when \(a\ne2b\).