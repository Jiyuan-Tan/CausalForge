The project proves an exact finite-class, finite-\(n\) productization theorem, derives baseline and oracle risks from block-specific loadings, and gives an explicit \(3q+1\)-variable LP plus an attaining independent-sign product law. It recovers the old two-class formula as a corollary and separately preserves the exponential SDP for genuine higher-rank or noncommuting kernels.