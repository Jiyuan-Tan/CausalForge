The calculation shows that baseline-gain retention and the variance-ratio maximin normalization can select disjoint designs even when both are applied to the same present-menu oracle and committed risks.