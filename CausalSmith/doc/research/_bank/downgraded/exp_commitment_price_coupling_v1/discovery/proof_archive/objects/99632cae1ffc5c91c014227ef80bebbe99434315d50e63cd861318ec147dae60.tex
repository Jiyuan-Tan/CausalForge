The project derives the exact piecewise finite-\(n\) frontier, proves sharpness through matching trace and eigenvalue bounds, supplies the attaining block-balanced product laws in every regime, establishes strict improvement over block complete randomization for both classes exactly off the elbow, and proves that a criterion-level application of the published variance-ratio maximin rule has a disjoint optimizer on an open parameter region.