{
  "agent": "coordinator",
  "round": 1,
  "callId": "main",
  "model": "codex",
  "prompt": "You are the COORDINATOR. A substrate module has PASSED review — it is correct, sorry-free, and reusable. Your job is to INTEGRATE it into the Causalean library cleanly and permanently: put it in the right place, reuse what already exists instead of reinventing, and document it to the project's standard. You do NOT re-judge the math.\n\n## What was built\nStaging files (namespace `CausalSmith.Substrate.MarkedVariableOrderFactorialCovariance.*`) under: <repo-root>/CausalSmith/CausalSmith/Substrate/MarkedVariableOrderFactorialCovariance\n- <repo-root>/CausalSmith/CausalSmith/Substrate/MarkedVariableOrderFactorialCovariance/Bounds.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/MarkedVariableOrderFactorialCovariance/Expansion.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/MarkedVariableOrderFactorialCovariance/PartialMatching.lean\n- <repo-root>/CausalSmith/CausalSmith/Substrate/MarkedVariableOrderFactorialCovariance/Statistic.lean\n\n## Requirement it fulfilled\n# Substrate requirement: marked-variable-order-factorial-covariance\n\n## Goal\n\nBuild reusable covariance infrastructure for normalized ordered injective\nproduct statistics of different orders, including one square-integrable marked\ncoordinate and mutually exclusive finite labels. The substrate must stop before\nany paper-specific Chebyshev-coefficient summation and must not state\n`linear_mark_factorial_covariance` or its `6^(2*K)` conclusion.\n\n## Provides (API contract)\n\n1. A finite type `PartialMatching r s`, or an equivalent bijection between\n   subsets of `Fin r` and `Fin s`, together with matching size and the fixed-size\n   family `partialMatchingsOfSize r s h`. Prove\n   `card = Nat.choose r h * Nat.choose s h * h.factorial`.\n2. A normalized ordered-product statistic compatible with\n   `Causalean.Stat.uStatisticOrder`: for coordinate functions\n   `f : Fin r -> X -> Real`, average `prod i, f i (sample (t i))` over\n   injective `t : Fin r -> Fin n`, normalized by `n.descFactorial r`.\n3. A mixed-order partial-matching product or covariance identity. For orders\n   `r,s`, expand the product/covariance of the normalized statistics as a finite\n   sum over partial matchings. A size-`h` matching has normalization\n   ```\n   (n.descFactorial (r + s - h) : Real) /\n     ((n.descFactorial r : Real) * (n.descFactorial s : Real))\n   ```\n   and its product-law moment is obtained by merging the matched coordinate\n   factors. Hypotheses should be limited to probability, measurability, and\n   integrability of the finitely many merged products; an L2 sufficient wrapper\n   is acceptable. Expose the `h = 0` centered correction explicitly.\n4. Mixed-order normalization bounds: if `r <= R`, `s <= R`, and\n   `4 * R^2 <= n`, then the size-`h` ratio is at most `exp 1 / n^h`, and the\n   disjoint correction is at most `2 * R^2 / n`.\n5. Optionally, a thin specialization `oneMarkSelectorKernel` for a finite label\n   map `cell : X -> iota`, an arm map, and a mark `v : X -> Real`, exposing the\n   merged-factor cases: no coincident marks uses first moments, coincident marks\n   uses one truncated second moment, and incompatible labels give zero. Its\n   conclusion must remain a pairwise/matching bound, never the paper's summed\n   Chebyshev rate.\n\nEquivalent canonical formulations are acceptable if they compose to all four\nrequired bullets without hiding the desired covariance conclusion in a bespoke\nstructure.\n\n## Statement / milestones\n\nConstruct the partial-matching type and prove its fixed-size cardinality; define\nthe normalized ordered-product statistic; prove the unequal-order product or\ncovariance expansion with the h = 0 centered correction exposed; and prove the\nstated factorial-ratio and disjoint-correction bounds under `4 * R^2 <= n`.\n\n## Standard reference\n\nThe standard framework is Hoeffding's overlap enumeration for U-statistic\nproducts and covariance, together with the falling-factorial count of pairs of\ninjective tuples classified by their shared sample indices. The requested\nextension allows unequal orders and a square-integrable marked coordinate.\n\n## Proposed imports\n\n- `Causalean.Stat.UStatistic.OrderM.Variance` for ordered tuples, product-law\n  transport, and L2 infrastructure.\n- Selected generic overlap facts from\n  `Causalean.Stat.UStatistic.OrderM.RemainderSecondMoment` only if generalized\n  to unequal orders; do not inherit its first-degeneracy premise.\n- `Mathlib.Data.Fintype.CardEmbedding`.\n- `Mathlib.Probability.ProductMeasure`.\n- Targeted covariance and integral modules as required.\n\nThe study substrate must not import any `CausalSmith.Stat.*_Research` module.\n\n## Staging prerequisite\n\nExtract or generalize the factorial normalization inequalities currently in\n`CausalSmith/Stat/STAT_DiscreteAteMinimaxLoggap_Research/Helpers/FactorialMoments.lean`\n(the `factorial_ratio_bound` and `factorial_cross_ratio_bound` cluster) into\n`CausalSmith/CausalSmith/Substrate/MarkedVariableOrderFactorialCovariance/`, or\nprove equivalent generic lemmas there. Keep compatibility wrappers in the\naccepted sibling research module if later promotion moves them.\n\n## Exact paper-local residual after substrate\n\n- Prove the bridge from `allBlockOrderedMarkedFactorial` to the generic\n  ordered-product statistic at order `j + 2`, including denominator positivity.\n- From `RealLaw.arm_outcome_factorization`, `outcomeMean_eq`, mean\n  normalization, and `SecondCentralMoment`, derive `abs z_ak <= s_ak / 2` and\n  `E[(Y/M)^2 * 1{X=k,A=a}] <= (5/4) * s_ak`, plus the required measurability and\n  integrability facts.\n- Instantiate the matching identity for the mark, cell, and arm indicator\n  pattern; eliminate incompatible cross-cell/arm matches and separate\n  mark-mark, other within-cell, and disjoint cross-cell terms.\n- Prove or reuse `shiftedCoefficient K j = gCoefficient K j`, apply the\n  coefficient envelope, use `p_k <= B/4` and\n  `4 * (K + 2) / m <= 3 * B/4`, and perform the paper-specific summations to\n  obtain `linear_mark_factorial_covariance`.\n\nThis residual remains part of the paper contribution and may not be moved into\nthe substrate gate.\n\n## Intended reuse\n\nThe infrastructure supports covariance calculations for mixed-order factorial\nestimators with unbounded but square-integrable marks, including marked\nspecies-count, causal, and missing-data estimators. It fills the gap between\nfixed-order Hoeffding projection theory and binary multinomial factorial-count\ncovariance calculations.\n\n## May assume / must derive\n\nMay assume existing Mathlib probability, finite-combinatorics, product-measure,\nintegral, and exponential-inequality APIs, plus the paper-independent ordered\ntuple and U-statistic infrastructure already in Causalean. Must derive the\npartial-matching count, unequal-order product/covariance decomposition, merged\nproduct-law moment representation, and normalization bounds. Any factorial-ratio\nfact currently available only in a paper research module must be generalized or\nreproved in the neutral study tree; the reusable substrate may not import that\npaper module.\n\n## Non-goals\n\n- No ATE model class, overlap assumption, cell-mass cutoff, estimator selector,\n  Chebyshev coefficient sum, minimax risk, or headline rate.\n- No assumption that directly implies the paper's covariance lemma.\n- No bounded-outcome replacement for the L2 marked-coordinate interface.\n- No `sorry`, custom axiom, or paper-specific substrate gate.\n\n## Known building blocks\n\n- `Causalean.Stat.UStatistic.OrderM.Basic` supplies ordered injective tuples,\n  fixed-order U-statistics, first projections, and degenerate remainders.\n- `Causalean.Stat.UStatistic.OrderM.Variance` and\n  `RemainderSecondMoment` supply same-order variance machinery, but currently do\n  not provide unequal-order nondegenerate cross moments.\n- The accepted discrete-ATE sibling proves scalar falling-factorial\n  normalization ratios and binary multinomial monomial covariance. These must\n  be generalized or extracted; they do not cover a real L2 mark collision.\n\n\n## Your tools\n- Read the staging files and inspect the Causalean tree (root = the directory containing `Causalean.lean`; sources under `Causalean/`).\n- Project search — run from `CausalSmith/tools`: `npm run search -- \"<concepts>\"` (find existing decls to reuse/dedup against), `npm run search -- --scope module \"<concepts>\"` (find the correct topical HOME file/area — use this for placement), `npm run search -- --type \"<pattern>\"` / `--goal \"<goal>\"`.\n- lean-lsp for signatures / declaration files.\n\n## What to decide\n1. PLACEMENT — merge-first. Find the substrate's natural topical home with `--scope module`. STRONGLY prefer MERGING each decl into an existing `Causalean/.../X.lean`; only CREATE a new topic file inside the narrowest faithful existing top-level subject area. Never invent a generic holding area. Target ≤600 lines; split before ~900 only for an independent topic/proof chain.\n2. NAMESPACE — rewrite every `CausalSmith.Substrate.MarkedVariableOrderFactorialCovariance` reference to the chosen Causalean namespace (the namespace of the file it lands in).\n3. REUSE / DEDUP — for each decl, search Causalean for one that already proves the SAME statement. If it exists, drop your reinvented version and use the existing one (add its import, point references at it). Reuse ONLY on genuine statement-equality — a rewire to a decl that merely looks similar will break the build. Dedup edits stay inside YOUR new content; never delete or alter an existing Causalean decl.\n4. DOCUMENTATION (docstring-canonical) — every declaration that will render as a public library card gets a `/-- … -/` docstring whose FIRST paragraph is a self-contained natural-language translation for an econometrician with no Lean background (no Lean syntax, no field projections like `B.sigma` — say \"the σ-algebra on B\"; no \"For every …\" preamble restating the signature). This includes named local definitions or instances when the index extracts and displays them. `doc:gen` hard-fails if any published card would show “No natural-language translation yet.” Every new file opens with a `/-! … -/` module docstring stating what it provides.\n5. HEADLINE + PAGE-DESCRIPTION curation — audit EVERY promoted Lean file and every newly created published namespace path before emitting the manifest. The documentation gate requires every file containing a public `theorem` or `lemma` to have at least one matching declaration in `headline_theorems` in the correct `doc/library_review/<Area>.json`. Curate MAIN results normally (identification theorem, estimand characterization, named decomposition, asymptotic linearity/normality/rate/optimality/efficiency result, sharp bound). If a theorem-bearing file contains only technical/supporting results, still choose its most representative reusable public theorem or lemma as that file's single required headline anchor; additional technical steps remain supporting-tier. Also give every NEW explorer page a short, reader-facing description: use the first paragraph of a roll-up module's `/-! ... -/` docstring when that module exists, otherwise add `namespace_intros[\"<path below Area>\"]` to the area sidecar. Include intermediate directories created by a new subtree (for example, adding `Causalean.Mathlib.AlgebraicGeometry.PolynomialImageDimension.*` also requires a description for `AlgebraicGeometry`). Preserve every existing sidecar entry and stage the COMPLETE updated sidecar with a `write_file` op. Before output, cross-check the promoted file list and every new page path against the staged sidecar; never rely on a later failed `doc:gen` run to discover omissions.\n6. API.md — for a NEW module, add a `<!-- GEN:Causalean.<Module> -->` / `<!-- /GEN -->` marker pair in `doc/API.md` where the per-decl table belongs (the system's `doc:gen` fills it — do NOT hand-write the table), plus any cross-cutting narrative for the new file.\n\n## Output — WRITE bodies to staging, emit a TINY manifest\nCRITICAL, read twice: do NOT put file contents inside the JSON. Large Lean bodies inside a JSON string are the #1 cause of a parse failure that aborts the whole integration (a truncated/mis-escaped string cannot be recovered). Instead:\n\n1. WRITE every file body and every insert patch as a SEPARATE FILE under the staging directory:\n   <repo-root>/CausalSmith/doc/study/marked-variable-order-factorial-covariance/coordinate_staging/round_1\n   Use short names, e.g. `<repo-root>/CausalSmith/doc/study/marked-variable-order-factorial-covariance/coordinate_staging/round_1/foo.lean`, `<repo-root>/CausalSmith/doc/study/marked-variable-order-factorial-covariance/coordinate_staging/round_1/merge_x.lean`, `<repo-root>/CausalSmith/doc/study/marked-variable-order-factorial-covariance/coordinate_staging/round_1/area.json`. Put the EXACT bytes you want written/inserted into each staged file (namespaced, docstringed, ready to apply).\n2. Emit ONLY a small JSON manifest that REFERENCES those staged files by `from` (relative to the staging dir). It contains no code — just short paths and one short anchor line per merge, so it always parses.\n\n`{ \"ops\": [ … ], \"notes\": \"<brief rationale>\" }`. `target` paths are RELATIVE to the Causalean root. Op kinds:\n\n- `{ \"kind\": \"create_file\", \"target\": \"Causalean/…/Foo.lean\", \"from\": \"foo.lean\", \"newModule\": \"Causalean.….Foo\" }` — write the staged `from` file as a NEW file at `target`. Set `newModule` (the Lean module name) ONLY for a new Lean module, so it is root-wired into `Causalean.lean`. Omit `newModule` for a new non-Lean record file.\n- `{ \"kind\": \"merge_lean\", \"target\": \"Causalean/…/X.lean\", \"anchor\": \"<a line already in X.lean>\", \"from\": \"merge_x.lean\" }` — INSERT the staged `from` content after the first line containing `anchor` (empty anchor → append at end of file). INSERT-ONLY: the system preserves every existing byte and ROLLS BACK if a merge would change existing content. To add an import to an existing file, use a separate `merge_lean` anchored on an existing `import …` line (its `from` file holds just the import line). Multiple `merge_lean` ops may target the same file. If dependent inserts share one file/anchor, combine them into one staged file in dependency order, or anchor each later insert on text introduced by the prior one. Pick a SHORT, UNIQUE anchor line (a section header, a decl name) so it is unambiguous and easy to escape in JSON.\n- `{ \"kind\": \"write_file\", \"target\": \"doc/library_review/<Area>.json\", \"from\": \"area.json\" }` — full-content write of a NON-Lean record file (headline sidecar, `doc/API.md`). Read the current file first, write the COMPLETE updated content to the staged `from` file (add entries/markers, never drop existing ones), and reference it here.\n\n## Hard rules\n- NEVER modify or delete an existing line in an existing Lean file. Integrating must be additive (new files + insert-only merges). If it genuinely requires editing an existing proven decl, do NOT — pick a different placement and explain in `notes`.\n- Final Lean content may import and reference only Mathlib/Causalean modules. Rewrite every temporary staging reference; no `CausalSmith` import or qualified reference may remain in a staged create/merge body.\n- Every `target` stays within the Causalean root; every `from` is a file you wrote under the staging dir.\n- Do NOT run `lake build` / `library_index` / `embed` / `doc:gen` yourself — the system runs the full gate and rolls back on any failure. Emit only the manifest.\n- Keep the manifest JSON small and valid: no code inside it, no trailing commas, all strings closed. If unsure, fewer ops with cleaner anchors.\n",
  "promptBytes": 15897,
  "rawOutput": "{\"ops\":[{\"kind\":\"merge_lean\",\"target\":\"Causalean/Stat/UStatistic/OrderM/Basic.lean\",\"anchor\":\"end Causalean.Stat\",\"from\":\"merge_basic.lean\"},{\"kind\":\"merge_lean\",\"target\":\"Causalean/Stat/UStatistic/OrderM/Variance.lean\",\"anchor\":\"end Causalean.Stat\",\"from\":\"merge_variance.lean\"},{\"kind\":\"create_file\",\"target\":\"Causalean/Stat/UStatistic/OrderM/PartialMatching.lean\",\"from\":\"partial_matching.lean\",\"newModule\":\"Causalean.Stat.UStatistic.OrderM.PartialMatching\"},{\"kind\":\"create_file\",\"target\":\"Causalean/Stat/UStatistic/OrderM/MixedOrderCovariance.lean\",\"from\":\"mixed_order_covariance.lean\",\"newModule\":\"Causalean.Stat.UStatistic.OrderM.MixedOrderCovariance\"},{\"kind\":\"create_file\",\"target\":\"Causalean/Stat/UStatistic/OrderM/MixedOrderBounds.lean\",\"from\":\"mixed_order_bounds.lean\",\"newModule\":\"Causalean.Stat.UStatistic.OrderM.MixedOrderBounds\"},{\"kind\":\"write_file\",\"target\":\"doc/library_review/Stat.json\",\"from\":\"stat.json\"},{\"kind\":\"write_file\",\"target\":\"doc/API.md\",\"from\":\"api.md\"}],\"notes\":\"Placed the reusable API in Stat/UStatistic/OrderM, merging generic statistics into Basic and unbiasedness into Variance. Added focused matching, mixed-covariance, and normalization-bound modules with canonical documentation and headline curation; the combined Lean content type-checks.\"}",
  "parsed": {
    "ops": [
      {
        "kind": "merge_lean",
        "target": "Causalean/Stat/UStatistic/OrderM/Basic.lean",
        "anchor": "end Causalean.Stat",
        "from": "merge_basic.lean"
      },
      {
        "kind": "merge_lean",
        "target": "Causalean/Stat/UStatistic/OrderM/Variance.lean",
        "anchor": "end Causalean.Stat",
        "from": "merge_variance.lean"
      },
      {
        "kind": "create_file",
        "target": "Causalean/Stat/UStatistic/OrderM/PartialMatching.lean",
        "from": "partial_matching.lean",
        "newModule": "Causalean.Stat.UStatistic.OrderM.PartialMatching"
      },
      {
        "kind": "create_file",
        "target": "Causalean/Stat/UStatistic/OrderM/MixedOrderCovariance.lean",
        "from": "mixed_order_covariance.lean",
        "newModule": "Causalean.Stat.UStatistic.OrderM.MixedOrderCovariance"
      },
      {
        "kind": "create_file",
        "target": "Causalean/Stat/UStatistic/OrderM/MixedOrderBounds.lean",
        "from": "mixed_order_bounds.lean",
        "newModule": "Causalean.Stat.UStatistic.OrderM.MixedOrderBounds"
      },
      {
        "kind": "write_file",
        "target": "doc/library_review/Stat.json",
        "from": "stat.json"
      },
      {
        "kind": "write_file",
        "target": "doc/API.md",
        "from": "api.md"
      }
    ],
    "notes": "Placed the reusable API in Stat/UStatistic/OrderM, merging generic statistics into Basic and unbiasedness into Variance. Added focused matching, mixed-covariance, and normalization-bound modules with canonical documentation and headline curation; the combined Lean content type-checks."
  },
  "ok": true,
  "durationMs": 564163
}
