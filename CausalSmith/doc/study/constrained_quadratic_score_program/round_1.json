{
  "round": 1,
  "fillers": [
    {
      "id": "projresidual-moments",
      "ok": true,
      "summary": " in `ProjectionResidual.lean`:\n\n- `integral_projResidual`\n- `integral_id_mul_projResidual`\n- `integral_sq_projResidual`\n- `integral_sq_mul_projResidual`\n\nRemaining in this file: none. `rg` finds no `sorry`/`admit`, and Lean LSP diagnostics report no errors or warnings for the file.\n\nBlocker: `lake build CausalSmith.Substrate.ConstrainedQuadraticScoreProgram.ProjectionResidual` could not run in this sandbox. Normal Lake hits a read-only lock write under `/<home>/lean-rademacher/...`; redirecting Lake state to `/tmp` then fails because network access is blocked while fetching dependencies."
    },
    {
      "id": "scoreprogram-fillers",
      "ok": true,
      "summary": "Closed all three target declarations in `ScoreProgram.lean`:\n\n- `optScore_feasible`\n- `optScore_cost`\n- `feasibleScore_cost_lower_bound`\n\nRemaining in `ScoreProgram.lean`: none. `rg` finds no `sorry` in the file, and Lean LSP diagnostics report no errors or warnings for the edited declarations.\n\nBlocker: I could not run the requested `lake build CausalSmith.Substrate.ConstrainedQuadraticScoreProgram.ScoreProgram` because Lake tries to write `/<home>/lean-rademacher/.lake/config/FoML/lakefile.olean.lock`, which is outside the writable sandbox and fails with `read-only file system`."
    }
  ],
  "build": {
    "ok": true,
    "errors": [],
    "sorryCount": 0,
    "perFile": {}
  }
}
