{
  "agent": "scaffolder",
  "round": 1,
  "callId": "main",
  "model": "opus",
  "prompt": "You are the SCAFFOLDER (opus) in an autonomous substrate-build loop. You build a generic, reusable Lean substrate module.\n\n## Requirement\n# Substrate requirement: bernstein_szego_trig\n\n## Goal\nThe sharp Bernstein/Szegő differential inequality for the trigonometric polynomial `Q(t) = R(cos t)` arising from a real algebraic polynomial `R` of degree ≤ β: the derivative is controlled by `β·√(‖Q‖∞² − Q²)`.\n\n## Provides (API contract)\n- `szego_deriv_sq_bound (R : Polynomial ℝ) (β : ℕ) (hβ : R.natDegree ≤ β) (M : ℝ) (hM : ∀ t, |R.eval (Real.cos t)| ≤ M)` :\n  `∀ t, (deriv (fun s => R.eval (Real.cos s)) t)^2 + β^2 * (R.eval (Real.cos t))^2 ≤ β^2 * M^2`.\n  (Equivalently `|Q'(t)| ≤ β·√(M² − Q(t)²)` where `Q t = R.eval (cos t)`.)\n\n## Statement / milestones\nLet `Q(t) = R.eval (cos t)` for `R : ℝ[X]`, `R.natDegree ≤ β`. Then `Q` is a real even trigonometric polynomial of degree ≤ β and `Q'(t)² + β²Q(t)² ≤ β²‖Q‖∞²`. Decomposition (zero-counting / Szegő route — build these sub-lemmas):\n1. `Q'(t) = -sin t · R'(cos t)`; `Q` is C^∞. (chain rule, `Polynomial.deriv`, `Real.deriv_cos`.)\n2. **≤2β zeros lemma** (the load-bearing step, absent from Mathlib): via `z = exp(i t)`, `cos t = (z + z⁻¹)/2`, so `Q(t) = R((z+z⁻¹)/2)`. Clearing denominators, `z^β · Q = P(z)` for an algebraic polynomial `P : ℂ[X]` of degree ≤ 2β; hence `Q` (as a function of `t ∈ [0,2π)`) has at most `2β` zeros counted with multiplicity — from `Polynomial.card_roots` / `Polynomial.roots` on `P`. State and prove this zero-count.\n3. **Szegő comparison**: fix `t₀`; the degree-β trig interpolant `S(t) = Q(t₀)·cos(β(t−t₀)) + (Q'(t₀)/β)·sin(β(t−t₀))` matches `Q` and `Q'` at `t₀`; if the inequality failed at `t₀`, `Q − S` (a degree ≤ β trig poly) would have `> 2β` zeros/sign changes on a period, contradicting step 2. Conclude the pointwise bound.\n\nIf a cleaner Mathlib-native route exists (e.g. a direct `Polynomial.Chebyshev`/`Complex` argument, or an existing Bernstein/Markov lemma), take it — the deliverable is `szego_deriv_sq_bound`, however proved.\n\n## Standard reference\nSzegő's inequality / Bernstein's inequality for trigonometric polynomials; see Rivlin, \"The Chebyshev Polynomials\" (1974), or DeVore–Lorentz \"Constructive Approximation\" Ch. 4 (Bernstein–Szegő). Standard, classical.\n\n## Intended reuse\nDischarges the last 2 sorries of `CausalSmith/Substrate/EhlichZellerMesh/Bernstein.lean` (`czTrig_szego_deriv` and, via it, `czTrig_arccos_lipschitz`), completing the Ehlich–Zeller mesh inequality and thereby the headline of run `exp_rollout_chebyshev_minimax / tv_envelope_rollout_design`. Needs the general R-of-arbitrary-degree-β statement.\n\n## May assume / must derive\n- MAY assume Mathlib real/complex analysis: `Real.cos`, `Real.sin`, `deriv`, chain rule, `Polynomial.eval`, `Polynomial.natDegree`, `Complex.exp`, `Polynomial.roots`, `Polynomial.card_roots_le_degree`.\n- MUST derive: the ≤2β-zeros lemma (step 2) and the Szegő comparison (step 3). Do NOT assume Bernstein/Szegő as an unproven axiom or `sorry`. If, after a genuine attempt, step 2 (the z=e^{it} zero-count) or step 3 needs a substantial upstream Mathlib development, STOP and report the precise remaining sub-lemma as the blocker — honest partial progress with a precise blocker is valuable.\n\n## Non-goals (optional)\nThe `L^p` Bernstein inequality, sharp constants beyond `β`, complex-coefficient trig polynomials, and the general (non-`R(cos t)`) trig-polynomial formulation are out of scope. Only the even `R(cos t)` form is needed.\n\n## Known building blocks (optional)\n- `Real.deriv_cos`, `Real.sin_sq_add_cos_sq`, `Polynomial.deriv`, chain rule (`deriv_comp` / `HasDerivAt.comp`).\n- `Complex.exp`, `Complex.cos`, the substitution `cos t = (e^{it}+e^{-it})/2`.\n- `Polynomial.roots`, `Polynomial.card_roots_le_degree`, `Multiset.card`.\n\n## Target module (optional)\n`Causalean.Mathlib.Analysis.BernsteinSzego`\n\n\n## Workspace\n- Write/extend Lean files under: /<repo-root>/CausalSmith/CausalSmith/Substrate/BernsteinSzegoTrig\n- Module prefix (one file per module): CausalSmith.Substrate.BernsteinSzegoTrig.<Name>\n- This is build round 1 of at most 10.\n\n## Your task this turn\n0. ASSESS CURRENT STATUS FROM GROUND TRUTH. You are a fresh invocation with NO memory of prior turns — your only context is this prompt plus what you read from disk. Read the current files under the Lean dir and check their real state (remaining `sorry`s and any errors) via lean-lsp diagnostics or a targeted build. Trust the files over the prior round report (a report can lag a partial filler edit). The report and your prior plan below tell you the INTENT; the files tell you the TRUTH.\n1. Search the existing library FIRST (run `npm run search -- \"<concept>\"` in CausalSmith/tools; use lean-lsp local/leansearch) to reuse, not reinvent.\n1b. FETCH THE PRIMARY SOURCE when possible. If the Requirement names a reference (or you find the canonical one), fetch it to ground the exact statement, hypotheses, and naming/conventions you formalize — via WebFetch/WebSearch or Bash `curl`/`wget`; for an arXiv paper download the LaTeX source and read the `.tex`, not the PDF. Skip only when no source is named or none is reachable; never block on it.\n2. UPDATE `plan_markdown` as a STRUCTURED STATUS LEDGER (this IS the memory the next turn inherits — make it concrete, not a vision doc). Use these sections: `## Done` (decls proven/closed), `## Remaining` (the open sorries, by file), `## Blocked` (what's stuck and why), `## Decisions` (design/API choices + what was tried and rejected, so the next turn doesn't repeat dead ends).\n3. WRITE or EXTEND the Lean scaffold: precise statements with `sorry` for unproven proofs. The file(s) MUST compile with only `sorry` warnings (no hard errors). Keep each file focused (<300 lines) with a `/-! -/` module docstring and a docstring on each public declaration (first paragraph = self-contained NL translation).\n4. DECIDE how many codex subagents to use to fill the sorries this round, and emit ONE prompt per subagent (partition the sorries as you judge best).\n\n## Integrity (HARD RULES — non-negotiable)\n- Do NOT LAUNDER. Never weaken, narrow, or special-case a statement, and never delete or relax a hypothesis that should be proven, just to make the build pass or satisfy the reviewer. The statements must stay the genuine, general results the Requirement asks for. Strengthening an assumption to dodge a hard case, replacing the real goal with a corollary, or hiding the difficulty in a definition all count as laundering — do not do it.\n- Do NOT create VACUOUS statements. No trivially-true claims, no `True`-like lemmas, no uninhabitable/contradictory hypotheses, no premises that are never satisfiable (an always-false hypothesis makes any conclusion hold for free). Every theorem must have real content and be applicable to the intended inhabited inputs.\n- When revising after a failed review, FIX THE PROOF / API — not the goalposts. Address the findings without diluting what the module proves.\n- If a clean, true, non-vacuous statement is genuinely out of reach, choose decision=\"escalate\" with the reason. Escalating is correct; laundering or shipping a vacuous statement is a failure.\n\n## Decisions\n- decision=\"build\": you wrote/updated the scaffold and want the fillers to run this round. Provide `codex_prompts` (>=1).\n- decision=\"review\": choose this ONLY after you have VERIFIED from ground truth that the module compiles with ZERO remaining sorries and zero errors (confirm via diagnostics/build this turn — do not infer it from the report) AND the statements are the genuine, non-vacuous results (no laundering). `codex_prompts` may be empty.\n- decision=\"escalate\": the goal is wrong, unrealistic, or only reachable by weakening it into something vacuous; set `escalation.reason`. The run halts.\n\nEmit ONLY the StructuredOutput JSON object: { decision, plan_markdown, codex_prompts: [{id, target_decls, prompt}], escalation? }.\n`plan_markdown` is your living status ledger (persisted to plan.md and fed back to you next turn) — keep it accurate.\n",
  "promptBytes": 8219,
  "rawOutput": "This is a fresh round 1 — no `BernsteinSzegoTrig` files exist yet. The deliverable is the sharp Szegő inequality. Let me verify the key Mathlib building blocks before scaffolding.\nKey lemmas exist. Let me verify my provable core (derivative + reduction) compiles before writing the scaffold.\nProven core compiles (only style lints). Now I'll write the scaffold. Creating the three files:\nNow the main `Szego.lean` with the interpolant and the deliverable theorem:\nNow let me verify all three files compile with only `sorry` warnings:\nAll three files build with only `sorry` warnings; `Basic.lean` is fully proven. Let me record ground truth and dispatch fillers.\n{\"decision\":\"build\",\"plan_markdown\":\"## Module: `CausalSmith.Substrate.BernsteinSzegoTrig`\\nDeliverable: `Szego.szego_deriv_sq_bound` — for `R : ℝ[X]`, `R.natDegree ≤ β`, and `M` bounding `|R(cos t)|` on the whole period, `(d/dt R(cos t))² + β²·R(cos t)² ≤ β²·M²`. This is the sharp Szegő/Bernstein trig inequality. Statement matches the requirement contract verbatim (squared form). Verified: whole module builds (`lake build …Szego`) with ONLY `sorry` warnings, no errors.\\n\\n## Files\\n- `Basic.lean` — **fully proven, 0 sorries.** `hasDerivAt_cosComp`, `deriv_cosComp`: chain-rule `d/dt R(cos t) = R'(cos t)·(−sin t)`.\\n- `TrigPoly.lean` — `IsTrigPolyLE n f` predicate (`f = ∑_{k≤n} a k cos(kt)+b k sin(kt)`) + zero-count crux.\\n- `Szego.lean` — interpolant `szegoInterp` + memberships + the deliverable.\\n\\n## Done\\n- `Basic.hasDerivAt_cosComp`, `Basic.deriv_cosComp` (proven).\\n- `Szego.szegoInterp_self` (proven: `S(t₀)=Q₀`).\\n- Reduction sanity-checked offline: main theorem follows from the pointwise trig Szegő at `x=cos t` via `sin²t=1−cos²t` (nlinarith). The deliverable IS the pointwise trig Szegő, so no separate \\\"algebraic\\\" layer is introduced (algebraic ⇔ trig form are the same content — avoided a redundant lemma).\\n\\n## Remaining (open sorries, by file)\\n- `TrigPoly.lean`: `isTrigPolyLE_const`, `IsTrigPolyLE.mono`, `IsTrigPolyLE.sub` (elementary coefficient algebra), `IsTrigPolyLE.card_zeros_le` (**the load-bearing crux**: nonzero deg-≤n trig poly has ≤2n zeros in [0,2π), via z=e^{it}→ ℂ[X] of deg ≤2n and `card_roots_le_degree`).\\n- `Szego.lean`: `szegoInterp_isTrigPolyLE` (angle-subtraction expansion), `cosComp_isTrigPolyLE` (Chebyshev-basis expansion, `Polynomial.Chebyshev.T_real_cos`), `szego_deriv_sq_bound` (**final comparison assembly** — HELD to round 2).\\n\\n## Blocked\\n- Nothing hard-blocked. Risk concentrated in `card_zeros_le` (crux) and the final assembly. If `card_zeros_le` needs the multiplicity-refined count for the `t₀` double-zero step, the strict-inequality contradiction (assume `A>M` strictly ⇒ 2β strict sign changes ⇒ 2β distinct zeros, plus `t₀` a further zero since `S'(t₀)=Q'(t₀)` blocks it being a simple sign-change) keeps the distinct-zero count sufficient — noted for the assembly round.\\n\\n## Decisions\\n- Deliverable stated directly as the trig pointwise inequality (no fake `algebraic_szego` layer). Verified the main←pointwise reduction is trivial, so the pointwise form is the honest target.\\n- `M` hypothesis is `∀ t, |R(cos t)| ≤ M` (whole-period sup bound), exactly what the comparison needs; implies `M ≥ 0`. Not weakened.\\n- `IsTrigPolyLE` uses explicit real Fourier coefficients `a b : ℕ → ℝ` (not the ℂ-exponential encoding) so memberships (const, sub, Chebyshev, interpolant) are elementary; the ℂ encoding is introduced only *inside* `card_zeros_le` where roots are counted.\\n- `szegoInterp` uses `Q₁/β`; well-defined for `β ≥ 1`. The `β=0` case of the deliverable is handled separately in the assembly (R constant ⇒ derivative 0). Noted for round 2.\\n- Round 2 plan: scaffold interpolant amplitude/derivative lemmas (`S'(t₀)=Q₁` for β≥1; `S(t)=A·cos(β(t−t₀)−ψ)`, `|S|≤A`, and its 2β extrema), then assemble `szego_deriv_sq_bound`.\\n\\n## This round's fillers (3)\\n- A: elementary `IsTrigPolyLE` closure + `szegoInterp_isTrigPolyLE`.\\n- B: `cosComp_isTrigPolyLE` (Chebyshev).\\n- C: `IsTrigPolyLE.card_zeros_le` (crux).\",\"codex_prompts\":[{\"id\":\"trig-algebra\",\"target_decls\":[\"CausalSmith.Substrate.BernsteinSzegoTrig.isTrigPolyLE_const\",\"CausalSmith.Substrate.BernsteinSzegoTrig.IsTrigPolyLE.mono\",\"CausalSmith.Substrate.BernsteinSzegoTrig.IsTrigPolyLE.sub\",\"CausalSmith.Substrate.BernsteinSzegoTrig.szegoInterp_isTrigPolyLE\"],\"prompt\":\"Work in the Lean 4 + Mathlib project at /<repo-root>/CausalSmith. Replace `sorry` in FOUR declarations. Do NOT change any statement/signature, do NOT add axioms, do NOT weaken hypotheses. Verify with `lake build CausalSmith.Substrate.BernsteinSzegoTrig.Szego` (must end with only `sorry` warnings from OTHER decls and no errors). Use lean-lsp tools (`lean_goal`, `lean_hover_info`, `lean_loogle`, `lean_leansearch`) to find exact lemma names.\\n\\nThe predicate (in `CausalSmith/Substrate/BernsteinSzegoTrig/TrigPoly.lean`) is:\\n`def IsTrigPolyLE (n : ℕ) (f : ℝ → ℝ) : Prop := ∃ a b : ℕ → ℝ, ∀ t, f t = ∑ k ∈ Finset.range (n+1), (a k * Real.cos ((k:ℝ)*t) + b k * Real.sin ((k:ℝ)*t))`.\\n\\nTargets:\\n1. `isTrigPolyLE_const (n) (c)` : `IsTrigPolyLE n (fun _ => c)`. Use `a = fun k => if k = 0 then c else 0`, `b = 0`. Then the sum collapses: only k=0 survives (`Real.cos 0 = 1`, `Real.sin ((k:ℝ)*t)` term killed by b=0). Use `Finset.sum_eq_single 0` or `simp [Finset.sum_ite_eq]`.\\n2. `IsTrigPolyLE.mono {n m f} (hf : IsTrigPolyLE n f) (hnm : n ≤ m)` : `IsTrigPolyLE m f`. Extend the coeff functions by zero past n; the extra terms (k from n+1..m) vanish. Use `Finset.sum_subset` with `Finset.range (n+1) ⊆ Finset.range (m+1)` (from `Nat.succ_le_succ hnm`), the extra summands being 0.\\n3. `IsTrigPolyLE.sub {n f g} (hf hg)` : `IsTrigPolyLE n (fun t => f t - g t)`. Take `a := a_f - a_g`, `b := b_f - b_g` (pointwise). Then `f t - g t = ∑ (…) - ∑ (…) = ∑ ((a_f-a_g) k * cos + (b_f-b_g) k * sin)` via `Finset.sum_sub_distrib` and `ring`/`ring_nf` inside the sum (`Finset.sum_congr`).\\n4. `szegoInterp_isTrigPolyLE (β) (Q₀ Q₁ t₀)` : `IsTrigPolyLE β (szegoInterp β Q₀ Q₁ t₀)` where `szegoInterp β Q₀ Q₁ t₀ t = Q₀ * Real.cos ((β:ℝ)*(t - t₀)) + (Q₁/β) * Real.sin ((β:ℝ)*(t - t₀))`. Expand via angle-subtraction: `Real.cos_sub`, `Real.sin_sub` give `cos(β t − β t₀) = cos(βt)cos(βt₀)+sin(βt)sin(βt₀)`, `sin(β t − β t₀)=sin(βt)cos(βt₀)−cos(βt)sin(βt₀)`. Note `(β:ℝ)*(t−t₀) = (β:ℝ)*t − (β:ℝ)*t₀`. So the interpolant `= A_β cos(βt) + B_β sin(βt)` for constants `A_β,B_β` depending on Q₀,Q₁,β,t₀. Provide `a := fun k => if k = β then A_β else 0`, `b := fun k => if k = β then B_β else 0`; the sum collapses to the k=β term (β ∈ range(β+1) since β < β+1). Use `Finset.sum_eq_single β`.\\n\\nKeep proofs clean; prefer `Finset.sum_eq_single`/`Finset.sum_congr`/`ring`. Report the final `lake build` tail.\"},{\"id\":\"chebyshev-expansion\",\"target_decls\":[\"CausalSmith.Substrate.BernsteinSzegoTrig.cosComp_isTrigPolyLE\"],\"prompt\":\"Work in the Lean 4 + Mathlib project at /<repo-root>/CausalSmith. Prove ONE declaration by replacing its `sorry` in `CausalSmith/Substrate/BernsteinSzegoTrig/Szego.lean`. Do NOT change the statement, add axioms, or weaken hypotheses. Verify with `lake build CausalSmith.Substrate.BernsteinSzegoTrig.Szego` (only `sorry` warnings from OTHER decls, no errors). Use lean-lsp tools to find exact names.\\n\\nTarget: `cosComp_isTrigPolyLE (R : Polynomial ℝ) (β : ℕ) (hβ : R.natDegree ≤ β) : IsTrigPolyLE β (fun t => R.eval (Real.cos t))`.\\n\\nThe predicate is `IsTrigPolyLE n f := ∃ a b : ℕ → ℝ, ∀ t, f t = ∑ k ∈ Finset.range (n+1), (a k * Real.cos ((k:ℝ)*t) + b k * Real.sin ((k:ℝ)*t))`.\\n\\nMath route (Chebyshev basis). Key Mathlib fact: `Polynomial.Chebyshev.T_real_cos (θ : ℝ) (n : ℤ) : Polynomial.eval (Real.cos θ) (Polynomial.Chebyshev.T ℝ n) = Real.cos (↑n * θ)`. So `T k (cos t) = cos (k t)` for `k : ℕ` (cast `k` to ℤ). Any polynomial `R` of degree ≤ β is a ℝ-linear combination of `T 0, …, T β` because `{T 0,…,T n}` is a basis of `{p : deg ≤ n}` (equivalently `T k` has degree exactly `k` with leading behaviour, and they are linearly independent / span). Then `R(cos t) = ∑_{k≤β} c_k · T k (cos t) = ∑_{k≤β} c_k · cos (k t)`, giving `a k = c_k`, `b k = 0`.\\n\\nStrategy options — pick what compiles:\\n(a) PREFERRED, elementary: prove by STRONG INDUCTION on `R.natDegree` (or on a degree bound) that every `R` with `R.natDegree ≤ β` yields `IsTrigPolyLE β (R∘cos)`. Base: `R` constant → `isTrigPolyLE_const` (already available, possibly still `sorry` but usable) composed appropriately; actually reduce a degree-d poly to lower degree by subtracting `(leadingCoeff/T_d.leadingCoeff)·T d`: since `T d` has degree d and `T_real_cos` turns it into `cos(d·t)` (a trig term of degree ≤ β), and the remainder `R − c·T d` has degree < d, apply the IH and add the single `cos(d t)` term. Use `IsTrigPolyLE.sub`, `IsTrigPolyLE.mono`, and the fact that `fun t => cos(d t)` is `IsTrigPolyLE d` (coefficients: a d = 1, rest 0) then `.mono` to β. Combine additively (you may need an `IsTrigPolyLE.add` helper — add it locally if useful, but the target set is only `cosComp_isTrigPolyLE`; a private `add` lemma is fine).\\n(b) If a clean `Polynomial.Chebyshev` spanning lemma exists in Mathlib (search `lean_leansearch \\\"Chebyshev polynomials form a basis\\\"`, `lean_loogle \\\"Polynomial.Chebyshev.T\\\"`), use it directly.\\n\\nSearch first: `lean_loogle` for `Polynomial.Chebyshev.T_natDegree`, degree of `T`, and any basis/span lemma. If you introduce local helper lemmas (e.g. `cos_natMul_isTrigPolyLE`, `IsTrigPolyLE.add`), place them ABOVE the target in the same file and keep them non-`sorry`. Report the final `lake build` tail.\"},{\"id\":\"zero-count-crux\",\"target_decls\":[\"CausalSmith.Substrate.BernsteinSzegoTrig.IsTrigPolyLE.card_zeros_le\"],\"prompt\":\"Work in the Lean 4 + Mathlib project at /<repo-root>/CausalSmith. Prove ONE hard declaration by replacing its `sorry` in `CausalSmith/Substrate/BernsteinSzegoTrig/TrigPoly.lean`. This is the load-bearing lemma of a Szegő-inequality substrate; treat it as a real project. Do NOT change the statement, add axioms, `sorry`, or weaken hypotheses. Verify with `lake build CausalSmith.Substrate.BernsteinSzegoTrig.TrigPoly` (only `sorry` warnings from OTHER decls in that file, no errors). Use lean-lsp tools (`lean_goal`, `lean_loogle`, `lean_leansearch`, `lean_hover_info`) heavily to find exact Mathlib names.\\n\\nPredicate: `IsTrigPolyLE n f := ∃ a b : ℕ → ℝ, ∀ t, f t = ∑ k ∈ Finset.range (n+1), (a k * Real.cos ((k:ℝ)*t) + b k * Real.sin ((k:ℝ)*t))`.\\n\\nTarget: `IsTrigPolyLE.card_zeros_le {n} {f} (hf : IsTrigPolyLE n f) (hne : ∃ t, f t ≠ 0) {S : Finset ℝ} (hS : ↑S ⊆ Set.Ico 0 (2*Real.pi)) (hzero : ∀ t ∈ S, f t = 0) : S.card ≤ 2*n`. I.e. a nonzero real trig polynomial of degree ≤ n has at most 2n zeros in one period.\\n\\nProof route (z = e^{it}). Obtain `a b` from `hf`. On the unit circle `z = Complex.exp (I t)`, we have `Real.cos(k t) = ((z^k + z^{-k})/2).re`… but cleaner: work entirely in ℂ. Define the complex polynomial `P : Polynomial ℂ` of degree ≤ 2n by\\n`P = ∑ k ∈ Finset.range (n+1), (Complex.ofReal (a k)/2 - Complex.ofReal (b k) * Complex.I/2) • X^(n+k)  +  (Complex.ofReal (a k)/2 + Complex.ofReal (b k)*Complex.I/2) • X^(n-k)`\\n(i.e. build it via `Finset.sum` of `Polynomial.C _ * Polynomial.X ^ _`). KEY IDENTITY: for every real t, with `z := Complex.exp (Complex.I * t)`,\\n`P.eval z = z^n * ((f t : ℝ) : ℂ)`.\\nThis follows from `cos(kt) = (e^{ikt}+e^{-ikt})/2`, `sin(kt) = (e^{ikt}-e^{-ikt})/(2i)` (Mathlib: `Complex.cos`, `Complex.sin`, `Complex.exp_mul_I`, `Real.cos`/`Complex.ofReal_cos` bridging `Complex.cos (t:ℂ) = (Real.cos t : ℂ)`; look up `Complex.ofReal_cos`, `Complex.exp_ofReal_mul_I_re`, `Complex.cos_eq`, `Complex.sin_eq`). Note `z^k = exp(I k t)`, `z^{-k} = exp(-I k t)` and `z ≠ 0` (`Complex.exp_ne_zero`).\\n\\nConsequences:\\n- `P ≠ 0`: if `P = 0` then `P.eval z = 0` for all t, so `z^n * (f t) = 0`, and `z^n ≠ 0`, giving `f t = 0` for all t, contradicting `hne`. (`natDegree ≤ 2n` also gives `P.eval` a genuine poly; but nonzeroness is what matters for roots.)\\n- For each `t ∈ S`, `f t = 0` so `P.eval (exp (I t)) = 0`, i.e. `exp (I t)` is a root of `P`.\\n- The map `t ↦ Complex.exp (Complex.I * t)` is INJECTIVE on `[0, 2π)` (distinct arguments in a length-2π window give distinct points on the circle). Prove via: if `exp(I s)=exp(I t)` with `s,t ∈ [0,2π)` then `s = t` — use `Complex.exp_eq_exp_iff_exists_int` / `Complex.exp_eq_one_iff`, periodicity `2π`, and the window bound. Search `lean_leansearch \\\"complex exponential injective on interval\\\"`.\\n- Therefore `S.image (fun t => Complex.exp (Complex.I * t))` has card `= S.card` (injective on S ⊆ [0,2π)) and is ⊆ the finset of roots of `P`. Distinct roots of a nonzero polynomial number ≤ its degree: use `Polynomial.card_roots_le_degree` / `Polynomial.roots` with `Multiset.toFinset`, or `Polynomial.setOf_isRoot_finite`; the count of distinct roots ≤ `P.natDegree ≤ 2n`.\\n- Bound `P.natDegree ≤ 2*n`: every monomial has exponent `n+k ≤ 2n` or `n-k ≤ n ≤ 2n` for `k ≤ n`; use `Polynomial.natDegree_sum_le`, `Polynomial.natDegree_C_mul_X_pow_le` etc.\\n\\nAssemble: `S.card = (S.image e).card ≤ P.roots.toFinset.card ≤ P.natDegree ≤ 2n`.\\n\\nThis is long. Introduce private helper lemmas ABOVE the target in the same file as needed (the eval identity, injectivity, natDegree bound), all non-`sorry`. Search for exact lemma names before using them; verify each with `lean_hover_info`. If you get genuinely stuck on ONE named sub-lemma after real effort, leave THAT sub-lemma the only `sorry`, finish everything else, and report precisely which sub-lemma blocked you and why. Report the final `lake build` tail.\"}]}\n{\"decision\":\"build\",\"plan_markdown\":\"## Module: `CausalSmith.Substrate.BernsteinSzegoTrig`\\nDeliverable: `Szego.szego_deriv_sq_bound` — for `R : ℝ[X]`, `R.natDegree ≤ β`, and `M` bounding `|R(cos t)|` on the whole period, `(d/dt R(cos t))² + β²·R(cos t)² ≤ β²·M²`. This is the sharp Szegő/Bernstein trig inequality. Statement matches the requirement contract verbatim (squared form). Verified: whole module builds (`lake build …Szego`) with ONLY `sorry` warnings, no errors.\\n\\n## Files\\n- `Basic.lean` — **fully proven, 0 sorries.** `hasDerivAt_cosComp`, `deriv_cosComp`: chain-rule `d/dt R(cos t) = R'(cos t)·(−sin t)`.\\n- `TrigPoly.lean` — `IsTrigPolyLE n f` predicate (`f = ∑_{k≤n} a k cos(kt)+b k sin(kt)`) + zero-count crux.\\n- `Szego.lean` — interpolant `szegoInterp` + memberships + the deliverable.\\n\\n## Done\\n- `Basic.hasDerivAt_cosComp`, `Basic.deriv_cosComp` (proven).\\n- `Szego.szegoInterp_self` (proven: `S(t₀)=Q₀`).\\n- Reduction sanity-checked offline: main theorem follows from the pointwise trig Szegő at `x=cos t` via `sin²t=1−cos²t` (nlinarith). The deliverable IS the pointwise trig Szegő, so no separate \\\"algebraic\\\" layer is introduced (algebraic ⇔ trig form are the same content — avoided a redundant lemma).\\n\\n## Remaining (open sorries, by file)\\n- `TrigPoly.lean`: `isTrigPolyLE_const`, `IsTrigPolyLE.mono`, `IsTrigPolyLE.sub` (elementary coefficient algebra), `IsTrigPolyLE.card_zeros_le` (**the load-bearing crux**: nonzero deg-≤n trig poly has ≤2n zeros in [0,2π), via z=e^{it}→ ℂ[X] of deg ≤2n and `card_roots_le_degree`).\\n- `Szego.lean`: `szegoInterp_isTrigPolyLE` (angle-subtraction expansion), `cosComp_isTrigPolyLE` (Chebyshev-basis expansion, `Polynomial.Chebyshev.T_real_cos`), `szego_deriv_sq_bound` (**final comparison assembly** — HELD to round 2).\\n\\n## Blocked\\n- Nothing hard-blocked. Risk concentrated in `card_zeros_le` (crux) and the final assembly. If `card_zeros_le` needs the multiplicity-refined count for the `t₀` double-zero step, the strict-inequality contradiction (assume `A>M` strictly ⇒ 2β strict sign changes ⇒ 2β distinct zeros, plus `t₀` a further zero since `S'(t₀)=Q'(t₀)` blocks it being a simple sign-change) keeps the distinct-zero count sufficient — noted for the assembly round.\\n\\n## Decisions\\n- Deliverable stated directly as the trig pointwise inequality (no fake `algebraic_szego` layer). Verified the main←pointwise reduction is trivial, so the pointwise form is the honest target.\\n- `M` hypothesis is `∀ t, |R(cos t)| ≤ M` (whole-period sup bound), exactly what the comparison needs; implies `M ≥ 0`. Not weakened.\\n- `IsTrigPolyLE` uses explicit real Fourier coefficients `a b : ℕ → ℝ` (not the ℂ-exponential encoding) so memberships (const, sub, Chebyshev, interpolant) are elementary; the ℂ encoding is introduced only *inside* `card_zeros_le` where roots are counted.\\n- `szegoInterp` uses `Q₁/β`; well-defined for `β ≥ 1`. The `β=0` case of the deliverable is handled separately in the assembly (R constant ⇒ derivative 0). Noted for round 2.\\n- Round 2 plan: scaffold interpolant amplitude/derivative lemmas (`S'(t₀)=Q₁` for β≥1; `S(t)=A·cos(β(t−t₀)−ψ)`, `|S|≤A`, and its 2β extrema), then assemble `szego_deriv_sq_bound`.\\n\\n## This round's fillers (3)\\n- A: elementary `IsTrigPolyLE` closure + `szegoInterp_isTrigPolyLE`.\\n- B: `cosComp_isTrigPolyLE` (Chebyshev).\\n- C: `IsTrigPolyLE.card_zeros_le` (crux).\",\"codex_prompts\":[{\"id\":\"trig-algebra\",\"target_decls\":[\"CausalSmith.Substrate.BernsteinSzegoTrig.isTrigPolyLE_const\",\"CausalSmith.Substrate.BernsteinSzegoTrig.IsTrigPolyLE.mono\",\"CausalSmith.Substrate.BernsteinSzegoTrig.IsTrigPolyLE.sub\",\"CausalSmith.Substrate.BernsteinSzegoTrig.szegoInterp_isTrigPolyLE\"],\"prompt\":\"Work in the Lean 4 + Mathlib project at /<repo-root>/CausalSmith. Replace `sorry` in FOUR declarations. Do NOT change any statement/signature, do NOT add axioms, do NOT weaken hypotheses. Verify with `lake build CausalSmith.Substrate.BernsteinSzegoTrig.Szego` (must end with only `sorry` warnings from OTHER decls and no errors). Use lean-lsp tools (`lean_goal`, `lean_hover_info`, `lean_loogle`, `lean_leansearch`) to find exact lemma names.\\n\\nThe predicate (in `CausalSmith/Substrate/BernsteinSzegoTrig/TrigPoly.lean`) is:\\n`def IsTrigPolyLE (n : ℕ) (f : ℝ → ℝ) : Prop := ∃ a b : ℕ → ℝ, ∀ t, f t = ∑ k ∈ Finset.range (n+1), (a k * Real.cos ((k:ℝ)*t) + b k * Real.sin ((k:ℝ)*t))`.\\n\\nTargets:\\n1. `isTrigPolyLE_const (n) (c)` : `IsTrigPolyLE n (fun _ => c)`. Use `a = fun k => if k = 0 then c else 0`, `b = 0`. Then the sum collapses: only k=0 survives (`Real.cos 0 = 1`, `Real.sin ((k:ℝ)*t)` term killed by b=0). Use `Finset.sum_eq_single 0` or `simp [Finset.sum_ite_eq]`.\\n2. `IsTrigPolyLE.mono {n m f} (hf : IsTrigPolyLE n f) (hnm : n ≤ m)` : `IsTrigPolyLE m f`. Extend the coeff functions by zero past n; the extra terms (k from n+1..m) vanish. Use `Finset.sum_subset` with `Finset.range (n+1) ⊆ Finset.range (m+1)` (from `Nat.succ_le_succ hnm`), the extra summands being 0.\\n3. `IsTrigPolyLE.sub {n f g} (hf hg)` : `IsTrigPolyLE n (fun t => f t - g t)`. Take `a := a_f - a_g`, `b := b_f - b_g` (pointwise). Then `f t - g t = ∑ (…) - ∑ (…) = ∑ ((a_f-a_g) k * cos + (b_f-b_g) k * sin)` via `Finset.sum_sub_distrib` and `ring`/`ring_nf` inside the sum (`Finset.sum_congr`).\\n4. `szegoInterp_isTrigPolyLE (β) (Q₀ Q₁ t₀)` : `IsTrigPolyLE β (szegoInterp β Q₀ Q₁ t₀)` where `szegoInterp β Q₀ Q₁ t₀ t = Q₀ * Real.cos ((β:ℝ)*(t - t₀)) + (Q₁/β) * Real.sin ((β:ℝ)*(t - t₀))`. Expand via angle-subtraction: `Real.cos_sub`, `Real.sin_sub` give `cos(β t − β t₀) = cos(βt)cos(βt₀)+sin(βt)sin(βt₀)`, `sin(β t − β t₀)=sin(βt)cos(βt₀)−cos(βt)sin(βt₀)`. Note `(β:ℝ)*(t−t₀) = (β:ℝ)*t − (β:ℝ)*t₀`. So the interpolant `= A_β cos(βt) + B_β sin(βt)` for constants `A_β,B_β` depending on Q₀,Q₁,β,t₀. Provide `a := fun k => if k = β then A_β else 0`, `b := fun k => if k = β then B_β else 0`; the sum collapses to the k=β term (β ∈ range(β+1) since β < β+1). Use `Finset.sum_eq_single β`.\\n\\nKeep proofs clean; prefer `Finset.sum_eq_single`/`Finset.sum_congr`/`ring`. Report the final `lake build` tail.\"},{\"id\":\"chebyshev-expansion\",\"target_decls\":[\"CausalSmith.Substrate.BernsteinSzegoTrig.cosComp_isTrigPolyLE\"],\"prompt\":\"Work in the Lean 4 + Mathlib project at /<repo-root>/CausalSmith. Prove ONE declaration by replacing its `sorry` in `CausalSmith/Substrate/BernsteinSzegoTrig/Szego.lean`. Do NOT change the statement, add axioms, or weaken hypotheses. Verify with `lake build CausalSmith.Substrate.BernsteinSzegoTrig.Szego` (only `sorry` warnings from OTHER decls, no errors). Use lean-lsp tools to find exact names.\\n\\nTarget: `cosComp_isTrigPolyLE (R : Polynomial ℝ) (β : ℕ) (hβ : R.natDegree ≤ β) : IsTrigPolyLE β (fun t => R.eval (Real.cos t))`.\\n\\nThe predicate is `IsTrigPolyLE n f := ∃ a b : ℕ → ℝ, ∀ t, f t = ∑ k ∈ Finset.range (n+1), (a k * Real.cos ((k:ℝ)*t) + b k * Real.sin ((k:ℝ)*t))`.\\n\\nMath route (Chebyshev basis). Key Mathlib fact: `Polynomial.Chebyshev.T_real_cos (θ : ℝ) (n : ℤ) : Polynomial.eval (Real.cos θ) (Polynomial.Chebyshev.T ℝ n) = Real.cos (↑n * θ)`. So `T k (cos t) = cos (k t)` for `k : ℕ` (cast `k` to ℤ). Any polynomial `R` of degree ≤ β is a ℝ-linear combination of `T 0, …, T β` because `{T 0,…,T n}` is a basis of `{p : deg ≤ n}` (equivalently `T k` has degree exactly `k` with leading behaviour, and they are linearly independent / span). Then `R(cos t) = ∑_{k≤β} c_k · T k (cos t) = ∑_{k≤β} c_k · cos (k t)`, giving `a k = c_k`, `b k = 0`.\\n\\nStrategy options — pick what compiles:\\n(a) PREFERRED, elementary: prove by STRONG INDUCTION on `R.natDegree` (or on a degree bound) that every `R` with `R.natDegree ≤ β` yields `IsTrigPolyLE β (R∘cos)`. Base: `R` constant → `isTrigPolyLE_const` (already available, possibly still `sorry` but usable) composed appropriately; actually reduce a degree-d poly to lower degree by subtracting `(leadingCoeff/T_d.leadingCoeff)·T d`: since `T d` has degree d and `T_real_cos` turns it into `cos(d·t)` (a trig term of degree ≤ β), and the remainder `R − c·T d` has degree < d, apply the IH and add the single `cos(d t)` term. Use `IsTrigPolyLE.sub`, `IsTrigPolyLE.mono`, and the fact that `fun t => cos(d t)` is `IsTrigPolyLE d` (coefficients: a d = 1, rest 0) then `.mono` to β. Combine additively (you may need an `IsTrigPolyLE.add` helper — add it locally if useful, but the target set is only `cosComp_isTrigPolyLE`; a private `add` lemma is fine).\\n(b) If a clean `Polynomial.Chebyshev` spanning lemma exists in Mathlib (search `lean_leansearch \\\"Chebyshev polynomials form a basis\\\"`, `lean_loogle \\\"Polynomial.Chebyshev.T\\\"`), use it directly.\\n\\nSearch first: `lean_loogle` for `Polynomial.Chebyshev.T_natDegree`, degree of `T`, and any basis/span lemma. If you introduce local helper lemmas (e.g. `cos_natMul_isTrigPolyLE`, `IsTrigPolyLE.add`), place them ABOVE the target in the same file and keep them non-`sorry`. Report the final `lake build` tail.\"},{\"id\":\"zero-count-crux\",\"target_decls\":[\"CausalSmith.Substrate.BernsteinSzegoTrig.IsTrigPolyLE.card_zeros_le\"],\"prompt\":\"Work in the Lean 4 + Mathlib project at /<repo-root>/CausalSmith. Prove ONE hard declaration by replacing its `sorry` in `CausalSmith/Substrate/BernsteinSzegoTrig/TrigPoly.lean`. This is the load-bearing lemma of a Szegő-inequality substrate; treat it as a real project. Do NOT change the statement, add axioms, `sorry`, or weaken hypotheses. Verify with `lake build CausalSmith.Substrate.BernsteinSzegoTrig.TrigPoly` (only `sorry` warnings from OTHER decls in that file, no errors). Use lean-lsp tools (`lean_goal`, `lean_loogle`, `lean_leansearch`, `lean_hover_info`) heavily to find exact Mathlib names.\\n\\nPredicate: `IsTrigPolyLE n f := ∃ a b : ℕ → ℝ, ∀ t, f t = ∑ k ∈ Finset.range (n+1), (a k * Real.cos ((k:ℝ)*t) + b k * Real.sin ((k:ℝ)*t))`.\\n\\nTarget: `IsTrigPolyLE.card_zeros_le {n} {f} (hf : IsTrigPolyLE n f) (hne : ∃ t, f t ≠ 0) {S : Finset ℝ} (hS : ↑S ⊆ Set.Ico 0 (2*Real.pi)) (hzero : ∀ t ∈ S, f t = 0) : S.card ≤ 2*n`. I.e. a nonzero real trig polynomial of degree ≤ n has at most 2n zeros in one period.\\n\\nProof route (z = e^{it}). Obtain `a b` from `hf`. On the unit circle `z = Complex.exp (I t)`, we have `Real.cos(k t) = ((z^k + z^{-k})/2).re`… but cleaner: work entirely in ℂ. Define the complex polynomial `P : Polynomial ℂ` of degree ≤ 2n by\\n`P = ∑ k ∈ Finset.range (n+1), (Complex.ofReal (a k)/2 - Complex.ofReal (b k) * Complex.I/2) • X^(n+k)  +  (Complex.ofReal (a k)/2 + Complex.ofReal (b k)*Complex.I/2) • X^(n-k)`\\n(i.e. build it via `Finset.sum` of `Polynomial.C _ * Polynomial.X ^ _`). KEY IDENTITY: for every real t, with `z := Complex.exp (Complex.I * t)`,\\n`P.eval z = z^n * ((f t : ℝ) : ℂ)`.\\nThis follows from `cos(kt) = (e^{ikt}+e^{-ikt})/2`, `sin(kt) = (e^{ikt}-e^{-ikt})/(2i)` (Mathlib: `Complex.cos`, `Complex.sin`, `Complex.exp_mul_I`, `Real.cos`/`Complex.ofReal_cos` bridging `Complex.cos (t:ℂ) = (Real.cos t : ℂ)`; look up `Complex.ofReal_cos`, `Complex.exp_ofReal_mul_I_re`, `Complex.cos_eq`, `Complex.sin_eq`). Note `z^k = exp(I k t)`, `z^{-k} = exp(-I k t)` and `z ≠ 0` (`Complex.exp_ne_zero`).\\n\\nConsequences:\\n- `P ≠ 0`: if `P = 0` then `P.eval z = 0` for all t, so `z^n * (f t) = 0`, and `z^n ≠ 0`, giving `f t = 0` for all t, contradicting `hne`. (`natDegree ≤ 2n` also gives `P.eval` a genuine poly; but nonzeroness is what matters for roots.)\\n- For each `t ∈ S`, `f t = 0` so `P.eval (exp (I t)) = 0`, i.e. `exp (I t)` is a root of `P`.\\n- The map `t ↦ Complex.exp (Complex.I * t)` is INJECTIVE on `[0, 2π)` (distinct arguments in a length-2π window give distinct points on the circle). Prove via: if `exp(I s)=exp(I t)` with `s,t ∈ [0,2π)` then `s = t` — use `Complex.exp_eq_exp_iff_exists_int` / `Complex.exp_eq_one_iff`, periodicity `2π`, and the window bound. Search `lean_leansearch \\\"complex exponential injective on interval\\\"`.\\n- Therefore `S.image (fun t => Complex.exp (Complex.I * t))` has card `= S.card` (injective on S ⊆ [0,2π)) and is ⊆ the finset of roots of `P`. Distinct roots of a nonzero polynomial number ≤ its degree: use `Polynomial.card_roots_le_degree` / `Polynomial.roots` with `Multiset.toFinset`, or `Polynomial.setOf_isRoot_finite`; the count of distinct roots ≤ `P.natDegree ≤ 2n`.\\n- Bound `P.natDegree ≤ 2*n`: every monomial has exponent `n+k ≤ 2n` or `n-k ≤ n ≤ 2n` for `k ≤ n`; use `Polynomial.natDegree_sum_le`, `Polynomial.natDegree_C_mul_X_pow_le` etc.\\n\\nAssemble: `S.card = (S.image e).card ≤ P.roots.toFinset.card ≤ P.natDegree ≤ 2n`.\\n\\nThis is long. Introduce private helper lemmas ABOVE the target in the same file as needed (the eval identity, injectivity, natDegree bound), all non-`sorry`. Search for exact lemma names before using them; verify each with `lean_hover_info`. If you get genuinely stuck on ONE named sub-lemma after real effort, leave THAT sub-lemma the only `sorry`, finish everything else, and report precisely which sub-lemma blocked you and why. Report the final `lake build` tail.\"}]}",
  "parsed": {
    "decision": "build",
    "plan_markdown": "## Module: `CausalSmith.Substrate.BernsteinSzegoTrig`\nDeliverable: `Szego.szego_deriv_sq_bound` — for `R : ℝ[X]`, `R.natDegree ≤ β`, and `M` bounding `|R(cos t)|` on the whole period, `(d/dt R(cos t))² + β²·R(cos t)² ≤ β²·M²`. This is the sharp Szegő/Bernstein trig inequality. Statement matches the requirement contract verbatim (squared form). Verified: whole module builds (`lake build …Szego`) with ONLY `sorry` warnings, no errors.\n\n## Files\n- `Basic.lean` — **fully proven, 0 sorries.** `hasDerivAt_cosComp`, `deriv_cosComp`: chain-rule `d/dt R(cos t) = R'(cos t)·(−sin t)`.\n- `TrigPoly.lean` — `IsTrigPolyLE n f` predicate (`f = ∑_{k≤n} a k cos(kt)+b k sin(kt)`) + zero-count crux.\n- `Szego.lean` — interpolant `szegoInterp` + memberships + the deliverable.\n\n## Done\n- `Basic.hasDerivAt_cosComp`, `Basic.deriv_cosComp` (proven).\n- `Szego.szegoInterp_self` (proven: `S(t₀)=Q₀`).\n- Reduction sanity-checked offline: main theorem follows from the pointwise trig Szegő at `x=cos t` via `sin²t=1−cos²t` (nlinarith). The deliverable IS the pointwise trig Szegő, so no separate \"algebraic\" layer is introduced (algebraic ⇔ trig form are the same content — avoided a redundant lemma).\n\n## Remaining (open sorries, by file)\n- `TrigPoly.lean`: `isTrigPolyLE_const`, `IsTrigPolyLE.mono`, `IsTrigPolyLE.sub` (elementary coefficient algebra), `IsTrigPolyLE.card_zeros_le` (**the load-bearing crux**: nonzero deg-≤n trig poly has ≤2n zeros in [0,2π), via z=e^{it}→ ℂ[X] of deg ≤2n and `card_roots_le_degree`).\n- `Szego.lean`: `szegoInterp_isTrigPolyLE` (angle-subtraction expansion), `cosComp_isTrigPolyLE` (Chebyshev-basis expansion, `Polynomial.Chebyshev.T_real_cos`), `szego_deriv_sq_bound` (**final comparison assembly** — HELD to round 2).\n\n## Blocked\n- Nothing hard-blocked. Risk concentrated in `card_zeros_le` (crux) and the final assembly. If `card_zeros_le` needs the multiplicity-refined count for the `t₀` double-zero step, the strict-inequality contradiction (assume `A>M` strictly ⇒ 2β strict sign changes ⇒ 2β distinct zeros, plus `t₀` a further zero since `S'(t₀)=Q'(t₀)` blocks it being a simple sign-change) keeps the distinct-zero count sufficient — noted for the assembly round.\n\n## Decisions\n- Deliverable stated directly as the trig pointwise inequality (no fake `algebraic_szego` layer). Verified the main←pointwise reduction is trivial, so the pointwise form is the honest target.\n- `M` hypothesis is `∀ t, |R(cos t)| ≤ M` (whole-period sup bound), exactly what the comparison needs; implies `M ≥ 0`. Not weakened.\n- `IsTrigPolyLE` uses explicit real Fourier coefficients `a b : ℕ → ℝ` (not the ℂ-exponential encoding) so memberships (const, sub, Chebyshev, interpolant) are elementary; the ℂ encoding is introduced only *inside* `card_zeros_le` where roots are counted.\n- `szegoInterp` uses `Q₁/β`; well-defined for `β ≥ 1`. The `β=0` case of the deliverable is handled separately in the assembly (R constant ⇒ derivative 0). Noted for round 2.\n- Round 2 plan: scaffold interpolant amplitude/derivative lemmas (`S'(t₀)=Q₁` for β≥1; `S(t)=A·cos(β(t−t₀)−ψ)`, `|S|≤A`, and its 2β extrema), then assemble `szego_deriv_sq_bound`.\n\n## This round's fillers (3)\n- A: elementary `IsTrigPolyLE` closure + `szegoInterp_isTrigPolyLE`.\n- B: `cosComp_isTrigPolyLE` (Chebyshev).\n- C: `IsTrigPolyLE.card_zeros_le` (crux).",
    "codex_prompts": [
      {
        "id": "trig-algebra",
        "target_decls": [
          "CausalSmith.Substrate.BernsteinSzegoTrig.isTrigPolyLE_const",
          "CausalSmith.Substrate.BernsteinSzegoTrig.IsTrigPolyLE.mono",
          "CausalSmith.Substrate.BernsteinSzegoTrig.IsTrigPolyLE.sub",
          "CausalSmith.Substrate.BernsteinSzegoTrig.szegoInterp_isTrigPolyLE"
        ],
        "prompt": "Work in the Lean 4 + Mathlib project at /<repo-root>/CausalSmith. Replace `sorry` in FOUR declarations. Do NOT change any statement/signature, do NOT add axioms, do NOT weaken hypotheses. Verify with `lake build CausalSmith.Substrate.BernsteinSzegoTrig.Szego` (must end with only `sorry` warnings from OTHER decls and no errors). Use lean-lsp tools (`lean_goal`, `lean_hover_info`, `lean_loogle`, `lean_leansearch`) to find exact lemma names.\n\nThe predicate (in `CausalSmith/Substrate/BernsteinSzegoTrig/TrigPoly.lean`) is:\n`def IsTrigPolyLE (n : ℕ) (f : ℝ → ℝ) : Prop := ∃ a b : ℕ → ℝ, ∀ t, f t = ∑ k ∈ Finset.range (n+1), (a k * Real.cos ((k:ℝ)*t) + b k * Real.sin ((k:ℝ)*t))`.\n\nTargets:\n1. `isTrigPolyLE_const (n) (c)` : `IsTrigPolyLE n (fun _ => c)`. Use `a = fun k => if k = 0 then c else 0`, `b = 0`. Then the sum collapses: only k=0 survives (`Real.cos 0 = 1`, `Real.sin ((k:ℝ)*t)` term killed by b=0). Use `Finset.sum_eq_single 0` or `simp [Finset.sum_ite_eq]`.\n2. `IsTrigPolyLE.mono {n m f} (hf : IsTrigPolyLE n f) (hnm : n ≤ m)` : `IsTrigPolyLE m f`. Extend the coeff functions by zero past n; the extra terms (k from n+1..m) vanish. Use `Finset.sum_subset` with `Finset.range (n+1) ⊆ Finset.range (m+1)` (from `Nat.succ_le_succ hnm`), the extra summands being 0.\n3. `IsTrigPolyLE.sub {n f g} (hf hg)` : `IsTrigPolyLE n (fun t => f t - g t)`. Take `a := a_f - a_g`, `b := b_f - b_g` (pointwise). Then `f t - g t = ∑ (…) - ∑ (…) = ∑ ((a_f-a_g) k * cos + (b_f-b_g) k * sin)` via `Finset.sum_sub_distrib` and `ring`/`ring_nf` inside the sum (`Finset.sum_congr`).\n4. `szegoInterp_isTrigPolyLE (β) (Q₀ Q₁ t₀)` : `IsTrigPolyLE β (szegoInterp β Q₀ Q₁ t₀)` where `szegoInterp β Q₀ Q₁ t₀ t = Q₀ * Real.cos ((β:ℝ)*(t - t₀)) + (Q₁/β) * Real.sin ((β:ℝ)*(t - t₀))`. Expand via angle-subtraction: `Real.cos_sub`, `Real.sin_sub` give `cos(β t − β t₀) = cos(βt)cos(βt₀)+sin(βt)sin(βt₀)`, `sin(β t − β t₀)=sin(βt)cos(βt₀)−cos(βt)sin(βt₀)`. Note `(β:ℝ)*(t−t₀) = (β:ℝ)*t − (β:ℝ)*t₀`. So the interpolant `= A_β cos(βt) + B_β sin(βt)` for constants `A_β,B_β` depending on Q₀,Q₁,β,t₀. Provide `a := fun k => if k = β then A_β else 0`, `b := fun k => if k = β then B_β else 0`; the sum collapses to the k=β term (β ∈ range(β+1) since β < β+1). Use `Finset.sum_eq_single β`.\n\nKeep proofs clean; prefer `Finset.sum_eq_single`/`Finset.sum_congr`/`ring`. Report the final `lake build` tail."
      },
      {
        "id": "chebyshev-expansion",
        "target_decls": [
          "CausalSmith.Substrate.BernsteinSzegoTrig.cosComp_isTrigPolyLE"
        ],
        "prompt": "Work in the Lean 4 + Mathlib project at /<repo-root>/CausalSmith. Prove ONE declaration by replacing its `sorry` in `CausalSmith/Substrate/BernsteinSzegoTrig/Szego.lean`. Do NOT change the statement, add axioms, or weaken hypotheses. Verify with `lake build CausalSmith.Substrate.BernsteinSzegoTrig.Szego` (only `sorry` warnings from OTHER decls, no errors). Use lean-lsp tools to find exact names.\n\nTarget: `cosComp_isTrigPolyLE (R : Polynomial ℝ) (β : ℕ) (hβ : R.natDegree ≤ β) : IsTrigPolyLE β (fun t => R.eval (Real.cos t))`.\n\nThe predicate is `IsTrigPolyLE n f := ∃ a b : ℕ → ℝ, ∀ t, f t = ∑ k ∈ Finset.range (n+1), (a k * Real.cos ((k:ℝ)*t) + b k * Real.sin ((k:ℝ)*t))`.\n\nMath route (Chebyshev basis). Key Mathlib fact: `Polynomial.Chebyshev.T_real_cos (θ : ℝ) (n : ℤ) : Polynomial.eval (Real.cos θ) (Polynomial.Chebyshev.T ℝ n) = Real.cos (↑n * θ)`. So `T k (cos t) = cos (k t)` for `k : ℕ` (cast `k` to ℤ). Any polynomial `R` of degree ≤ β is a ℝ-linear combination of `T 0, …, T β` because `{T 0,…,T n}` is a basis of `{p : deg ≤ n}` (equivalently `T k` has degree exactly `k` with leading behaviour, and they are linearly independent / span). Then `R(cos t) = ∑_{k≤β} c_k · T k (cos t) = ∑_{k≤β} c_k · cos (k t)`, giving `a k = c_k`, `b k = 0`.\n\nStrategy options — pick what compiles:\n(a) PREFERRED, elementary: prove by STRONG INDUCTION on `R.natDegree` (or on a degree bound) that every `R` with `R.natDegree ≤ β` yields `IsTrigPolyLE β (R∘cos)`. Base: `R` constant → `isTrigPolyLE_const` (already available, possibly still `sorry` but usable) composed appropriately; actually reduce a degree-d poly to lower degree by subtracting `(leadingCoeff/T_d.leadingCoeff)·T d`: since `T d` has degree d and `T_real_cos` turns it into `cos(d·t)` (a trig term of degree ≤ β), and the remainder `R − c·T d` has degree < d, apply the IH and add the single `cos(d t)` term. Use `IsTrigPolyLE.sub`, `IsTrigPolyLE.mono`, and the fact that `fun t => cos(d t)` is `IsTrigPolyLE d` (coefficients: a d = 1, rest 0) then `.mono` to β. Combine additively (you may need an `IsTrigPolyLE.add` helper — add it locally if useful, but the target set is only `cosComp_isTrigPolyLE`; a private `add` lemma is fine).\n(b) If a clean `Polynomial.Chebyshev` spanning lemma exists in Mathlib (search `lean_leansearch \"Chebyshev polynomials form a basis\"`, `lean_loogle \"Polynomial.Chebyshev.T\"`), use it directly.\n\nSearch first: `lean_loogle` for `Polynomial.Chebyshev.T_natDegree`, degree of `T`, and any basis/span lemma. If you introduce local helper lemmas (e.g. `cos_natMul_isTrigPolyLE`, `IsTrigPolyLE.add`), place them ABOVE the target in the same file and keep them non-`sorry`. Report the final `lake build` tail."
      },
      {
        "id": "zero-count-crux",
        "target_decls": [
          "CausalSmith.Substrate.BernsteinSzegoTrig.IsTrigPolyLE.card_zeros_le"
        ],
        "prompt": "Work in the Lean 4 + Mathlib project at /<repo-root>/CausalSmith. Prove ONE hard declaration by replacing its `sorry` in `CausalSmith/Substrate/BernsteinSzegoTrig/TrigPoly.lean`. This is the load-bearing lemma of a Szegő-inequality substrate; treat it as a real project. Do NOT change the statement, add axioms, `sorry`, or weaken hypotheses. Verify with `lake build CausalSmith.Substrate.BernsteinSzegoTrig.TrigPoly` (only `sorry` warnings from OTHER decls in that file, no errors). Use lean-lsp tools (`lean_goal`, `lean_loogle`, `lean_leansearch`, `lean_hover_info`) heavily to find exact Mathlib names.\n\nPredicate: `IsTrigPolyLE n f := ∃ a b : ℕ → ℝ, ∀ t, f t = ∑ k ∈ Finset.range (n+1), (a k * Real.cos ((k:ℝ)*t) + b k * Real.sin ((k:ℝ)*t))`.\n\nTarget: `IsTrigPolyLE.card_zeros_le {n} {f} (hf : IsTrigPolyLE n f) (hne : ∃ t, f t ≠ 0) {S : Finset ℝ} (hS : ↑S ⊆ Set.Ico 0 (2*Real.pi)) (hzero : ∀ t ∈ S, f t = 0) : S.card ≤ 2*n`. I.e. a nonzero real trig polynomial of degree ≤ n has at most 2n zeros in one period.\n\nProof route (z = e^{it}). Obtain `a b` from `hf`. On the unit circle `z = Complex.exp (I t)`, we have `Real.cos(k t) = ((z^k + z^{-k})/2).re`… but cleaner: work entirely in ℂ. Define the complex polynomial `P : Polynomial ℂ` of degree ≤ 2n by\n`P = ∑ k ∈ Finset.range (n+1), (Complex.ofReal (a k)/2 - Complex.ofReal (b k) * Complex.I/2) • X^(n+k)  +  (Complex.ofReal (a k)/2 + Complex.ofReal (b k)*Complex.I/2) • X^(n-k)`\n(i.e. build it via `Finset.sum` of `Polynomial.C _ * Polynomial.X ^ _`). KEY IDENTITY: for every real t, with `z := Complex.exp (Complex.I * t)`,\n`P.eval z = z^n * ((f t : ℝ) : ℂ)`.\nThis follows from `cos(kt) = (e^{ikt}+e^{-ikt})/2`, `sin(kt) = (e^{ikt}-e^{-ikt})/(2i)` (Mathlib: `Complex.cos`, `Complex.sin`, `Complex.exp_mul_I`, `Real.cos`/`Complex.ofReal_cos` bridging `Complex.cos (t:ℂ) = (Real.cos t : ℂ)`; look up `Complex.ofReal_cos`, `Complex.exp_ofReal_mul_I_re`, `Complex.cos_eq`, `Complex.sin_eq`). Note `z^k = exp(I k t)`, `z^{-k} = exp(-I k t)` and `z ≠ 0` (`Complex.exp_ne_zero`).\n\nConsequences:\n- `P ≠ 0`: if `P = 0` then `P.eval z = 0` for all t, so `z^n * (f t) = 0`, and `z^n ≠ 0`, giving `f t = 0` for all t, contradicting `hne`. (`natDegree ≤ 2n` also gives `P.eval` a genuine poly; but nonzeroness is what matters for roots.)\n- For each `t ∈ S`, `f t = 0` so `P.eval (exp (I t)) = 0`, i.e. `exp (I t)` is a root of `P`.\n- The map `t ↦ Complex.exp (Complex.I * t)` is INJECTIVE on `[0, 2π)` (distinct arguments in a length-2π window give distinct points on the circle). Prove via: if `exp(I s)=exp(I t)` with `s,t ∈ [0,2π)` then `s = t` — use `Complex.exp_eq_exp_iff_exists_int` / `Complex.exp_eq_one_iff`, periodicity `2π`, and the window bound. Search `lean_leansearch \"complex exponential injective on interval\"`.\n- Therefore `S.image (fun t => Complex.exp (Complex.I * t))` has card `= S.card` (injective on S ⊆ [0,2π)) and is ⊆ the finset of roots of `P`. Distinct roots of a nonzero polynomial number ≤ its degree: use `Polynomial.card_roots_le_degree` / `Polynomial.roots` with `Multiset.toFinset`, or `Polynomial.setOf_isRoot_finite`; the count of distinct roots ≤ `P.natDegree ≤ 2n`.\n- Bound `P.natDegree ≤ 2*n`: every monomial has exponent `n+k ≤ 2n` or `n-k ≤ n ≤ 2n` for `k ≤ n`; use `Polynomial.natDegree_sum_le`, `Polynomial.natDegree_C_mul_X_pow_le` etc.\n\nAssemble: `S.card = (S.image e).card ≤ P.roots.toFinset.card ≤ P.natDegree ≤ 2n`.\n\nThis is long. Introduce private helper lemmas ABOVE the target in the same file as needed (the eval identity, injectivity, natDegree bound), all non-`sorry`. Search for exact lemma names before using them; verify each with `lean_hover_info`. If you get genuinely stuck on ONE named sub-lemma after real effort, leave THAT sub-lemma the only `sorry`, finish everything else, and report precisely which sub-lemma blocked you and why. Report the final `lake build` tail."
      }
    ]
  },
  "ok": true,
  "durationMs": 1046225
}
