% Auto-generated by CausalSmith/doc/templates/render.py — do not edit by hand.
\documentclass{article}
\usepackage{amsmath, amssymb, amsthm, mathtools, enumitem, hyperref}

\newtheorem{definition}{Definition}
\newtheorem{assumption}{Assumption}
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newtheorem{remark}{Remark}
\newtheorem{example}{Example}
\newcommand{\E}{\mathbb{E}}
\newcommand{\Pr}{\mathbb{P}}
\newcommand{\cH}{\mathcal{H}}
\newcommand{\cR}{\mathcal{R}}
\newcommand{\one}{\mathbf{1}}
\newcommand{\transpose}{^{\top}}

\title{Panel question prompt: {{ questionId }}}
\author{Causalean/Panel prompt builder}
\date{}

\begin{document}
\maketitle

\section*{Common prompt block}

You are writing a self-contained econometric theory note as a LaTeX source
file. Return only the complete LaTeX source. Do not wrap the answer in
Markdown fences. The output should compile as a standalone article.

Use the following global setup unless the question-specific prompt below
explicitly changes it.

\paragraph{Five-axis grammar.}
\begin{itemize}[leftmargin=2em]
  \item DGP (response surface): \texttt{ {{ dgpName }} }.
  \item Baseline (untreated-outcome restriction): \texttt{ {{ baselineName }} }.
  \item Assignment (treatment-path distribution): \texttt{ {{ assignmentName }} }.
  \item Regression formula $(B, F, R, w)$: \texttt{ {{ regressionName }} }.
  \item Target (linear functional of cell means): \texttt{ {{ targetName }} }.
\end{itemize}

\paragraph{Panel and treatment.}
Balanced panel indexed by units $i = 1, \ldots, N$ and periods
$t = 1, \ldots, T$. Treatment is binary unless otherwise stated:
$D_{it} \in \{0,1\}$. For boundary periods, $D_{i, t-k} = 0$ whenever $t-k$
is outside $\{1, \ldots, T\}$.

\paragraph{Potential outcomes.}
The outcome has finite memory $p$:
$Y_{it} = Y_{it}(H_{it})$, $H_{it} = (D_{it}, D_{i,t-1}, \ldots, D_{i, t-p})$.
No anticipation: $Y_{it}$ does not depend on future treatments.
The all-zero history is denoted $0_p$. Define the response surface
$m_{it}(h) = \E[Y_{it}(h)]$ and the history effect
$\tau_{it}(h) = m_{it}(h) - m_{it}(0_p)$.

\paragraph{Untreated baseline.}
Per the named instance \texttt{ {{ baselineName }} }, the untreated potential
outcome satisfies a baseline restriction; if your proof needs a stronger
or weaker condition, state it explicitly.

\paragraph{Regression algebra.}
Use the Frisch--Waugh--Lovell theorem. Let $F$ denote the nuisance design
matrix (usually unit and time fixed effects). Let $M_F$ be the residual
maker. For regressors $B$, define
\begin{equation*}
  \widehat{\theta}
  = (B\transpose M_F B)^{-1} B\transpose M_F Y
\end{equation*}
when $B\transpose M_F B$ is invertible; otherwise, state the rank condition
or use the Moore--Penrose inverse and explain the estimable projection.

\paragraph{Causal interpretation.}
Derive the coefficient in terms of potential-outcome contrasts or
response-surface cell means. Whenever possible, rewrite the result as
\emph{target effect + contamination terms}. Do not claim causal
identification unless the coefficient equals the stated target under
explicit assumptions.

\paragraph{Proof style.}
Theorem-proof style: definitions, assumptions, theorem, proof, remarks,
sanity checks. The proof should be in natural language but
equation-by-equation, so it can later be translated into Lean. Explicitly
state all support, rank, and boundary conditions.

\section*{Question-specific prompt}

{% block question_specific %}
% Each q?_prompt.tex.j2 overrides this block.
{% endblock %}

\section*{Notes from the registry}

{{ notes }}

\end{document}
