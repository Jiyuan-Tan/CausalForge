# Papersmith P1 checkpoint review — 2026-07-02 (orchestrator, user-authorized auto-approve)

Run: exp_rollout_chebyshev_minimax / tv_envelope_rollout_design. State at review: stage_completed=P1, checkpoint_pending=outline, revision_round=0.

## Notation-resolvability scan — PASS
Every `\mathrm`-named operator used in a frozen env resolves to exactly one defining env, back-anchored via `\ref{obj:...}`:
- S_{k,q}→def:budgeted-schedule-class; 𝒫_β→def:rollout-law-class; W_β(p)→def:unbiased-weight-set;
  p^{Ch}→def:chebyshev-schedule; A_β / M_{β,k,q}→def:amplification-criterion;
  R_exact / Γ_P(p)→def:exact-nested-risk (Γ_P defined inline at use); T_β inline;
  K(c)/x_q/λ(q)/C_±(·)→their home lemmas (oversampled-norming, continuous-endpoint, chebyshev-minimax).
- All 10 `\ref{obj:...}` targets (ass:static-rollout-consistency, ass:beta-order-polynomial,
  ass:round-mean-variance-envelope, ass:low-budget-cap, def:budgeted-schedule-class, def:rollout-law-class,
  def:unbiased-weight-set, def:chebyshev-schedule, def:amplification-criterion, def:exact-nested-risk) exist as envs.
- No unresolvable symbol. C_{eq}, ρ(q), p^{eq} appear only in the notation table or inline-at-use
  (schedule given by explicit formula), not as unresolved operator uses — acceptable.

## Faithfulness — PASS
- thm:chebyshev-minimax: correct quantifier structure (C_-(q_max) chosen FIRST, uniform in c; then ∀c>1 ∃C_+(c,q_max)),
  matched two-sided bound on both A_β(p) and M_{β,k,q}, β-independent base (1+√(1-q))²/q. Matches the banked kernel.
- Equal-spacing benchmark A_β(p^{eq}) ≤ 9(β/q)^{2β} vs the β-free Chebyshev base — the contribution
  (Chebyshev removes the β from the exponent base in the low-budget regime) reads honestly.

## oeq:exact-nested-minimax pre-flag — BENIGN (not a drift)
The P1-time equivalence note ("claims Chebyshev rate-feasibility without explicitly carrying…") is benign:
the remark (rendered remarkv) labels ONLY exact optimality — whether p^{Ch}(k,q) attains the nested inf R_exact —
as the open question, and line 280 explicitly disclaims "it does not assert the separate rate-feasibility bound."
The rate-feasibility half is carried as PROVED lemmas (lem:exact-risk-envelope-upper: R_exact ≤ (σ₀²/n)M_{β,k,q};
lem:exact-chebyshev-rate-feasible: the p^{Ch} exact risk ≤ (σ₀²/n)C_+·(base)^{2β}). Correct open/proved split, no overclaim.
TODO at P3: confirm the oeq crosswalk maps to an open/no-decl marker, NOT a proved Lean decl.

## Verdict: APPROVE P1 → resume into P2. Pre-warming the EXP research Lean modules before P2/P3.

---

# Papersmith P2 checkpoint review — 2026-07-02 (auto-approve)

State: stage_completed=P2, checkpoint_pending=draft, revision_round=0.

## Frozen fidelity — PASS
- Frozen-env count parity: paper.tex 25 == formal_layer.tex 25 (no env dropped/added).
- P2 frozen-drift lint passed (stage checkpointed, did not throw) ⇒ frozen bodies verbatim.
- No objid leak: grep for `obj:` outside \ref/\label/\eqref/\autoref in sections/*.tex is empty.

## 7 P1 cross-reference advisories — ALL resolved by drafted prose (env-only checker couldn't see prose)
- τ_P: defined in Setup prose "The endpoint contrast studied below is τ_P=m_P(1)-m_P(0)" (01_setup L47).
- p^{eq}(β,q): defined in Setup prose with \leanref{sym:p^eq} + entries qj/β (01_setup L130).
- Γ_P(p): defined in Setup prose with \leanref{sym:Gamma_P} as design cov matrix of (Ȳ_0..Ȳ_k) (01_setup L152).
- admissible schedule: tied to S_{k,q} via Definition~\ref{obj:def:budgeted-schedule-class} (01_setup L150).
- monotone Bernoulli rollout / rate-feasible / 𝒫_β ambient-params: contextualized in Discussion prose
  (03_discussion L3, L29-34); ambient-param suppression is standard convention, made explicit in Setup.
Verdict: paper is self-contained; the advisories are env-only and the full paper prose discharges each.

## Overclaim check (oeq open/proved split) — PASS (scrupulously honest)
- 03_discussion L5: thm:chebyshev-minimax proves optimality for the ENVELOPE criterion only; "does not prove
  that the same schedule solves the exact nested covariance problem."
- oeq L29: "does not assert the separate rate-feasibility bound."
- lem:exact-risk-envelope-upper prose L62: one-sided; "does not supply a lower bound for R_exact,
  and does not identify the exact-risk minimizing schedule."
- lem:exact-chebyshev-rate-feasible prose L88: "Even if exact nested optimality remains open, the
  Chebyshev-Lobatto schedule delivers … worst-case exact variance no larger than the low-budget bound."
No overstatement anywhere.

## Verdict: APPROVE P2 → resume into P3 (equivalence gate + audits). Watch oeq crosswalk → open/no-decl marker.
