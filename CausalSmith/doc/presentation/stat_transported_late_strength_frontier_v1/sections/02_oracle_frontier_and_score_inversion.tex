\section{Oracle Frontier and Score Inversion}

The minimax comparison now turns from identification to expected length.  The relevant scalar is the effective strength \(t_n=n\mu_n^2/\kappa_n\): the numerator records the transported first-stage signal and the denominator records the Kish dispersion of the transport weights.  Weak-IV robust confidence sets are naturally built by inverting a reduced-form score restriction, following the same principle behind Anderson--Rubin and Fieller inference \citep{AndersonRubin1949,Fieller1954,StaigerStock1997,AndrewsStockSun2019}.  In the transported problem, the oracle score weights each source observation by the known density ratio and uses the known source propensity.

\begin{definitionv}{def:inversion-handle}[Score-inversion set $C_n$]
For each \(\vartheta\in\Theta\), define the source score
\[
w(X)\left\{\frac{Z}{e(X)}-\frac{1-Z}{1-e(X)}\right\}(Y-\vartheta D).
\]
Define the empirical second moment of the oracle weights by
\[
\widehat\kappa_n=\frac1n\sum_{i=1}^n w(X_i)^2.
\]
With oracle-known \(w\) and \(e\), the score-inversion confidence set is
\[
C_n
=
\left\{
\vartheta\in\Theta:
\left|
\frac1n\sum_{i=1}^n
w(X_i)\left\{\frac{Z_i}{e(X_i)}-\frac{1-Z_i}{1-e(X_i)}\right\}(Y_i-\vartheta D_i)
\right|
\le
L_\alpha\sqrt{\frac{\widehat\kappa_n}{n}}
\right\}.
\]
For oracle-known \(w\) and \(e\), this score is a source-sample statistic.
\end{definitionv}

The set in \cref{obj:def:inversion-handle} contains exactly those candidate ratios whose transported reduced-form residual is small on the scale \(L_\alpha\sqrt{\widehat\kappa_n/n}\).  The radius uses the empirical second moment of the oracle weights, so covariate shift enters the score through the same dispersion term that appears in the effective-strength index.  Because the construction is restricted to \(\Theta\), its length is measured on the compact causal range established in \cref{obj:prop:compact-causal-range}.

The first result gives the converse side of the oracle benchmark.  It compares all oracle honest procedures in \(\mathfrak C^{\mathrm{or}}\) under the full transported class and under the uniform finite-cell submodel.

\begin{theoremv}{thm:oracle-converse}[Oracle frontier converse]
Fix \(c>0\), \(\varepsilon\in(0,1/2)\), and \(\alpha\in(0,1)\).  There exist constants \(c_0>0\) and \(t_c>0\), depending on \(c\), \(\varepsilon\), and \(\alpha\), such that the following holds.

\begin{itemize}
\item \textbf{(Array size.)} The target sample sizes satisfy \(N_n/n\to c\).
\item \textbf{(Cell scale.)} The sequence \(k_n\) is positive, satisfies \(k_n\to\infty\), and satisfies \(k_n/\sqrt n\to0\).
\item \textbf{(Measurable finite carriers.)} For each \(n\), the covariate space contains \(k_n\) distinct measurable singleton carrier cells.
\item \textbf{(Transported-IV class.)} Every array in the transported model class \(\mathcal P_n\) of \cref{obj:def:model-class} satisfies \cref{obj:ass:full-data-support,obj:ass:population-presence,obj:ass:two-sample-array,obj:ass:instrument-overlap,obj:ass:source-observation,obj:ass:iv-randomization,obj:ass:iv-exclusion,obj:ass:iv-monotonicity,obj:ass:outcome-transport,obj:ass:receipt-transport,obj:ass:target-complier-positivity,obj:ass:transport-domination,obj:ass:weight-envelope,obj:ass:weight-second-moment,obj:ass:degrading-array}.
\item \textbf{(Finite-cell submodel.)} Every array in the finite-cell class \(\mathcal N_n\) of \cref{obj:def:finite-cell-class} satisfies \cref{obj:ass:finite-cell-source}.
\end{itemize}

Then, for every \(t_0>0\), the oracle values of \cref{obj:def:oracle-value,obj:def:finite-cell-oracle-value} obey
\[
c_0\min\{1,t_0^{-1/2}\}\le V^\star(t_0)
\]
and
\[
c_0\min\{1,t_0^{-1/2}\}\le V_{\mathcal N}^\star(t_0).
\]
Moreover, for every \(t_0\in(0,t_c]\),
\[
c_0\le V^\star(t_0).
\]
The same constants \(c_0\) and \(t_c\) apply uniformly over all decompositions of the effective strength \(t_0\) into the transported first-stage mean \(\mu_n\) and the weight dispersion \(\kappa_n\).
\end{theoremv}

\Cref{obj:thm:oracle-converse} establishes that honest expected length is bounded below at the order \(\min\{1,t_0^{-1/2}\}\).  The constants \leanref{sym:c_0}{$c_0$} and \leanref{sym:t_c}{$t_c$} depend on the sampling ratio, overlap, and noncoverage level, while the final uniformity clause indexes the lower bound by the combined effective strength uniformly over decompositions into first-stage weakness and weight dispersion.  The finite-cell inequality records the same frontier lower bound inside the uniform-cell submodel, which is the model used for feasible weight learning in the next section.

The matching construction is the score inversion from \cref{obj:def:inversion-handle}.  Its role is parallel to Anderson--Rubin confidence sets for weak instruments: each hypothesized ratio turns the problem into a reduced-form moment condition, and inversion of that moment yields an honest set even when the denominator is small \citep{AndersonRubin1949,Dufour1997,StockWrightYogo2002,Moreira2003,AndrewsMoreiraStock2006,Mikusheva2010,AndrewsStockSun2019}.

The attainment theorem uses an ambient nonemptiness hypothesis to ensure that the comparison domain contains an admissible geometry.  The score-inversion sequence itself is global: when it is evaluated at a law in \(\mathcal P_n\), it uses that law's canonical density ratio and row-\(n\) propensity.

\begin{theoremv}{thm:oracle-score-inversion-attainment}[Oracle score attainment]
Fix sequences \(N_n\) and \(k_n\), constants \(c,\varepsilon,\alpha\), the transported-IV model class \(\mathcal P_n\) in \cref{obj:def:model-class}, and the score-inversion construction in \cref{obj:def:inversion-handle}.  Assume:
\begin{itemize}
\item \textbf{(Sampling ratio.)} \(c>0\) and \(N_n/n\to c\).
\item \textbf{(Overlap and noncoverage.)} \(0<\varepsilon<1/2\) and \(0<\alpha<1\).
\item \textbf{(Envelope scale.)} \(k_n>0\) for every \(n\), \(k_n\to\infty\), and \(k_n/\sqrt n\to0\).
\item \textbf{(Nonempty admissible carrier.)} There is an admissible deterministic geometry \(\mathfrak g\in\mathcal G\) satisfying \cref{obj:def:admissible-geometry-class} at scale \(k_n\) and overlap constant \(\varepsilon\).
\item \textbf{(Uniform model restrictions.)} Every \(P\in\mathcal P_n\) satisfies the two-sample condition in \cref{obj:ass:two-sample-array}, the overlap condition in \cref{obj:ass:instrument-overlap}, the weight envelope in \cref{obj:ass:weight-envelope}, the weight second-moment bound in \cref{obj:ass:weight-second-moment}, and the degrading-array condition in \cref{obj:ass:degrading-array}.
\end{itemize}
Let
\[
L_\alpha=\left(\frac{8}{\alpha\varepsilon^2}\right)^{1/2}.
\]
For each \(n\), define
\[
H_i=\frac{Z_i}{e(X_i)}-\frac{1-Z_i}{1-e(X_i)},\qquad
\widehat A_n=\frac1n\sum_{i=1}^n w(X_i)H_iY_i,\qquad
\widehat B_n=\frac1n\sum_{i=1}^n w(X_i)H_iD_i,
\]
and
\[
\widehat\kappa_n=\frac1n\sum_{i=1}^n w(X_i)^2,\qquad
C_n=\left\{\vartheta\in\Theta:
\left|\widehat A_n-\vartheta\widehat B_n\right|
\le L_\alpha\sqrt{\frac{\widehat\kappa_n}{n}}\right\}.
\]
Then the score-inversion sequence \((C_n)\) belongs to the oracle honest class \(\mathfrak C^{\mathrm{or}}\) of \cref{obj:def:oracle-honesty}.  Moreover, with
\[
C_0=\max\left\{2,\ 4L_\alpha+\frac8{\varepsilon^2}\right\},
\]
for every \(t_0>0\), the frontier risk from \cref{obj:def:frontier-risk} satisfies
\[
R((C_n),t_0)\le C_0\min\{1,t_0^{-1/2}\}.
\]
Consequently the oracle value in \cref{obj:def:oracle-value} satisfies the same attainable frontier order over fixed positive effective-strength thresholds.
\end{theoremv}

\Cref{obj:thm:oracle-score-inversion-attainment} gives the upper-bound side.  The statistic \(\widehat A_n-\vartheta\widehat B_n\) is the transported sample analogue of the reduced form for \(Y-\vartheta D\), and the confidence set collects the values of \(\vartheta\) whose score lies within the oracle radius.  The constant \leanref{sym:C_0}{$C_0$} is universal over the stated class once \(\alpha\) and \(\varepsilon\) are fixed through \(L_\alpha\).

The carrier clause supplies the nonempty comparison domain used to state attainability.  The transported score statistic in \cref{obj:thm:oracle-score-inversion-attainment} uses the \(w\) and \(e\) supplied by each evaluated law.

Combining \cref{obj:thm:oracle-converse,obj:thm:oracle-score-inversion-attainment}, the oracle minimax expected length is characterized, up to constants, by
\[
\min\{1,t_0^{-1/2}\}.
\]
For small effective strength, the compact parameter space yields a constant-length frontier.  As \(t_0\) grows, the length contracts at the inverse-square-root rate in the effective strength.  The rate therefore treats a weak transported first stage and high transport dispersion symmetrically through \(n\mu_n^2/\kappa_n\), which is the quantity carried forward to the feasible finite-cell procedures.
