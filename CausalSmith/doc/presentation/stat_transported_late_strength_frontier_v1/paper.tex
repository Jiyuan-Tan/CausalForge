\documentclass[11pt]{article}

\usepackage[margin=1.1in]{geometry}

\usepackage{amssymb,mathtools,natbib}

\input{paper_macros.tex}

\title{Transported Complier Effects with Weak First Stages}

\author{CausalSmith}

\date{\today}

\begin{document}

\maketitle



\begin{abstract}
This paper studies honest confidence sets for a transported complier average causal effect in a two-sample encouragement design.  A source population supplies an instrumental-variables experiment with noncompliance, while a target population supplies the covariate law.  Under the transported-IV model class, with source contrasts satisfying the stated measurability and integrability conditions, the transported Wald ratio equals the target complier contrast and lies in the compact range \([-1,1]\).  The paper characterizes the oracle minimax expected-length frontier for honest confidence sets over transported triangular arrays satisfying the stated overlap, sample-size, envelope, second-moment, and degrading-array conditions, with weight second moment \(\kappa_n\) and transported first-stage mean \(\mu_n\).  The frontier is indexed by the effective strength
\[
t_n=\frac{n\mu_n^2}{\kappa_n}.
\]
For every fixed threshold \(t_0>0\), oracle procedures that know the transport weight and source propensity have minimax expected length of order
\[
\min\{1,t_0^{-1/2}\}.
\]
A transported Anderson--Rubin score-inversion set attains this order. The unit-transport-weight least-favorable slice is embedded in the transported class, so its weak-ratio lower bound applies to the larger transported model. Up to constants depending on the sampling ratio, overlap, and noncoverage level, the same order holds conditionally on any admissible fixed covariate-shift geometry. In finite-cell designs, target cell frequencies and a collision-scale U-statistic yield feasible score-inversion procedures that learn the target weights from the target covariate sample and attain the same order under the stated cell-growth and overlap conditions.
\end{abstract}

\section{Introduction}

Encouragement experiments with imperfect take-up identify complier effects in the source population under the usual instrumental-variables restrictions \citep{Bloom1984,ImbensAngrist1994,AngristImbensRubin1996}.  Many applications also require a target-population interpretation: the source experiment is observed in one population, while the policy-relevant covariate distribution belongs to another.  Transportability and generalizability methods address this covariate-shift problem through support and weighting conditions \citep{HotzImbensMortimer2005,ColeStuart2010,StuartColeBradshawLeaf2011,Tipton2013,Dahabreh2020,DahabrehPetitoRobertson2020,Nguyen2017,PearlBareinboim2011,BareinboimPearl2013,BareinboimPearl2016}.  For transported noncompliance, the estimand is a ratio: a transported reduced form divided by a transported first stage.  This ratio structure makes weak identification central when the target complier share is small or when transport weights are dispersed.

This paper characterizes honest expected length for that transported complier ratio.  The source sample contains \((X,Z,D,Y)\) from an encouragement design, and the target sample contains covariates \(X\).  The target-to-source density ratio \(w=dP_T/dP_S^X\) transports source conditional contrasts to the target covariate law.  Under the transported-IV model class \(\mathcal P_n\) in \cref{obj:def:model-class}, with source contrasts satisfying the stated measurability and integrability conditions, \cref{obj:prop:compact-causal-range} shows that the transported reduced form equals the target complier-weighted outcome contrast, the transported first stage equals the target complier share, and their ratio equals the target complier effect \(\theta_T\).  Bounded outcomes and binary treatment receipt place \(\theta_T\) in \(\Theta=[-1,1]\), so expected length is measured on a fixed compact parameter space.

The main statistical object is the effective strength
\[
t_n=\frac{n\mu_n^2}{\kappa_n},
\]
where \(\mu_n\) is the transported first-stage mean and \(\kappa_n=\mathbb E_S[w(X)^2]\) is the Kish-type dispersion scale of the transport weights.  This scalar combines two sources of difficulty.  A small \(\mu_n\) is the familiar weak-first-stage problem from IV ratio inference \citep{AndersonRubin1949,Fieller1954,StaigerStock1997,StockWrightYogo2002,Moreira2003,AndrewsMoreiraStock2006,Mikusheva2010,AndrewsStockSun2019}.  A large \(\kappa_n\) records the variance inflation from transporting the source experiment to a different target covariate law.  The frontier conditions allow \(k_n\to\infty\), \(k_n=o(\sqrt n)\), and \(\mu_n\to0\), so the analysis covers triangular arrays in which covariate shift grows while the transported first stage weakens.

The oracle benchmark allows procedures to use the transport weight \(w\) and the source propensity \(e\).  \Cref{obj:thm:oracle-converse} proves that every oracle honest confidence-set sequence over \(\mathcal P_n\) has limiting worst-case expected length at least a constant times
\[
\min\{1,t_0^{-1/2}\}
\]
over laws satisfying \(t_n\ge t_0\).  The same lower bound already holds on the uniform finite-cell submodel \(\mathcal N_n\).  \Cref{obj:thm:oracle-score-inversion-attainment} gives the matching construction: invert the transported residual score
\[
\frac1n\sum_{i=1}^n
w(X_i)\left\{\frac{Z_i}{e(X_i)}-\frac{1-Z_i}{1-e(X_i)}\right\}(Y_i-\vartheta D_i)
\]
with a radius proportional to \(\sqrt{\widehat\kappa_n/n}\).  This Anderson--Rubin style inversion is honest over \(\mathcal P_n\) and attains the same expected-length order for every fixed \(t_0>0\).

The fixed-geometry results isolate the role of covariate shift.  For an admissible deterministic geometry \(\mathfrak g=(P_S^X,w,e)_{n\ge1}\), \cref{obj:def:fixed-geometry-slice} holds the source covariate law, target weight, and propensity fixed while allowing the causal response law to vary.  \Cref{obj:thm:fixed-geometry-frontier} establishes the same two-sided order
\[
V_{\mathfrak g}^{\star}(t_0)\asymp \min\{1,t_0^{-1/2}\}
\]
with constants uniform over admissible geometries.  The lower bound uses the least-favorable family in \cref{obj:def:least-favorable-witness}: the complier probability is proportional to the transport weight, so the transported first stage is calibrated to \(t_0\), and a local complier-outcome tilt moves the target ratio while the observed-data laws remain close in total variation as recorded in \cref{obj:def:geometry-handle}.  \Cref{obj:prop:no-shift-reduction} specializes this comparison to \(w=1\), where \(\kappa_n=1\), \(P_T=P_S^X\), and the effective strength reduces to \(n\mu_n^2\).

The feasible results replace oracle-known weights by target-sample learning in finite-cell designs.  In the uniform finite-cell class \(\mathcal N_n\), the source covariate law assigns mass \(1/k_n\) to each cell and the source propensity equals \(1/2\).  \Cref{obj:thm:finite-cell-unknown-weight-attainment} constructs a sample-only score-inversion set using target empirical cell frequencies, source cellwise reduced-form and first-stage contrasts, and a target-sample U-statistic for the collision scale.  Under \(N_n/n\to c>0\) and \(k_n=o(\sqrt n)\), this feasible procedure is honest over \(\mathcal N_n\) and attains the oracle frontier order.  \Cref{obj:thm:regular-cell-unknown-weight-attainment} extends the construction to regular finite-cell designs with known source cell probabilities bounded between fixed multiples of \(1/k_n\) and known cell-varying propensities satisfying overlap.

The contribution is therefore a matched minimax expected-length characterization for transported complier ratios under the stated transported-IV, overlap, positivity, support, envelope, dispersion, sample-size, and cell-growth conditions.  The analysis connects transported noncompliance methods such as \citet{RudolphVanderLaan2017} and \citet{ChenHuang2025} with weak-identification-robust LATE and two-sample IV inference \citep{ChoiGuShen2018,ChoiShen2019,Ma2023,SmuclerLanniMasip2025}.  For the compact transported complier ratio under the stated transported-IV restrictions and growth conditions, the frontier is indexed by \(n\mu_n^2/\kappa_n\), so first-stage weakness and transport-weight dispersion enter through a common scale.

The appendix gives the proof details and a verification note for the checked mathematical statements; see \cref{sec:deferred-proofs}.

\section{Setup, Transport, and Honest Risk}

At row \(n\), the two-population full-data potential-outcome world is governed by \(P_n^F\) on \(S\), \(X\), \(D(0),D(1)\), and \(Y(0),Y(1)\).  The source population, \(S=1\), supplies an encouragement experiment with observed variables \(Z\), \(D\), and \(Y\); the target population, \(S=0\), supplies the covariate law to which the complier effect is transported.  The measurable covariate space is \(\mathcal X\).  Potential outcomes are bounded because the expected-length problem is posed over a compact causal range, and treatment receipt is binary because compliance type is the margin through which the encouragement identifies the complier contrast.

\begin{definitionv}{synth_1}[Inverse-propensity instrument score $H(O)$]
For a source observation $O=(X,Z,D,Y)$ and source propensity $e$, the inverse-propensity instrument score is the function $H$ given by \[H(O)=\frac{Z}{e(X)}-\frac{1-Z}{1-e(X)}.\]
\end{definitionv}

The score in \cref{obj:synth_1} is the source inverse-propensity contrast used to recover conditional reduced-form and first-stage contrasts under the source experiment.  Its normalization keeps the assignment contrast centered conditionally on covariates when the source propensity \(e(X)\) is bounded away from zero and one.

The full-data and sampling primitives are fixed by the following population and array conditions.

\begin{assumptionv}{ass:full-data-support}[Full-data support]
The full-data law satisfies
\[
P_n^F\{S\in\{0,1\},\ D(0),D(1)\in\{0,1\},\ Y(0),Y(1)\in[0,1]\}=1.
\]
\end{assumptionv}

This support restriction is specific to this analysis: it places the ratio parameter on a bounded outcome scale and makes the induced complier contrast live in a fixed compact interval.

\begin{assumptionv}{ass:population-presence}[Population presence]
For each \(s\in\{0,1\}\),
\[
P_n^F(S=s)>0.
\]
\end{assumptionv}

Population presence is the standard two-population support condition, ensuring that the source and target conditional laws are well defined \citep{ChenHuang2025}.

\begin{assumptionv}{ass:two-sample-array}[Two-sample array]
For a transported triangular array on the measurable covariate space \(\mathcal X\), let \(P_S\) denote the index-\(n\) observed source law induced on \(O_i^S=(X_i,Z_i,D_i,Y_i)\), and let \(P_T\) denote the index-\(n\) target covariate law on \(X_j^T\). The two sample factors satisfy:
\begin{itemize}
\item \textbf{(Source factor.)} For every \(n\), \(P_S\) is a probability measure.
\item \textbf{(Target factor.)} For every \(n\), \(P_T\) is a probability measure.
\item \textbf{(Sample-size ratio.)} \(0<c\) and \(N_n/n\to c\).
\end{itemize}
\end{assumptionv}

The observed two-sample triangular-array sampling world treats the source observations \(O_i^S\) and target covariates \(X_j^T\) as the two empirical inputs, with source law \(P_S\), target covariate law \(P_T\), target sample size \(N_n\), and positive sample-size limit \(c\).  This is the standard independent two-sample triangular-array sampling condition used in transported noncompliance designs \citep{ChenHuang2025}.

The next block gives the source instrumental-variables structure.  These restrictions follow the usual LATE logic for an encouragement design, adapted to the transported two-population notation of the present problem \citep{ImbensAngrist1994,AngristImbensRubin1996}.

\begin{assumptionv}{ass:instrument-overlap}[Instrument overlap]
The instrument-overlap constant satisfies \(0<\varepsilon<1/2\), and the source propensity score satisfies
\[
\varepsilon\le e(X)\le 1-\varepsilon
\]
\(P_S^X\)-almost surely.
\end{assumptionv}

Instrument overlap is the standard instrument positivity condition, here indexed by the constant \(\varepsilon\), and it keeps inverse-propensity contrasts uniformly controlled \citep{ChenHuang2025}.

\begin{assumptionv}{ass:source-observation}[Source observation law]
Conditional on \(S=1\), the observed source law satisfies:
\begin{itemize}
\item \textbf{(Assignment propensity.)} \(P(Z=1\mid X,S=1)=e(X)\).
\item \textbf{(Treatment receipt.)} \(D=D(Z)\).
\item \textbf{(Observed outcome.)} \(Y=Y(D)\).
\end{itemize}
\end{assumptionv}

The source observation law is the standard assignment and consistency condition linking the observed source experiment to receipt and outcome potential outcomes \citep{ChenHuang2025}.

\begin{assumptionv}{ass:iv-randomization}[Instrument randomization]
The instrument satisfies
\[
Z\perp (Y(0),Y(1),D(0),D(1))\mid (X,S=1).
\]
\end{assumptionv}

Instrument randomization is the standard conditional instrument exchangeability condition; within source covariate strata, the encouragement is as good as randomized for the relevant potential outcomes \citep{ChenHuang2025}.

\begin{assumptionv}{ass:iv-exclusion}[Instrument exclusion]
For every \(z,s\in\{0,1\}\),
\[
Y^Z(z)=Y(D(z))
\]
\(P_n^F\)-almost surely conditional on \(S=s\).
\end{assumptionv}

The exclusion restriction is the standard condition that the assignment potential outcome \(Y^Z(z)\) operates through treatment receipt \citep{ChenHuang2025}.

\begin{assumptionv}{ass:iv-monotonicity}[Instrument monotonicity]
For every \(s\in\{0,1\}\),
\[
D(1)\ge D(0)
\]
\(P_n^F\)-almost surely conditional on \(S=s\).
\end{assumptionv}

Monotonicity is the standard no-defiers condition, allowing the receipt contrast to be interpreted as a complier share in each population state \citep{ChenHuang2025}.

Transport enters through an outcome-contrast restriction, an integrated first-stage restriction, and covariate-support inclusion.  The outcome condition is conditional in \(X\), whereas the receipt condition is imposed at the target average; this distinction is the transport structure used by the identification result below.

\begin{assumptionv}{ass:outcome-transport}[Outcome-contrast transport]
For \(P_T\)-almost every \(x\),
\[
\mathbb E_{P_n^F}\!\left[Y^Z(1)-Y^Z(0)\mid X=x,S=0\right]
=
\mathbb E_{P_n^F}\!\left[Y^Z(1)-Y^Z(0)\mid X=x,S=1\right].
\]
\end{assumptionv}

Outcome-contrast transport is the standard conditional transport of the assignment-outcome contrast across the two population states \citep{ChenHuang2025}.

\begin{assumptionv}{ass:receipt-transport}[Receipt-contrast transport]
The target-population average of the conditional treatment-receipt contrast agrees across the two population states:
\[
\mathbb E_{X\mid S=0}\!\left[
  \mathbb E_{P_n^F}\!\left[D(1)-D(0)\mid X,S=0\right]
\right]
=
\mathbb E_{X\mid S=0}\!\left[
  \mathbb E_{P_n^F}\!\left[D(1)-D(0)\mid X,S=1\right]
\right].
\]
\end{assumptionv}

Receipt-contrast transport equates the target-law average compliance margin across populations while allowing covariate-specific receipt contrasts to differ \citep{ChenHuang2025}.  This condition is the denominator link that represents the target complier share by the source conditional first stage averaged over \(P_T\).

\begin{assumptionv}{ass:target-complier-positivity}[Target complier positivity]
The target population has positive complier mass:
\[
P_n^F\!\left(D(1)>D(0)\mid S=0\right)>0.
\]
\end{assumptionv}

Target complier positivity is the standard positive target complier share condition, so the transported complier ratio is well defined \citep{ChenHuang2025}.

\begin{assumptionv}{ass:transport-domination}[Transport domination]
The target covariate law is absolutely continuous with respect to the source covariate marginal on \(\mathcal X\):
\[
P_T\ll P_S^X.
\]
\end{assumptionv}

Transport domination is the standard covariate-support inclusion condition for generalizing source contrasts to the target covariate distribution \citep{ChenHuang2025}.  It yields the transport and overlap geometry through the source marginal \(P_S^X\), the density-ratio weight \(w(X)=dP_T/dP_S^X(X)\), and the source propensity.

The next definition collects the identification functionals built from this geometry: the source conditional contrasts, their transported means, and the effective-strength index.

\begin{definitionv}{synth_6}[Transported contrasts and effective strength]
Let \(w(X)=dP_T/dP_S^X(X)\) be a nonnegative version of the transport density ratio.  The source conditional assignment-outcome and receipt contrasts are
\[
\Delta_Y(x)=\mathbb E_{P_n^F}\!\left[Y^Z(1)-Y^Z(0)\mid X=x,\ S=1\right],
\qquad
\Delta_D(x)=\mathbb E_{P_n^F}\!\left[D(1)-D(0)\mid X=x,\ S=1\right].
\]
Their transported means and the transport-weight dispersion are
\[
\mu_{Y,n}=\mathbb E_{P_S^X}\!\left[w(X)\,\Delta_Y(X)\right],
\qquad
\mu_n=\mathbb E_{P_S^X}\!\left[w(X)\,\Delta_D(X)\right],
\qquad
\kappa_n=\mathbb E_{P_S^X}\!\left[w(X)^2\right],
\]
and the effective identification strength at index \(n\) is \(t_n=n\mu_n^2/\kappa_n\).
\end{definitionv}

The remaining restrictions define the weak-first-stage geometry.  They separate the size of the transport weights from the transported first stage, producing a single effective-strength index for honest expected length.

\begin{assumptionv}{ass:weight-envelope}[Weight envelope]
The transport weight satisfies
\[
0 \le w(X) \le 2k_n
\]
\(P_S\)-almost surely.
\end{assumptionv}

The weight envelope is specific to this analysis: the sequence \(k_n\) controls the largest admissible density-ratio values while permitting the amount of covariate shift to grow with \(n\).

\begin{assumptionv}{ass:weight-second-moment}[Weight second moment]
The second moment of the transport weight satisfies
\[
\kappa_n \le k_n .
\]
\end{assumptionv}

The second-moment bound is specific to this analysis and records the Kish-type dispersion scale \(\kappa_n=\mathbb E_S[w(X)^2]\) that governs the variance of transported inverse-propensity averages.

\begin{assumptionv}{ass:degrading-array}[Degrading array rates]
The array satisfies
\[
k_n \to \infty,
\qquad
k_n=o(n^{1/2}),
\qquad
\mu_n\to 0 .
\]
\end{assumptionv}

These rate restrictions are specific to this analysis: the envelope scale diverges at a sub-root-\(n\) rate, and the transported first-stage mean \(\mu_n\) enters the weak-identification regime.  Together with the dispersion scale, they define the effective-strength scalar \(t_n=n\mu_n^2/\kappa_n\) used in the risk comparison.

\begin{assumptionv}{ass:finite-cell-source}[Uniform finite cells]
The source covariate marginal \(P_S^X\) is supported on exactly \(k_n\) measurable cells, assigns mass \(1/k_n\) to each cell, and satisfies
\[
e(X)=1/2
\]
\(P_S\)-almost surely.
\end{assumptionv}

The uniform finite-cell condition is specific to this analysis.  It creates the sample-only cell-learning submodel used for feasible procedures, with equal source cell masses and a randomized source encouragement probability.

The full model and the finite-cell submodel collect these assumptions at each row of the triangular array.

\begin{definitionv}{def:model-class}[Transported LATE model class \(\mathcal P_n\)]
The transported LATE model class at index \(n\) is
\[
\mathcal P_n
=
\bigl\{(P_n^F,e): (P_n^F,e)\text{ satisfies the following conditions at index }n\bigr\}.
\]
The required conditions are:
\begin{itemize}
\item \textbf{(Full-data support.)} \cref{obj:ass:full-data-support}.
\item \textbf{(Population presence.)} \cref{obj:ass:population-presence}.
\item \textbf{(Two-sample array.)} \cref{obj:ass:two-sample-array}.
\item \textbf{(Instrument overlap.)} \cref{obj:ass:instrument-overlap}.
\item \textbf{(Source observation.)} \cref{obj:ass:source-observation}.
\item \textbf{(Instrument randomization.)} \cref{obj:ass:iv-randomization}.
\item \textbf{(Instrument exclusion.)} \cref{obj:ass:iv-exclusion}.
\item \textbf{(Instrument monotonicity.)} \cref{obj:ass:iv-monotonicity}.
\item \textbf{(Outcome transport.)} \cref{obj:ass:outcome-transport}.
\item \textbf{(Receipt transport.)} \cref{obj:ass:receipt-transport}.
\item \textbf{(Target complier positivity.)} \cref{obj:ass:target-complier-positivity}.
\item \textbf{(Transport domination.)} \cref{obj:ass:transport-domination}.
\item \textbf{(Weight envelope.)} \cref{obj:ass:weight-envelope}.
\item \textbf{(Weight second moment.)} \cref{obj:ass:weight-second-moment}.
\item \textbf{(Degrading array.)} \cref{obj:ass:degrading-array}.
\end{itemize}
\end{definitionv}

The transported LATE model class \(\mathcal P_n\) is the main domain for the oracle minimax statements.  It combines the source IV restrictions with the transport and dispersion conditions that make the ratio target comparable across rows.

The finite-cell class is the transported model with one added row restriction:
\[
\mathcal N_n=\{P\in\mathcal P_n:P\text{ satisfies }\cref{obj:ass:finite-cell-source}\}.
\]
Thus the IV, transport, overlap, envelope, second-moment, and degrading-array conditions are inherited from \(\mathcal P_n\).  The added content is the uniform source-cell geometry in \cref{obj:ass:finite-cell-source}; the anchored checklist form of \(\mathcal N_n\) is deferred to \cref{sec:deferred-proofs}.

Before defining risk, we record the oracle inputs and the two score radii used later.  Oracle procedures receive the source sample, the target covariate sample, the transport weight, and the source propensity.

\begin{definitionv}{synth_2}[Score-inversion critical radius $L_\alpha$]
We define the score-inversion critical radius by \(L_\alpha=\{8/(\alpha\varepsilon^2)\}^{1/2}\), where \(\alpha\in(0,1)\) is the noncoverage level and \(\varepsilon\) is the source instrument-overlap constant.
\end{definitionv}

The constant in \cref{obj:synth_2} combines the noncoverage level \(\alpha\) with instrument overlap.  It will determine the radius of the score-inversion set in the oracle construction.

The coverage requirements below are stated for the following causal estimand.

\begin{definitionv}{synth_7}[Target complier effect $\theta_T$]
The target complier effect is the ratio
\[
\theta_T
=
\frac{\mathbb E_{P_n^F}\!\left[\{Y(1)-Y(0)\}\,\mathbf 1\{D(1)=1,\ D(0)=0\}\mid S=0\right]}
     {P_n^F\!\left\{D(1)=1,\ D(0)=0\mid S=0\right\}},
\]
the mean effect of treatment receipt among target-population compliers.  Under target complier positivity the denominator is positive and \(\theta_T=\mathbb E_{P_n^F}\!\left[Y(1)-Y(0)\mid D(1)=1,\ D(0)=0,\ S=0\right]\); when the denominator is zero, the ratio is taken to be zero.
\end{definitionv}

\begin{definitionv}{def:oracle-honesty}[Oracle honest class $\mathfrak C^{\mathrm{or}}$]
The oracle honest class is the class of oracle procedures \((C_n)_{n\ge1}\) satisfying \(0<\alpha<1\) and the following conditions.  For every \(n\), \(C_n\) maps each input
\[
\left((O_i^S)_{i=1}^n,(X_j^T)_{j=1}^{N_n},w,e\right),
\qquad
w:\mathcal X\to\mathbb R,\quad w(x)\ge0,\quad e:\mathcal X\to\mathbb R,
\]
to a subset of \(\Theta=[-1,1]\).  For every \(n\) and every measurable such pair \((w,e)\), the sample-by-parameter graph
\[
\left\{
\left(((O_i^S)_{i=1}^n,(X_j^T)_{j=1}^{N_n}),\vartheta\right):
\vartheta\in
C_n\!\left((O_i^S)_{i=1}^n,(X_j^T)_{j=1}^{N_n},w,e\right)
\right\}
\]
is measurable.  For every \(n\), every \(P\in\mathcal P_n\), and every pair \(w,w'\) of measurable nonnegative versions of \(dP_T/dP_S^X\), the evaluations using the row-\(n\) propensity score agree for the two-sample law almost surely:
\[
C_n\!\left((O_i^S)_{i=1}^n,(X_j^T)_{j=1}^{N_n},w,e\right)
=
C_n\!\left((O_i^S)_{i=1}^n,(X_j^T)_{j=1}^{N_n},w',e\right).
\]
Finally,
\[
\liminf_{n\to\infty}
\begin{cases}
\displaystyle \inf_{P\in\mathcal P_n}
P\!\left(\theta_T\in C_n\right), & \mathcal P_n\neq\varnothing,\\[0.8em]
1, & \mathcal P_n=\varnothing
\end{cases}
\ge 1-\alpha,
\]
where in the probability term \(C_n\) is evaluated at the canonical row-\(n\) density ratio \(dP_T/dP_S^X\) and the row-\(n\) propensity score.
\end{definitionv}

The procedure and minimax-risk world treats each confidence set \(C_n\) as a random subset of the compact parameter space \(\Theta=[-1,1]\).  Oracle honesty, written \(\mathfrak C^{\mathrm{or}}\), requires uniform asymptotic coverage over \(\mathcal P_n\) while allowing the procedure to use the canonical nonnegative weight input \(w\) and the propensity.

\begin{definitionv}{def:frontier-risk}[Frontier risk $R((C_n),t_0)$]
For a confidence-set sequence \((C_n)_{n\ge1}\) and threshold \(t_0\), the limiting worst-case expected length above effective strength \(t_0\) is
\[
R((C_n),t_0)
=
\limsup_{n\to\infty}
\sup_{P\in\mathcal P_n:\,t_n\ge t_0}
\mathbb E_P[\lambda(C_n)].
\]
\end{definitionv}

The risk \(R((C_n),t_0)\) measures limiting worst-case Lebesgue length \(\lambda(C_n)\) over laws whose effective strength exceeds the positive threshold \(t_0\).  This conditioning by \(t_n\) is the paper's way of comparing honest procedures at a fixed level of transported identification strength.

\begin{definitionv}{def:oracle-value}[Oracle value $V^\star(t_0)$]
For any fixed effective-strength threshold \(t_0\) with \(0<t_0\), the oracle minimax expected-length value is
\[
V^\star(t_0)
=
\inf_{(C_n)\in\mathfrak C^{\mathrm{or}}}
R((C_n),t_0).
\]
\end{definitionv}

The value \(V^\star(t_0)\) is the oracle benchmark over honest procedures with known transport weights and propensities.

\begin{definitionv}{def:finite-cell-oracle-value}[Finite-cell oracle value $V_{\mathcal N}^\star(t_0)$]
The finite-cell oracle minimax expected-length value at threshold \(t_0\) is
\[
V_{\mathcal N}^\star(t_0)
=
\inf_{(C_n)\in\mathfrak C^{\mathrm{or}}}
\limsup_{n\to\infty}
\sup_{P\in\mathcal N_n:\,t_n\ge t_0}
\mathbb E_P[\lambda(C_n)].
\]
\end{definitionv}

The finite-cell oracle value \(V_{\mathcal N}^\star(t_0)\) applies the same oracle procedure class to the uniform finite-cell submodel.  It gives the comparison point for feasible cell-based weight learning.

\begin{definitionv}{def:finite-cell-feasible-honesty}[Feasible finite-cell class $\mathfrak C^{\mathrm{cell}}$]
The feasible finite-cell honest class is
\[
\mathfrak C^{\mathrm{cell}}
=
\left\{
(C_n)_{n\ge1}:
C_n
=
C_n\!\left((O_i^S)_{i=1}^n,(X_j^T)_{j=1}^{N_n}\right)
\subseteq\Theta,\ 
\liminf_{n\to\infty}\inf_{P\in\mathcal N_n}
P(\theta_T\in C_n)
\ge 1-\alpha
\right\}.
\]
\end{definitionv}

The feasible class \(\mathfrak C^{\mathrm{cell}}\) uses the source observations and target covariates as the procedure inputs in the uniform finite-cell design.  Its coverage requirement remains uniform over \(\mathcal N_n\).

The identification result connects the transported functionals of \cref{obj:synth_6} to the target complier effect of \cref{obj:synth_7}: the inverse-propensity score recovers the source conditional contrasts \(\Delta_Y\) and \(\Delta_D\) from observed source moments, transport moves \(\mu_{Y,n}\) and \(\mu_n\) to target complier functionals, and the Wald ratio equals \(\theta_T\).

\begin{theoremv}{prop:compact-causal-range}[Compact causal range]
Fix \(n\), and let \(P\) be a transported array with sample-size and envelope schedules \(N_n\) and \(k_n\), constants \(c\) and \(\varepsilon\), and model class \(\mathcal P_n\) as in \cref{obj:def:model-class}. Suppose that:
\begin{itemize}
\item \textbf{(Model membership.)} \(P\in\mathcal P_n\) at the index \(n\).
\item \textbf{(Source contrasts.)} The assignment outcome contrast and the receipt contrast for the source population satisfy the usual measurability and integrability conditions under the source covariate marginal \(P_S^X\).
\end{itemize}
Then \(\Delta_Y\) and \(\Delta_D\) represent the observed source outcome and receipt contrasts: for every measurable covariate set \(A\),
\[
\int_{\{O:X\in A\}} H(O)Y\,dP_S(O)
=
\int_A \Delta_Y(x)\,dP_S^X(x),
\qquad
\int_{\{O:X\in A\}} H(O)D\,dP_S(O)
=
\int_A \Delta_D(x)\,dP_S^X(x).
\]
Moreover, the transported reduced-form outcome mean satisfies
\[
\mu_{Y,n}
=
\mathbb E_{P_n^F}\!\left[
\{Y(1)-Y(0)\}\mathbf 1\{D(1)=1,\ D(0)=0\}\mid S=0
\right],
\]
the transported first-stage mean satisfies
\[
\mu_n
=
P_n^F\{D(1)=1,\ D(0)=0\mid S=0\},
\]
and the Wald ratio equals the target complier-conditional contrast,
\[
\frac{\mu_{Y,n}}{\mu_n}=\theta_T.
\]
Finally, \(\theta_T\in\Theta=[-1,1]\).
\end{theoremv}

\Cref{obj:prop:compact-causal-range} establishes the identification link used throughout the paper: the weighted source reduced form equals the target complier-weighted outcome contrast, the weighted source first stage equals the target complier share, and their ratio is the target complier-conditional contrast.  The bounded outcome support then places the estimand in \(\Theta\), making expected length on a fixed compact parameter space the relevant risk criterion.

For later fixed-geometry arguments, we also record the notation for the centered least-favorable law and the two distance measures used to compare local alternatives.

\begin{definitionv}{synth_5}[Centered fixed-geometry law $Q_{n,0}^{\mathfrak g}$]
We write $Q_{n,0}^{\mathfrak g}$ for the $h=0$ member of the fixed-geometry least-favorable family: for the fixed admissible geometry $\mathfrak g=(P_S^X,w,e)_{n\ge1}$, conditional on $X=x$ the compliance types $(D(0),D(1))=(0,1),(1,1),(0,0)$ have probabilities $p_n(x),(1-p_n(x))/2,(1-p_n(x))/2$, $Y(0)=0$, $Y(1)$ is Bernoulli$(1/2)$ for every compliance type, the same conditional full-data law is used in both populations, source encouragement is drawn with propensity $e(X)$, and the notation denotes the resulting joint observed-data law together with the fixed oracle inputs.
\end{definitionv}

The centered law in \cref{obj:synth_5} is the baseline member of the fixed-geometry least-favorable family used in the lower-bound construction.

\begin{definitionv}{synth_3}[Chi-square separation of the fixed-geometry submodel]
For the fixed-geometry laws \(Q_{n,h}^{\mathfrak g}\) and \(Q_{n,0}^{\mathfrak g}\), we write \(\chi^2(Q_{n,h}^{\mathfrak g}\Vert Q_{n,0}^{\mathfrak g})\) for the chi-square divergence \[\chi^2(Q_{n,h}^{\mathfrak g}\Vert Q_{n,0}^{\mathfrak g})=\int \left(\frac{dQ_{n,h}^{\mathfrak g}}{dQ_{n,0}^{\mathfrak g}}-1\right)^2\,dQ_{n,0}^{\mathfrak g},\] whenever \(Q_{n,h}^{\mathfrak g}\ll Q_{n,0}^{\mathfrak g}\), with value \(+\infty\) otherwise.
\end{definitionv}

\begin{definitionv}{synth_4}[Total variation separation of least-favorable laws]
We write \(\|Q_{n,h}^{\mathfrak g}-Q_{n,0}^{\mathfrak g}\|_{\mathrm{TV}}\) for the total variation distance between the index-\(n\) joint observed-data laws with fixed oracle inputs generated by the geometry \(\mathfrak g\) at local complier-outcome tilt \(h\) and at the center \(h=0\), namely \(\sup_A |Q_{n,h}^{\mathfrak g}(A)-Q_{n,0}^{\mathfrak g}(A)|\) over measurable events \(A\) in the corresponding source-sample, target-covariate-sample, and oracle-input space.
\end{definitionv}

The separations in \cref{obj:synth_3,obj:synth_4} calibrate the statistical distance between fixed-geometry local alternatives.  They provide the notation needed to express how lower-bound perturbations move the transported complier contrast while remaining close in observed-data law.

\section{Oracle Frontier and Score Inversion}

The minimax comparison now turns from identification to expected length.  The relevant scalar is the effective strength \(t_n=n\mu_n^2/\kappa_n\): the numerator records the transported first-stage signal and the denominator records the Kish dispersion of the transport weights.  Weak-IV robust confidence sets are naturally built by inverting a reduced-form score restriction, following the same principle behind Anderson--Rubin and Fieller inference \citep{AndersonRubin1949,Fieller1954,StaigerStock1997,AndrewsStockSun2019}.  In the transported problem, the oracle score weights each source observation by the known density ratio and uses the known source propensity.

\begin{definitionv}{def:inversion-handle}[Score-inversion set $C_n$]
For each \(\vartheta\in\Theta\), define the source score
\[
w(X)\left\{\frac{Z}{e(X)}-\frac{1-Z}{1-e(X)}\right\}(Y-\vartheta D).
\]
Define the empirical second moment of the oracle weights by
\[
\widehat\kappa_n=\frac1n\sum_{i=1}^n w(X_i)^2.
\]
With oracle-known \(w\) and \(e\), the score-inversion confidence set is
\[
C_n
=
\left\{
\vartheta\in\Theta:
\left|
\frac1n\sum_{i=1}^n
w(X_i)\left\{\frac{Z_i}{e(X_i)}-\frac{1-Z_i}{1-e(X_i)}\right\}(Y_i-\vartheta D_i)
\right|
\le
L_\alpha\sqrt{\frac{\widehat\kappa_n}{n}}
\right\}.
\]
For oracle-known \(w\) and \(e\), this score is a source-sample statistic.
\end{definitionv}

The set in \cref{obj:def:inversion-handle} contains exactly those candidate ratios whose transported reduced-form residual is small on the scale \(L_\alpha\sqrt{\widehat\kappa_n/n}\).  The radius uses the empirical second moment of the oracle weights, so covariate shift enters the score through the same dispersion term that appears in the effective-strength index.  Because the construction is restricted to \(\Theta\), its length is measured on the compact causal range established in \cref{obj:prop:compact-causal-range}.

The first result gives the converse side of the oracle benchmark.  It compares all oracle honest procedures in \(\mathfrak C^{\mathrm{or}}\) under the full transported class and under the uniform finite-cell submodel.

\begin{theoremv}{thm:oracle-converse}[Oracle frontier converse]
Fix \(c>0\), \(\varepsilon\in(0,1/2)\), and \(\alpha\in(0,1)\).  There exist constants \(c_0>0\) and \(t_c>0\), depending on \(c\), \(\varepsilon\), and \(\alpha\), such that the following holds.

\begin{itemize}
\item \textbf{(Array size.)} The target sample sizes satisfy \(N_n/n\to c\).
\item \textbf{(Cell scale.)} The sequence \(k_n\) is positive, satisfies \(k_n\to\infty\), and satisfies \(k_n/\sqrt n\to0\).
\item \textbf{(Measurable finite carriers.)} For each \(n\), the covariate space contains \(k_n\) distinct measurable singleton carrier cells.
\item \textbf{(Transported-IV class.)} Every array in the transported model class \(\mathcal P_n\) of \cref{obj:def:model-class} satisfies \cref{obj:ass:full-data-support,obj:ass:population-presence,obj:ass:two-sample-array,obj:ass:instrument-overlap,obj:ass:source-observation,obj:ass:iv-randomization,obj:ass:iv-exclusion,obj:ass:iv-monotonicity,obj:ass:outcome-transport,obj:ass:receipt-transport,obj:ass:target-complier-positivity,obj:ass:transport-domination,obj:ass:weight-envelope,obj:ass:weight-second-moment,obj:ass:degrading-array}.
\item \textbf{(Finite-cell submodel.)} Every array in the finite-cell class \(\mathcal N_n\) of \cref{obj:def:finite-cell-class} satisfies \cref{obj:ass:finite-cell-source}.
\end{itemize}

Then, for every \(t_0>0\), the oracle values of \cref{obj:def:oracle-value,obj:def:finite-cell-oracle-value} obey
\[
c_0\min\{1,t_0^{-1/2}\}\le V^\star(t_0)
\]
and
\[
c_0\min\{1,t_0^{-1/2}\}\le V_{\mathcal N}^\star(t_0).
\]
Moreover, for every \(t_0\in(0,t_c]\),
\[
c_0\le V^\star(t_0).
\]
The same constants \(c_0\) and \(t_c\) apply uniformly over all decompositions of the effective strength \(t_0\) into the transported first-stage mean \(\mu_n\) and the weight dispersion \(\kappa_n\).
\end{theoremv}

\Cref{obj:thm:oracle-converse} establishes that honest expected length is bounded below at the order \(\min\{1,t_0^{-1/2}\}\).  The constants \(c_0\) and \(t_c\) depend on the sampling ratio, overlap, and noncoverage level, while the final uniformity clause indexes the lower bound by the combined effective strength uniformly over decompositions into first-stage weakness and weight dispersion.  The finite-cell inequality records the same frontier lower bound inside the uniform-cell submodel, which is the model used for feasible weight learning in the next section.

The matching construction is the score inversion from \cref{obj:def:inversion-handle}.  Its role is parallel to Anderson--Rubin confidence sets for weak instruments: each hypothesized ratio turns the problem into a reduced-form moment condition, and inversion of that moment yields an honest set even when the denominator is small \citep{AndersonRubin1949,Dufour1997,StockWrightYogo2002,Moreira2003,AndrewsMoreiraStock2006,Mikusheva2010,AndrewsStockSun2019}.

The attainment theorem uses an ambient nonemptiness hypothesis to ensure that the comparison domain contains an admissible geometry.  The score-inversion sequence itself is global: when it is evaluated at a law in \(\mathcal P_n\), it uses that law's canonical density ratio and row-\(n\) propensity.

\begin{theoremv}{thm:oracle-score-inversion-attainment}[Oracle score attainment]
Fix sequences \(N_n\) and \(k_n\), constants \(c,\varepsilon,\alpha\), the transported-IV model class \(\mathcal P_n\) in \cref{obj:def:model-class}, and the score-inversion construction in \cref{obj:def:inversion-handle}.  Assume:
\begin{itemize}
\item \textbf{(Sampling ratio.)} \(c>0\) and \(N_n/n\to c\).
\item \textbf{(Overlap and noncoverage.)} \(0<\varepsilon<1/2\) and \(0<\alpha<1\).
\item \textbf{(Envelope scale.)} \(k_n>0\) for every \(n\), \(k_n\to\infty\), and \(k_n/\sqrt n\to0\).
\item \textbf{(Nonempty admissible carrier.)} There is an admissible deterministic geometry \(\mathfrak g\in\mathcal G\) satisfying \cref{obj:def:admissible-geometry-class} at scale \(k_n\) and overlap constant \(\varepsilon\).
\item \textbf{(Uniform model restrictions.)} Every \(P\in\mathcal P_n\) satisfies the two-sample condition in \cref{obj:ass:two-sample-array}, the overlap condition in \cref{obj:ass:instrument-overlap}, the weight envelope in \cref{obj:ass:weight-envelope}, the weight second-moment bound in \cref{obj:ass:weight-second-moment}, and the degrading-array condition in \cref{obj:ass:degrading-array}.
\end{itemize}
Let
\[
L_\alpha=\left(\frac{8}{\alpha\varepsilon^2}\right)^{1/2}.
\]
For each \(n\), define
\[
H_i=\frac{Z_i}{e(X_i)}-\frac{1-Z_i}{1-e(X_i)},\qquad
\widehat A_n=\frac1n\sum_{i=1}^n w(X_i)H_iY_i,\qquad
\widehat B_n=\frac1n\sum_{i=1}^n w(X_i)H_iD_i,
\]
and
\[
\widehat\kappa_n=\frac1n\sum_{i=1}^n w(X_i)^2,\qquad
C_n=\left\{\vartheta\in\Theta:
\left|\widehat A_n-\vartheta\widehat B_n\right|
\le L_\alpha\sqrt{\frac{\widehat\kappa_n}{n}}\right\}.
\]
Then the score-inversion sequence \((C_n)\) belongs to the oracle honest class \(\mathfrak C^{\mathrm{or}}\) of \cref{obj:def:oracle-honesty}.  Moreover, with
\[
C_0=\max\left\{2,\ 4L_\alpha+\frac8{\varepsilon^2}\right\},
\]
for every \(t_0>0\), the frontier risk from \cref{obj:def:frontier-risk} satisfies
\[
R((C_n),t_0)\le C_0\min\{1,t_0^{-1/2}\}.
\]
Consequently the oracle value in \cref{obj:def:oracle-value} satisfies the same attainable frontier order over fixed positive effective-strength thresholds.
\end{theoremv}

\Cref{obj:thm:oracle-score-inversion-attainment} gives the upper-bound side.  The statistic \(\widehat A_n-\vartheta\widehat B_n\) is the transported sample analogue of the reduced form for \(Y-\vartheta D\), and the confidence set collects the values of \(\vartheta\) whose score lies within the oracle radius.  The constant \(C_0\) is universal over the stated class once \(\alpha\) and \(\varepsilon\) are fixed through \(L_\alpha\).

The carrier clause supplies the nonempty comparison domain used to state attainability.  The transported score statistic in \cref{obj:thm:oracle-score-inversion-attainment} uses the \(w\) and \(e\) supplied by each evaluated law.

Combining \cref{obj:thm:oracle-converse,obj:thm:oracle-score-inversion-attainment}, the oracle minimax expected length is characterized, up to constants, by
\[
\min\{1,t_0^{-1/2}\}.
\]
For small effective strength, the compact parameter space yields a constant-length frontier.  As \(t_0\) grows, the length contracts at the inverse-square-root rate in the effective strength.  The rate therefore treats a weak transported first stage and high transport dispersion symmetrically through \(n\mu_n^2/\kappa_n\), which is the quantity carried forward to the feasible finite-cell procedures.

\section{Fixed Geometry and Feasible Cell Weight Learning}

The oracle comparison in \cref{obj:thm:oracle-converse,obj:thm:oracle-score-inversion-attainment} treats the transport weight and source propensity as inputs to the procedure.  This section separates two questions that are useful for interpreting that benchmark.  First, it holds the transport geometry fixed and records the local least-favorable family that delivers the same inverse-square-root dependence on effective strength.  Second, it shows how the weight-dispersion scale can be learned from target covariates in finite-cell experiments while preserving the expected-length order.

A fixed geometry records the source covariate law, the target-to-source weight, and the source propensity as a deterministic triangular array.  The following definition isolates the admissible arrays used throughout the section.

\begin{definitionv}{def:admissible-geometry-class}[Admissible geometry class \(\mathcal G\)]
\(\mathcal G\) is the class of deterministic arrays \(\mathfrak g=(P_S^X,w,e)_{n\ge1}\) satisfying the following conditions:
\begin{itemize}
\item \textbf{(Source law.)} \(P_S^X\) is a probability law on \(\mathcal X\).
\item \textbf{(Instrument overlap.)} \(e:\mathcal X\to[\varepsilon,1-\varepsilon]\).
\item \textbf{(Transport weights.)} \(w:\mathcal X\to[0,2k_n]\), \(\mathbb E_S[w(X)]=1\), and
\[
\kappa_n=\mathbb E_S[w(X)^2]\le k_n .
\]
\item \textbf{(Target law.)} For every measurable \(A\subseteq\mathcal X\),
\[
P_T(A)=\mathbb E_S\!\left[w(X)\mathbf 1\{X\in A\}\right].
\]
\end{itemize}
\end{definitionv}

The class \(\mathcal G\) fixes the \(\mathfrak g\) component of the transported experiment while retaining the same overlap, envelope, normalization, and second-moment controls used in the oracle frontier.  The target law is generated from the source covariate law by the density ratio, so the geometry keeps the covariate-shift component explicit and leaves the causal response law to vary within the model.

\begin{definitionv}{def:fixed-geometry-slice}[Fixed-geometry slice \(\mathcal P_n(\mathfrak g)\)]
For \(\mathfrak g\in\mathcal G\),
\[
\mathcal P_n(\mathfrak g)
=
\left\{
P\in\mathcal P_n:
(P_S^X,w,e)\text{ equals the index-}n\text{ component of }\mathfrak g
\right\}.
\]
\end{definitionv}

The slice \(\mathcal P_n(\mathfrak g)\) is the subproblem in which the causal and outcome laws vary subject to the transported LATE restrictions, while the source covariate marginal, target density ratio, and propensity are held fixed.

\begin{definitionv}{def:fixed-geometry-honesty}[Fixed-geometry oracle honesty \(\mathfrak C^{\mathrm{or}}(\mathfrak g)\)]
For \(\mathfrak g\in\mathcal G\) as in \cref{obj:def:admissible-geometry-class}, \(\mathfrak C^{\mathrm{or}}(\mathfrak g)\) is the class of sequences \((C_n)_{n\ge1}\) such that
\[
C_n
=
C_n\!\left((O_i^S)_{i=1}^n,(X_j^T)_{j=1}^{N_n},w,e\right)
\subseteq \Theta
\]
and
\[
\liminf_{n\to\infty}
\inf_{P\in\mathcal P_n(\mathfrak g)}
P(\theta_T\in C_n)
\ge 1-\alpha .
\]
\end{definitionv}

The class \(\mathfrak C^{\mathrm{or}}(\mathfrak g)\) imposes honesty over the fixed-geometry slice.  Procedures may use the fixed oracle inputs, and the coverage requirement remains uniform over the causal response laws compatible with that geometry.

\begin{definitionv}{def:fixed-geometry-value}[Fixed-geometry value \(V_{\mathfrak g}^\star(t_0)\)]
For \(\mathfrak g\in\mathcal G\) as in \cref{obj:def:admissible-geometry-class}, the fixed-geometry minimax expected-length value at threshold \(t_0\) is
\[
V_{\mathfrak g}^\star(t_0)
=
\inf_{(C_n)\in\mathfrak C^{\mathrm{or}}(\mathfrak g)}
\limsup_{n\to\infty}
\sup_{P\in\mathcal P_n(\mathfrak g):\,t_n\ge t_0}
\mathbb E_P[\lambda(C_n)] .
\]
\end{definitionv}

The value \(V_{\mathfrak g}^\star(t_0)\) measures the same expected-length criterion as \cref{obj:def:oracle-value}, now conditional on a fixed admissible geometry.  This formulation makes clear that the effective-strength comparison is a statement about the response-law variation remaining after the covariate-shift array has been fixed.

The lower-bound construction varies a single complier-outcome tilt while preserving the fixed geometry.  It uses the same target-first-stage calibration as the oracle converse, with a local radius chosen so that the observed-data laws remain close in total variation.

\begin{definitionv}{def:least-favorable-witness}[Least-favorable witness \(Q_{n,h}^{\mathfrak g}\)]
Fix \(\mathfrak g=(P_S^X,w,e)_{n\ge1}\in\mathcal G\) and \(t_0>0\). Set
\[
\mu_n=\left(\frac{t_0\kappa_n}{n}\right)^{1/2},
\qquad
p_n(x)=\frac{\mu_n w(x)}{\kappa_n},
\qquad
\rho=\frac{1-\alpha}{8},
\qquad
H(t_0)=\min\{1/4,\rho t_0^{-1/2}\}.
\]
For each \(|h|\le H(t_0)\), conditional on \(X=x\), assign compliance types by
\[
\Pr\{(D(0),D(1))=(0,1)\mid X=x\}=p_n(x),
\]
\[
\Pr\{(D(0),D(1))=(1,1)\mid X=x\}
=
\Pr\{(D(0),D(1))=(0,0)\mid X=x\}
=
\frac{1-p_n(x)}{2}.
\]
Set \(Y(0)=0\). For compliers, let \(Y(1)\) be Bernoulli with mean \(1/2+h\); for always-takers and never-takers, let \(Y(1)\) be Bernoulli with mean \(1/2\). Use this same conditional full-data law in both populations, draw source encouragement with propensity \(e(X)\), and write \(Q_{n,h}^{\mathfrak g}\) for the resulting joint observed-data law together with the fixed oracle inputs.
\end{definitionv}

In \cref{obj:def:least-favorable-witness}, the complier probability \(p_n(x)\) is proportional to the transport weight, so the transported first stage is exactly calibrated to the threshold \(t_0\).  The parameter \(\rho\) fixes the Le Cam separation scale, and the tilt \(h\) moves the target complier contrast while preserving the same source and target covariate geometry.

The notation for the centered law and the two distances is fixed in \cref{obj:synth_5,obj:synth_3,obj:synth_4}.  The fixed-geometry lower bound also uses the separation calculation recorded in \cref{obj:def:geometry-handle}: a tilt of order \(t_0^{-1/2}\) moves the transported complier contrast while the observed-data laws remain close in chi-square and total variation.

The next theorem gives the fixed-geometry frontier for arbitrary admissible deterministic geometries.  It combines the lower-bound calibration in \cref{obj:def:least-favorable-witness,obj:def:geometry-handle} with the oracle score construction from \cref{obj:thm:oracle-score-inversion-attainment}.

\begin{theoremv}{thm:fixed-geometry-frontier}[Fixed-geometry frontier]
Let \(c>0\), \(0<\varepsilon<1/2\), and \(0<\alpha<1\). Assume the following conditions hold:
\begin{itemize}
\item \textbf{(Target-sample ratio.)} The target sample sizes satisfy \(N_n/n\to c\).
\item \textbf{(Envelope scale.)} The sequence \(k_n\) is positive for every \(n\), satisfies \(k_n\to\infty\), and obeys \(k_n/\sqrt n\to0\).
\item \textbf{(Geometry.)} The deterministic geometry array \(\mathfrak g\) satisfies \cref{obj:def:admissible-geometry-class} with scale \(k_n\) and overlap constant \(\varepsilon\).
\item \textbf{(Fixed-geometry slices.)} For every \(n\) and every transported array whose \(n\)th law lies in the fixed-geometry slice \(\mathcal P_n(\mathfrak g)\) of \cref{obj:def:fixed-geometry-slice}, the conditions in
\cref{obj:ass:two-sample-array,obj:ass:instrument-overlap,obj:ass:weight-envelope,obj:ass:weight-second-moment,obj:ass:degrading-array}
hold.
\end{itemize}

For every \(t_0>0\), define
\[
L_\alpha=\left(\frac{8}{\alpha\varepsilon^2}\right)^{1/2},
\qquad
c_\alpha=\frac{3(1-\alpha)^2}{16},
\qquad
C_\alpha=\max\left\{2,\,4L_\alpha+\frac{8}{\varepsilon^2}\right\}.
\]
Then the fixed-geometry minimax value of \cref{obj:def:fixed-geometry-value} satisfies
\[
c_\alpha\min\{1,t_0^{-1/2}\}
\le
V_{\mathfrak g}^{\star}(t_0)
\le
C_\alpha\min\{1,t_0^{-1/2}\}.
\]
The constants \(c_\alpha\) and \(C_\alpha\) are uniform over admissible deterministic geometries \(\mathfrak g\).
\end{theoremv}

\Cref{obj:thm:fixed-geometry-frontier} establishes that fixing the covariate-shift geometry leaves the same expected-length order as the full oracle comparison: \(\min\{1,t_0^{-1/2}\}\).  The constants are uniform over the admissible geometry class, so the effective-strength index absorbs both first-stage weakness and weight dispersion across the fixed-geometry slices.

A useful specialization arises when the target and source covariate laws coincide.  In that case the dispersion term collapses to one, and the transported effective strength reduces to the familiar sample-size-scaled first-stage square.

\begin{theoremv}{prop:no-shift-reduction}[Single-population frontier reduction]
On a measurable covariate space \(\mathcal X\), let \(\mathfrak g\) be a deterministic geometry array satisfying \cref{obj:def:admissible-geometry-class} with scale \(k_n\) and overlap constant \(\varepsilon\). Suppose that
\begin{itemize}
\item \textbf{(Sampling ratio.)} \(c>0\) and \(N_n/n \to c\).
\item \textbf{(Overlap and noncoverage.)} \(0<\varepsilon<1/2\) and \(0<\alpha<1\).
\item \textbf{(Cell scale.)} \(k_n>0\) for every \(n\), \(k_n\to\infty\), and \(k_n/\sqrt n\to 0\).
\item \textbf{(Unit transport weight.)} For every \(n\), \(w_n(X)=1\) \(P_{S,n}^X\)-almost surely under \(\mathfrak g\).
\end{itemize}
Then, for every \(n\),
\[
\kappa_n=1
\qquad\text{and}\qquad
P_{T,n}=P_{S,n}^X .
\]
Moreover, for every transported array \(P\) and every \(n\), if \(P\in\mathcal P_n(\mathfrak g)\) in the sense of \cref{obj:def:fixed-geometry-slice}, then its effective strength satisfies
\[
t_n
=
n\mu_n^2 .
\]
Finally, for every \(t_0>0\), let
\[
L_\alpha=\sqrt{\frac{8}{\alpha\varepsilon^2}},
\qquad
c_\alpha=\frac{3(1-\alpha)^2}{16},
\qquad
C_\alpha=\max\left\{2,\;4L_\alpha+\frac{8}{\varepsilon^2}\right\}.
\]
The fixed-geometry minimax value \(V_{\mathfrak g}^\star(t_0)\) from \cref{obj:def:fixed-geometry-value} obeys
\[
c_\alpha \min\{1,t_0^{-1/2}\}
\le
V_{\mathfrak g}^\star(t_0)
\le
C_\alpha \min\{1,t_0^{-1/2}\}.
\]
\end{theoremv}

\Cref{obj:prop:no-shift-reduction} records the unit-weight specialization of \cref{obj:thm:fixed-geometry-frontier}.  With unit transport weight, the target law equals the source covariate law, \(\kappa_n=1\), and the rate is indexed by \(n\mu_n^2\).  The same compact-parameter expected-length order therefore recovers the usual weak-first-stage scaling within the transported notation.

We now turn from oracle-known weights to sample-only finite-cell score inversion. In the uniform finite-cell model of \cref{obj:def:finite-cell-class}, the target sample identifies the target cell frequencies, while the source sample estimates the cellwise outcome and receipt contrasts. This is the finite-cell analogue of inverse-probability weighting in semiparametric transport and missing-data problems \citep{HiranoImbensRidder2003,RobinsRotnitzkyZhao1994,VanDerLaanRubin2006,Chernozhukov2018}.

\begin{theoremv}{thm:finite-cell-unknown-weight-attainment}[Finite-cell sample-only score-inversion attainment]
Let \(N_n\) and \(k_n\) be positive integer sequences, let \(c,\varepsilon,\alpha\in\mathbb R\), and suppose the following conditions hold.
\begin{itemize}
\item \textbf{(Sampling ratio.)} \(c>0\) and \(N_n/n\to c\).
\item \textbf{(Overlap and coverage levels.)} \(0<\varepsilon<1/2\) and \(0<\alpha<1\).
\item \textbf{(Cell growth.)} \(k_n>0\) for every \(n\), \(k_n\to\infty\), and \(k_n/\sqrt n\to0\).
\item \textbf{(Ambient carrier.)} For every \(n\), the measurable covariate space contains \(k_n\) distinct measurable singleton cells.
\item \textbf{(Finite-cell rows.)} For every row \(n\) and every transported array \(P\in\mathcal N_n\), the row satisfies the two-sample condition in \cref{obj:ass:two-sample-array}, the uniform finite-cell source condition in \cref{obj:ass:finite-cell-source}, and the degrading-array condition in \cref{obj:ass:degrading-array}, with \(\mathcal N_n\) as in \cref{obj:def:finite-cell-class}.
\end{itemize}
Put
\[
B_c=32(1+c^{-1}),\qquad
L_{\alpha,c}=\left(\frac{2B_c}{\alpha}\right)^{1/2},\qquad
C_0=\max\{2,4\sqrt2\,L_{\alpha,c}+8B_c\}.
\]
Then \(C_0>0\), and there exists a sample-only finite-cell confidence-set sequence \(C=(C_n)\) in the feasible class \(\mathfrak C^{\mathrm{cell}}\) of \cref{obj:def:finite-cell-feasible-honesty} such that, for every \(n\) and every \(P\in\mathcal N_n\), \(C_n\) agrees \(P\)-almost surely with the finite-cell score-inversion rule using critical constant \(L_{\alpha,c}\). Equivalently, on the finite-cell experiment, this rule is given by
\[
H_i=2(2Z_i-1),\qquad
\widehat p_{x,n}=\frac1{N_n}\sum_{j=1}^{N_n}\mathbf 1\{X_j^T=x\},
\]
and, for \(G\in\{Y,D\}\),
\[
\widehat m_{G,n}(x)=\frac{k_n}{n}\sum_{i=1}^n
\mathbf 1\{X_i=x\}H_iG_i,\qquad
\widehat M_{G,n}=\sum_{x=1}^{k_n}\widehat p_{x,n}\widehat m_{G,n}(x).
\]
For \(N_n\ge2\), define
\[
\widehat\kappa_{U,n}
=\frac{k_n}{N_n(N_n-1)}
\sum_{j\ne\ell}\mathbf 1\{X_j^T=X_\ell^T\},\qquad
\widehat K_n=1+\widehat\kappa_{U,n},
\]
and set
\[
C_n=\left\{\vartheta\in\Theta:
|\widehat M_{Y,n}-\vartheta\widehat M_{D,n}|
\le L_{\alpha,c}\sqrt{\widehat K_n/n}\right\},
\]
with \(C_n=\Theta\) at indices for which \(N_n<2\). For every \(t_0>0\),
\[
\limsup_{n\to\infty}\sup_{P\in\mathcal N_n:\,t_n\ge t_0}
\mathbb E_P[\lambda(C_n)]
\le
C_0\min\{1,t_0^{-1/2}\}.
\]
Moreover, for every \(t_0>0\), the feasible finite-cell honest minimax risk satisfies
\[
\frac{3(1-\alpha)^2}{16}\min\{1,t_0^{-1/2}\}
\le
\inf_{\widetilde C\in\mathfrak C^{\mathrm{cell}}}
\limsup_{n\to\infty}\sup_{P\in\mathcal N_n:\,t_n\ge t_0}
\mathbb E_P[\lambda(\widetilde C_n)].
\]
\end{theoremv}

\Cref{obj:thm:finite-cell-unknown-weight-attainment} constructs a feasible confidence set from the source observations and target covariates.  The empirical target frequencies \(\widehat p_{x,n}\) estimate the transported mixing weights, the cellwise contrasts \(\widehat m_{G,n}(x)\) estimate the source reduced-form and first-stage contrasts, and \(\widehat\kappa_{U,n}\) is a target-sample U-statistic for the weight-dispersion scale.  The resulting inversion set has the same \(\min\{1,t_0^{-1/2}\}\) expected-length order as the oracle benchmark on the uniform finite-cell class, with the lower bound showing that this order is minimax sharp within \(\mathfrak C^{\mathrm{cell}}\).

For the uniform-cell benchmark, the inputs are the source records \((X,Z,D,Y)\), the target covariates \(X^T\), the known cell partition, and the known propensity \(1/2\). Tabulate target frequencies, compute \(H_i=2(2Z_i-1)\) and the source cell contrasts, aggregate the two transported moments, estimate dispersion with the target collision statistic, and invert the displayed affine score over \(\Theta\). The regular-cell version follows the same steps while taking the known \(q_{x,n}\) and \(e_{x,n}\) as additional inputs. These constructions are minimax attainability benchmarks under the stated growth conditions; their declared inputs fix the cell structure and propensities used by the rules.

The uniform-cell construction uses known equal source cell masses and the propensity \(e(X)=1/2\).  A regular finite-cell variant allows unequal but controlled source cell probabilities and a known cell-varying propensity.  The source-cell probabilities \(q_{x,n}\) are bounded between constants \(c_-\) and \(c_+\) times \(1/k_n\), which preserves the same dispersion order while accommodating regular discrete designs.

\begin{definitionv}{def:regular-finite-cell-class}[Regular finite-cell class $\mathcal N_n^{\mathrm{reg}}$]
For fixed constants \(0<c_-\le 1\le c_+<\infty\), define
\[
\mathcal N_n^{\mathrm{reg}}
=
\left\{
P\in\mathcal P_n:
\begin{array}{l}
P_S^X \text{ is supported on exactly } k_n \text{ measurable cells},\\
q_{x,n}=P_S^X(X \text{ lies in cell } x),\\
c_-/k_n\le q_{x,n}\le c_+/k_n \text{ for every } x
\end{array}
\right\}.
\]
\end{definitionv}

The regular class \(\mathcal N_n^{\mathrm{reg}}\) keeps the finite support structure while allowing cell masses to vary within fixed multiplicative bounds.  This is the discrete counterpart of overlap and bounded-density conditions commonly used in transport and weighting analyses \citep{ChenHuang2025,RudolphVanderLaan2017,ZengKennedyBodnarNaimi2024}.

\begin{definitionv}{def:regular-cell-feasible-honesty}[Regular-cell feasible class $\mathfrak C^{\mathrm{reg}}$]
For a noncoverage level \(0<\alpha<1\), define \(\mathfrak C^{\mathrm{reg}}\) as the class of regular-cell procedures \((C_n)_{n\ge 1}\) with the following properties. For each \(n\), \(C_n\) is a \(\Theta\)-valued random set whose inputs are \((O_i^S)_{i=1}^n\), \((X_j^T)_{j=1}^{N_n}\), the realized regular-cell support, and the known cell-indexed vectors \((q_{x,n})_{x=1}^{k_n}\) and \((e_{x,n})_{x=1}^{k_n}\). For every realized regular-cell support and every pair of real \(k_n\)-vectors \((q_{x,n})_{x=1}^{k_n}\) and \((e_{x,n})_{x=1}^{k_n}\), the sample-by-\(\theta\) graph of \(C_n\), restricted to source and target samples on that support, is measurable. These procedures satisfy
\[
1-\alpha\le
\liminf_{n\to\infty}
\inf_{P\in\mathcal N_n^{\mathrm{reg}}}
P(\theta_T\in C_n),
\]
with the rowwise infimum evaluated as \(1\) when \(\mathcal N_n^{\mathrm{reg}}\) is empty.
\end{definitionv}

The feasible class \(\mathfrak C^{\mathrm{reg}}\) formalizes procedures that know the regular source-cell probabilities and the cell-indexed propensity, but learn the target cell frequencies from the target sample.  The measurability clause ensures that the inversion rule is a well-defined random set on each realized finite carrier.

\begin{theoremv}{thm:regular-cell-unknown-weight-attainment}[Regular-cell weight attainment]
Fix \(c,\varepsilon,\alpha,c_-,c_+\in\mathbb R\) and sequences \(N_n\) and \(k_n\).  Suppose that
\begin{itemize}
\item \textbf{(Sample-size ratio.)} \(0<c\) and \(N_n/n\to c\).
\item \textbf{(Overlap and coverage levels.)} \(0<\varepsilon<1/2\) and \(0<\alpha<1\).
\item \textbf{(Regular cell constants.)} \(0<c_-\le 1\) and \(1\le c_+\).
\item \textbf{(Cell growth.)} \(k_n>0\) for every \(n\), \(k_n\to\infty\), and \(k_n/\sqrt n\to0\).
\item \textbf{(Measurable carrier.)} For every \(n\), the covariate carrier contains \(k_n\) injected measurable singleton cells.
\item \textbf{(Model restrictions.)} Every array \(P\in\mathcal N_n^{\mathrm{reg}}\) from \cref{obj:def:regular-finite-cell-class} with constants \(c,\varepsilon,c_-,c_+\) satisfies \cref{obj:ass:two-sample-array}, \cref{obj:ass:instrument-overlap}, and \cref{obj:ass:degrading-array}.
\end{itemize}
On \(\mathcal N_n^{\mathrm{reg}}\), let \(q_{x,n}=P_S^X(X=x)\) and define
\[
H_i=\frac{Z_i}{e(X_i)}-\frac{1-Z_i}{1-e(X_i)}.
\]
For \(G\in\{Y,D\}\), set
\[
\widehat p_{x,n}=\frac1{N_n}\sum_{j=1}^{N_n}\mathbf 1\{X_j^T=x\},
\qquad
\widehat m_{G,n}(x)=\frac1{nq_{x,n}}\sum_{i=1}^n
\mathbf 1\{X_i=x\}H_iG_i,
\]
and
\[
\widehat M_{G,n}=\sum_{x=1}^{k_n}\widehat p_{x,n}\widehat m_{G,n}(x).
\]
For \(N_n\ge2\), set
\[
\widehat\kappa_{U,n}
=
\frac1{N_n(N_n-1)}\sum_{j\ne\ell}
\frac{\mathbf 1\{X_j^T=X_\ell^T\}}{q_{X_j^T,n}},
\qquad
\widehat K_n=1+\widehat\kappa_{U,n},
\]
and put \(C_n^{\mathrm{reg}}=\Theta\) when \(N_n<2\).  Let
\[
B_{\varepsilon,c}=8(\varepsilon^{-2}+c^{-1}).
\]
For every fixed \(L\ge\{2B_{\varepsilon,c}/\alpha\}^{1/2}\), the sequence
\[
C_n^{\mathrm{reg}}
=
\left\{\vartheta\in\Theta:
|\widehat M_{Y,n}-\vartheta\widehat M_{D,n}|
\le L\sqrt{\widehat K_n/n}\right\}
\]
for \(N_n\ge2\), together with the preceding convention for \(N_n<2\), belongs to \(\mathfrak C^{\mathrm{reg}}\) from \cref{obj:def:regular-cell-feasible-honesty} and is constructed from the known regular source-cell probabilities, the known propensity, and the source and target samples.  Moreover, for every fixed \(t_0>0\),
\[
\limsup_{n\to\infty}\sup_{P\in\mathcal N_n^{\mathrm{reg}}:\,t_n\ge t_0}
\mathbb E_P[\lambda(C_n^{\mathrm{reg}})]
\le
\max\{2,4\sqrt2L+8B_{\varepsilon,c}\}
\min\{1,t_0^{-1/2}\}.
\]
The corresponding minimax risk over \(\mathfrak C^{\mathrm{reg}}\) is bounded below by
\[
\frac{3(1-\alpha)^2}{16}\min\{1,t_0^{-1/2}\}.
\]
Thus, with known regular source-cell probabilities and a known cell-varying propensity satisfying the stated overlap, the feasible regular-cell construction attains the oracle frontier order on \(\mathcal N_n^{\mathrm{reg}}\).
\end{theoremv}

\Cref{obj:thm:regular-cell-unknown-weight-attainment} extends finite-cell score inversion to regular source-cell masses and known cell-varying propensities.  The statistic uses inverse-propensity source contrasts, target empirical cell frequencies, and a regular-cell U-statistic dispersion proxy.  Under the stated sampling, overlap, and cell-growth conditions, the construction is honest over \(\mathfrak C^{\mathrm{reg}}\) and attains the same expected-length order as the fixed-geometry and uniform finite-cell procedures.

Together, \cref{obj:thm:fixed-geometry-frontier,obj:thm:finite-cell-unknown-weight-attainment,obj:thm:regular-cell-unknown-weight-attainment} show how the oracle frontier carries over from fixed weights to sample-learned finite-cell weights.  The common rate \(\min\{1,t_0^{-1/2}\}\) is indexed by the effective strength \(n\mu_n^2/\kappa_n\), with \(\kappa_n\) interpreted either as an oracle dispersion measure or as a target-sample-estimated finite-cell collision scale.

\section{Related Literature and Discussion}

The paper belongs first to the LATE and principal-stratification literature. The source experiment uses the encouragement-design logic of \citet{Bloom1984}, \citet{ImbensAngrist1994}, and \citet{AngristImbensRubin1996}: conditional randomization, exclusion, and monotonicity turn the source reduced form and first stage into a complier contrast. Principal stratification supplies the population-level interpretation of compliance types and target complier effects \citep{BalkePearl1997,ImbensRubin1997,FrangakisRubin2002,ImbensRosenbaum2005,HeckmanVytlacil2005}. In the present setting, \cref{obj:prop:compact-causal-range} identifies the transported reduced form and transported first stage through weighted source contrasts and places the target complier contrast inside the compact parameter space \(\Theta=[-1,1]\).

The weak-identification component follows the robust ratio-inference tradition. Anderson--Rubin and Fieller procedures invert reduced-form score restrictions, giving ratio inference through the score restriction itself \citep{AndersonRubin1949,Fieller1954}. Subsequent work characterizes the consequences of weak instruments for distributional approximation, confidence sets, and expected length \citep{GleserHwang1987,Dufour1997,BoundJaegerBaker1995,StaigerStock1997,StockWrightYogo2002,Moreira2003,AndrewsMoreiraStock2006,Mikusheva2010,AndrewsStockSun2019,BertanhaMoreira2020,Ma2023,SmuclerLanniMasip2025}. The oracle score construction in \cref{obj:def:inversion-handle} is the transported analogue of this logic: it tests candidate values by centering the weighted reduced form after subtracting the candidate times the weighted first stage. \Cref{obj:thm:oracle-converse,obj:thm:oracle-score-inversion-attainment} then characterize the minimax expected-length order for the transported ratio as \(\min\{1,t_0^{-1/2}\}\).

Transportability and generalizability enter through the target-to-source covariate weighting structure. Work on transporting and generalizing treatment effects emphasizes support, covariate reweighting, and target-population interpretation \citep{HotzImbensMortimer2005,PearlBareinboim2011,BareinboimPearl2013,BareinboimPearl2016,ColeStuart2010,StuartColeBradshawLeaf2011,Tipton2013,Dahabreh2020,DahabrehPetitoRobertson2020,Nguyen2017}. Here the transported-IV restrictions in \cref{obj:def:model-class} combine those support and contrast conditions with an IV compliance structure. The effective-strength scalar \(t_n=n\mu_n^2/\kappa_n\) summarizes the transported first-stage mean \(\mu_n\) and Kish dispersion \(\kappa_n\), so the expected-length comparison uses a single scale for weak compliance and covariate reweighting.

The feasible procedures connect to semiparametric weighting and modern transported noncompliance methods. Inverse-probability weighting, augmented estimation, and sample-splitting ideas motivate estimating transported nuisance components while retaining valid target-population interpretation \citep{HiranoImbensRidder2003,RobinsRotnitzkyZhao1994,VanDerLaanRubin2006,Chernozhukov2018}.

\paragraph{Transported noncompliance.}
\Citet{ChenHuang2025}, cited as an arXiv working paper, identify and estimate transported CACE in a two-sample generalizability design with source noncompliance and target population information; their inferential object is transported-CACE identification and estimation. \Citet{RudolphVanderLaan2017}, a journal article, develop robust estimators for encouragement-design effects transported across sites. \Citet{Ross2026}, also a journal article, study trial transport when participation changes adherence, with transported trial effects as the inferential target. The present paper adds the honest minimax expected-length frontier for a compact transported-CACE ratio under a triangular-array weak first stage and Kish weight dispersion, together with finite-cell procedures that attain the oracle order.

\paragraph{Weak-IV and two-sample IV inference.}
\Citet{Ma2023}, cited as an arXiv working paper, gives identification-robust LATE inference with high-dimensional covariates. \Citet{SmuclerLanniMasip2025}, also cited as an arXiv working paper, studies the properties of LATE confidence sets obtained by score-test inversion. \Citet{ChoiGuShen2018} and \citet{ChoiShen2019} instead give weak-instrument-robust inference for linear IV coefficients assembled from two samples. The present paper's source experiment and target covariate sample define a covariate-transport problem, and its positive scope is an honest frontier indexed by \(n\mu_n^2/\kappa_n\) with finite-cell target-weight learning.

Relative to transported-ATE efficiency and minimax benchmarks, the parameter here is a compact transported complier ratio whose denominator may shrink along the triangular array. Under the stated transported-IV, bounded-outcome, overlap, and dispersion conditions, the frontier is indexed by the ratio strength \(n\mu_n^2/\kappa_n\), whereas regular transported-ATE benchmarks use root-\(n\) target-mean scales.

Relative to classical weak-IV and two-sample IV inference, the present setting adds target-to-source covariate weighting and finite-cell target-law learning. Under the stated finite-cell and regular-cell conditions, the expected-length order records first-stage weakness and the dispersion induced by transporting the covariate distribution through the single scale \(n\mu_n^2/\kappa_n\).

Taken together, the results give a finite expected-length characterization for transported complier ratios in the stated transported-IV model classes, with the stated overlap, transport, positivity, envelope, dispersion, sample-size, and cell-growth conditions. The compact causal range in \cref{obj:prop:compact-causal-range} keeps length measured on \(\Theta\), while \cref{obj:thm:fixed-geometry-frontier} shows that the fixed-geometry minimax value is indexed by the effective-strength threshold \(t_0\). This synthesis places transported noncompliance on the same weak-ratio inference scale as classical weak-IV analysis, with \(\kappa_n\) recording the additional variation induced by moving from the source covariate law to the target covariate law.

\section{Appendix: Proofs, Auxiliary Arguments, and Verification Note}
\label{sec:deferred-proofs}

The appendix gives the mathematical arguments in dependency order.  It begins with the compact causal-range calculation, then proves the fixed-geometry frontier and its unit-weight specialization, then gives the oracle lower and upper bounds, and finally treats the regular and uniform finite-cell constructions.  Throughout, confidence sets take values in the compact parameter space \(\Theta\), and expected length is the Lebesgue length criterion from \cref{obj:def:frontier-risk}.

The finite-cell submodel is \(\mathcal P_n\) plus the uniform finite-cell source condition in \cref{obj:ass:finite-cell-source}.  For reference, the anchored formal checklist is:

\begin{definitionv}{def:finite-cell-class}[Finite-cell class \(\mathcal N_n\)]
The finite-cell submodel at index \(n\) is
\[
\mathcal N_n
=
\bigl\{P\in\mathcal P_n: P\text{ satisfies the following conditions at index }n\bigr\}.
\]
The required conditions are:
\begin{itemize}
\item \textbf{(Full-data support.)} \cref{obj:ass:full-data-support}.
\item \textbf{(Population presence.)} \cref{obj:ass:population-presence}.
\item \textbf{(Two-sample array.)} \cref{obj:ass:two-sample-array}.
\item \textbf{(Instrument overlap.)} \cref{obj:ass:instrument-overlap}.
\item \textbf{(Source observation.)} \cref{obj:ass:source-observation}.
\item \textbf{(Instrument randomization.)} \cref{obj:ass:iv-randomization}.
\item \textbf{(Instrument exclusion.)} \cref{obj:ass:iv-exclusion}.
\item \textbf{(Instrument monotonicity.)} \cref{obj:ass:iv-monotonicity}.
\item \textbf{(Outcome transport.)} \cref{obj:ass:outcome-transport}.
\item \textbf{(Receipt transport.)} \cref{obj:ass:receipt-transport}.
\item \textbf{(Target complier positivity.)} \cref{obj:ass:target-complier-positivity}.
\item \textbf{(Transport domination.)} \cref{obj:ass:transport-domination}.
\item \textbf{(Weight envelope.)} \cref{obj:ass:weight-envelope}.
\item \textbf{(Weight second moment.)} \cref{obj:ass:weight-second-moment}.
\item \textbf{(Degrading array.)} \cref{obj:ass:degrading-array}.
\item \textbf{(Finite-cell source.)} \cref{obj:ass:finite-cell-source}.
\end{itemize}
\end{definitionv}

The fixed-geometry lower bounds use the centered law and distance notation in \cref{obj:synth_5,obj:synth_3,obj:synth_4}.  The following anchored statement records the separation calculation for the least-favorable laws generated by \cref{obj:def:least-favorable-witness}.

\begin{definitionv}{def:geometry-handle}[Geometry handle \(Q_{n,h}^{\mathfrak g}\)]
Fix an admissible deterministic array \(\mathfrak g=(P_S^X,w,e)_{n\ge1}\). For \(t_0>0\), set
\[
\mu_n=\left(\frac{t_0\kappa_n}{n}\right)^{1/2},
\qquad
p_n(x)=\frac{\mu_n w(x)}{\kappa_n}.
\]
For \(0<\alpha<1\), let \(\rho=(1-\alpha)/8\). As the complier outcome mean varies over \(h\in\mathbb R\) with
\[
|h|\le \min\{1/4,\rho t_0^{-1/2}\},
\]
using the chi-square divergence and total variation distance defined in \cref{obj:synth_3,obj:synth_4}, the corresponding least-favorable laws satisfy
\[
1+\chi^2(Q_{n,h}^{\mathfrak g}\Vert Q_{n,0}^{\mathfrak g})
\le \exp(8t_0h^2),
\qquad
\|Q_{n,h}^{\mathfrak g}-Q_{n,0}^{\mathfrak g}\|_{\mathrm{TV}}
\le 2\rho .
\]
\end{definitionv}

The proof of \cref{obj:prop:compact-causal-range} expands the inverse-propensity score in \cref{obj:synth_1} conditional on \(X\).  Conditional instrument randomization and the source observation law turn the source score moments into the conditional assignment contrasts \(\Delta_Y(x)\) and \(\Delta_D(x)\).  Exclusion and monotonicity express those contrasts through complier potential outcomes, while \cref{obj:ass:outcome-transport,obj:ass:receipt-transport,obj:ass:transport-domination} move the reduced form and first stage to the target covariate law.  The bounded outcome support in \cref{obj:ass:full-data-support} gives the compact range \([-1,1]\), so the transported Wald ratio is interpreted as the target complier-conditional contrast in \cref{obj:prop:compact-causal-range}.

The lower-bound arguments for \cref{obj:thm:oracle-converse,obj:thm:fixed-geometry-frontier} use the least-favorable family in \cref{obj:def:least-favorable-witness}.  That construction sets the complier probability proportional to the transport weight, which calibrates the transported first stage to the selected effective-strength threshold.  Varying the complier outcome tilt changes the target complier contrast by a controlled amount, while \cref{obj:def:geometry-handle} bounds the chi-square and total-variation separations between the tilted law and the centered law.  Le Cam's testing argument then converts closeness of the observed-data laws into a lower bound on the expected length of any honest confidence set.  The single-population specialization in \cref{obj:prop:no-shift-reduction} follows by setting \(w=1\), which gives \(\kappa_n=1\) and the effective-strength index \(n\mu_n^2\).

For the oracle score-inversion result, \cref{obj:def:inversion-handle} is an Anderson--Rubin style inversion of the transported residual moment, in the tradition of \citet{AndersonRubin1949} and \citet{Fieller1954}.  At the true value \(\theta_T\), the centered score has variance controlled by the oracle weight second moment and the overlap constant.  The radius \(L_\alpha\sqrt{\widehat\kappa_n/n}\) combines that variance control with a uniform probability bound, yielding the honesty assertion in \cref{obj:thm:oracle-score-inversion-attainment}.  The expected-length calculation uses the same score inequality together with the effective-strength lower restriction \(t_n\ge t_0\), producing the order \(\min\{1,t_0^{-1/2}\}\).

The finite-cell arguments show that sample-learned weights preserve the oracle rate.  For \cref{obj:thm:finite-cell-unknown-weight-attainment}, the target empirical frequencies estimate target cell probabilities, the source cellwise averages estimate reduced-form and first-stage contrasts, and the U-statistic \(\widehat\kappa_{U,n}\) estimates the collision scale associated with target-to-source weight dispersion.  The sub-root cell-growth condition ensures that the additional sampling variation from cell learning remains on the same scale as the oracle score radius.  The regular-cell extension in \cref{obj:thm:regular-cell-unknown-weight-attainment} repeats this control with known source-cell masses \(q_{x,n}\) and known cell-varying propensity, using the regularity constants \(c_-\) and \(c_+\) to keep the cellwise variance terms uniformly bounded.

\paragraph{Verification note.}
The top-level theorem statements and proof obligations corresponding to \cref{obj:prop:compact-causal-range,obj:thm:fixed-geometry-frontier,obj:prop:no-shift-reduction,obj:thm:oracle-converse,obj:thm:oracle-score-inversion-attainment,obj:thm:regular-cell-unknown-weight-attainment,obj:thm:finite-cell-unknown-weight-attainment} are checked in Lean 4.  The auxiliary notation objects \cref{obj:synth_1,obj:synth_2,obj:synth_3,obj:synth_4,obj:synth_5} organize the manuscript notation used by those checked declarations, and \cref{obj:def:geometry-handle} records the checked fixed-geometry separation calculation used in the lower-bound proofs.

\begin{proof}[Proof of \cref{obj:prop:compact-causal-range}]
Write
\[
H(O)=
\begin{cases}
1/e(X), & Z=1,\\
-1/\{1-e(X)\}, & Z=0.
\end{cases}
\]
\begin{enumerate}
\item Model membership in \(\mathcal P_n\), as specified in \cref{obj:def:model-class}, supplies the full-data support, population presence, two-sample laws, overlap, source observation, randomization, exclusion, monotonicity, transport, target-complier positivity, domination, and envelope facts used below.  The source observation map \((S,X,D(0),D(1),Y(0),Y(1),Z)\mapsto (X,Z,D(Z),Y(D(Z)))\) is measurable, and its \(X\)-marginal agrees with the source full-data \(X\)-marginal.  Therefore overlap transfers to the observed source law:
\[
\varepsilon\le e(X)\le 1-\varepsilon
\qquad P_S\text{-a.s.}
\]
Consequently \(H\) is measurable and \(|H|\le 1/\varepsilon\) \(P_S\)-almost surely.  Since the observed outcome lies in \([0,1]\) and the receipt indicator is bounded by one, both \(H(O)Y\) and \(H(O)D\) are integrable under \(P_S\).

\item The inverse-propensity calculation used for the full-data contrasts is as follows.  Let \(F_0,F_1\) be measurable and integrable under \(P_n^F(\cdot\mid S=1)\), and assume that the assigned-source inverse-propensity integrand displayed below is integrable.  For each measurable \(A\), conditional randomization and overlap identify the \(Z=z\) slice of the assigned source law, mapped back to full data, as the source full-data law weighted by \(e(X)\) when \(z=1\) and by \(1-e(X)\) when \(z=0\).  Splitting the assigned law into the two slices gives
\[
\int_{\{X\in A\}}
\left[
\frac{\mathbf 1\{Z=1\}}{e(X)}F_1
-\frac{\mathbf 1\{Z=0\}}{1-e(X)}F_0
\right] dP_{\mathrm{assign},S}
=
\int_{\{X\in A\}}(F_1-F_0)\,dP_n^F(\cdot\mid S=1).
\]
On the \(Z=1\) slice the density \(e(X)\) cancels \(1/e(X)\), and on the \(Z=0\) slice the density \(1-e(X)\) cancels \(-1/\{1-e(X)\}\); the two slice integrals then add to the displayed contrast.

\item The chosen version \(\Delta_Y\) is measurable, and the source-observation facts give, for every measurable covariate set \(A\),
\[
\int_{\{O:X\in A\}}H(O)Y\,dP_S(O)
=
\int_A \Delta_Y(x)\,dP_S^X(x).
\]
For the source full-data comparison used in the transport step, apply the preceding identity with \(F_z=Y(D(z))\).  The required integrability of the assigned-source inverse-propensity integrand follows by pulling back the already established integrability of \(H(O)Y\) through the source observation map.  Pulling the observed integral back through that map and splitting over \(Z,D(0),D(1)\) gives
\[
\int_{\{O:X\in A\}}H(O)Y\,dP_S(O)
=
\int_{\{X\in A\}}\{Y(D(1))-Y(D(0))\}\,dP_n^F(\cdot\mid S=1).
\]
Exclusion rewrites \(Y(D(z))\) as \(Y^Z(z)\), and the source outcome-transport identity rewrites the last display as the integral over \(A\) of the source conditional assignment-outcome contrast.  Hence \(\Delta_Y\) agrees \(P_S^X\)-almost surely with that source contrast, while the displayed source-observation identity is the required observed source representation.

\item The chosen version \(\Delta_D\) is measurable, and the source-observation facts give, for every measurable covariate set \(A\),
\[
\int_{\{O:X\in A\}}H(O)D\,dP_S(O)
=
\int_A \Delta_D(x)\,dP_S^X(x).
\]
For the source full-data comparison, apply the inverse-propensity identity with \(F_z=D(z)\), written as the real indicator of receipt.  The required integrability of the assigned-source inverse-propensity integrand follows by pulling back the already established integrability of \(H(O)D\) through the source observation map.  Pulling the observed integral back and splitting over \(Z,D(0),D(1)\) gives
\[
\int_{\{O:X\in A\}}H(O)D\,dP_S(O)
=
\int_{\{X\in A\}}\{D(1)-D(0)\}\,dP_n^F(\cdot\mid S=1).
\]
The source receipt-transport identity rewrites the last display as the integral over \(A\) of the source conditional receipt contrast.  Hence \(\Delta_D\) agrees \(P_S^X\)-almost surely with that source contrast, while the displayed source-observation identity is the required observed source representation.

\item For the transported reduced-form outcome mean, the already proved source almost-sure equality between \(\Delta_Y\) and the source conditional assignment-outcome contrast first rewrites the integrand in \(\mu_{Y,n}\).  The Radon--Nikodym transport identity and target domination then change measure from \(P_S^X\) to \(P_T\).  The target outcome-transport equality replaces the transported source contrast by the target contrast, and the target full-data integral identity gives
\[
\mu_{Y,n}
=
\int \{Y^Z(1)-Y^Z(0)\}\,dP_n^F(\cdot\mid S=0).
\]
Exclusion changes \(Y^Z(z)\) to \(Y(D(z))\).  Finally, monotonicity permits a pointwise case split over \(D(0),D(1)\), yielding
\[
\mu_{Y,n}
=
\int
\{Y(1)-Y(0)\}\mathbf 1\{D(1)=1,\ D(0)=0\}
\,dP_n^F(\cdot\mid S=0).
\]

\item For the transported first-stage mean, the weighted source representation first unfolds \(\mu_n\) as
\[
\mu_n=\int w(x)\Delta_D(x)\,dP_S^X(x).
\]
The envelope \(0\le w\le 2k_n\), the bound \(|H|\le 1/\varepsilon\), and the receipt-indicator bound give integrability of the weighted observed receipt-score integrand.  Separately, the envelope and \(0\le\Delta_D\le1\) give integrability of \(w\Delta_D\).  Using the source almost-sure equality between \(\Delta_D\) and the source receipt contrast, Radon--Nikodym transport, the target receipt-transport equality, and then the target full-data receipt identity,
\[
\mu_n
=
\int \{D(1)-D(0)\}\,dP_n^F(\cdot\mid S=0).
\]
Monotonicity turns \(D(1)-D(0)\) into \(\mathbf 1\{D(1)=1,D(0)=0\}\), and integration of this indicator gives
\[
\mu_n
=
P_n^F\{D(1)=1,\ D(0)=0\mid S=0\}.
\]

\item By the definition of \(\theta_T\), the numerator is the target complier-weighted outcome contrast and the denominator is the target complier share.  Substituting the two identities just proved gives
\[
\frac{\mu_{Y,n}}{\mu_n}=\theta_T.
\]

\item Let \(\mu=P_n^F(\cdot\mid S=0)\) and \(C=\{D(1)=1,D(0)=0\}\).  Full-data support makes \(P_n^F\) a probability law, and together with population presence for \(S=0\) it makes \(\mu\) a probability law.  Target-complier positivity gives \(0<\mu(C)\).  The conditional law \(\mu\) is absolutely continuous with respect to \(P_n^F\), so bounded full-data support gives
\[
-1\le Y(1)-Y(0)\le 1
\qquad \mu\text{-a.s.}
\]
The set \(C\) is measurable, so integral monotonicity gives
\[
-\mu(C)
\le
\int (Y(1)-Y(0))\mathbf 1_C\,d\mu
\le
\mu(C).
\]
Dividing by the positive number \(\mu(C)\) yields \(\theta_T\in[-1,1]=\Theta\).
\end{enumerate}
\end{proof}

\begin{proof}[Proof of \cref{obj:thm:fixed-geometry-frontier}]
Fix \(t_0>0\).  The assumptions on \(N_n\), \(k_n\), \(\varepsilon\), and the admissible geometry imply that, for all sufficiently large \(n\), the fixed-geometry slice contains a law with effective strength at least \(t_0\).  On those indices, the liminf coverage term is the ordinary slice infimum, and the strength-restricted risk supremum is over a nonempty set.

1. For the lower bound, set
\[
\rho=\frac{1-\alpha}{8},
\qquad
H=\min\{1/4,\rho t_0^{-1/2}\}.
\]
Then \(\rho>0\) and \(H>0\).  The fixed-geometry least-favorable construction supplies, for all sufficiently large \(n\), a family \(\{Q_{n,h}^{\mathfrak g}: |h|\le H\}\) from \cref{obj:def:least-favorable-witness,obj:def:geometry-handle}.  Each member lies in the fixed-geometry slice of \cref{obj:def:fixed-geometry-slice}; the centered member has effective strength \(t_0\); the target contrast equals \(1/2+h\); and
\[
\|Q_{n,h}^{\mathfrak g}-Q_{n,0}^{\mathfrak g}\|_{\mathrm{TV}}\le 2\rho .
\]


2. Let \((C_n)\in\mathfrak C^{\mathrm{or}}(\mathfrak g)\) be any fixed-geometry honest oracle procedure.  For each row define
\[
a_n=\inf_{P\in\mathcal P_n(\mathfrak g)}
P\{\theta_T(P)\in C_n\},
\qquad
r_n=\sup_{\substack{P\in\mathcal P_n(\mathfrak g)\\ t_n(P)\ge t_0}}
\mathbb E_P[\lambda(C_n)] .
\]
The coverage values lie in \([0,1]\).  Since every confidence set is contained in \(\Theta=[-1,1]\), every expected length is at most \(2\), and the strength-restricted risk terms are bounded between \(0\) and \(2\) for all sufficiently large \(n\).

3. Fix a sufficiently large \(n\) and put \(I=[1/2-H,1/2+H]\).  For \(u\in I\), compare the law \(Q_{n,u-1/2}^{\mathfrak g}\) to the centered law \(Q_{n,0}^{\mathfrak g}\).  Because the target contrast under \(Q_{n,u-1/2}^{\mathfrak g}\) is \(u\), fixed-geometry coverage gives
\[
a_n\le
Q_{n,u-1/2}^{\mathfrak g}\{u\in C_n\}.
\]
The total-variation bound gives the comparison with \(Q_{n,0}^{\mathfrak g}\).  Applying the coverage-to-expected-length reduction over the interval \(I\), whose Lebesgue length is \(2H\), yields
\[
2H(a_n-2\rho)
\le
\mathbb E_{Q_{n,0}^{\mathfrak g}}[\lambda(C_n)]
\le r_n .
\]


4. Pass the preceding inequality to limits.  The honesty condition in \cref{obj:def:fixed-geometry-honesty} gives
\[
1-\alpha\le \liminf_{n\to\infty} a_n .
\]
The affine map \(x\mapsto 2H(x-2\rho)\) is monotone, and the boundedness from Step 2 justifies the liminf--limsup comparison.  Hence every honest \((C_n)\) satisfies
\[
2H(1-\alpha-2\rho)
\le
\limsup_{n\to\infty} r_n
=
R_{\mathfrak g}((C_n),t_0).
\]


5. The constants reduce by elementary arithmetic.  If \(\rho t_0^{-1/2}\le 1/4\), then \(H=\rho t_0^{-1/2}\) and
\[
2H(1-\alpha-2\rho)
=
\frac{3(1-\alpha)^2}{16}\,t_0^{-1/2}.
\]
If \(1/4\le \rho t_0^{-1/2}\), then \(H=1/4\), and the same expression dominates
\[
\frac{3(1-\alpha)^2}{16}\min\{1,t_0^{-1/2}\}.
\]
Thus
\[
c_\alpha\min\{1,t_0^{-1/2}\}
\le R_{\mathfrak g}((C_n),t_0)
\]
for every \((C_n)\in\mathfrak C^{\mathrm{or}}(\mathfrak g)\).  The procedure \(C_n=\Theta\) has coverage one on every row, so the honest class is nonempty; taking the infimum in \cref{obj:def:fixed-geometry-value} gives the lower bound for \(V_{\mathfrak g}^{\star}(t_0)\).

6. For the upper bound, put
\[
L=L_\alpha=\left(\frac{8}{\alpha\varepsilon^2}\right)^{1/2}
\]
and define an oracle score-inversion rule using the fixed geometry:
for every sufficiently large index, take the set in \cref{obj:def:inversion-handle} with \(w\) equal to the Radon--Nikodym weight of the target covariate law relative to the source covariate law in \(\mathfrak g\), and with propensity \(e\) equal to the fixed propensity in \(\mathfrak g\). Choose any measurable value at the finitely many earlier indices; changing finitely many values leaves the limiting statements unchanged. Measurability and containment in \(\Theta\) follow from the measurability of the fixed weight and propensity and from the construction of the inversion set.

7. Fix \(n>0\) and \(P\in\mathcal P_n(\mathfrak g)\).  The slice condition identifies the oracle weight and propensity with those of \(P\), up to the admissible almost-sure weight version.  The compact range result in \cref{obj:prop:compact-causal-range} gives \(\theta_T\in\Theta\), identifies the transported first stage with the target complier share, and gives
\[
\frac{\mu_{Y,n}}{\mu_n}=\theta_T,
\qquad
\mu_n>0 .
\]
The Radon--Nikodym change-of-measure identity gives \(\mathbb E_S[w(X)]=1\), so the Kish dispersion satisfies \(\kappa_n\ge1\).  A conditional-expectation pullout identity converts weighted source-score integrals into the corresponding covariate integrals.  Applying it to the receipt score gives mean \(\mu_n\), while instrument overlap bounds its variance by \(\kappa_n/\varepsilon^2\).  Chebyshev's inequality therefore yields
\[
P\!\left(\mu_n/2<
\left|\widehat B_n-\mu_n\right|\right)
\le
\frac{4}{\varepsilon^2 t_n}.
\]
The generic affine-inversion length bound then gives the pointwise inequality
\[
\mathbb E_P[\lambda(C_n)]
\le
\max\left\{2,\,4L+\frac{8}{\varepsilon^2}\right\}
\min\{1,t_n^{-1/2}\}.
\]
Changing finitely many initial indices leaves the limiting expected-length bound unchanged.

8. Define
\[
\delta_n=
\begin{cases}
1, & n=0,\\
16k_n^2/n, & n>0 .
\end{cases}
\]
Since \(k_n/\sqrt n\to0\), we have \(\delta_n\to0\).

9. The same fixed row \(n>0\) also gives the coverage bound.  For the true \(\theta_T\), the transported residual score has mean zero and variance at most \(4\kappa_n/\varepsilon^2\).  The empirical Kish statistic has mean \(\kappa_n\), and the envelope \(0\le w\le2k_n\) gives
\[
P(\widehat\kappa_n<\kappa_n/2)\le \frac{16k_n^2}{n\kappa_n}
\le \delta_n .
\]
With
\[
a=L\sqrt{\frac{\kappa_n}{2n}},
\]
the identity \(L^2=8/(\alpha\varepsilon^2)\) and Chebyshev's inequality give
\[
P\!\left(
\left|\widehat A_n-\theta_T\widehat B_n\right|>a
\right)\le \alpha .
\]
On the complement of these two bad events,
\[
\left|\widehat A_n-\theta_T\widehat B_n\right|
\le
L\sqrt{\widehat\kappa_n/n},
\]
so \(\theta_T\in C_n\).  Hence, for every \(P\in\mathcal P_n(\mathfrak g)\),
\[
1-\alpha-\delta_n
\le
P\{\theta_T(P)\in C_n\}.
\]
Changing the value assigned at \(n=0\) leaves the limiting coverage statement unchanged.

10. The coverage values lie in \([0,1]\), the slice is nonempty for all sufficiently large \(n\), and \(\delta_n\to0\).  The preceding coverage inequality therefore implies
\[
(C_n)\in\mathfrak C^{\mathrm{or}}(\mathfrak g)
\]
in the sense of \cref{obj:def:fixed-geometry-honesty}.

11. Let
\[
C_0=\max\left\{2,\,4L+\frac{8}{\varepsilon^2}\right\}.
\]
The pointwise length inequality in Step 7 and the strength restriction \(t_n\ge t_0\) imply, row by row and then after taking the limsup frontier risk in \cref{obj:def:frontier-risk},
\[
R_{\mathfrak g}((C_n),t_0)
\le
C_0\min\{1,t_0^{-1/2}\}.
\]
Since \((C_n)\) is admissible for the infimum in \cref{obj:def:fixed-geometry-value}, and the risk functional is nonnegative by compactness of \(\Theta\), this gives
\[
V_{\mathfrak g}^{\star}(t_0)
\le
C_0\min\{1,t_0^{-1/2}\}.
\]
Substituting \(L=L_\alpha\) makes \(C_0=C_\alpha\).

Combining Steps 5 and 11 gives the asserted two-sided bound.  The constants used in both arguments depend only on \(\alpha\) and \(\varepsilon\), so they are uniform over all admissible deterministic geometries \(\mathfrak g\).
\end{proof}

\begin{proof}[Proof of \cref{obj:prop:no-shift-reduction}]
Fix the admissible geometry array \(\mathfrak g\). We prove the three asserted conclusions in the order in which they appear.

\begin{enumerate}
\item For each \(n\), the unit-weight hypothesis gives
\[
w_n(x)^2=1
\qquad
P_{S,n}^X\text{-a.s.}
\]
Hence, by the definition of the geometry second moment and because \(P_{S,n}^X\) is a probability law under \cref{obj:def:admissible-geometry-class},
\[
\kappa_n
=
\int w_n(x)^2\,dP_{S,n}^X(x)
=
\int 1\,dP_{S,n}^X
=
1 .
\]
For the equality of covariate laws, let \(A\subseteq\mathcal X\) be measurable. The change-of-measure clause in \cref{obj:def:admissible-geometry-class} gives
\[
P_{T,n}(A)
=
\int_A w_n(x)\,dP_{S,n}^X(x).
\]
The same almost-sure equality \(w_n=1\) remains valid after restricting \(P_{S,n}^X\) to \(A\), so
\[
P_{T,n}(A)
=
\int_A 1\,dP_{S,n}^X
=
P_{S,n}^X(A).
\]
Since this holds for every measurable \(A\), the measures agree:
\[
P_{T,n}=P_{S,n}^X .
\]


\item Let \(P\in\mathcal P_n(\mathfrak g)\). By \cref{obj:def:fixed-geometry-slice}, its source covariate marginal is \(P_{S,n}^X\) from \(\mathfrak g\), and its Radon--Nikodym transport weight agrees \(P_{S,n}^X\)-almost surely with the geometry weight \(w_n\). Therefore the law-induced Kish dispersion of \(P\) equals the geometry second moment:
\[
\int \{w_n^P(x)\}^2\,dP_{S,n}^X(x)
=
\int w_n(x)^2\,dP_{S,n}^X(x)
=
\kappa_n .
\]
By the first step, this common value is \(1\). Substituting this into the definition of effective strength in \cref{obj:def:frontier-risk} yields
\[
t_n
=
\frac{n\mu_n^2}{\kappa_n}
=
\frac{n\mu_n^2}{1}
=
n\mu_n^2 .
\]


\item Fix \(t_0>0\). The hypotheses on \(c\), \(N_n/n\), \(\varepsilon\), \(\alpha\), \(k_n\), and the admissibility of \(\mathfrak g\) are exactly the structural hypotheses required by \cref{obj:thm:fixed-geometry-frontier}. For every \(P\in\mathcal P_n(\mathfrak g)\), the defining membership condition in \cref{obj:def:fixed-geometry-slice} contains the transported-IV model assumptions needed there: the two-sample array condition, instrument overlap, the weight envelope, the weight second-moment condition, and the degrading-array condition. Applying \cref{obj:thm:fixed-geometry-frontier} therefore gives, with
\[
L_\alpha=\sqrt{\frac{8}{\alpha\varepsilon^2}},
\qquad
c_\alpha=\frac{3(1-\alpha)^2}{16},
\qquad
C_\alpha=\max\left\{2,\;4L_\alpha+\frac{8}{\varepsilon^2}\right\},
\]
the bounds
\[
c_\alpha \min\{1,t_0^{-1/2}\}
\le
V_{\mathfrak g}^\star(t_0)
\le
C_\alpha \min\{1,t_0^{-1/2}\}.
\]

\end{enumerate}
\end{proof}

\begin{proof}[Proof of \cref{obj:thm:oracle-converse}]
\begin{enumerate}
\item Set
\[
c_0=\frac{3(1-\alpha)^2}{16},
\qquad
t_c=1 .
\]
Since \(0<\alpha<1\), \(1-\alpha>0\), and hence \(c_0>0\); also \(t_c>0\).  Fix an arbitrary measurable covariate space, arbitrary arrays \(N_n,k_n\) satisfying the displayed size and cell-scale assumptions, and a threshold \(t_0>0\).

\item Choose the measurable singleton carriers \(x_{n,i}\), \(i\in\{1,\ldots,k_n\}\), supplied by the carrier assumption.  Let
\[
\mu_n=\sum_{i=1}^{k_n} k_n^{-1}\delta_{x_{n,i}} .
\]
The positivity of \(k_n\) gives \(k_n k_n^{-1}=1\), so each \(\mu_n\) is a probability measure.  Define the deterministic geometry
\[
P_{S,n}^X=\mu_n,\qquad P_{T,n}=\mu_n,\qquad w_n\equiv 1,\qquad e_n\equiv \frac12 .
\]
The probability, overlap, envelope, second-moment, and domination requirements in \cref{obj:def:admissible-geometry-class} follow directly from these definitions and from \(0<\varepsilon<1/2\), with the change-of-measure identity reducing to integration of the constant weight \(1\).

\item The fixed-geometry slice associated with this \(\mathfrak g\) is contained in the finite-cell class \(\mathcal N_n\) of \cref{obj:def:finite-cell-class} at each \(n\).  Indeed, the source covariate law is the uniform measure on the displayed \(k_n\) carriers, the carrier cells are measurable singletons, each cell has source mass \(1/k_n\), the target law agrees with the source law, and the transport weight is \(1\) almost surely.

\item Consider the oracle procedure \(D^{\mathrm{full}}\) that always returns the whole compact parameter space \(\Theta=[-1,1]\).  Its graph is measurable, its values are subsets of \(\Theta\), and it is invariant to almost-sure changes of the supplied weight.  For every transported-IV row \(P\), \cref{obj:prop:compact-causal-range} places \(\theta_T(P,n)\) in \(\Theta\), so the coverage of \(D^{\mathrm{full}}\) is exactly one.  Taking the infimum over the nonempty transported class preserves the value one, and therefore \(D^{\mathrm{full}}\) is oracle-honest in the sense of \cref{obj:def:oracle-honesty}.  Thus the class over which \(V^\star(t_0)\) and \(V_{\mathcal N}^\star(t_0)\) take their infima is nonempty.

\item On the unit-weight geometry just constructed, the weight is \(1\) almost surely for every \(n\).  Applying \cref{obj:prop:no-shift-reduction} yields, for every \(t_0>0\),
\[
c_0\min\{1,t_0^{-1/2}\}
\le
V_{\mathfrak g}^\star(t_0),
\]
where \(V_{\mathfrak g}^\star(t_0)\) is the fixed-geometry value from \cref{obj:def:fixed-geometry-value}.

\item Let \(C\) be any oracle-honest procedure.  Restricting \(C\) to the fixed geometry gives a fixed-geometry honest procedure, and the global frontier risk from \cref{obj:def:frontier-risk} dominates the corresponding fixed-geometry risk.  Hence
\[
V_{\mathfrak g}^\star(t_0)\le R((C_n),t_0).
\]
Taking the infimum over all oracle-honest \(C\) in \cref{obj:def:oracle-value} gives
\[
c_0\min\{1,t_0^{-1/2}\}\le V^\star(t_0).
\]

\item For the finite-cell value, first note the two boundedness facts used in the limsup comparison.  The length criterion is always between \(0\) and \(2\), since it is Lebesgue length after intersecting with \(\Theta=[-1,1]\).  Therefore every fixed-geometry risk term is nonnegative: if the strength-indexed slice is empty the supremum is zero, and otherwise the expected length of any selected law is nonnegative.  The same length bound gives an eventual upper bound by \(2\) for the relevant fixed and finite-cell risk terms.

\item Fix an arbitrary oracle-honest procedure \(C\).  Build a fixed-geometry oracle procedure \(D\) by feeding \(C\) the observed sample together with the canonical Radon--Nikodym weight \(dP_{T,n}/dP_{S,n}^X\) of \(\mathfrak g\) and the fixed propensity \(e_n\).  On every row \(P\in\mathcal P_n(\mathfrak g)\), the source law, target law, weight, and propensity agree with those inputs, so \(D\)'s confidence set, coverage probability, and expected length agree with those of \(C\) on that row.  For all sufficiently large \(n\), the fixed-geometry strength slice is nonempty, which supplies the required coverage comparison; the coverage probabilities lie between \(0\) and \(1\), so the liminf comparison transfers oracle honesty of \(C\) to fixed-geometry honesty of \(D\).

\item For all sufficiently large \(n\), the fixed-geometry strength slice has admissible laws.  On such rows,
\[
\sup_{P\in\mathcal P_n(\mathfrak g):\,t_n\ge t_0}
\mathbb E_P\lambda(D_n)
\le
\sup_{P\in\mathcal N_n:\,t_n\ge t_0}
\mathbb E_P\lambda(C_n),
\]
because \(\mathcal P_n(\mathfrak g)\subseteq\mathcal N_n\) and the expected lengths of \(D\) and \(C\) agree on the slice.  The nonnegativity and boundedness from the previous step allow the limsup comparison, hence the fixed-geometry risk of \(D\) is bounded by the finite-cell oracle risk of \(C\).  Taking the infimum over oracle-honest \(C\), and using the fixed-geometry lower bound, gives
\[
c_0\min\{1,t_0^{-1/2}\}
\le
V_{\mathcal N}^\star(t_0).
\]

\item Combining the preceding inequalities proves the two displayed lower bounds for every \(t_0>0\).  If \(0<t_0\le t_c=1\), then \(t_0^{-1/2}\ge1\), so
\[
\min\{1,t_0^{-1/2}\}=1,
\]
and the oracle lower bound yields \(c_0\le V^\star(t_0)\).  The constants \(c_0\) and \(t_c\) were chosen before the covariate space, arrays, transported laws, and effective-strength decomposition; the argument uses a row only through the threshold condition \(t_n=n\mu_n^2/\kappa_n\ge t_0\).  Thus the same constants apply uniformly over all decompositions of \(t_0\) into \(\mu_n\) and \(\kappa_n\).
\end{enumerate}
\end{proof}

\begin{proof}[Proof of \cref{obj:thm:oracle-score-inversion-attainment}]
\begin{enumerate}
\item \textbf{Construction.}
Choose an admissible geometry \(\mathfrak g\) supplied by the hypothesis.  For every \(t_0>0\), the fixed-geometry construction supplies admissible comparison laws for all sufficiently large \(n\), and hence the full class \(\mathcal P_n\) is nonempty at those indices.  Set
\[
L_\alpha=\left(\frac{8}{\alpha\varepsilon^2}\right)^{1/2},
\qquad
C_0=\max\left\{2,\ 4L_\alpha+\frac8{\varepsilon^2}\right\}.
\]
Since \(C_0\ge 2\), \(C_0>0\).  Define \(C_n\) by the score-inversion construction in \cref{obj:def:inversion-handle}, using the oracle weight and propensity as inputs.  Thus, for every source sample,
\[
C_n=\left\{\theta\in\Theta:
\left|\widehat A_n-\theta\widehat B_n\right|
\le L_\alpha\sqrt{\widehat\kappa_n/n}\right\}.
\]
The range condition follows from the defining intersection with \(\Theta\).  The graph is measurable because \(\widehat A_n\), \(\widehat B_n\), and \(\widehat\kappa_n\) are measurable functions of the source sample whenever the supplied weight and propensity are measurable.  If two versions of the oracle weight agree almost surely under \(P_S^X\), then the corresponding inversion sets agree almost surely under the two-sample law.

\item \textbf{Weighted conditional-expectation reduction.}
The proof uses the following pull-out identity.  Suppose \(F\) is a source-observation function, \(d\) is a covariate function, and \(w\) is a measurable transport weight.  If, for every measurable \(A\),
\[
\int_{\{o:X(o)\in A\}} F(o)\,dP_S(o)
=
\int_A d(x)\,dP_S^X(x),
\]
then
\[
\int w(X(o))F(o)\,dP_S(o)
=
\int w(x)d(x)\,dP_S^X(x).
\]
Indeed, the set-integral identity identifies \(d(X)\) with the conditional expectation of \(F\) given \(X\); multiplying by the \(X\)-measurable function \(w(X)\) and integrating gives the displayed equality.  The measurability and integrability requirements are supplied by overlap, the weight envelope, and the bounded source-observation variables.

\item \textbf{Pointwise expected length.}
Fix \(n>0\) and \(P\in\mathcal P_n\).  The compact causal-range conclusion in \cref{obj:prop:compact-causal-range} gives \(\theta_n=\theta_T(P,n)\in\Theta\), identifies the transported first stage with the target complier share, and therefore gives \(\mu_n>0\).  The Radon--Nikodym weight has mean one under \(P_S^X\), so its second moment \(\kappa_n\) satisfies \(\kappa_n\ge1\).

Let
\[
B_n=\frac1n\sum_{i=1}^n w(X_i)H_iD_i .
\]
The score identities and the pull-out identity give \(\mathbb E_P B_n=\mu_n\).  The overlap bound gives
\[
\operatorname{Var}_P(B_n)\le \frac{\kappa_n}{\varepsilon^2 n},
\]
and hence
\[
\Pr_P\left\{\frac{\mu_n}{2}<|B_n-\mu_n|\right\}
\le
\frac{4}{\varepsilon^2 t_n},
\qquad
t_n=\frac{n\mu_n^2}{\kappa_n}.
\]
The empirical Kish statistic \(\widehat\kappa_n=n^{-1}\sum_i w(X_i)^2\) is nonnegative and has mean \(\kappa_n\).  Applying the affine score-inversion length bound to \(\widehat A_n\), \(\widehat B_n\), and \(\widehat\kappa_n\) gives
\[
\mathbb E_P\lambda(C_n)
\le
C_0\min\{1,t_n^{-1/2}\}.
\]

\item \textbf{Pointwise coverage.}
Define
\[
\delta_n=
\begin{cases}
1, & n=0,\\
16k_n^2/n, & n>0.
\end{cases}
\]
The scale condition \(k_n/\sqrt n\to0\) gives \(\delta_n\to0\).  Changing finitely many initial indices leaves the limiting coverage requirement unchanged.  For every sufficiently large \(n\), set
\[
S_n=\widehat A_n-\theta_n\widehat B_n,
\qquad
K_n=\widehat\kappa_n,
\]
and use the ratio identity from \cref{obj:prop:compact-causal-range}.  The pull-out identity gives mean zero for the affine score \(w(X_i)H_i(Y_i-\theta_nD_i)\), and overlap and boundedness give
\[
\operatorname{Var}_P(S_n)\le \frac{4\kappa_n}{\varepsilon^2 n}.
\]
The envelope \(0\le w\le 2k_n\) gives the empirical-Kish lower-tail bound
\[
\Pr_P\{K_n<\kappa_n/2\}
\le
\frac{16k_n^2}{n\kappa_n}
\le
\frac{16k_n^2}{n}.
\]
With
\[
a_n=L_\alpha\sqrt{\frac{\kappa_n}{2n}},
\]
the identity \(L_\alpha^2=8/(\alpha\varepsilon^2)\) and Chebyshev's inequality yield
\[
\Pr_P\{|S_n|>a_n\}\le\alpha.
\]
On the complement of these two bad events,
\[
|S_n|\le a_n\le L_\alpha\sqrt{K_n/n},
\]
and \(\theta_n\in\Theta\), so \(\theta_n\in C_n\).  Therefore, uniformly over \(P\in\mathcal P_n\),
\[
1-\alpha-\delta_n
\le
\Pr_P\{\theta_n\in C_n\}.
\]

\item \textbf{Oracle honesty.}
Coverage probabilities lie in \([0,1]\), the class is nonempty for all sufficiently large \(n\), and \(\delta_n\to0\).  On those indices, the infimum over \(P\in\mathcal P_n\) agrees with the oracle coverage infimum for the constructed score-inversion procedure.  Taking the liminf of these infima in \cref{obj:def:oracle-honesty} gives
\[
1-\alpha
\le
\liminf_{n\to\infty}\inf_{P\in\mathcal P_n}
\Pr_P\{\theta_T(P,n)\in C_n\}.
\]
Thus \((C_n)\in\mathfrak C^{\mathrm{or}}\).

\item \textbf{Frontier risk of the constructed sequence.}
Fix \(t_0>0\).  Changing finitely many initial indices leaves the limiting risk unchanged, and every sufficiently large \(n\) uses the pointwise expected-length bound above.  Since \(t\mapsto \min\{1,t^{-1/2}\}\) is decreasing on \((0,\infty)\), every row with \(t_n\ge t_0\) satisfies
\[
\mathbb E_P\lambda(C_n)
\le
C_0\min\{1,t_0^{-1/2}\}.
\]
Taking the supremum over such \(P\) and then the limsup in \cref{obj:def:frontier-risk} gives
\[
R((C_n),t_0)
\le
C_0\min\{1,t_0^{-1/2}\}.
\]

\item \textbf{Oracle value.}
For every admissible geometry \(\mathfrak g'\in\mathcal G\), \cref{obj:thm:fixed-geometry-frontier} gives
\[
\frac{3(1-\alpha)^2}{16}\min\{1,t_0^{-1/2}\}
\le
V_{\mathfrak g'}^\star(t_0)
\le
C_0\min\{1,t_0^{-1/2}\}.
\]
Using the selected geometry \(\mathfrak g\), every globally oracle-honest procedure induces a fixed-geometry honest procedure, so the same lower bound applies to the global oracle risk of any \((E_n)\in\mathfrak C^{\mathrm{or}}\).  Since the constructed \((C_n)\) is oracle honest, the infimum defining \(V^\star(t_0)\) is over a nonempty class.  Also, \(\lambda(A)\le\lambda(\Theta)=2\) for every reported set \(A\), so the relevant suprema are uniformly bounded for the limsup comparison.  Hence the risks over oracle-honest procedures are bounded below, and evaluating the infimum at the constructed sequence gives
\[
V^\star(t_0)
\le
C_0\min\{1,t_0^{-1/2}\}.
\]
Combining the displayed bounds proves the asserted frontier-risk bound and the corresponding attainable oracle-value order for every fixed \(t_0>0\).
\end{enumerate}
\end{proof}

\begin{proof}[Proof of \cref{obj:thm:regular-cell-unknown-weight-attainment}]
\begin{enumerate}
\item The measurable-carrier hypothesis and the regular-cell mass bounds give a member of \(\mathcal N_n^{\mathrm{reg}}\) in every row \(n\), so the coverage term in \(\mathfrak C^{\mathrm{reg}}\) is evaluated by an ordinary infimum.  Put
\[
B_{\varepsilon,c}=8(\varepsilon^{-2}+c^{-1}),
\qquad
C_0=\max\{2,4\sqrt2L+8B_{\varepsilon,c}\}.
\]
Then \(B_{\varepsilon,c}\ge0\), \(L\ge0\), and \(C_0>0\).  Define \(C_n^{\mathrm{reg}}\) by the regular-cell inversion rule, with the stated \(\Theta\)-valued convention when \(N_n<2\).  The rule is the measurable regular-cell procedure obtained by applying the known source masses \(q_{x,n}\), the known propensity values, and the source and target samples to the score inversion map.

\item For every \(P\in\mathcal N_n^{\mathrm{reg}}\), the constructed random set agrees almost surely with the regular-cell inversion rule using the ambient source-cell mass function and propensity score.  This is the representation used in the honesty argument.  On rows with \(2\le N_n\), that same inversion rule is the affine set
\[
\left\{\vartheta\in\Theta:
|\widehat M_{Y,n}-\vartheta\widehat M_{D,n}|
\le L\sqrt{\widehat K_n/n}\right\},
\]
whereas on rows with \(N_n<2\) it is \(\Theta\) by definition.  Since the class is nonempty in every row, the coverage term in \cref{obj:def:regular-cell-feasible-honesty} is
\[
\inf_{P\in\mathcal N_n^{\mathrm{reg}}}
P(\theta_T\in C_n^{\mathrm{reg}}).
\]
The uniform coverage bound for the regular-cell inversion rule applies at the critical inequality
\(L\ge\{2B_{\varepsilon,c}/\alpha\}^{1/2}\), and yields
\[
1-\alpha\le
\liminf_{n\to\infty}
\inf_{P\in\mathcal N_n^{\mathrm{reg}}}
P(\theta_T\in C_n^{\mathrm{reg}}).
\]
Thus \((C_n^{\mathrm{reg}})\in\mathfrak C^{\mathrm{reg}}\).

\item Fix \(t_0>0\).  From \(N_n/n\to c>0\), eventually \(n>0\) and \(2\le N_n\).  On this tail, for every \(P\in\mathcal N_n^{\mathrm{reg}}\), the compact causal-range identities give a positive transported first stage \(\mu_n\).  The regular-cell moment calculations give integrability of the outcome and receipt score moments, nonnegativity of \(\widehat K_n\), and
\[
\mathbb E_P[\widehat K_n]=1+\kappa_n\le2\kappa_n .
\]
The first-stage deviation event satisfies
\[
P\{|\widehat M_{D,n}-\mu_n|>\mu_n/2\}
\le
\frac{4B_{\varepsilon,c}}{t_n},
\qquad
t_n=\frac{n\mu_n^2}{\kappa_n}.
\]
Using the almost-sure representation from the preceding step together with the nondegenerate identity for the regular-cell inversion rule, the expected length of \(C_n^{\mathrm{reg}}\) is the expected length of this affine inversion set for all sufficiently large \(n\).  Applying the inflated affine-inversion length bound with \(Y=8B_{\varepsilon,c}\) gives, uniformly over \(P\in\mathcal N_n^{\mathrm{reg}}\),
\[
\mathbb E_P[\lambda(C_n^{\mathrm{reg}})]
\le
C_0\min\{1,t_n^{-1/2}\}
\]
on that tail.  The abstract frontier-risk monotonicity then converts the pointwise bound into
\[
\limsup_{n\to\infty}\sup_{P\in\mathcal N_n^{\mathrm{reg}}:\,t_n\ge t_0}
\mathbb E_P[\lambda(C_n^{\mathrm{reg}})]
\le
C_0\min\{1,t_0^{-1/2}\}.
\]

\item For the lower bound, choose the injected singleton carrier in each row and put the same uniform law on the source and target covariates:
\[
\nu_n=\sum_{i=1}^{k_n} k_n^{-1}\delta_{x_{i,n}},
\qquad
w_n\equiv1,
\qquad
e_n\equiv\frac12 .
\]
This deterministic geometry is admissible: \(\nu_n\) is a probability measure supported on the chosen cells, the propensity obeys the overlap interval, the weight envelope and second-moment bounds hold, and the equal cell masses satisfy
\[
\frac{c_-}{k_n}\le \frac1{k_n}\le \frac{c_+}{k_n}.
\]
Consequently, every fixed-geometry slice for this geometry is contained in \(\mathcal N_n^{\mathrm{reg}}\).  By \cref{obj:thm:fixed-geometry-frontier}, its fixed-geometry honest minimax risk is bounded below by
\[
\frac{3(1-\alpha)^2}{16}\min\{1,t_0^{-1/2}\}.
\]

\item Let \(C\in\mathfrak C^{\mathrm{reg}}\) be arbitrary.  Construct a fixed-geometry oracle procedure \(D\) by feeding \(C\) the observed two-sample draw, the selected regular-cell design, the mass vector \(q_{x,n}=1/k_n\), and the propensity vector \(e_{x,n}=1/2\) whenever the sample lies on the selected support; on the complementary event set \(D_n=\Theta\).  Under every law in the fixed-geometry slice, the support event has probability one, so \(D_n\) and \(C_n\) agree almost surely after the slice is viewed as a regular-cell law.  Hence their coverages agree on the slice, and on the tail where the fixed-geometry slice has admissible laws, the fixed-geometry coverage infimum is at least the corresponding regular-cell infimum.  Thus \(D\) is fixed-geometry honest.  The same almost-sure equality gives equality of expected lengths on the slice, while every set length is bounded by \(\lambda(\Theta)=2\); therefore the fixed-geometry risk of \(D\) is at most the regular-cell risk of \(C\).  Combining this domination with the previous fixed-geometry lower bound and then taking the infimum over all \(C\in\mathfrak C^{\mathrm{reg}}\) proves
\[
\frac{3(1-\alpha)^2}{16}\min\{1,t_0^{-1/2}\}
\le
\inf_{C\in\mathfrak C^{\mathrm{reg}}}
\limsup_{n\to\infty}
\sup_{P\in\mathcal N_n^{\mathrm{reg}}:\,t_n\ge t_0}
\mathbb E_P[\lambda(C_n)].
\]
Together with the constructed upper bound, this gives the stated regular-cell attainment conclusion.
\end{enumerate}
\end{proof}

\begin{proof}[Proof of \cref{obj:thm:finite-cell-unknown-weight-attainment}]
\begin{enumerate}
\item
First record that every finite-cell row class contains an admissible law.  The carrier condition, the sampling-ratio limit, the positivity and growth conditions on \(k_n\), and the overlap bounds give an element of \(\mathcal N_n\) for each \(n\).  Define
\[
B_c=32(1+c^{-1}),\qquad
L_{\alpha,c}=\left(\frac{2B_c}{\alpha}\right)^{1/2},\qquad
C_0=\max\{2,4\sqrt2\,L_{\alpha,c}+8B_c\}.
\]
To compare with the regular-cell construction, set
\[
x=4+\frac3c,\qquad a=x^{-1/2},\qquad
\bar\varepsilon=\frac14+\frac12 a .
\]
Since \(c>0\), \(x>4\), hence \(\sqrt{x}>2\), \(0<a<1/2\), and
\(0<\bar\varepsilon<1/2\).  Moreover
\[
\bar\varepsilon\sqrt{x}\ge 1,
\qquad
\bar\varepsilon^{-2}\le x .
\]
With
\[
B=8(\bar\varepsilon^{-2}+c^{-1}),
\]
this gives \(0\le B\le B_c\).  Consequently
\[
\left(\frac{2B}{\alpha}\right)^{1/2}\le L_{\alpha,c},
\]
and \(C_0>0\) because \(C_0\ge2\).

\item
Apply \cref{obj:thm:regular-cell-unknown-weight-attainment} with overlap level
\(\bar\varepsilon\), regular-cell constants \(c_-=c_+=1\), and critical constant
\(L_{\alpha,c}\).  The hypotheses required there are supplied by the corresponding regular-cell row assumptions: the row supplies the two-sample condition, bounded overlap for its own propensity, and the degrading-array condition. The common propensity \(1/2\) enters the uniform finite-cell argument through \cref{obj:ass:finite-cell-source} and the reduction to the regular-cell design with \(c_-=c_+=1\). The preceding bound on \(L_{\alpha,c}\) supplies the required critical-value inequality. Thus there is a regular-cell procedure \(C^{\mathrm{reg}}\) whose set agrees almost surely with the regular-cell score-inversion rule, which is honest, and whose risk is bounded by
\[
\max\{2,4\sqrt2\,L_{\alpha,c}+8B\}\min\{1,t_0^{-1/2}\}
\]
for every \(t_0>0\).

\item
Transport this regular-cell rule to the finite-cell experiment.  On each finite-cell row, the finite-cell inversion rule, the ambient regular-cell rule, and the regular-cell procedure just obtained agree almost surely after identifying the uniform source-cell masses and the common propensity \(1/2\).  This reduction gives finite-cell honesty for the sample-only procedure
\[
C_n(s)=\text{the finite-cell score-inversion set with critical constant }L_{\alpha,c}.
\]
Equivalently, this is exactly the rule displayed in the theorem statement, with \(C_n=\Theta\) when \(N_n<2\).

\item
For the upper expected-length bound, use the regular-cell risk bound from Step 2, the risk comparison induced by the reduction to the regular-cell design, and the constant comparison \(B\le B_c\).  Since
\[
\min\{1,t_0^{-1/2}\}\ge0,
\]
\[
\max\{2,4\sqrt2\,L_{\alpha,c}+8B\}
\le
\max\{2,4\sqrt2\,L_{\alpha,c}+8B_c\}=C_0
\]
implies
\[
\limsup_{n\to\infty}\sup_{P\in\mathcal N_n:\,t_n\ge t_0}
\mathbb E_P[\lambda(C_n)]
\le
C_0\min\{1,t_0^{-1/2}\}.
\]
This proves the asserted attainment bound for every \(t_0>0\).

\item
For the converse bound, fix \(t_0>0\).  Choose, for every \(n\), an injection from \(\{1,\ldots,k_n\}\) into the measurable carrier with measurable singleton image.  In the deterministic geometry \(\mathfrak g\), take both covariate laws to be
\[
P_{S,n}^X=P_{T,n}=\sum_{i=1}^{k_n} k_n^{-1}\delta_{x_{i,n}},
\]
take \(w=1\), and take \(e=1/2\).  The cancellation \(k_n k_n^{-1}=1\) shows that these covariate laws are probability measures.  The bounds \(0<\varepsilon<1/2\) put the propensity inside the overlap interval, while \(k_n\ge1\) gives the weight envelope and second-moment requirements.  Hence \(\mathfrak g\in\mathcal G\).

\item
For this geometry, the selected cell range is measurable, has both \(P_{S,n}^X\)- and \(P_{T,n}\)-probability one, and each chosen singleton has mass \(k_n^{-1}\).  Therefore any row in the fixed-geometry slice \(\mathcal P_n(\mathfrak g)\) satisfies the finite-cell source condition with this cell injection, and the remaining model-class conditions are inherited from the fixed-geometry slice.  Thus every such row lies in \(\mathcal N_n\).  The same measurable range also makes the event that all source and target sample covariates lie in the selected range a measurable sample event.

\item
Invoke the lower-bound half of \cref{obj:thm:fixed-geometry-frontier} for the geometry \(\mathfrak g\):
\[
\frac{3(1-\alpha)^2}{16}\min\{1,t_0^{-1/2}\}
\le
V_{\mathfrak g}^{\star}(t_0).
\]
It is enough to show that \(V_{\mathfrak g}^{\star}(t_0)\) is bounded above by the feasible finite-cell minimax value.

\item
Take an arbitrary feasible finite-cell honest procedure \(\widetilde C\).  Construct a fixed-geometry oracle procedure \(D\) as follows: on samples whose source and target covariates all lie in the selected cell range, set
\[
D_n=\widetilde C_n;
\]
off that support, set \(D_n=\Theta\).  The graph measurability follows by splitting the sample space into the measurable support event and its complement; weight-invariance is immediate because this sample-and-support rule is fixed on each such event.

\item
On every fixed-geometry row, the source and target covariate laws assign probability one to the selected range, so the product sampling law assigns probability one to the support event from Step 8.  Hence \(D_n\) and \(\widetilde C_n\) agree almost surely on that row.  Their coverage probabilities and expected lengths are therefore equal on fixed-geometry rows.  For all sufficiently large \(n\), the fixed-geometry threshold slice has admissible laws, and the finite-cell row class has admissible laws for every \(n\).  On those rows, the inclusion from Step 6 and the equality of coverage probabilities give
\[
\inf_{P\in\mathcal N_n}\Pr_P\{\theta_T\in \widetilde C_n\}
\le
\inf_{P\in\mathcal P_n(\mathfrak g)}
\Pr_P\{\theta_T\in D_n\}.
\]
The coverage quantities are bounded below by \(0\), and the fixed-geometry coverage infimum is eventually bounded above by \(1\).  Passing to liminf therefore preserves the displayed inequality, so finite-cell honesty of \(\widetilde C\) gives fixed-geometry oracle honesty of \(D\).

\item
For the risk comparison, first note that every set length is computed inside \(\Theta=[-1,1]\), so every realized length is at most \(2\).  Hence, for \(\widetilde C\), each finite-cell expected length is at most \(2\).  For all sufficiently large \(n\), the fixed-geometry threshold slice has admissible laws.  On those \(n\), the equality of expected lengths on fixed-geometry rows and the inclusion from Step 6 give
\[
\sup_{P\in\mathcal P_n(\mathfrak g):\,t_n\ge t_0}
\mathbb E_P[\lambda(D_n)]
\le
\sup_{P\in\mathcal N_n:\,t_n\ge t_0}
\mathbb E_P[\lambda(\widetilde C_n)],
\]
where the finite-cell supremum is controlled by the preceding length bound.  The fixed-geometry supremum is eventually nonnegative because the threshold slice is nonempty and expected length is nonnegative.  The compact parameter space bounds every finite-cell supremum by \(2\) under the stated supremum convention.  These lower and upper bounds supply the boundedness hypotheses for passing the eventual inequality through limsup, yielding
\[
\text{fixed-geometry risk of }D
\le
\limsup_{n\to\infty}
\sup_{P\in\mathcal N_n:\,t_n\ge t_0}
\mathbb E_P[\lambda(\widetilde C_n)].
\]

\item
It remains to place the fixed-geometry value below the risk of this particular \(D\).  The same length bound applies to every fixed-geometry oracle procedure: for any such procedure, every fixed-geometry expected length is at most \(2\).  The supremum convention and nonnegativity of expected length keep each fixed-geometry row supremum between \(0\) and \(2\) on the relevant tail.  These facts imply that every fixed-geometry oracle risk is nonnegative, so the collection of fixed-geometry honest risks is bounded below by \(0\).  Since \(D\) is fixed-geometry honest by Step 9, the defining infimum for \(V_{\mathfrak g}^{\star}(t_0)\) satisfies
\[
V_{\mathfrak g}^{\star}(t_0)
\le
\text{fixed-geometry risk of }D.
\]
Combining this with Step 10 gives
\[
V_{\mathfrak g}^{\star}(t_0)
\le
\limsup_{n\to\infty}\sup_{P\in\mathcal N_n:\,t_n\ge t_0}
\mathbb E_P[\lambda(\widetilde C_n)].
\]
Taking the infimum over all feasible finite-cell honest \(\widetilde C\) and combining with Step 7 yields
\[
\frac{3(1-\alpha)^2}{16}\min\{1,t_0^{-1/2}\}
\le
\inf_{\widetilde C\in\mathfrak C^{\mathrm{cell}}}
\limsup_{n\to\infty}\sup_{P\in\mathcal N_n:\,t_n\ge t_0}
\mathbb E_P[\lambda(\widetilde C_n)] .
\]
This completes the proof.
\end{enumerate}
\end{proof}

\bibliographystyle{plainnat}

\bibliography{references}

\end{document}
