\section{Appendix D: Verification Note}

This appendix records the verification scope for the formal layer used by the preceding results. The checked material covers the anchored definitions, auxiliary lemmas, propositions, and theorems that organize the two distance-based experiments: the unsigned boundary-regression risks in \cref{obj:def:cty-distance-risk,obj:def:point-indexed-distance-risk} and the known-geometry signed-distance risk in \cref{obj:def:cty-a1-a2-outer-risk}. These objects are stated in the paper as mathematical definitions and results, and the surrounding prose uses them as the reference points for the econometric interpretation.

The verification covers the algebraic and measure-theoretic dependencies among the displayed formal statements. In the unsigned experiment, this includes the rate definition in \cref{obj:def:frontier-rates}, the law class in \cref{obj:def:cty-nonparametric-class}, the distance observation in \cref{obj:def:cty-distance-data}, the two decision classes in \cref{obj:def:cty-distance-decision-class,obj:def:point-indexed-distance-decision-class}, and the two risk functionals in \cref{obj:def:cty-distance-risk,obj:def:point-indexed-distance-risk}. The lower-bound statements in \cref{obj:thm:cty-same-class-log-converse,obj:thm:point-indexed-distance-log-converse} are checked relative to the auxiliary support-boundary packing and direct-product statements used in Appendix A.

For the signed-distance experiment, the checked layer covers the known geometry and signed-distance data in \cref{obj:def:cty-known-geometry,obj:def:cty-known-geometry-signed-distance-data}, the A1/A2 law class in \cref{obj:def:cty-a1-a2-class}, the known-geometry decision class in \cref{obj:def:cty-a1-a2-point-indexed-decision-class}, and the outer-risk criterion in \cref{obj:def:cty-a1-a2-outer-risk}. It also covers the formal assembly of the estimator in \cref{obj:def:cty-stabilized-local-polynomial-estimator}, the lower bound in \cref{obj:thm:cty-a1-a2-point-indexed-log-converse-all-orders}, the expected outer-risk upper bound in \cref{obj:prop:cty-a1-a2-winsorized-expected-outer-upper}, and the matched-rate conclusion in \cref{obj:thm:cty-a1-a2-winsorized-matched-frontier}. The econometric interpretation of the signed-distance conditions follows the distance-based boundary framework of \citet{CattaneoTitiunikYu2026Distance}, while the outer-expectation convention follows standard empirical-process usage \citep{VanderVaart1996}.

The theorem-local assumptions state the citation boundary for analytic ingredients invoked by the results. In particular, the distance-identification, first-order bias, and maximal-inequality conditions appearing in \cref{obj:prop:cty-a1-a2-winsorized-expected-outer-upper,obj:thm:cty-a1-a2-winsorized-matched-frontier} are part of the mathematical hypotheses under which the corresponding conclusions are established. The formal development checks that, once those stated conditions are supplied, the estimator and minimax-risk conclusions follow with the constants and rates displayed in the paper.

The theorem statements and proofs for the formal layer are machine-checked in Lean 4.






