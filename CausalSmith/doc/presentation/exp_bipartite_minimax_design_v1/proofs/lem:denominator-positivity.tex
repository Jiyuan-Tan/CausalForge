\begin{proof}[Proof of \cref{obj:lem:denominator-positivity}]
Put
\[
K=\epsilon^{-1}\max\{1,\epsilon^{-(\bar d-1)}\},
\qquad
C=2\max\{0,\bar D\}K .
\]
Since \(\epsilon>0\) gives \(\epsilon^{-1}>0\) and \(\max\{1,\epsilon^{-(\bar d-1)}\}\ge1\), we have \(K\ge0\) and hence \(C\ge0\).
% lean: CausalSmith.Experimentation.BipartiteMinimaxDesign.denominatorKernelBound_nonneg
Because \cref{obj:ass:bounded-overlap-dependency} gives \(\bar D>0\), also \(\max\{0,\bar D\}=\bar D\) and \(C=2\bar DK\).

Fix \(n\ge1\) and a feasible \(q\in\mathcal P_{n,B_n,\epsilon}\). Since \(|O_n|=n\ge1\), the outcome population is nonempty. Feasibility gives \(\epsilon\le q_k\le1-\epsilon\) for every \(k\in I_n\), and \(0<\epsilon<1/2\) therefore gives
\[
0<\epsilon\le q_k\le 1-\epsilon<1
\qquad(k\in I_n),
\]
so every exposure probability \(\pi_i^1(q)=\prod_{k\in N_i(G_n)}q_k\) and \(\pi_i^0(q)=\prod_{k\in N_i(G_n)}(1-q_k)\) is strictly positive.

\textit{Step 1 (uniform bound on the overlap loads).} We claim that, for all \(i,j\in O_n\),
\[
r_{ij}^1(G_n,q)\le K
\qquad\text{and}\qquad
r_{ij}^0(G_n,q)\le K .
\]
If \(S_{ij}(G_n)=\varnothing\), both loads are \(0\le K\). Otherwise write \(S=S_{ij}(G_n)\) and pick \(k\in S\). Since \(S\subseteq N_i(G_n)\), \cref{obj:ass:bounded-outcome-degree} gives \(|S|\le d_i\le\bar d\). The displayed floor bounds give \(0\le q_\ell^{-1}\le\epsilon^{-1}\) for every \(\ell\), so deleting the single factor \(k\) leaves
\[
\prod_{\ell\in S\setminus\{k\}}q_\ell^{-1}
\le\left(\epsilon^{-1}\right)^{|S|-1}
=\epsilon^{-(|S|-1)}
\le\epsilon^{-(\bar d-1)}
\le\max\{1,\epsilon^{-(\bar d-1)}\},
\]
the middle inequality because \(0<\epsilon\le1\) makes \(t\mapsto\epsilon^{-t}\) nondecreasing and \(|S|\le\bar d\). Restoring the deleted factor, which is at most \(\epsilon^{-1}\), gives
\[
\prod_{\ell\in S}q_\ell^{-1}
=q_k^{-1}\prod_{\ell\in S\setminus\{k\}}q_\ell^{-1}
\le\epsilon^{-1}\max\{1,\epsilon^{-(\bar d-1)}\}=K,
\]
whence \(r_{ij}^1(G_n,q)=\prod_{\ell\in S}q_\ell^{-1}-1\le K-1\le K\). The control load is identical with \(q_\ell\) replaced by \(1-q_\ell\), for which the floor gives \(0\le(1-q_\ell)^{-1}\le\epsilon^{-1}\) as well.
% lean: CausalSmith.Experimentation.BipartiteMinimaxDesign.r1_le_denominatorKernelBound
% lean: CausalSmith.Experimentation.BipartiteMinimaxDesign.r0_le_denominatorKernelBound

\textit{Step 2 (mean and variance of the treated denominator).} Write \(X_1=D_1(q,Z)=\sum_{i\in O_n}T_i(Z)/\pi_i^1(q)\). Under \cref{obj:ass:independent-heterogeneous-bernoulli} the coordinates of \(Z\) are independent, so
\[
\mathbb E_q\!\left[T_i(Z)\right]
=\prod_{k\in N_i(G_n)}\mathbb E_q\!\left[Z_k\right]
=\prod_{k\in N_i(G_n)}q_k
=\pi_i^1(q),
\qquad\text{hence}\qquad
\mathbb E_q\!\left[\frac{T_i(Z)}{\pi_i^1(q)}\right]=1 ,
\]
and summing over the \(n\) outcome units gives
\[
\mathbb E_q[X_1]=n .
\]
% lean: CausalSmith.Experimentation.BipartiteMinimaxDesign.treatDenominator_mean
Because each summand of \(X_1\) has mean one, the covariance of two summands is exactly the treated overlap load,
\[
\operatorname{Cov}_q\!\left(\frac{T_i(Z)}{\pi_i^1(q)},\frac{T_j(Z)}{\pi_j^1(q)}\right)
=\mathbb E_q\!\left[
\left(\frac{T_i(Z)}{\pi_i^1(q)}-1\right)
\left(\frac{T_j(Z)}{\pi_j^1(q)}-1\right)\right]
=r_{ij}^1(G_n,q),
\]
since \(T_i(Z)T_j(Z)\) is the indicator that every intervention in \(N_i(G_n)\cup N_j(G_n)\) is treated, so its expectation divided by \(\pi_i^1(q)\pi_j^1(q)\) equals \(\prod_{k\in S_{ij}(G_n)}q_k^{-1}\) when \(S_{ij}(G_n)\neq\varnothing\), and equals \(1\) otherwise. Expanding the variance of the sum into these pair covariances,
\[
\operatorname{Var}_q(X_1)
=\sum_{i\in O_n}\sum_{j\in O_n}r_{ij}^1(G_n,q).
\]
Fix \(i\). The load \(r_{ij}^1(G_n,q)\) vanishes unless \(S_{ij}(G_n)\neq\varnothing\), that is unless \(j\) lies in the closed overlap neighborhood of \(i\), whose cardinality is at most \(\Delta_n\le\bar D\) by \cref{obj:ass:bounded-overlap-dependency}; on that set Step 1 bounds each load by \(K\). Hence
\[
\sum_{j\in O_n}r_{ij}^1(G_n,q)
\le\left|\{j\in O_n:S_{ij}(G_n)\neq\varnothing\}\right|K
\le\bar DK ,
\]
and summing over the \(n\) outcome units,
\[
\operatorname{Var}_q(X_1)\le n\bar DK .
\]
% lean: CausalSmith.Experimentation.BipartiteMinimaxDesign.treatDenominator_var_le

\textit{Step 3 (Chebyshev).} On the event \(\{X_1=0\}\) one has \(|X_1-\mathbb E_q[X_1]|=n\), so \(\{X_1=0\}\subseteq\{n\le|X_1-\mathbb E_q[X_1]|\}\). Chebyshev's inequality applied at level \(n>0\) therefore yields
\[
\Pr_q\!\left(D_1(q,Z)=0\right)
\le\Pr_q\!\left(n\le\left|X_1-\mathbb E_q[X_1]\right|\right)
\le\frac{\operatorname{Var}_q(X_1)}{n^2}
\le\frac{n\bar DK}{n^2}
=\frac{\bar DK}{n}.
\]
% lean: CausalSmith.Experimentation.BipartiteMinimaxDesign.treatDenominator_zero_prob_le

\textit{Step 4 (the control arm and the union bound).} Set \(X_0=D_0(q,Z)=\sum_{i\in O_n}C_i(Z)/\pi_i^0(q)\). Replacing \(q_k\) by \(1-q_k\) throughout Steps 2 and 3 — the coordinates \(1-Z_k\) are again independent, with means \(1-q_k\), and the resulting pair covariances are the control loads \(r_{ij}^0(G_n,q)\), bounded by \(K\) in Step 1 — gives
\[
\mathbb E_q[X_0]=n,
\qquad
\operatorname{Var}_q(X_0)\le n\bar DK,
\qquad
\Pr_q\!\left(D_0(q,Z)=0\right)\le\frac{\bar DK}{n}.
\]
% lean: CausalSmith.Experimentation.BipartiteMinimaxDesign.ctrlDenominator_mean
% lean: CausalSmith.Experimentation.BipartiteMinimaxDesign.ctrlDenominator_var_le
% lean: CausalSmith.Experimentation.BipartiteMinimaxDesign.ctrlDenominator_zero_prob_le
Finally, the probability of a union is at most the sum of the probabilities, so
\[
\Pr_q\!\left(D_1(q,Z)=0\ \text{or}\ D_0(q,Z)=0\right)
\le\frac{\bar DK}{n}+\frac{\bar DK}{n}
=\frac{2\bar DK}{n}
=\frac{C}{n}.
\]
The constant \(C\) depends only on \(\epsilon,\bar d,\bar D\), so the bound holds for every feasible \(q\) at every \(n\ge1\), which is the assertion.
% lean: FiniteDesign.Pr_or_le
% lean: CausalSmith.Experimentation.BipartiteMinimaxDesign.denominator_positivity
\end{proof}
